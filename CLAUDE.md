# CLAUDE.md — 퓨처나우 플랫폼 작업 규율

> 이 파일은 클로드코드의 작업 규율이다. 매 작업 전 `architecture.md`와 함께 읽는다.
> `architecture.md` = 확정 사양(단일 진실) · `plan.md` = 보류·향후 · 본 파일 = 작업 방식.

---

## 0. 문서 우선순위

1. `architecture.md`를 먼저 정독한다. 그것이 단일 진실이다.
2. 본 파일의 규율을 지킨다.
3. **§12 불변식 24가 이 파일 안에서도 가장 위다.** §1~§11이 *어떻게 일하는가*라면 §12는
   *무엇을 어기지 않는가*다. **발주서·메모가 불변식과 어긋나면 불변식이 이긴다** — 멈추고 보고한다.
4. 충돌·의문이 생기면 **임의로 결정하지 말고 지휘부에 질문**한다. 특히 `/contracts` 관련.

---

## 1. 계약 우선 (Contract-first)

- `/src/contracts`의 타입은 시스템의 척추다. **변경은 지휘부 승인 후에만** 한다.
- `/src/core`는 `/src/instruments`를 **참조하지 않는다.** 진단도 코어를 직접 참조하지 않는다. 양쪽 다 `/src/contracts`만 바라본다.
- 계약을 우회하는 지름길(코어에서 진단 내부를 들여다보기, 진단끼리 코드 공유)을 만들지 않는다. 이 경계가 흐려지면 플랫폼 전체가 무너진다.

## 2. 코어는 채점하지 않는다

- 코어(`/src/core`)에 채점·리포트·진단 고유 로직을 **한 줄도** 넣지 않는다. 코어는 인증·차수·코치·응답봉투·알림·공용UI만 운영한다.
- 채점·리포트·알림 규칙은 전부 `/src/instruments/futurenow` 안에 둔다.

## 3. 소스코드 보존 규율 (지휘부 원칙)

- 기존 코드를 수정할 때는 **개선에 필요한 부분만** 고친다. 그 외 부분을 임의로 축약·요약·생략·재작성하지 않는다.
- 리팩터링이 필요하면 범위를 먼저 보고하고 승인받는다.

## 4. SAIL을 건드리지 않는다

- 현재 운영 중인 SAIL은 별도 작업공간(클코3) 소관이다. 본 작업공간에서 SAIL 코드를 수정하지 않는다.
- SAIL의 추상 이관은 코어 패키지 추출 이후다(`plan.md` §1). 지금 SAIL을 추상에 맞춰 미리 고치지 않는다.
- SAIL은 **참고·차용의 원본**으로만 본다(스키마·RLS 헬퍼·단계형 흐름·나침반 차트).

## 5. 마이그레이션 규율

- 마이그레이션은 `/supabase/migrations`에 타임스탬프 파일로 추가한다.
- **이미 적용된 마이그레이션 파일을 수정하지 않는다.** 변경은 항상 새 마이그레이션으로.
- RLS는 `SECURITY DEFINER` 헬퍼(`is_admin` 등) 패턴을 따른다(architecture §6.1). 정책끼리 테이블을 직접 참조해 재귀를 만들지 않는다.
- SAIL과 **동일 Supabase 프로젝트**를 공유한다. 기존 SAIL 테이블(`users` 등)과 충돌하지 않도록, 공유 테이블 변경은 반드시 지휘부 확인 후.

## 6. 스택 버전 주의

- Next.js 16은 학습 데이터와 다른 breaking change가 많다. 코드를 짜기 전에 `node_modules/next/dist/docs/`의 해당 가이드를 읽는다(SAIL `AGENTS.md` 교훈).
- React 19 · Tailwind 4 · Supabase SSR(`@supabase/ssr`) 최신 규약을 따른다.

## 7. 측정·강의 어휘 분리

- 문항(`Item.prompt`)에 구인명·STEP·강의 어휘('시들음'·'원씽' 등)를 **노출하지 않는다.**
- `code`·`polarity`·구인은 참여자 렌더 경로에 두지 않는다. 그 명명은 리포트(B③)에서만.

## 8. 디자인 시스템 — 확정 전 UI 임의 디자인 금지

- `design_system.md`가 도착하기 전까지 **UI를 임의로 디자인하지 않는다.** 색·폰트·레이아웃·컴포넌트 시안은 지휘부가 확정해 전달한다.
- 그 전 단계에서 UI가 불가피하면, **기능 골격만(스타일 미적용 placeholder)** 만들고 디자인은 비워 둔다.

## 9. 검증

- 채점(B②)은 **단위테스트 필수.** 전용노선과 산출이 동일함을 테스트로 증명한다(architecture §9.3 규칙별 케이스).
- 경계에서 `answers`·`subjectProfile`를 zod로 검증한다. DB는 느슨(JSONB), 코드 경계는 엄격.
- RLS 격리(실명제·전화번호 운영자 전용)를 권한별로 테스트한다.
- **게이트 화면 검증 규율(강화 2026-07-02)**: 접근 게이트가 걸린 화면은 `web_fetch`/HTTP **200을 정상 통과로 판정하지 않는다.** Next 서버 컴포넌트 `redirect()`는 307이 아니라 **200 + 클라이언트 리다이렉트 페이로드(`__NEXT_REDIRECT`)**로 서빙될 수 있다(미인증 시 미들웨어가 307/login 하는 것과 다른 경로). 따라서 게이트 화면은 **본문에 민감 콘텐츠 0 + 리다이렉트 목적지 확인**으로 판정한다. 게이트-데이터 순서(게이트가 먼저)를 유지하는 한 누출은 없다.

## 10. 보안

- 전화번호는 `user_contacts`에 격리하고 `getPhone` 게이트로만 접근한다. `CoreUser`에 전화번호를 싣지 않는다.
- `responses`·`alerts`는 불변(UPDATE/DELETE 정책 없음).
- 비밀키·환경변수를 코드·로그에 노출하지 않는다.

## 11. 작업 보고 (보고 규격 — 명문화 2026-06-26)

모든 작업 완료 보고는 다음을 **빠짐없이** 포함한다.

1. **구현/변경 파일 목록과 각 항목 상태**(완·부분·보류).
2. **검증 결과**: 타입(`tsc`)·린트(`eslint`)·테스트 수.
   **테스트 집계는 `통과/스킵/총`을 한 줄에 적는다**(명문화 2026-08-27) — `983 통과`만 적으면
   총이 몇인지 읽는 사람이 모르고, 스킵이 늘어난 것과 통과가 준 것이 구별되지 않는다.
   집계 기준이 어긋나 지휘부와 숫자가 갈린 사례가 이미 다섯 번 있었다(`taken`/`remaining` ·
   `feed_posts` 3행 · §4 스키마 · 불변식 참조 12/14 · `text-muted` 91/87). **여섯째를 만들지 않는다.** 보안 관련은 **역할별 실측 여부**(RLS 격리가 역할별로 실제로 검증됐는지).
3. **`architecture.md` 정합(필수 — 명문화 2026-07-02)** — 모든 작업은 완료 전 `architecture.md`를 **최신 상태로 동기화**한다(신축·수정의 단일 진실). 반영 항목(ADR·§4·§5·§7·§10·§11·plan 확정 등)을 보고에 명시하고, 진짜로 사양 변화가 없으면 **"문서 델타 0 + 사유"를 명시**한다(무기재 금지). 새 결정은 ADR로 번호를 이어 붙인다(중복·결번 확인).
4. **계약(`/contracts`) 대비 이탈 여부와 사유** — 임의 변경 금지, 필요시 질문(변경은 견고화 방향 + 지휘부 승인 후에만).
5. **막힌 지점·판단 필요 사항**.

- **문서 정합은 완료판정(DoD) 게이트다** — 커밋·푸시 **전에** `architecture.md`(필요 시 `plan.md`·`design_system.md`)를 함께 갱신한다. 코드만 푸시하고 문서를 미룬 상태로 "완료"를 보고하지 않는다. 문서-코드 드리프트는 **작업 미완**으로 간주한다.
- **작업 완료 보고는 GitHub 푸시를 전제로 하며, 커밋 해시·변경 파일을 포함한다**(지휘부가 매 단계 클론·감리). 비밀(`.env.local`·service_role 등) 추적 제외를 push 전 재확인한다.
- 컨텍스트 한계가 가까워지면 핸드오버 요약을 남긴다.

---

## 12. 불변식 24 — 어기지 않는 것

§1~§11이 **어떻게 일하는가**라면, 이 절은 **무엇을 어기지 않는가**다.
새 기능마다 다시 판단하지 않기 위해 한 자리에 모았다.

번호는 발주서·메모·코드 주석이 인용하는 **영구 식별자**다. **번호를 바꾸거나 재사용하지 않는다.**
규율이 폐기되면 항목을 지우지 말고 `폐기` 표시와 사유를 남긴다 — 번호를 지우면 옛 주석이 미아가 된다.

각 항목의 `→`는 상세가 있는 곳이다.

### 경계 (1~5)

| # | 불변식 | 상세 |
|---|---|---|
| 1 | **코어는 채점하지 않는다.** `/src/core`에 채점·리포트·진단 고유 로직을 한 줄도 두지 않는다 | → §2 |
| 2 | **`/core ↛ /instruments`, 진단 ↛ 코어.** 양쪽 다 `/contracts`만 본다. 진단끼리 코드를 공유하지 않는다 | → §1 |
| 3 | **`/contracts` 변경은 지휘부 승인 후에만.** 임의 변경 시 질문한다 | → §1 |
| 4 | **`/app`·`/instruments`의 테이블 직접 접근은 0건이다.** 갈무리조차 `CoreContext` 계약 메서드를 지난다 | ADR-80·121 |
| 5 | **리포트 차트군은 인스트루먼트 소유다.** 코어가 아니다 | ADR-21 |

### 측정·어휘 (6~11)

| # | 불변식 | 상세 |
|---|---|---|
| 6 | **`Item.prompt`에 구인명·STEP·강의 어휘를 노출하지 않는다.** 명명은 B③ 리포트에서만 | → §7 |
| 7 | **`code`·`polarity`·구인을 참여자 렌더 경로에 두지 않는다.** 참여자 미러 회귀가드가 지킨다 | → §7 |
| 8 | **가치 카드 `category`는 참여자 렌더 타입에 없다.** 규칙이 아니라 타입으로 막았다 | ADR-121 |
| 9 | **참여자 진입 화면에 경고색을 두지 않는다.** 의미색은 인도자 화면·돌봄 배너에만 | ADR-112 |
| 10 | **코드가 원형·감정을 판정하지 않는다.** 사실만 내고 판정은 인도자가 한다 | ADR-114 |
| 11 | **갈무리에 막대·게이지·색·백분위·평균·정렬키를 두지 않는다.** 순위를 만들지 않는다 | ADR-86 |

### 데이터·보안 (12~19)

| # | 불변식 | 상세 |
|---|---|---|
| 12 | **`responses`·`alerts`는 불변이다.** UPDATE/DELETE 정책을 두지 않는다 | ADR-73이 관리 경로 예외를 명시 |
| 13 | **전화번호는 `user_contacts`에 격리한다.** `CoreUser`에 `phone`이 없다. **게이트 없는 경로를 만들지 않는다** | ADR-04·75·78 · ADR-122 ⑫ |
| 14 | **RLS는 `SECURITY DEFINER` 헬퍼로 한다.** 정책끼리 테이블을 직접 참조하지 않는다(42P17 재귀) | → §5 |
| 15 | **적용된 마이그레이션 파일을 수정하지 않는다.** 변경은 항상 새 파일로 | → §5 |
| 16 | **`step_private`·`suggestion_anon`을 권한 등급으로 뚫지 않는다.** 운영자도 코치와 **동일하게** 볼 수 없다 — 서열의 문제가 아니라 **참여자에게 한 약속**이기 때문이다. 참여자는 `나만 봅니다`·`익명으로 남깁니다`를 읽고 썼고 그 문장에 예외가 없었다. 안전상 필요한 경우는 인도자가 판단해 올리는 **별도 절차**로 다룬다 | ↓ 12.2 |
| 17 | **`PROXY_MATCHER`를 좁히지 않는다.** 신원 헤더 strip 커버리지가 깨지면 위조 주입 구멍이 생긴다. 제외는 정적 자산에 한한다 | ADR-66 |
| 18 | **참여자 원문(실명·연락처와 결합된 발언)을 커밋하지 않는다.** `.gitignore`는 `/docs/*` + negation 구조를 유지한다 — `/docs/`로 바꾸면 negation이 죽는다 | ADR-96·119 |
| 19 | **게이트 화면은 HTTP 200을 통과로 판정하지 않는다.** 본문 민감 콘텐츠 0 + 리다이렉트 목적지로 판정한다 | ADR-93 · → §9 |

### 작업 방식 (20~24)

| # | 불변식 | 상세 |
|---|---|---|
| 20 | **`design_system.md` 확정 전 UI를 임의로 디자인하지 않는다.** 불가피하면 기능 골격만 만들고 부재 부품을 보고한다 | → §8 |
| 21 | **SAIL을 건드리지 않는다.** 클코3 소관이며 공유 테이블 변경은 지휘부 확인 후 | → §4 |
| 22 | **승인된 ADR의 근거를 다룰 때는 전부 인용한 뒤 각각을 따로 판정한다** | ADR-94 |
| 23 | **사본이 둘이면 잠금으로 묶는다.** 임계·색·문안을 두 곳에 두지 않는다 | ADR-106·108·112·119 |
| 24 | **문서 정합이 완료 판정(DoD) 게이트다.** 커밋 전에 `architecture.md`를 갱신한다. 코드만 푸시하고 "완료" 보고를 하지 않는다 | → §11 |

---

### 12.1 이 목록을 쓰는 법

- **인용은 번호로 한다** — `불변식 17`. 코드 주석·보고서·발주서가 같은 식별자를 쓴다
- **주석에는 번호와 요지를 함께 적는다** — `// 불변식 17 — matcher 를 좁히지 말 것`.
  번호만 적으면 이 파일을 열어야 뜻을 안다
- **충돌하면 불변식이 이긴다.** 발주서가 불변식과 어긋나면 멈추고 보고한다
  (3차 §9.6 선례 — 발주 §6.3이 탭바 제작을 지시했으나 불변식 20이 이겼고 그것이 옳았다)
- **예외는 항목 안에 적는다.** ADR-73이 불변식 12의 관리 경로 예외를 명시한 것이 그 방식이다.
  별도 문서에 예외를 두면 다음 사람이 본문만 읽고 판단한다

### 12.2 불변식 16 — 운영자 겸임과의 관계

운영자는 당분간 인도자와 **공동 진행**하므로 인도자 역할을 겸한다(2026-08 지휘부 확인).
그래도 16번은 유효하다. **막는 것이 없기 때문이다.**

ADR-74가 운영자에게 인도자 콘솔 전체 감독을 준다 — 갈무리·진단 결과·리포트·돌봄 신호·
참여 흐름이 모두 열려 있고, 자기 기수만 보는 인도자보다 오히려 넓다.
**막힌 두 칸은 인도자도 못 본다.** 동료보다 덜 보는 것이 아니라 같은 자리에 선다.

익명이 특히 그렇다. 기수가 7~12명이므로 **한 번 열면 누가 썼는지 사실상 드러난다.**
그리고 익명 제안은 "정말 익명인가"에 대한 신뢰 위에만 선다.
**닫힌 것은 언제든 열 수 있으나, 열린 것은 다시 닫아도 신뢰가 돌아오지 않는다.**

정책을 바꾸려면 열람 권한이 아니라 **참여자 대면 문안을 먼저 고친다** —
`나만 봅니다`를 `나와 운영진만 봅니다`로. 그리고 **이미 받은 응답에 소급하지 않는다.**

### 12.3 개정 이력

| 판 | 날짜 | 내용 |
|---|---|---|
| 초판 | 2026-08-27 | 1차 발주서 §11 원본을 정본화. 3차 T-6이 목록 부재를 발견.<br>13번은 ADR-75·78 실측에 맞춰 고쳐 적었다(게이트가 하나라는 뜻이 아니라 게이트 없는 경로가 없다는 뜻).<br>16번에 §12.2 근거를 붙였다(운영자 인도자 겸임 확인). |
