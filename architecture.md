# 퓨처나우 진단 플랫폼 — architecture.md

> 본 문서는 시스템의 **단일 진실(single source of truth)**이다.
> 확정된 모든 구조·계약·데이터 모델·보안 규칙·진단 사양이 여기 담긴다.
> 작업 중 결정·구현된 결과물의 설계는 반드시 이 문서에 반영한다.
> 보류·향후 항목은 `plan.md`, 작업 규율은 `CLAUDE.md`에 둔다.
>
> 문서 버전: **v1.0** (거점=SAIL 승격 · 코어(CoreContext) 구현 · 가입-by-코드/Q1~Q3 확정 · B①·B②·B④ + 문항 원문 · AlertSignal(ADR-19) · 디자인 시스템 v3 구현(색 토큰·공용 UI 12종·응답 위젯 5종·리포트 시각화 5종·ResponseRunner) · 코치 콘솔·본부(코치 승인·역할관리) · 참여 프로필(ADR-32))
> **v1.0 도달(2026-06-30~07-01)**: X1 색 팔레트 확정 · X2 공통 셸 모드(AppHeader root/sub/flow) · 진입 1~3(공개 소개 현관·플로우 헤더·참여자 홈) · 차수 소개(description) · 진단-1(재진단 허용+dedup ADR-33 · 중간저장 `response_drafts` ADR-34) · 완료 후 착지(A-2, Completion→홈) · 마감 a11y(오류 텍스트 대비). 프로덕션 라이브(Vercel, futurenow-platform.vercel.app).
> **UX 2차 트랙 A(진행 중, 2026-07-02)**: A1 셸 홈 복귀 어포던스(ADR-45) · A2 내 정보 완결(프로필·KPC 편집)+성별 전 서비스 공통 상수(ADR-46) · A3 본부 코치 신청 큐(승인 대기 구분)+운영자 로그인 알림(ADR-47) · A4 성별 남/여 2값(마이그 `20260702002311`·ADR-48) · A5 코드 전달(복사·공유·`?code=` deep-link·ADR-49) · A6 빈/로딩/에러 상태 감사+ConsoleHome 빈 상태(ADR-50). **트랙 A(화면 완결성) 완료.**
> **트랙 A′(네비게이션 통합 홈, 진행 중, 2026-07-02)**: A′-1 역할 감금 해제(통합 홈·비대칭 개방 — /home·/my/cohorts 개방·loginOutcome 전원 /home·MemberHome 운영 카드·ADR-51, ADR-45 부분 대체) · A′-2 홈 복귀 homeHref 통일(콘솔·본부·차수·리포트·내정보 전부 /home · CoachInfoGate flow→sub) · A′-3 인증 영역 현관 복귀·상호 전환(login·signup·reset·reset/confirm → `/`·상호링크, signup 막다른 상태 해소·ADR-52) · A′-4 차수 상세 뒤로가기 출처 기반(`?from=`·ADR-53) · A′-5 root 홈 인지성(라벨드 홈·로고=서비스·ADR-54). **트랙 A′(네비게이션 통합 홈) 완료** — 역할 감금 해제·전 화면 홈 복귀 `/home` 통일·인증 현관 복귀·출처 기반 뒤로가기·홈 인지성. 비대칭 개방(홈 전원·콘솔/본부 게이트) 유지, 데이터 RLS 불변.
> **트랙 B(사후 진단·차수 라이프사이클, 진행 중, 2026-07-02)**: B-1 사후 인프라·코치 개시(`cohorts.post_opened_at`·`open_post_wave` RPC·my_cohorts/listCohortsByCoach 반환 확장·CohortDetail 개시 컨트롤·ADR-55, responses UNIQUE 미추가로 ADR-33 유지) · B-2 참여자 사후 진입(JoinClient `?wave=post` 파라미터화·MemberHome/MyCohorts '사후 진단하기'·ADR-56) · B-3 사전↔사후 비교 리포트(참여자 미러 2개·코치 그룹 평균 2개, 자동 감지·순화/리얼 이원 불변·ADR-57 — 사전→세미나→사후→비교 서사 완결) · B-4 차수 라이프사이클(마감=진입 봉인·데이터 미봉인·CreateCohort 죽은 wave UI 제거·ADR-58). **트랙 B(사후 진단·차수 라이프사이클) 완료** — 코치 사후 개시 → 참여자 사후 진입 → 사전↔사후 비교 → 마감. 하부(wave 인프라) 재사용, responses UNIQUE 미추가(ADR-33 유지).
> **A·B 정합 마감(2026-07-02)**: 다차원 재점검(17 에이전트·적대 검증) 확정 9→7건 수정 — med 3(내 리포트 전역 개방·admin 홈 승인대기 알림·CohortDetail 재공유 배선)·low 4(loginOutcome role/왕복 정리·copy.ts orphan 제거·스테일 주석 동기화). ADR-59. 보안·마이그·계약 G1 clean.
> **트랙 C(성능·측정 기반, 진행 중, 2026-07-02)**: C-1 측정(전역 하한 ~1.1–1.4s = getUser 2회+SELECT, 콘솔 N+1 ~610ms/차수, force-dynamic 16면) · C-2 `currentUser` 요청 단위 메모이즈(ADR-60 — /coach 게이트 3×→1×, 검증 우회 아님) · C-3 콘솔 차수 루프 병렬화(ADR-61 — 순차→`Promise.all`, N=3 스케일링 평탄화) · C-4 낙관적 UI 시범(ADR-62 — 차수 이름 변경, refresh 전체 재렌더→낙관적 즉시 반영·실패 롤백). **트랙 C(성능) 봉인** — 측정 하한 확인 → currentUser 메모이즈 → 콘솔 병렬화 → 낙관적 UI. 보안 사안 2건(이중 getUser dedup·getClaims 교체)은 별도 승인 대기.
> **트랙 D(general 공개 체험 진단, 2026-07-02)**: D-1 예약 코드 차수 인프라(마이그 `20260702131316` — cohorts CHECK 예약어 `JOINF` + general 차수 시드[운영자 소유·상시 active·무기한·무제한 정원]) · D-2 진입 UI(CodeInput 하단 '체험 진단 시작하기' → `onCode('JOINF')` 딥링크 동형 → CohortPreview `isGeneral`) · D-3 정책·리포트(사전 wave 고정·참여자 본인 순화 거울·운영자 콘솔 재사용 노출). ADR-63. **트랙 D 봉인** — 워크스루 실측: /join 체험 진입 노출·`?code=JOINF` 200·general 참여자 `/my/cohorts` 열람·본인 리포트 200 미러 렌더·신호 0(순화 유지). 확정: 코드=`JOINF`·콘솔 노출·CodeInput 하단.
> **B③ AI 해석(비차단·코치 검수, 진행 중, 2026-07-02)**: B③-A 비차단 생성(ADR-64 — 코치 리포트 렌더 경로에서 aiChat 동기 await 제거, 첫 열람 34,892ms→2,466ms) · B③-B 코치 검수 UI(ADR-65 — 다듬기=setCoachInterpretation·AI 원문으로=clearCoachInterpretation·RLS 소유코치/운영자 격리 실측). **B③ 봉인**(비차단 + 코치 검수).
> **S-1 인증 왕복 최적화(검증 신뢰 실증, 2026-07-02)**: S-1 이중 getUser dedup(ADR-66 — proxy 검증 신원 헤더 전달·strip 신뢰 경계·요청당 Auth 왕복 2→1·role SELECT 유지·세션 갱신 유지, 위조/미인증 거부 로컬+프로덕션 실증). **matcher 불변식(위조-strip 커버리지) 명문화**: `PROXY_MATCHER` 단일 출처(proxy.guard) + `proxyMatcherCovers` 회귀 테스트가 "matcher 를 좁히지 말 것"(정적 자산 외 전 경로 strip 유지·신규 라우트 기본 커버·allowlist 전환 금지)을 강제.
> **S-2 파킹(2026-07-03)**: getClaims 로컬 검증 교체는 선결 실증상 **기술적 가능**(프로젝트 JWT=ES256 비대칭·JWKS 존재·getClaims 지원)하나, 실이득이 왕복 1→0(사용자-비가시 — S-1 실측상 in-region 왕복은 TTFB 노이즈 수준)에 그치는 반면 **검증 신뢰 변화**(getClaims 는 서명·만료만 로컬 검증하고 서버측 사용자 상태[삭제·차단]는 만료 ~1h 까지 미확인; role 은 SELECT 라 즉시 반영)와 세션 갱신 fallback 분기 복잡도를 수반한다 → **파킹**. **재개 트리거: Auth 서버 부하가 실측 제약이 될 때에 한해 재검토**(그때 getClaims+만료-fallback 설계·삭제/차단 지연 posture 실증 후 배선). role 클레임화는 배제 유지(§1).
> **차수 삭제 + '인도자' 용어 통일(2026-07-03)**: 차수 하드삭제(ADR-67 — 예약 general 삭제금지·운영자 임의·코치 빈차수만·2단계 컨펌·계약 +deleteCohort·마이그 0) + 사용자-노출 '코치'→'인도자' 통일(코드/DB 식별자·`/coach` URL·KPC·AI 프롬프트는 유지). 초기 테스트 차수 정리(JOINF·타 계정 데이터 보존). **이중 getUser(proxy↔page)·getClaims 검증 교체는 보안 사안 — 별도 보고·승인 후.**
> **남은 미결(plan.md)**: B③ 리포트 **자동 해석 문구(AI 생성)** 구현 대기 — 시각화 5종은 구현 완료, AI 게이트웨이 위치(plan §1)·Q5(문구 검수) 결정 선결. 그 외 다크 모드 색·접근성 키보드 정밀화는 후속.

---

## 0. 문서 체계와 사용 규칙

| 파일 | 역할 | 변경 권한 |
|---|---|---|
| `architecture.md` | 확정 구조·계약·사양. 단일 진실 | 지휘부 승인 후 갱신. 구현 결과는 즉시 반영 |
| `plan.md` | 보류·향후 업그레이드·미해결 질문 | 자유 추가, 착수 시 architecture로 승격 |
| `CLAUDE.md` | 클로드코드 작업 규율 | 지휘부 |
| `design_system.md` | UI/UX 토큰·컴포넌트·시안 | **지휘부 확정 후 별도 전달** (현재 미존재) |

`/contracts` 폴더의 타입을 바꾸는 것은 시스템의 척추를 바꾸는 일이다. **계약 변경은 지휘부 승인을 받는다.**

---

## 1. 비전과 범위

퓨처나우는 여러 진단 서비스를 수용하는 **공용 플랫폼의 첫 번째 인스트루먼트**다. 백지에서 짓지 않고, 가장 성숙한 형제 시스템 SAIL의 아키텍처를 토대로 삼는다. 퓨처나우로 플러그인 계약을 실전 검증한 뒤, 검증된 계약 위로 SAIL을 이관한다.

운영 규모는 월 500명 이하·1회기 1그룹 10명 수준이다. 처리량이 아니라 **정밀·개방·검증**에 최적화한다.

서비스는 **3계층 구조**다.

| 계층 | 역할 | 코어 role |
|---|---|---|
| 운영자(본부) | 코치 승인·인스트루먼트 관리·전체 통계·민감정보 열람 | `admin` |
| 코치·강사 | 차수 개설·참여 관리·리포트 열람·돌봄 알림 수신 | `coach` |
| 참여자(멤버) | 진단 응답 | `user` |

코치는 코칭 전문가이나 온라인 시스템 초보자다. **코치 UX는 극단적으로 단순·안내형으로 짓는다.**

---

## 2. 아키텍처 개관 — 3층 모델

```
┌──────────────────────────────────────────────────────────┐
│  3층 — 진단별 전용 UX        퓨처나우 UX  │  SAIL UX        │  ← 진단마다 따로
├──────────────────────────────────────────────────────────┤
│  2층 — 진단별 전용 엔진      퓨처나우 엔진 │  SAIL 엔진      │  ← 코드 비공유
├──────────────────────────────────────────────────────────┤
│  ── 플러그인 계약(contract) ── 응답흐름·채점·리포트·알림 4종 │  ← 한 규격
├──────────────────────────────────────────────────────────┤
│  1층 — 공유 코어 런타임 (채점하지 않음)                     │  ← 한 번만 만듦
│       신원·계정 · 차수·코치·가입 · 응답봉투 · 알림·돌봄     │
│       공용 UI·디자인 토큰                                   │
├──────────────────────────────────────────────────────────┤
│  Supabase 공유 DB · Vercel — 동일 auth.users · 교차 자동 로그인 │
└──────────────────────────────────────────────────────────┘
```

**핵심 원리 (이것이 "질 저하 절대 금지"를 구조로 보장한다)**

1. **코어는 채점하지 않는다.** 코어는 신원·차수·코치·응답봉투·알림·공용UI를 *운영*하는 런타임(OS)이다. 채점 로직 한 줄도 코어에 두지 않는다.
2. **엔진은 쌓이지 않고 꽂힌다.** 퓨처나우 엔진과 SAIL 엔진은 코드를 한 줄도 공유하지 않는다. 둘 다 코어의 플러그인 계약을 *각자* 구현해 꽂는다. 상속(stack)이 아니라 플러그인(plug).
3. **메뉴는 콘솔 차원이다.** 한 플랫폼·한 로그인·한 콘솔 안에서 코치가 진단을 메뉴로 고른다. 고르는 순간 그 진단 전용 모듈이 작동한다. 아래(계정·차수·코치·알림)는 하나로 흐르고, 위(진단의 살)는 완전히 갈라진다.

### 2.1 빌드 전략 — "C를 전제로 한 A"

| 단계 | 전략 | 이유 |
|---|---|---|
| 현재 ~ 퓨처나우 1차 완성 | **A안**: 코어+퓨처나우 단일 레포·단일 작업공간(클코1) | 계약이 진화 중이고, 퓨처나우가 그 계약의 검증자다. 한 몸으로 움직여야 검증이 빠르다 |
| 계약 안정화 후 | **C안**: 코어를 독립 패키지로 추출 | 계약이 굳으면 병렬이 안전. SAIL·퓨처나우가 추출된 코어에 의존 |

지금부터 **폴더는 C를 전제로 분리**해 둔다(§4). 나중에 패키지로 떼어낼 때 이사가 쉽도록.

---

## 3. 기술 스택 (확정)

SAIL 노선을 채택한다. 형제 시스템 분석에서 검증된 조합이다.

| 영역 | 채택 | 비고 |
|---|---|---|
| 프레임워크 | Next.js 16 + React 19 + TypeScript | App Router. **확정 버전: Next 16.2.9 · React 19.2.4 · TS 5.** Next 16 규약: 세션 갱신 미들웨어는 `proxy.ts`(nodejs 런타임), `cookies()`/`headers()` **async** → core/auth 구현에 반영. **breaking change 다수 → CLAUDE.md §6** |
| 백엔드·DB | Supabase (Postgres + Auth + RLS) · @supabase/ssr 0.12 | **거점 확정: SAIL 프로젝트 `zdoytzmvcafcebytttrm` 승격**(ADR-13). 같은 `auth.users` 공유 → 교차 자동 로그인. SAIL(`sail-diagnosis`)·TCI 와 점진 통합 |
| 상태관리 | Zustand | SAIL 계승. *UI 단계에서 설치* |
| 시각화 | Recharts 3 | 레이더·막대·나침반 게이지. *UI 단계에서 설치* |
| 리포트 | @react-pdf/renderer | TCI 깊이 계승. 개인 분석보고서 다중 페이지. *UI 단계에서 설치* |
| 스타일 | Tailwind CSS 4 | 디자인 토큰 기반 |
| 애니메이션 | framer-motion | SAIL 계승 (절제 사용). *UI 단계에서 설치* |
| 검증(zod) | zod 4 | `answers`·`subjectProfile` 경계 검증(CLAUDE §9) |
| 테스트 | Vitest 4 (+ vite-tsconfig-paths) | 단위·로직·RLS 격리 테스트. 컴포넌트 렌더는 디자인 확정 후(jsdom) |
| AI 해석 | Claude API 게이트웨이 | 분석보고서 자동 문구. **위치는 plan.md 미결**(Railway vs Supabase Edge Function) |
| 배포 | Vercel | |

> **구현 메모(v0.2)**: 토대 단계에서는 **비시각 의존성만** 설치했다(Next·React·Tailwind 4·@supabase/ssr 0.12·zod 4·Vitest 4). UI 전용 라이브러리(Recharts·framer-motion·@react-pdf/renderer·Zustand)는 디자인 시스템 확정 단계에서 설치한다.

lifegraph의 Firebase는 **미채택**. 통합 시 Supabase로 이관(plan.md).

---

## 4. 코드베이스 구조 (C-ready)

```
/src
  /core                ← 1층 공유 런타임. 향후 독립 패키지로 추출
    /auth              인증·세션·현재 사용자
    /identity          users·role·신원 필수성 정책 강제
    /cohort            차수·가입코드·참여(enrollment)·코치 승인
    /response          응답 봉투 저장/조회 · 응답 러너(ResponseRunner)
    /alert             알림 전달·돌봄 명단
    /ui                공용 컴포넌트 (Button·Card·Slider·ProgressBar …)
  /contracts           ← A·B 계약 타입. 코어·진단이 공유(서로 직접 참조 금지)
    core-context.ts    A: CoreContext (코어 → 진단)
    instrument.ts      B: InstrumentModule 및 4종 플러그인 인터페이스
    domain.ts          공용 도메인 타입 (Role·Wave·CoreUser·ResponseEnvelope …)
    vocab.ts           전 서비스 공통 규약 **값**(런타임 상수 — 성별). domain.ts(타입 전용)와 분리·직접 import(ADR-46)
  /instruments
    /futurenow         ← 퓨처나우 전용 엔진+UX (계약 구현체)
      flow.ts          B① 응답 스키마
      scoring.ts       B② 7규칙 채점
      report.tsx       B③ 화면·PDF·그룹 리포트
      alerts.ts        B④ Red Flag·돌봄 트리거
      schema.ts        answers·profile zod 스키마
      profileVocab.ts  퓨처나우 프로필 도메인 값(종교·KPC형식·생년상한 — 성별 제외, ADR-46)
      copy.ts          참여자 노출 문구(존대체)·리포트 명명(어휘 분리)
      /values          가치 카드(ADR-121) — cards·ordering·stages·copy·pairwise·reflection
    (/sail)            ← 추후 이관 (plan.md)
/supabase
  /migrations          타임스탬프 마이그레이션 (절대 기존 파일 수정 금지)
```

**경계 규율**: `/core`는 `/instruments`를 참조하지 않는다. 진단도 코어를 직접 참조하지 않는다. 양쪽 다 `/contracts`만 바라본다. 이 한 겹이 나중에 레포를 갈라도 계약이 깨지지 않게 막는다.

---

## 5. 데이터 모델

### 5.1 신원 vs 진단별 참여 프로필 — 분리선 (ADR-02)

| 항목 | 자리(소유) | 저장 위치 | 성격 |
|---|---|---|---|
| 계정 신원(이메일·인증·role) | 코어 | `users` | 한 사람=한 계정, 전 진단 공유, 항구적 |
| 이름·전화번호 | **코어 공용 필드** | `users` / `user_contacts` | 항구적 신원, 한 번 받아 재사용. 필수성은 정책 |
| 닉네임 | 코어 | `users.nickname` | 표시명(실명일 수도 가명일 수도) |
| 차수 참여 | 코어 | `enrollments` | 참여자 ↔ 차수 |
| 성별·생년·종교·신앙연수(신원 부가) | **코어** | `user_profiles` | 계정 단위 재사용·users 본체 미오염(ADR-04 격리 선례·ADR-37). 전부 nullable — 필수성은 폼/IdentityPolicy |
| 참여 스냅샷(성별·생년·종교·신앙연수 사본 + 참여계기 motivation) | **진단** | `responses.subjectProfile` | 응답 시점 스냅샷(불변) — 신원 부가는 계정(`user_profiles`)에서 복사·박제, 참여계기는 진단 고유(ADR-32·44) |
| 문항 응답 | 진단 | `responses.answers` | 코어 불가시 |

**저장처 분리(S1~S4 확정)**: 이름=`users.name` · 전화=`user_contacts`(민감 게이트) · 신원 부가(성별·생년·종교·신앙연수)=`user_profiles` · 코치 인증번호(KPC)=`coach_applications.kpc_number`. 각 값의 소유·민감도가 다르므로 물리 분리한다 — `CoreUser`엔 phone·profile·KPC를 싣지 않고 게터(`getPhone`·`getProfile`·`getMyCoachKpc`)로만 접근(ADR-04 최소노출 계승·ADR-37).

**허용값 소유 계층(A2·ADR-46 / 값 A4·ADR-48)**: 성별 허용값 `GENDERS = ['남','여']`(전 서비스 일관 2값 — 지휘부 확정)은 **전 서비스 공통 상수**(`src/contracts/vocab.ts` — 형제 인스트루먼트도 공유), 종교 목록·KPC 형식·생년 상한은 **퓨처나우 소유**(`src/instruments/futurenow/profileVocab.ts`). 성별 상수는 `user_profiles.gender`의 **SQL CHECK 와 값이 일치해야 하며**(TS·SQL 이원 원천 — SQL은 상수 미참조), 값 변경 시 마이그(CHECK+`handle_new_user` sanitize)를 반드시 동반한다(A4 마이그 `20260702002311` 이행 — DROP→변환→ADD 순서).

### 5.2 신원 필수성 정책 (ADR-03)

이름·전화번호는 공용 필드이되, 필수/선택은 **(진단 × 역할)** 정책으로 진단이 선언하고 코어가 강제한다. (아래 표는 **코어 기본 정책**이다. 진단은 `IdentityPolicy`로 자기 요건을 강화할 수 있다 — futurenow는 참여자 이름을 required로 올린다·ADR-42, 아래 문단.)

| 필드 | 코치 | 참여자 | 운영자 |
|---|---|---|---|
| 이메일 | 필수 | 필수 | 필수 (코어 고정) |
| 이름 | 필수 | 선택 | 필수 |
| 전화번호 | 필수 | 선택 | 선택 |

참여자에게는 실명·전화를 **강제하지 않는다.** 퓨처나우가 실명제를 지향하더라도 코어가 참여자에게 실명을 강제하지 않는다(권장 수준). 실명제라는 진단별 정책은 진단 안에서 다룬다.

**퓨처나우 한정 강화(UX통합가입, 2026-07-01 · ADR-42)**: futurenow는 참여자 이름도 **필수**로 선언한다(코치 명단 식별). 이는 코어 ADR-03을 **반전하지 않는다** — `CoreUser.name`은 여전히 nullable이고 DB 불변식도 아니다. 필수성은 진단이 소유한 `IdentityPolicy` 데이터(`futurenowIdentityPolicy.byRole.user.name = 'required'`)와 폼 게이트로만 강제한다. 성별·생년의 '필수'도 같은 방식(폼 게이트, DB nullable)이다.

### 5.3 전화번호 — 민감 채널 (ADR-04)

전화번호는 **민감 채널**이다. RLS는 행을 가리지 **열을 못 가리므로**, 전화번호를 **물리적으로 분리**한다. 이 근거는 열람 범위가 어떻게 바뀌든 그대로다 — `user_contacts` 가 존재하는 이유가 그것이다.

- `users`에서 전화번호 제거 → `user_contacts` 테이블로 격리.
- 본인은 자기 번호 열람·수정. 운영자는 전체 열람. **테이블 RLS 에 코치 정책은 없다**(직접 SELECT 전면 차단 — 코치 상호 비열람 포함).
- `CoreUser` 타입에 `phone` 없음 → 진단 엔진·콘솔이 `currentUser()`로 전화번호를 받을 경로 자체가 없음.

**열람 범위 정정 (ADR-75·78, 2026-08-26 문서 정합).** 초판은 *"운영자 전용. 같은 차수 코치도 볼 수 없다"* 고 단언했으나 **더 이상 사실이 아니다.** ADR-75 가 그 완화를 명시 결정했고, `cohort_member_detail`(DEFINER · `is_admin OR is_cohort_coach`)이 **자기 조원에 한해** 인도자에게 전화번호를 내주며 `MemberProfilePanel`(ADR-78)이 화면에 띄운다. 완화의 통로가 **테이블 RLS 가 아니라 DEFINER RPC** 라는 점이 요점이다 — 위 셋(격리·정책 부재·타입 부재)은 한 줄도 무너지지 않았고, 넓힌 곳만 함수 하나로 좁게 열렸다.

**접근은 "게이트 하나"가 아니라 "게이트 없는 경로가 없음"이다.** 전화번호 표면 전수(실측 2026-08-26)는 **egress 넷 + 전화번호를 키로 받는 anon 함수 둘**이며 ADR-122 ⑫에 목록으로 있다. 넷 모두 게이트가 걸려 있다. **목록을 줄여 적지 않는다** — 불완전한 목록은 목록이 없는 것보다 나쁘다. 없으면 세어 보지만 있으면 믿는다.

### 5.4 코어 테이블 (초기 마이그레이션 사양)

SAIL의 검증된 스키마를 토대로 한다. 컬럼·정책의 출처는 SAIL `20260528*` 마이그레이션이다.

> **거점 확정·적용 완료 (directive 2026-06-26 · ADR-13~16)**: 거점 = **SAIL 프로젝트** `zdoytzmvcafcebytttrm` 를 승격(신규 프로젝트 미신설, 같은 `auth.users` 공유). 코어 테이블은 SAIL 의 기존 **`public` 스키마**에 둔다(구 "core 스키마 분리" ADR-11 **폐기**). `public.users` 를 **재사용**해 코어 신원으로 삼되 `full_name`→`name` 표준화·`phone` 분리(`user_contacts`)로 정비한다. 확정 실명: `users`·`user_contacts`·`cohorts`(`instrument_id` 보유)·`enrollments`·`responses`·`alerts`. SAIL 잔존 테이블 `groups`/`group_members`/`results` 는 유지(점진 통합 — 클로드3 조율). **마이그레이션 `20260626120000_core_platform_upgrade.sql` 은 거점에 적용 완료**(원격 version `20260626064658`).
>
> **참여 식별자 — `instrument_id` 규약**: 퓨처나우 = `'futurenow'`, SAIL = `'sail'`. 진단 간 격리는 `instrument_id` + 차수(코치) RLS 로 성립(ADR-14). 코어 신원 모델에는 `coach_applications` 가 포함되며, 코치 승인 흐름은 **구현·적용 완료**다: `decide_coach_application`(원자 승격 DEFINER, 마이그 `20260628095527`·ADR-24)·`set_user_role`(운영자 직접 승강, `20260629000321`·ADR-28)·자기강등/자가승격 방지(`20260629100002`).
>
> **코치 레코드화 (S1~S4, 2026-07-01)**: `coach_applications`는 라이브에만 있고 repo에 `CREATE`가 없던 드리프트를 backfill 마이그(`20260701061038`)로 정확 복원해 편입했고, **UNIQUE(user_id)**(한 사람=한 행 — 신청제·지정제 공용, 재신청=행 갱신)와 **`kpc_number`**(형식 CHECK, 실검증은 plan.md)를 더했다(`20260701061054`). 자가 신청 = `create_coach_application`(self-scoped DEFINER·status='pending' 고정·upsert·ADR-39, `20260701070126`), 코치 본인 KPC 보완 = `set_my_coach_kpc`(role=coach·형식검증·role/status 무오염·ADR-40, `20260701080201`). 참여 프로필은 코어 별도 테이블 `user_profiles`(ADR-37, `20260701061118`) — `handle_new_user` name 키 버그 수정 + 프로필 저장(ADR-41, `20260701061220`).

```
users              -- auth.users 1:1. id·email·name·nickname·role(user/coach/admin)
user_contacts      -- 민감 신원 격리. user_id PK·phone. RLS: 본인+운영자만
cohorts            -- 코치 소유 차수. id·coach_id·instrument_id·name·code(가입코드)
                   --   ·status(active/archived)·max_members·expires_at·post_opened_at(사후 개시 시점 nullable, ADR-55)
                   --   code CHECK = '^[…]{5}$' OR 'JOINF'(general 예약어·ADR-63). general 차수 1건(운영자 소유·상시 active·무기한) = 공개 체험 진입
enrollments        -- 차수 참여 조인. (cohort_id, user_id) PK·joined_at
user_profiles      -- 신원 부가(계정 단위). user_id PK·gender·birth_year·religion·faith_years·updated_at. RLS 본인+운영자 SELECT(코치 열람은 DEFINER RPC cohort_member_profiles). ADR-37
coach_applications -- 코치 자기등록·지정 승인. UNIQUE(user_id)(한 사람=한 행)·status(pending/approved/rejected)·motivation·kpc_number(KPC 형식 '^KPC[0-9]{5}$')·reviewer 흔적. ADR-39/40
responses          -- 응답 봉투. id·instrument_id·cohort_id·user_id·wave
                   --   ·answers(JSONB)·subject_profile(JSONB)·created_at. immutable(ADR-09)
                   --   (user,cohort,wave) UNIQUE 없음 — 재진단 허용의 의도된 설계(ADR-33·latestPerUser dedup). 트랙 B에서 미추가 확정
alerts             -- Red Flag·돌봄. response_id·cohort_id·severity·reason. 점수·원문 미적재
value_assessments  -- 가치 카드(ADR-121). UNIQUE(user_id,cohort_id)·stage 4값·progress/candidates JSONB.
                   --   쓰기 전량 DEFINER RPC(테이블 쓰기 회수·D1). SELECT=본인+차수 인도자+운영자.
response_drafts    -- 제출 전 작성본(중간저장). PK(user_id,cohort_id,wave)·instrument_id·answers(JSONB)·updated_at
                   --   가변(upsert 덮어쓰기). RLS 본인 한정. 제출 성공 시 정리. responses 와 분리(ADR-34, 진단-1B)

-- ── 1차 신설(ADR-122·123) ────────────────────────────────────────────────────
memberships        -- 회원 상태. user_id PK·status·valid_until·decided_by/at·decision_note·signup_note
                   --   **저장 enum 에 'cohort' 가 없다** — 차수 소속은 산출이지 저장이 아니다(ADR-122 ①).
                   --   판정은 member_state() 한 곳: held > cohort > 저장 > pending, 만료는 산출.
                   --   cohorts.kind('seminar'/'general'/'trash'/'test')가 그 판정의 축이다.
news_posts         -- 소식. published_at IS NULL = 초안(상태 컬럼 없음 — 발행 시각이 곧 공개 여부).
                   --   본문 평문 렌더. 쓰기 DEFINER RPC(news_upsert/news_delete), 읽기 RLS 둘.
library_items      -- 자료실 3단(tier public/member/coach). 파일은 **비공개 버킷 하나** + 만료형 서명 URL.
                   --   'public' 은 비로그인 열람 허용이라는 뜻이지 공개 버킷이 아니다(ADR-123 ③).
contact_messages   -- 문의 적재(메일 아님 — SMTP 부재 실측). anon 도 쓴다. 가드는 contact_submit 안에만.

-- ── 2차 신설(ADR-124) ────────────────────────────────────────────────────────
feed_posts         -- 동행 피드 글. cohort_id·author_id·body(≤2000)·photo_path·deleted_at/by.
                   --   **없는 컬럼이 설계다**: title·category·tags·updated_at·집계·session_no 전부 없다.
                   --   CHECK: 살아 있는 행은 본문 또는 사진이 있어야 한다(묘비는 예외).
feed_comments      -- 1단 댓글. **parent_id 컬럼이 없다**(대댓글 금지를 스키마가 보장). cohort_id 미복사.
feed_reactions     -- 이모지 응원. PK(post_id,user_id) = 한 사람 한 반응(수가 점수가 되지 않게).
                   --   CHECK 는 feed_emojis() 단일 출처를 본다. TS FEED_EMOJI 와는 통합테스트가 짝을 잠근다.
news_comments      -- 소식 댓글. 비로그인 읽기 ○ / 쓰기 ✕. 발행된 글에만 붙는다.

-- ── 3차 T-6 에서 채운 누락분 ─────────────────────────────────────────────────
-- **본 저장소 소유**
checkins           -- 회차 갈무리(ADR-80). (cohort_id,user_id,session_no) · answers(JSONB)
                   --   ·has_content(쓰기 시점 계산)·submitted_at·edit_count. 쓰기 전량 DEFINER RPC.
cohort_sessions    -- 회차 일정(ADR-80). (cohort_id,session_no) PK · held_at·opens_at·closes_at.
                   --   행이 없으면 '준비 중'(정상 상태). 개폐 판정의 유일한 출처.
report_interpretations -- 코치 리포트 해석(ADR-36). ai_content(불변·감사) + coach_content(수정본).
                   --   유효 문구 = coach_content ?? ai_content. 코치·운영자만(RLS).
user_consents      -- 개인정보 동의(ADR-76). (user_id,type) · version·agreed_at.
                   --   type: privacy_use · sensitive_use · coach_pledge · forum_match(ADR-122).

-- **본 저장소 밖에서 적용됨** — 파일이 supabase/migrations 에 없다(3차 T-6 발견)
deltanote_preregistrations -- 원장 `20260816231058`. 저장소에 CREATE 가 없다.

-- **SAIL 소유 · 본 작업공간 불가침**(CLAUDE §4). public 스키마를 공유한다.
results            -- SAIL 결과(immutable). 원장 `20260528000000`. 위 '불변 원칙'이 계승했다는 그 표다.
groups             -- SAIL 그룹. 원장 `20260528120000`
group_members      -- SAIL 그룹 참여. 원장 `20260528120000`
```

> **§4 드리프트 정정 2차(2026-08-27 · 3차 T-6).** 위 "3차 T-6 에서 채운 누락분" 여덟은
> **2차 정정에서도 빠져 있었다.** 그때 채운 것은 1·2차가 만든 표뿐이고, 그보다 **먼저 있던 것**
> (갈무리·해석·동의)과 **다른 주인의 것**(SAIL 셋 · 저장소 밖 하나)은 세지 않았다.
> **"무엇을 세는가"를 정하지 않고 세면 두 번 세도 빠진다.** 이번에는 라이브 `information_schema`
> 전수(26)와 대조했다 — 문서 18 → 26, 차이 0.
>
> **§4 드리프트 정정(2026-08-27).** S-1~S-4 의 신설 표 넷(`memberships`·`news_posts`·`library_items`
> ·`contact_messages`)이 ADR 행에만 적히고 이 블록에는 올라오지 않았다. 2차 표 넷을 넣으면서 함께 채웠다.
> 이 블록이 스키마의 단일 진실이라고 적어 두고 실제와 어긋나 있으면, 다음 사람은 여기를 믿지 않고 DB 를 다시 센다.

**불변 원칙**: `responses`·`alerts`는 INSERT/SELECT만, UPDATE/DELETE 정책 없음(차단). SAIL의 immutable `results` 철학 계승. **예외**: `response_drafts`는 작성 중 보존이라 가변(본인 upsert/delete만, ADR-34) — 정식 응답(`responses`)의 불변성과 무관. `user_profiles`·`coach_applications`도 계정/코치 신원이라 본인 upsert 가변(신원값 정정·KPC 보완) — 불변 원칙 대상 아님(users·user_contacts 동류).

**가입코드 형식**: 5자리 영숫자, 혼동 글자(0/O/1/I/L) 제외 (SAIL 계승).

### 5.5 가입·인증 흐름 (Q1~Q3 확정 · 2026-06-26)

plan Q1~Q3 을 확정한다(과거 plan.md §3 → 본 절로 승격).

| # | 결정 | 근거 |
|---|---|---|
| Q1 | **로그인 기반**(비로그인 미허용) | 실명제·인도자 전용. 현 RLS(`auth.uid()` 요구)와 일치 |
| Q2 | **차수 가입 시 계정 생성** | enrollment·응답이 `auth.uid()` 에 묶임 |
| Q3 | **페어링 키 = `user_id + cohort_id + instrument_id`**, `wave` 로 사전/사후 구분 | 로그인 기반 자연 키 |

**흐름**(`/join`): 코드 입력 → `previewCohortByCode`(차수 공개 메타) → 로그인/신규 가입 →
`enrollByCode`(정원·만료·중복 검사 후 `enrollments` INSERT) → `ResponseRunner`(saveResponse) →
**완료(§7.5)**. 현 RLS 가 이미 이 전제로 짜여 정책 변경이 없다.

완료 화면의 *갈망 거울*(②방향·③갈망·⑤믿음)은 **퓨처나우 인스트루먼트 소유**(`participantMirror(scores)`)이며,
앱 액션 `finalizeResponse` 가 채점(B②) 후 호출해 반환값에 실어 보낸다 — **CoreContext·InstrumentModule 인터페이스
무변경**(G1 보호, ADR-27). 참여자엔 측정·severity·돌봄 신호 0건(코치 경로 전용). 거울 산출 실패 시 ①헤더+④핸드오프만
보이는 우아한 저하. (남은 미결: plan Q4 리포트 열람 주체 · Q5 AI 문구 검수.)

**라우트 맵**(전부 기존 메서드 위 배선 — 계약 변경 0):
| 라우트 | 주체 | 데이터 |
|---|---|---|
| `/` | 방문자 | 현관 — 참여하기→/join · **로그인(전 역할 공용)→/login**(상단 "이미 참여하셨나요? 로그인" + 하단 로그인 버튼) · 인도자 회원가입→/signup. 정적, 데이터 없음 |
| `/home` | 모든 로그인 사용자 | 통합 홈 허브(A′-1·ADR-51). 게이트: **미인증→/login 만**(역할 리다이렉트 제거·전원 개방). 인사 + (코치·운영자)**운영 카드**(→/coach·/admin) + [코드로 세미나 참여]→/join + [내 차수]→/my/cohorts. 셸 헤더+로그아웃 |
| `/my/cohorts` | 모든 로그인 사용자 | 내 차수 목록(Step 1.2·A′-1 개방). `listMyCohorts`(my_cohorts DEFINER RPC, auth.uid() 스코프) — 차수명·코치명·status·사전/사후 진행·**post_opened**. 사전 미완→/join · **사후 개시·미완→[사후 진단하기]→/join?wave=post(B-2)** · 그 외→[내 리포트]. 게이트: 미인증→/login |
| `/my/cohorts/[cohortId]/report` | 전 로그인 사용자(본인 참여분) | 내 리포트 **순화 뷰**(Step 1.3, ADR-27/30·role 게이트 제거 ADR-59 — RLS 본인 스코프). `listResponses`(본인 pre·post, self-read)→각 wave `latestPerUser`→score→`participantMirror`→`MirrorView`. **사전·사후 모두 있으면 미러 2개 나란히 비교(B-3·ADR-57), 하나면 단독**. 측정·severity 0(순화 유지). 계약·DB 무변경 |
| `/my/cohorts/[cohortId]/values` | 전 로그인 사용자(본인 참여분) | **가치 카드**(ADR-121). 72장을 좁혀 핵심가치 3개를 찾는 2단계 과제 — 1차(1회차 이전·집)와 2차(1회차 중). 한 라우트가 `stage` 로 화면을 가른다(중단·재개 주소가 상태가 되지 않게). 게이트 두 겹: `/my` 접두라 미인증은 proxy 가 차단하고 **차수 소속은 페이지가 `listMyCohorts()` 로 확인**. 쓰기는 서버 액션→CoreContext→DEFINER RPC(직접 테이블 접근 0) |
| `/c/[code]/values` | 로그인 사용자 | **가치 카드 짧은 공유 경로**(ADR-121). 갈무리 QR 경로와 동형 — 코드→차수 해석→미인증 `/login?returnTo=`→UUID 경로로 redirect. **비멤버 처리가 갈린다**: 인도자 차수는 안내로 정지(ADR-89 — 링크 유출 시 실명제 차수 자기등록 차단), `GENERAL_CODE` 만 가입 허용(체험은 자기등록이 목적) |
| `/coach/cohort/[cohortId]/values` | 코치/운영자 | **가치 카드 열람**(ADR-121). `listCohortValueAssessments` — 같은 차수만(RLS). 최종 3개·라벨·대조 원문·정합 판정. 참여자 화면에 열람 고지를 둔 근거가 이 화면이다 |
| `/login` | 전 역할 | `signInWithPassword` → **전원 `/home`**(A′-1 `loginOutcome` 통일 — 역할 분기 제거). 로그인 전용(가입은 /signup·/join) |
| `/signup` | 스태프/일반 | `signUp`(트리거가 users role=user 생성) → 세션 시 `/home`(A′-1 loginOutcome). 확인 필요 시 안내. **출구(A′-3): 로그인·현관(`/`)** — 막다른 상태 해소 |
| `/reset` | 공개 | 비밀번호 재설정 요청(Step 2.3). `resetPasswordForEmail`(redirectTo=origin/reset/confirm). enumeration 방지(동일 안내). 비번=auth.users. **출구(A′-3): 로그인·현관** |
| `/reset/confirm` | 공개 | 새 비밀번호 설정(Step 2.3). 복구 세션 게이트(있을 때만 `updateUser`) → `/home`. 만료 시 재요청 안내. **출구(A′-3): 로그인·현관(전 단계)** |
| `/account` | 로그인(3페르소나) | 내 정보(Step 2.5·**A2 완결**). 이름=`setName`(users.name)·**연락처(전화·주소·계좌)=`setContact`**(user_contacts, 부분 upsert·ADR-76)·**프로필(성별·생년·종교·신앙연수)=`setProfile`**·**(코치)KPC=`setMyCoachKpc`**·비번=`updateUser`. 프리필=`getContactDetail`/`getProfile`/`getMyCoachKpc`. **모든 필드 나중에 수정·추가 가능**(지휘부 2026-07-09). role 쓰기 경로 없음(2.S2 봉쇄). 게이트 미인증→/login |
| `/admin` | 운영자 | 두 섹션 구분(A3) — **승인 대기**(`listCoachApplications('pending')`→`decideCoachApplication` 승인/거절, 승인 시 user→coach 원자 승격) + **멤버 관리**(`listUsers`+`setUserRole` 승격/강등, 이름 클릭→세부 `memberDetailAction`[신원+활동] ADR-71, 세부 내 하드삭제 `deleteMember` ADR-70). 운영자 게이트(§8.6) |
| `/join` | 참여자 | preview→enroll→runner→finalize(거울). 코드 진입(참여자 가입 결속). **`?code=` 초대 링크 deep-link(A5)**. **`?wave=post` 사후 진입(B-2)** — getSchema(post)·wave='post' 저장(기본 pre). `?cohort=` 재진입과 함께 실림. **general 체험(D-2·ADR-63)**: CodeInput 하단 '체험 진단 시작하기' → `onCode(GENERAL_CODE='JOINF')`(딥링크 동형) → CohortPreview `isGeneral`(인도자·인원 숨김·체험 문구) → 기존 enroll→runner 합류. 사전 wave 고정(general 사후 없음) |
| `/coach` | 코치/운영자 | 차수 목록 = **운영자 `listAllCohorts`(전체 인도자 차수·소유자명 표시)** / 인도자 `listCohortsByCoach(me.id)`(ADR-74) + 차수별 `buildCohortRoster`(먼저 챙길 분=`listAlerts` care/red_flag). `/coach/cohorts`(모든 차수)도 동일 분기. **(운영자) 승인 대기 N건 배너→/admin**(콘솔 진입 시). admin 로그인 착지는 **/home**(loginOutcome 전원 /home·ADR-51) — 홈 '본부' 카드에 승인 대기 건수 노출(ADR-59, 배너와 이원) |
| `/coach/new` | 코치/운영자 | `createCohort` |
| `/coach/cohort/[cohortId]` | 코치/운영자 | `getCohort`·`listEnrollments`·`listResponses`·`listAlerts`·`listCohortMembers` → 3숫자·3묶음 + 관리(마감·정원=`updateCohort`, **사후 진단 개시=`openPostWave`**·ADR-55). **명단 행 휴지통=`removeCohortMember`**(차수에서 제거·2단계 컨펌·소유 코치/운영자만·ADR-73). **뒤로=진입 출처(`?from=` 콘솔/목록, 기본 목록·A′-4)** |
| `/coach/cohort/[cohortId]/report/[responseId]` | 코치/운영자 | `getResponse`→B② `score`→`ReportScreen`(재사용). 접근=responses RLS(차수 코치+운영자+본인). 참여자 UI 경로 없음 |

---

## 6. 보안·RLS

### 6.0 저장소 규율 — 참여자 원문은 커밋하지 않는다 (ADR-96)

**금지 대상은 개인을 지목할 수 있는 형태다**(ADR-99 로 문언 정정). 구체적으로 — **실명·연락처, 그리고 실명이나 식별 가능한 속성과 결합된 발언 원문.** 녹취록·상담 기록처럼 둘이 함께 있는 문서는 어떤 저장소에도 커밋하지 않는다. git 은 삭제가 사실상 불가능하므로 유입 자체를 막는다.

**이름과 분리된 발언 원문은 무방하다.** 요지로 바꾸면 판단의 근거가 얇아지므로 — 예컨대 ERRC 기입 순서를 뒤집은 판단(ADR-98)은 "잘못된 부분에 집중하니 불편했다"는 **인과**에 걸려 있어, 요지 서술로는 뒷사람이 그 결정을 감리할 수 없다 — **결합만 끊고 원문은 남긴다.**

판정은 **관찰**이지 저울질이 아니다: 이름이 붙어 있는가만 본다. **결합 여부가 애매하면 커밋하지 않고 먼저 보고한다.** 관찰로 끝나지 않는 경우만 보고 대상이다.

`.gitignore` 는 **기본 차단 + 명시 허용**이다(ADR-97) — `/docs/*` 로 한 겹 막고 `!/docs/tasks/`·`!/docs/reports/` 만 연다. 앞 슬래시는 저장소 루트 앵커다(없으면 하위 디렉터리의 다른 `docs` 까지 함께 막힌다). **문법 함정**: `/docs/` 로 디렉터리 자체를 제외하면 그 안의 negation 이 동작하지 않으므로 반드시 `/docs/*` 여야 한다 — 이 구조를 단순화하지 말 것. **지시서·보고서 두 폴더도 공개 저장소에 올라간다** — 실측 수치·집계·문안은 되지만 실명·발언 원문은 안 되고, 지시서에 실명이 섞여 오면 클코1 은 **커밋하지 않고 먼저 보고한다.** 이 규율은 저장소 공개 여부와 무관하다 — private 으로 바꿔도 협업자가 늘면 접근이 함께 넓어지고 이력은 여전히 지우기 어렵다. **녹취록은 버전 관리가 필요한 물건이 아니다.**

### 6.1 SECURITY DEFINER 헬퍼 (ADR-05) — SAIL 핫픽스 계승

RLS 정책이 서로의 테이블을 참조하면 무한 재귀(Postgres 42P17)가 난다. SAIL이 이미 겪고 해결했다. **권한 판정을 `SECURITY DEFINER` 함수로 감싸 재귀를 끊는다.** 이 패턴을 그대로 계승한다.

```
is_admin(uid) · is_group_coach(group_id, uid) · is_group_member(group_id, uid) · user_role(uid)
-- 모두 SECURITY DEFINER, STABLE, SET search_path=public. anon·authenticated 에 EXECUTE 부여.
```

> **구현 주 (ADR-13~16, 적용됨)**: 헬퍼는 SAIL 의 기존 **`public` 스키마**에 둔다(구 ADR-11 의 `core` 스키마안 폐기). 기존 SAIL 헬퍼 `is_admin`·`user_role`·`is_group_coach`·`is_group_member`(SAIL `groups` 용)는 **재사용**하고, 차수(cohort)용 신규 헬퍼 `is_cohort_coach`·`is_cohort_member`·`resolve_cohort_by_code` 를 더한다(모두 `SECURITY DEFINER`, `SET search_path=public`, `anon·authenticated` EXECUTE). 코어 RLS(`cohorts`·`enrollments`·`responses`·`alerts`)는 이 cohort 헬퍼를 호출한다. `resolve_cohort_by_code(code)` 는 차수 **UUID** 를 반환한다(비참여자 가입용; 이후 `enrollments` INSERT 로 가입). SAIL 전화 헬퍼 `email_by_phone`·`phone_exists` 는 `user_contacts` 기준으로 재지정돼 SAIL 전화 로그인을 보존한다(ADR-16).

### 6.2 가시성 매트릭스 (ADR-06)

| 데이터 | 본인 | 같은 차수 코치 | 다른 코치 | 운영자 |
|---|---|---|---|---|
| 이름·닉네임 | ○ | ○ | ✕ | ○ |
| **전화번호** | ○(본인) | **△(자기 조원만 · ADR-75·78)** | ✕ | ○ |
| 응답·점수 | ○(본인) | ○ | ✕ | ○ |
| 알림·돌봄 | — | ○ | ✕ | ○ |
| **회차 갈무리**(`checkins`) | ○(본인·전 회차) | ○ | ✕ | ○ | 
| ↳ 한 걸음(`step_what`·`step_when`·`step_blocker`) | ○ | △ | ✕ | △ |
| ↳ 바라는 점(`suggestion`) | ○ | △ | ✕ | △ |
| ↳ 편지 사진(`checkin-photos`) | ○(삭제 가능) | ○(삭제 ✕) | ✕ | ○(삭제 가능) |

**전화번호 △의 뜻(ADR-75·78)**: 인도자는 **자기 차수 조원에 한해** 본다. 통로는 테이블 RLS 가 아니라 DEFINER RPC `cohort_member_detail`(`is_admin OR is_cohort_coach`)이고, `user_contacts` 직접 SELECT 는 여전히 코치 전면 차단이다(§5.3). 다른 차수 조원은 ✕. 초판 표기 `✕` 는 ADR-75 완화 이후 갱신되지 않은 드리프트였고 2026-08-26 정정했다 — 같은 문서가 자기와 모순인 채로 두면 감사가 문언만 보고 표면을 다 봤다고 믿는다.

**갈무리 △의 뜻(ADR-86)**: 한 걸음은 참여자가 `step_private` 토글을 켜면 인도자·운영자 모두에게 **블록 전체가 가려진다**(내용 0, '비공개로 두었다'는 사실만 표시). 바라는 점은 참여자가 `suggestion_anon` 을 켜면 **이름을 떼고** 차수 단위 무기명 섹션에만 나온다. 둘 다 **권한 등급으로 뚫지 않는다** — 운영자도 코치와 동일하다(참여자에게 한 약속이지 역할 통제가 아니다). 그 밖의 모든 응답 키는 본인·같은 차수 코치·운영자가 읽는다(저장 화면 고지 `save.notice2` "적으신 내용은 인도자와 운영자가 읽습니다."가 근거).

**실명제·인도자 전용**: SAIL의 익명 URL 공유 모델(`user_id IS NULL` 허용 SELECT 절)은 **제거**한다. 퓨처나우 응답은 본인·같은 차수 코치·운영자만 본다.

---

## 7. 작업 A — 코어 경계 (CoreContext)

방향: **코어 → 진단** (코어가 제공하는 서비스 표면). 진단 엔진은 이걸 호출만 한다.

```ts
// /contracts/domain.ts
type Role = 'user' | 'coach' | 'admin';
type Wave = 'pre' | 'post' | null;            // 단발 진단은 null
type InstrumentId = string;                    // 'futurenow' | 'sail' | …
type FieldRequirement = 'required' | 'optional' | 'hidden';

interface CoreUser {
  id: string;                 // auth.users.id
  email: string;              // 전 역할 필수
  name: string | null;        // 공용 필드, 필수성은 정책이 결정
  nickname: string | null;
  role: Role;
  // phone 없음 — 민감 채널로 분리(getPhone 게이트로만 접근)
}

interface IdentityPolicy {
  byRole: Record<Role, { name: FieldRequirement; phone: FieldRequirement }>;
  // email은 항상 required라 정책 대상 아님
}

interface Cohort {
  id: string;
  coachId: string;
  instrumentId: InstrumentId;
  name: string;
  code: string;
  status: 'active' | 'archived';
  maxMembers: number;
  expiresAt: string | null;
}

interface Enrollment { cohortId: string; userId: string; joinedAt: string; }

// 가입 결정용 차수 공개 메타(Cohort 도메인 밖 — coachName·memberCount 포함, 민감정보 미포함). ADR-22
interface CohortPreviewMeta {
  id: string; name: string; coachName: string | null; instrumentId: InstrumentId;
  memberCount: number; status: 'active' | 'archived'; expiresAt: string | null;
}

interface ResponseEnvelope<TAnswers = unknown, TProfile = unknown> {
  id: string;
  instrumentId: InstrumentId;
  cohortId: string | null;
  userId: string | null;      // 실명제 진단은 NOT NULL을 진단이 강제
  wave: Wave;
  answers: TAnswers;          // 진단 소유 — 코어 불가시
  subjectProfile: TProfile;   // 진단별 참여 프로필 — 진단 소유
  createdAt: string;
}

interface SaveResponseInput<TAnswers, TProfile> {
  instrumentId: InstrumentId;
  cohortId: string | null;
  userId: string | null;
  wave: Wave;
  answers: TAnswers;
  subjectProfile: TProfile;
}

interface AlertInput {
  responseId: string;
  cohortId: string;
  severity: 'info' | 'care' | 'red_flag';
  reason: string;             // 진단이 명명 (예: '활력 위기신호')
  // 점수·원문은 싣지 않는다 — 측정/강의 어휘 분리. 맥락은 코치 콘솔에서만.
}

// 읽기용 알림(인도자 콘솔의 '먼저 챙길 분'). AlertInput(쓰기)에 id·createdAt 부가. ADR-23
// 돌봄 신호의 **저장된 출처** — 재채점으로 재유도하지 않는다(drift 방지).
interface Alert {
  id: string; responseId: string; cohortId: string | null;
  severity: 'info' | 'care' | 'red_flag'; reason: string; createdAt: string;
}

// 차수 멤버 최소 참조(id+name만). cohort_member_directory(DEFINER) RPC — users RLS 미확대, 최소 노출. ADR-24
interface MemberRef { userId: string; name: string | null; }

// 코치 신청(USER→COACH 승격 대기). 본부 §8.6 [승인 대기]. 읽기=운영자 전용, 결정=decide_coach_application RPC(원자 승격). ADR-24
interface CoachApplication {
  id: string; userId: string; applicantName: string | null;
  status: 'pending' | 'approved' | 'rejected';
  motivation: string | null; reviewedBy: string | null; reviewedAt: string | null;
  reviewNote: string | null; createdAt: string;
}
```

```ts
// /contracts/core-context.ts
interface CoreContext {
  // 인증·신원
  currentUser(): Promise<CoreUser | null>;
  requireRole(role: Role): Promise<void>;   // 비동기(ADR-18) — 현재 사용자 해석 후 역할 검사

  // 민감 채널 — 운영자 또는 본인만 성공. 그 외 호출 시 코어가 차단
  getPhone(userId: string): Promise<string | null>;
  setPhone(userId: string, phone: string): Promise<void>;

  // 차수·참여
  previewCohortByCode(code: string): Promise<CohortPreviewMeta | null>; // 가입 결정용 공개 메타(coachName·memberCount). ADR-22
  resolveCohortByCode(code: string): Promise<Cohort | null>;   // 차수 도메인 본체(가입-후/코치 경로)
  enrollByCode(code: string): Promise<Enrollment>;             // 코드로 현재 사용자를 차수에 가입(ADR-17)
  createCohort(input: { name: string; instrumentId: InstrumentId; maxMembers?: number; description?: string; expiresAt?: string | null }): Promise<Cohort>; // 차수 개설(코치/운영자). 앱측 코드 생성+재시도, DDL 0. ADR-25
  updateCohort(cohortId: string, patch: { name?: string; description?: string | null; maxMembers?: number; status?: 'active' | 'archived'; expiresAt?: string | null }): Promise<Cohort>; // 차수 부분수정(코치/운영자). coach_id·instrument_id·code·id 불변. ADR-26
  getCohort(cohortId: string): Promise<Cohort>;
  listCohortsByCoach(coachId: string): Promise<Cohort[]>;       // 코치 차수 목록(콘솔 홈). RLS: 본인/운영자. ADR-23
  listCohortMembers(cohortId: string): Promise<MemberRef[]>;    // 차수 멤버 id+name(코치/운영자). RPC cohort_member_directory. ADR-24
  listEnrollments(cohortId: string): Promise<Enrollment[]>;

  // 응답 봉투 (answers·profile 타입은 진단이 지정)
  saveResponse<A, P>(input: SaveResponseInput<A, P>): Promise<string>;
  getResponse<A, P>(responseId: string): Promise<ResponseEnvelope<A, P>>;
  listResponses<A, P>(query: {
    instrumentId: InstrumentId;
    cohortId?: string;
    userId?: string;
    wave?: Wave;
  }): Promise<ResponseEnvelope<A, P>[]>;

  // 알림 (진단이 트리거, 코어가 전달)
  raiseAlert(input: AlertInput): Promise<void>;
  listAlerts(cohortId: string): Promise<Alert[]>;              // '먼저 챙길 분'의 저장된 출처. RLS: 차수 코치/운영자. ADR-23

  // 본부 — 코치 신청 승인/거절(USER→COACH 승격). 운영자 전용.
  listCoachApplications(status?: 'pending' | 'approved' | 'rejected'): Promise<CoachApplication[]>; // 운영자 전용. ADR-24
  decideCoachApplication(input: { applicationId: string; decision: 'approved' | 'rejected'; note?: string }): Promise<void>; // RPC decide_coach_application(원자 승격). ADR-24
}
```

---

## 8. 작업 B — 플러그인 계약 4종

방향: **진단 → 코어** (코어가 진단에게 요구하는 구현). 진단은 아래 `InstrumentModule`을 코어에 등록한다.

```ts
// /contracts/instrument.ts
type AnswerValue = number | string | boolean | null;
type Answers = Record<string /* item.code */, AnswerValue>;

interface InstrumentModule<A = Answers, P = unknown, S = unknown> {
  id: InstrumentId;
  identityPolicy: IdentityPolicy;    // §5.2
  flow: ResponseFlowPlugin;          // B①
  scoring: ScoringPlugin<A, S>;      // B②
  report: ReportPlugin<S>;           // B③
  alerts: AlertPlugin<S>;            // B④
  answersSchema: unknown;            // zod — A 경계 검증
  profileSchema: unknown;            // zod — P 경계 검증
}
```

### 8.1 B① 응답 흐름 계약 (확정)

설계 원칙: **선언형 우선 + 탈출구.** 진단은 문항·척도·블록을 *선언*만 하고, 코어 러너가 위젯·제약무작위·진행저장·재개·검증·접근성을 수행한다. 표준 위젯으로 안 되는 블록(향후 라이프커브)은 `CustomBlock`으로 내려간다.

```ts
// /contracts/instrument.ts (B①)
type ScaleKind = 'bipolar' | 'likert' | 'numeric' | 'text' | 'check';
interface BipolarScale { kind: 'bipolar'; points: number; leftLabel: string; rightLabel: string; }
interface LikertScale  { kind: 'likert';  points: number; minLabel: string; maxLabel: string; centerLabel?: string; }
interface NumericScale { kind: 'numeric'; min: number; max: number; input: 'slider' | 'number'; suffix?: string; }
interface TextScale    { kind: 'text';    multiline: boolean; placeholder?: string; maxLen?: number; }
interface CheckScale   { kind: 'check';   label: string; }
type ScaleDef = BipolarScale | LikertScale | NumericScale | TextScale | CheckScale;

type Polarity = 'positive' | 'negative' | 'neutral';
interface Item {
  code: string;        // 저장 키 (영구·불변) — 예 'A2','C5','B1'
  prompt: string;      // 참여자에게 보이는 유일한 문자열
  scale: ScaleDef;
  required: boolean;
  polarity: Polarity;  // 내부 전용 — 배열 제약·역채점 근거. 화면 비노출
}

interface StandardBlock {
  id: string; kind: 'standard';
  title: string; intro?: string;     // intro·title은 참여자 노출(존대체)
  optional?: boolean;                 // F섹션 '선택' 표시
  items: Item[];
  ordering: OrderingPolicy;
}
interface CustomBlock {
  id: string; kind: 'custom';
  title: string; optional?: boolean;
  component: string;                  // customComponents 등록 키
  emits: string[];                    // 채우는 answers 코드 목록 — 검증용
}
type Block = StandardBlock | CustomBlock;

type OrderingPolicy =
  | { mode: 'fixed' }
  | { mode: 'constrained-shuffle'; firstPolarity?: Polarity; maxConsecutiveSameNegative?: number };

interface ResponseSchema { instrumentId: InstrumentId; wave: Wave; blocks: Block[]; }

interface CustomFlowComponentProps {
  value: Record<string, AnswerValue>;
  onChange: (code: string, v: AnswerValue) => void;
  context: CoreContext;
}
type CustomFlowComponent = (props: CustomFlowComponentProps) => unknown;

interface ResponseFlowPlugin {
  getSchema(wave: Wave): ResponseSchema;
  customComponents?: Record<string, CustomFlowComponent>;
}
```

**코어 러너 (코어가 제공, 진단은 호출만)**
```ts
interface ResponseRunnerProps {
  schema: ResponseSchema;
  context: CoreContext;
  cohortId: string | null;
  wave: Wave;
  onComplete: (responseId: string) => void;
}
// 코어 책임: 위젯 렌더 · 제약무작위 배열 · 진행 저장/재개 · 필수 검증 ·
//           모바일 7블록 흐름 · 접근성 · 완료 시 context.saveResponse() 호출
```

**B①이 구조로 지키는 것**
- 저장은 코드, 화면은 일련번호. `answers` 키는 불변 코드. 참여자가 보는 번호는 렌더 시점의 임시 번호.
- 측정/강의 어휘 분리. 참여자에 닿는 문자열은 `prompt`·`title`·`intro` 뿐. `code`·`polarity`·구인·STEP은 렌더 경로에 없음.
- wave 분기로 사전·사후 페어링. `getSchema('pre'|'post')`는 같은 코드를 쓰되 `intro` 서사만 바꿈.

### 8.2 B② 채점 계약 (구현 완료 — 2026-06-27)

```ts
interface ScoringPlugin<A = Answers, S = unknown> {
  score(answers: A, ctx: { wave: Wave }): S;
}
```
퓨처나우 `TScores` 형상은 §9.3의 7규칙 산출물이다. **전용노선과 채점이 한 치도 다르지 않아야 하며, 단위테스트로 못 박는다.** **구현 완료**(2026-06-27): `scoring.ts` 의 `FuturenowScores`(vitality·redFlag·grow(GROW+F)·trap·compass·gap·faith). 산출물엔 구인 식별자만, 강의 명명 없음(§9.6).

### 8.3 B③ 리포트 계약 (사양 확정·구현 대기)

```ts
interface ReportPlugin<S = unknown> {
  renderScreen(scores: S, prev?: S): unknown;          // 화면 결과(나침반 게이지·레이더·막대)
  renderPdf(scores: S, profile: unknown, prev?: S): unknown;  // react-pdf 개인 분석보고서
  renderGroup(all: S[]): unknown;                      // 그룹 평균 레이더(1주차 오프닝)
}
```
측정 어휘의 진단명(시들음·원씽 등)은 **이 단계에서 비로소 등장**한다(예: "활력 지수가 낮게 나왔습니다 — 이를 '시들음'이라 부릅니다"). 디자인 시스템 확정 후 시각 사양을 채운다.

### 8.4 B④ 알림 트리거 계약 (구현 완료 — 2026-06-27, ADR-19)

```ts
type AlertSignal = Pick<AlertInput, 'severity' | 'reason'>;   // ADR-19 — 진단은 severity·reason 만
interface AlertPlugin<S = unknown> {
  evaluate(scores: S, answers: Answers): AlertSignal[];
}
```
퓨처나우 트리거는 §9.3 규칙 2(Red Flag)·돌봄 체크다. **구현 완료**(2026-06-27, `alerts.ts`): A2·A5·A4 모두 ≥4 → `red_flag`('활력 위기신호') · 돌봄 체크 → `care`('돌봄 요청 신호'), 둘 다면 red_flag 우선. **ADR-19**: evaluate 반환을 `AlertSignal`(severity·reason)로 정직화 — `responseId`·`cohortId` 는 코어가 saveResponse 후 주입해 완전한 `AlertInput` 으로 raiseAlert.

---

## 9. 퓨처나우 인스트루먼트 사양

### 9.1 문항 코드 매핑 (31문항 · 사전·종료 공용)

저장 키는 아래 원 코드다. 화면 일련번호는 제약무작위로 매겨지는 임시값이다.

| 번호 | 코드 | 구인 | 척도 | 채점 | 블록 |
|---|---|---|---|---|---|
| 1 | NAV1 | 나침반-동기(회피↔접근) | bipolar5 | 우측 가점 | 나침반 |
| 2 | NAV2 | 나침반-기준(비교↔자기기준) | bipolar5 | 우측 가점 | 나침반 |
| 3 | NAV3 | 나침반-시선(결정론↔목적론) | bipolar5 | 우측 가점 | 나침반 |
| 4 | NAV4 | 나침반-리셋(매몰비용↔제로베이스) | bipolar5 | 우측 가점 | 나침반 |
| 5 | A1 | 활력-기대 | likert5 | 정 | 지금의 나 |
| 6 | C3 | R-현실인식 | likert5 | 정 | 지금의 나 |
| 7 | A2 | 활력-정체감 | likert5 | 역·위기▲ | 지금의 나 |
| 8 | C6 | O-원씽 | likert5 | 정 | 지금의 나 |
| 9 | D1 | 함정-관성 | likert5 | 역·함정 | 지금의 나 |
| 10 | C2 | G-조감도 | likert5 | 정 | 지금의 나 |
| 11 | A5 | 활력-도파민 | likert5 | 역·위기▲ | 지금의 나 |
| 12 | C8 | W-피드백 | likert5 | 정 | 지금의 나 |
| 13 | C5 | O-우선순위 | likert5 | 역 | 지금의 나 |
| 14 | A3 | 활력-몰입 | likert5 | 정 | 지금의 나 |
| 15 | D2 | 함정-준비 | likert5 | 역·함정 | 지금의 나 |
| 16 | C1 | G-재해석 | likert5 | 정 | 지금의 나 |
| 17 | A4 | 활력-시들음 | likert5 | 역·위기▲ | 지금의 나 |
| 18 | C7 | W-실행지속 | likert5 | 정 | 지금의 나 |
| 19 | D3 | 함정-안주 | likert5 | 역·함정 | 지금의 나 |
| 20 | C4 | R-습관자각 | likert5 | 정 | 지금의 나 |
| 21 | C9 | F-정체성 | likert5 | 정 | 지금의 나 |
| 22 | F1 | 믿음-의미 | likert5 | 정·선택 | 믿음의 자리 |
| 23 | F2 | 믿음-실행 | likert5 | 정·선택 | 믿음의 자리 |
| 24 | B1 | 간격-일 Work | numeric 0~10 | 레이더 | 간격 |
| 25 | B2 | 간격-재정 Wealth | numeric 0~10 | 레이더 | 간격 |
| 26 | B3 | 간격-관계 Relationships | numeric 0~10 | 레이더 | 간격 |
| 27 | B4 | 간격-건강 Health | numeric 0~10 | 레이더 | 간격 |
| 28 | B5 | 간격-기여 Contribution | numeric 0~10 | 레이더 | 간격 |
| 29 | E1 | 기대(주관식) | text | 서술 | 묻는 시간 |
| 30 | E2 | 정서(주관식) | text | 서술 | 묻는 시간 |
| 31 | E3 | 요청(주관식·선택) | text | 서술 | 묻는 시간 |

부가: 들어가며(조감도 한 문장, text) · 돌봄 체크(31번 뒤, check) · 마지막 다짐(check).

### 9.2 7블록 흐름 (모바일)

| 블록 | 문항 | 배열 |
|---|---|---|
| 들어가며 | 조감도 한 문장 | fixed |
| 나침반 | NAV1~4 | fixed |
| 지금의 나 | A1~A5·C1~C9·D1~D3 (5~21) | **constrained-shuffle** (첫 문항 positive, 부정 2연속 금지) |
| 믿음의 자리 | F1·F2 | fixed · optional |
| 간격 | B1~B5 | fixed |
| 나에게 묻는 시간 | E1~E3 + 돌봄 체크 | fixed |
| 마지막 한 걸음 | 다짐 체크 | fixed |

### 9.3 산출규칙 7종 (B②·B③·B④의 사양)

1. **활력 지수 (5~25)**: `A1 + A3 + (6−A2) + (6−A5) + (6−A4)`. 구간(확정 2026-06-28): **시들음 ≤10**(Languishing 신호 → 1주차 전 가벼운 안부 권장) · **중간 11~17** · **번성 18~25**. 구간 명명은 B③ 리포트에서만(§9.4).
2. **Red Flag (최우선)**: `A2·A5·A4(7·11·17) 모두 4~5점` 또는 `돌봄 체크` → 개별 연락·돌봄 명단. 점수 공개·지목 없이 개인 면담으로 연결. → B④ `severity:'red_flag'|'care'`.
3. **준비도 GROW+F**: G=`avg(C2,C1)` · R=`avg(C3,C4)` · O=`avg(C6, 6−C5)` · W=`avg(C8,C7)` · F=`C9`(보조 F1·F2). 축별 평균 막대. 그룹 평균 낮은 축 = 보강 포인트.
4. **함정 유형**: `D1(관성)·D2(준비)·D3(안주)` 중 최고점 = 주 함정. 소그룹 편성 기준.
5. **나침반 4축**: `NAV1~4` 좌(1)~우(5). 사전-사후 '바늘 이동' = 세미나 효과 헤드라인.
6. **간격 레이더**: `B1~B5` 오각형. 그룹 평균 레이더 = 1주차 오프닝.
7. **믿음의 자리**: `F1·F2` 무응답·저점은 실패가 아니라 목회적 신호. 점수로 다루지 않고 5주차 FAITH 수위 조절 참고로만.

### 9.4 측정·강의 어휘 분리 (불변 원칙)

문항에는 '시들음·원씽' 등 강의 어휘를 노출하지 않는다(사회적 바람직성 편향 차단). 그 명명은 **리포트 단계(B③)에서** 비로소 한다. 코드·구인·STEP은 참여자 렌더 경로에 두지 않는다(B① 타입이 강제).

### 9.5 사전·사후

동일 번호·동일 코드·동일 구인에 종료 시점 문장으로 치환(`getSchema('post')`). 비교 뷰: 나침반 바늘 이동·활력 변화·간격 축소를 나란히. `subjectProfile`은 응답마다 박제(불변).

### 9.6 구현 메모 (2026-06-27 · B①·B②·B④ 구현, 문항 원문 반영)

순수 로직(화면 없음). `/instruments/futurenow`: `flow.ts`·`scoring.ts`·`alerts.ts`·`schema.ts`·`copy.ts`.

- **역채점 범위 확정**: `6 − x` 는 **A2·A5·A4(활력)·C5(GROW O)** 에만 적용. 함정 `D1·D2·D3` 은 규칙④에서 **원점수 최고점**(역채점 아님, 동점 시 앞선 코드).
- **부가 항목 코드 확정**(§9.1 부가에 코드 부여): 들어가며=`INTRO`(text) · 돌봄 체크=`CARE`(check) · 마지막 다짐=`COMMIT`(check). (지휘부 승인 2026-06-27.)
- **문항 원문 반영 완료**: 검증된 copy deck 을 `copy.ts` 에 verbatim 담고 `flow.ts` 가 참조(구조/문구 분리). 1~28 prompt·양극 레이블은 wave 공용, intro·E1~E3·들어가며·체크 label·간격 intro 는 `getSchema('pre'|'post')`에서 wave 분기. 블록 title 은 copydeck(나의 나침반·다섯 영역의 간격 등). 화면 공용 안내 문구(보안 고지 등)는 StartGuide·ResponseRunner 인라인(구 `copy.notices`·`profileLabels` 미사용 export 는 정합 마감 시 제거·ADR-59).
  - *보류*: likert 중앙 레이블 '보통'(블록2)은 현 `LikertScale` 계약에 필드가 없어 미반영(렌더러 파생 또는 추후 계약 보강). 간격 종료의 '5주 전' 값은 B③ 비교뷰 사안 — flow 는 '오늘' 값만 수집.
- **subjectProfile 형상**(진단 소유 · ADR-32 기준 · 2026-07-01 정합): `birthYear`·`gender`(수집 필수) · `religion`·`faithYears`(선택) — 계정(`user_profiles`)에서 복사·박제하는 4필드(`copy.profileFieldsByWave`, pre·post 동일). 스냅샷 zod(`futurenowProfileSchema`)는 birthYear·gender 를 nullable·optional 로 완화(계정값 NULL 가능 — 필수성은 DB 불변식이 아니라 S3 `ProfileForm`/`IdentityPolicy`가 강제·코치 화면 NULL 폴백). `motivation`(참여 계기)은 **계정이 아니라 응답 전용 선택 필드**(사전 wave 스냅샷·시점 종속) — `ProfileForm`이 프리필/스킵과 무관하게 수집, `profileFieldsByWave`엔 미포함(ADR-44). 실명은 코어 `users.name`, 전화는 `user_contacts` 재사용 — profile 에 두지 않음(ADR-02·04). (구 `ageBand`·`writtenAt` 기재는 폐기 — 코드 부재, ADR-32로 대체.)
- **B④ 정직화(ADR-19)**: `AlertPlugin.evaluate` 반환을 `AlertSignal`(severity·reason)로 변경. `responseId`·`cohortId` 는 코어가 saveResponse 후 주입 → 완전한 `AlertInput` 으로 raiseAlert. 책임 경계와 일치(진단=신호, 코어=식별자).

---

## 10. 디자인 시스템 — **v3 도착·구현 (응답 + 리포트 + 진입 흐름 + 콘솔) / 운영자 화면 대기**

상세는 [`design_system.md`](design_system.md) (v3). 본 절은 요약·구현 상태.

**v3 범위(구현 완료)**: 색 3단 토큰 · 타이포 · 응답 위젯 5종 · 리포트 시각화 5종 · 종합 배치 · **참여 진입 흐름(§7) · 코치 콘솔(§8)**.

- **색 3단 토큰**(§1): 원천 hex → 역할(semantic) → 컴포넌트. `src/app/globals.css` 에 §1.1~1.4 구현. **컴포넌트는 2차 역할 토큰만 참조**(hex·`--navy-*`·`--gold-*` 직접 참조 금지). 색값은 **잠정**(첫 화면 확정 후 재평가). 선택색 = `--color-accent`(골드). 다크 토큰은 역할만 재지정.
- **타이포·간격**(§2·§3): Pretendard, 숫자 tabular-nums, 타이포 7토큰(display~micro), `--tap-min:44px` 등. globals.css.
- **공용 UI 12종**(§9, 코어 `src/core/ui`, 인스트루먼트 중립): Button·Card·ProgressBar·SegmentBar·DotScale·NumberSlider·TextArea·CheckRow·StickyScaleHeader·**OtpInput·Stepper·ListRow**. 스타일은 `src/core/ui/ui.css`(역할 토큰·`--care-*`만). 리포트 차트군은 코어가 아니라 **인스트루먼트 소유**(ADR-21).
- **진입 흐름(§7) + 코치 콘솔(§8)** — **앱 레이어**(`src/app/_screens/`, 코어 UI·인스트루먼트를 합성). 진입(갱신 2026-07-01): **공개 소개 현관(`/`)** → `/join`: 코드입력(CodeInput)→차수 미리보기(CohortPreview + 세미나 소개)→로그인/가입(AuthGate, 이름·전화 미요구 ADR-03)→시작 안내(StartGuide, 보안 고지·버튼=동의)→**참여 프로필(ProfileForm, ADR-32)**→러너→**완료(Completion 갈망 거울, ADR-27)→자기 홈(A-2)**. 콘솔: 홈(먼저 챙길 분 최상단·돌봄 우선)·차수 개설(3스텝)·차수 상세(3숫자+명단 3묶음)·모든 차수. **인도자 화면만 의미색(저채도 `--care-*`), 참여자 진입 화면 경고색 배제.** 미리보기 `/preview/entry`·`/preview/console`(**보호 라우트 — 세션+`role !== 'user'`, ADR-93**). **CohortPreview 메타 타입은 앱 로컬**(`_screens/types.ts`) — 계약 승격은 보류(아래).
- **응답 위젯 5종 + 러너**(§4): 나침반=세그먼트바(중앙 유지)·리커트=행스택+척도 sticky+도트22px(히트44px)·간격=슬라이더+숫자·주관식=텍스트영역·체크=행토글(경고색 금지·골드 선택). `src/core/response/ResponseRunner.tsx`(시각부: 블록 흐름·위젯 렌더·진행·필수 게이팅·제약무작위 배열[`ordering.ts`]·완료 시 `saveResponse`). 참여자 화면 경고색 배제(§0.4). 미리보기 라우트 `/preview`·**`/preview/site`(부품 9종 전시 — ADR-125)**(**보호 — ADR-93.** `futurenowFlow` 를 클라이언트로 import 하므로 문항 원문 전량이 번들에 실린다).
- **리포트 시각화 5종 + 배치**(§5·§6, B③ 구현 완료): 나침반=덤벨·간격=레이더(사후 네이비13% 면+사전 회색 점선)·GROW+F=충전막대(사후 네이비·사전 회색)·활력=띠 이동(시들음/중간/번성 저채도 구간+상태배지)·돌봄 신호=조건부 배너(저채도 `--care-*`). 배치: 돌봄→헤드라인(활력·나침반)→깊이(간격·GROW)→주관식, 데스크톱 2×2/모바일 1열. **본문 시각물 네이비·회색, 의미색은 돌봄 배너에만.** 명명(시들음·원씽)은 리포트에서만(§9.4). `src/instruments/futurenow/report/*` + `report.tsx`(ReportPlugin: renderScreen·renderGroup·renderPdf[react-pdf, 서버 전용]). 미리보기 `/preview/report`(**보호 — ADR-93.** 리포트 구조·명명이 본문에 그대로 나온다). **InstrumentModule 최종 조립** = `src/instruments/futurenow/index.ts`.
- **경계 결정(directive 2026-06-28, ADR-21)**: 리포트 차트군(Dumbbell·Radar·ChargeBars·VitalityBand·CareBanner)은 **인스트루먼트 소유** 확정(`report/visuals.tsx`) — 진단별 명명·데이터가 박히므로 코어 중립 부품이 아니다. design_system §7 의 '코어' 기재는 **오기로 정정**. 진단↛코어 경계(CLAUDE §1) 유지, 차트는 공유 디자인 토큰만 참조. **활력 구간 경계 확정**(11~17 중간·18~25 번성). **PDF 생성 라우트(renderToBuffer)는 다음 단위**(renderPdf 구현·타입·빌드는 완료, 서버 전용).
- **보류(design_system §9)**: 코치/운영자 콘솔·`CohortPreview`. **착수 금지.**
- **셸 홈 복귀(트랙 A1·ADR-45, 2026-07-02)**: root 화면 우측 액션(`HeaderActions`)에 홈 아이콘 링크(`homeHref`) — `usePathname`으로 **현재=홈이면 생략**(자기참조 방지). sub 화면은 `AppHeader`(variant='sub')가 이미 홈 아이콘을 렌더하므로 액션엔 미전달(중복 회피). **(A′-2·ADR-51)** 전 화면 homeHref 를 **통합 홈 `/home`**으로 통일(역할별 거점 폐지) — 콘솔·본부·차수·리포트·내 정보 어디서든 홈 아이콘·로고가 `/home`으로 복귀. `CoachInfoGate` flow→sub(홈 복귀 어포던스 부여). **(A′-5·ADR-54)** 우측 홈 어포던스=아이콘+'홈' 라벨(인지성), root 로고=서비스 정체성(aria "홈" 제거·홈 링크는 유지) — 로고/홈 역할 분리.
- **러너 후속(갱신 2026-07-01)**: **진행 저장/재개 — 구현 완료**(진단-1B·ADR-34: `response_drafts` 서버 draft + localStorage 자동 + `draftLocation` 안 푼 첫 필수 문항 재계산, step 미저장으로 셔플 안전). **subjectProfile 수집 화면 — 구현 완료**(`ProfileForm`·ADR-32, `/join` 흐름 `start→profile→runner`). **접근성 키보드 정밀화는 미구현 유지**(plan §2 — 후속).

---

## 11. ADR (Architecture Decision Records)

| # | 결정 | 근거 |
|---|---|---|
| ADR-01 | 공유 코어 + 진단별 전용 모듈(플러그인 계약) | 통합하되 질 저하 금지. 채점·리포트 엔진은 비공유 |
| ADR-02 | 신원=코어 / 진단별 참여프로필=responses.subjectProfile | 실명제 퓨처나우·익명 SAIL 공존 |
| ADR-03 | 이름·전화 공용 필드 + (진단×역할) 필수성 정책 | 코치 강제·참여자 선택, 교차 재사용 |
| ADR-04 | 전화번호 user_contacts 격리·운영자 전용·getPhone 게이트 | 코치 상호 비열람. RLS 열 제한 한계 보완 |
| ADR-05 | RLS는 SECURITY DEFINER 헬퍼로 재귀 회피 | SAIL 핫픽스 계승 |
| ADR-06 | 실명제·인도자 전용. 익명 URL SELECT 절 제거 | 퓨처나우 보안 전제 |
| ADR-07 | C를 전제로 한 A: 단일 레포·폴더 분리 → 후일 패키지 추출 | 계약 진화 중 한 몸 검증, 후일 병렬 |
| ADR-08 | B① 선언형 우선 + CustomBlock 탈출구 | 응답 품질 바닥값 공유, 특수 블록 허용 |
| ADR-09 | responses·alerts 불변(UPDATE/DELETE 차단) | SAIL immutable results 철학 |
| ADR-10 | 저장=원코드 / 화면=제약무작위 임시번호 | 배열이 섞여도 채점·페어링 불변 |
| ADR-11 | ~~코어를 전용 `core` 스키마에 격리~~ | **폐기됨(ADR-13/15로 대체)**. 거점이 SAIL 프로젝트로 확정되며 기존 `public` 재사용이 더 단순·정합적이라 판단 <br><br>**〔정정 주 2026-08-29 — 이 번호에 잘못 걸린 인용이 있다〕** `design_system.md` §0-1 *색은 3단으로만 흐른다* 가 근거로 **ADR-11 을 인용한다.** **이 행과 무관하다** — 이 행은 스키마 격리 건이고 색과 관계가 없다. **행이 바뀐 적도 없다**: `git` 이력상 ADR-11 은 저장소 **첫 커밋**(`b7ab2fa` 2026-06-27)부터 **이미 폐기 표시된 스키마 건**이었고, 인용은 **같은 날 나중 커밋**(`838e761` 디자인 시스템 v1)이 붙였다. **ADR-12 와 같은 사건이고 다른 종류다** — 같은 커밋이 한 블록에서 `(ADR-11)`·`(ADR-12)` 와 머리 참조 줄 `ADR-04·11·12·20` 을 함께 적었으므로 **원인은 하나**(번호를 먼저 인용하고 행을 쓰지 않는 습관)이지만, 12 는 **없는 번호**(미아 참조 → 결번 처리)이고 11 은 **있는 번호인데 다른 결정**(오귀속)이다. **뜻은 `출처 표기가 틀렸다` 쪽이다** — 번호가 다른 결정에 붙은 것이 아니라(이 행의 내용은 처음부터 지금까지 그대로다), *색 3단* 이라는 결정이 **자기 번호가 없는 채로 남의 번호를 빌려 인용됐다.** 실측: `architecture.md` 의 어느 ADR 행도 *색 3단 흐름*을 정의하지 않는다(ADR-133 은 **소유 경계**이지 3단 흐름이 아니다). **→ 그 결정은 이제 `ADR-139` 다**(지휘부 채번 승인 2026-08-29 · *이미 인용되며 유효하다면 결정으로 실재하고, 실재하는 것은 번호를 가져야 한다*). `design_system.md` §0-1 의 인용도 139 로 옮겼다. **이 행에는 뜻을 덧붙이지 않았다** — 소급 부여가 아니고, 번호를 재사용하지도 지우지도 않았다. **어느 쪽에서 들어와도 반대편에 닿는다**: 139 본문이 이 번호를 가리키고 이 주가 139 를 가리킨다. |
| ADR-12 | *(결번 — **소급 부여 금지**)* | **원문이 존재한 적이 없다.** `architecture.md` 전 리비전 131개를 전수 조회했고 어떤 형태로도 ADR-12 정의가 나오지 않았다(2026-08-28 실측). 인용은 `838e761`(2026-06-27 · 디자인 시스템 v1)이 `design_system.md` §0-2 에 `(ADR-12)` 를 적으면서 **처음이자 유일하게** 생겼고, 그 시점 표는 ADR-11 다음이 곧 ADR-13 이었다 — **번호를 먼저 인용하고 행을 쓰지 않은 것**이다. 이후 보고서 둘이 이것을 `결번 1건(기존 상태)` 으로 여섯 달 넘겨 왔다(2026-08-19 · 2026-08-26). **이 번호에 지금 와서 뜻을 붙이지 않는다** — 옛 인용이 가리키던 것이 무엇이었는지 아무도 증명할 수 없고, 소급 부여는 없는 근거를 만드는 일이다. 그 문장의 결정 가치는 **ADR-133 으로 새로 채번**해 살렸다(출처를 그 행에 적었다). **이 행은 지우지 않는다** — 번호를 비워 두면 다음 사람이 다시 쓴다(§12.1 계열). `tests/adrNumbers.test.ts` 가 결번을 **살아 있는 근거로 인용하는 것**을 막는다. |
| ADR-13 | 거점 = SAIL 프로젝트(`zdoytzmvcafcebytttrm`) 승격, 같은 `auth.users` 공유, 신규 프로젝트 미신설 | 교차 자동 로그인. 운영 단순화. 적용 완료 2026-06-26 |
| ADR-14 | 진단 격리 = `instrument_id` + 차수(코치) RLS | 코치는 자기 차수(단일 instrument)의 응답만 열람 → instrument 격리 성립 |
| ADR-15 | SAIL 기존 `public.users` 를 코어 신원으로 공유 재사용 | SAIL `results`·2축 채점은 인스트루먼트 잔류, 통합은 점진(클로드3 조율). `full_name`→`name` 표준화·`phone` 분리 |
| ADR-16 | SAIL 전화 로그인(`email_by_phone`)은 `user_contacts` 기반으로 격리 보존 | 퓨처나우는 전화 로그인 미사용. 통합 시 재설계(plan.md) |
| ADR-17 | 가입-by-코드: `resolve_cohort_by_code` 를 공개 메타(비민감) 반환 정의자로 확장 + `CoreContext.enrollByCode` 계약 추가 | 미가입자도 코드로 차수를 확인·가입해야. 차수·참여는 코어 책임. 민감정보(응답·명단·전화) 미노출 |
| ADR-18 | `requireRole` 를 `Promise<void>` 로 비동기화 | 숨은 "현재 사용자 선행 해석" 전제를 타입으로 끌어올려 견고화(계약은 견고화 방향으로만 변경) |
| ADR-19 | `AlertPlugin.evaluate` 반환을 `AlertSignal`(severity·reason)로 정직화 | 진단은 신호만, `responseId`·`cohortId` 는 코어가 saveResponse 후 주입. 책임 경계와 일치 |
| ADR-20 | `LikertScale.centerLabel?` 추가(척도 레이블 데이터 소유) | 중앙 레이블('보통' 등)을 진단이 데이터로 선언. 렌더러는 있으면 표기, 없으면 생략 |
| ADR-21 | 리포트 차트군은 **인스트루먼트 소유**(코어 아님) | 진단별 명명·데이터 결속 → 진단↛코어 경계(CLAUDE §1) 유지. design_system §7 '코어' 기재 정정(directive 2026-06-28) |
| ADR-22 | `CohortPreviewMeta` + `previewCohortByCode` 추가(가입 결정용 공개 메타) | `resolveCohortByCode`(Cohort 본체)와 목적 분리 — 미가입자 가입 결정용 비민감 메타(coachName·memberCount). RPC 메타를 버리지 않고 매핑. DB 무변경(directive 2026-06-28 승인) |
| ADR-23 | `Alert` 읽기 타입 + `listCohortsByCoach`·`listAlerts` 추가(콘솔 실데이터) | 콘솔 홈 = 코치 차수목록 + '먼저 챙길 분'. 돌봄은 안전 신호 → **저장된 알림을 읽는다**(listAlerts), `listResponses`+재채점 금지(채점 로직 변경 시 저장본과 drift). RLS(cohorts_select·alerts_select) 그대로 사용, DB 무변경(directive 2026-06-28 승인) |
| ADR-24 | 본부 데이터 계층: `MemberRef`·`CoachApplication` + `listCohortMembers`·`listCoachApplications`·`decideCoachApplication` | **이름 가시성**(plan Q6): users RLS 확대(전 행 노출) 대신 `cohort_member_directory`(DEFINER, **id+name만**) 채택 — SAIL `users` 보존·ADR-04 최소노출. **코치 승격**: 상태변경+role 승격 원자성 위해 `decide_coach_application`(DEFINER) — 내부 is_admin·FOR UPDATE·status='pending' 가드·`role='user'`만 승격. 읽기(listCoachApplications)는 RPC 불요(coach_apps_select=admin + users 조인). directive 2026-06-28 승인 |
| ADR-25 | `createCohort` = **앱측 코드 생성 + 충돌 재시도**(DEFINER RPC·마이그레이션 0) | `cohorts_insert` RLS(coach_id=auth.uid() AND user_role∈{coach,admin})가 권한을 이미 받음 → 새 RPC 불요. 유일 설계점은 유니크 코드: 앱이 `crypto.getRandomValues`로 5자리(알파벳 `ABCDEFGHJKMNPQRSTUVWXYZ23456789` = DB `cohorts_code_check`와 글자 일치) 생성, 23505 충돌 시 재시도(≤5). `Math.random` 금지(초대 수단=예측불가). directive 2026-06-28 승인 |
| ADR-26 | `updateCohort` = **앱측 부분수정**(불변필드 제외) + `cohorts_update` WITH CHECK 부기 | 마감(status=archived)·정원 수정은 `cohorts_update` RLS(USING=소유 코치/운영자)가 받음 → 메서드만. patch는 `name·description·maxMembers·status·expiresAt`만 — `coach_id`(소유이전)·`instrument_id`(불일치)·`code`(링크파손)·`id` 불변. 기존 정책에 `WITH CHECK` 부재 → raw UPDATE 소유이전 가능했음 → 부기 픽스(USING+WITH CHECK 둘 다 소유 강제). 행 0=미존재/RLS차단→CoreNotFound. directive 2026-06-28 승인 |
| ADR-27 | 참여자 완료 §7.5 *갈망 거울* = **퓨처나우 인스트루먼트 소유 + 앱층 조합**(계약 변경 0) | 갈망/지향 언어는 퓨처나우 고유 지식 → `participantMirror(scores)` 인스트루먼트 내부 export(InstrumentModule 인터페이스 미추가). 앱 `finalizeResponse` 가 반환값에 동봉, `Completion`(앱)이 구조 렌더. CoreContext·DB 무변경(G1 보호). 참여자엔 측정·severity·돌봄 0건. 산출 실패 시 ①+④ 우아한 저하. 다인스트루먼트 일반화는 추후. directive 2026-06-28 승인 |
| ADR-28 | 본부 멤버 역할 관리: `MemberSummary` + `listUsers` + `setUserRole`(set_user_role RPC) | 운영자가 멤버를 **직접 승격/강등**(상시 권한)하는 본부 §8.6 첫 조각. `decide_coach_application`(자가 신청 승인)과 **공존** — 전자는 운영자 권한, 후자는 신청 기반. 역할 변경은 민감 → DEFINER RPC가 가드 강제(내부 is_admin·역할 화이트리스트·자기강등 방지). 읽기(listUsers)는 `users_select`=admin 직접 select. directive 2026-06-29 승인 |
| ADR-29 | 멤버 본인 차수 읽기: `MyCohortSummary` + `listMyCohorts`(my_cohorts RPC) | 멤버는 cohorts RLS상 자기 차수도 직접 못 읽음 → **DEFINER RPC가 비민감 메타만**(차수명·코치명·status·진행·가입일; coach_id·code·max_members 미반환) `auth.uid()` 기준 반환. cohorts·enrollments·responses **RLS 불변**(옵션 A). 진행=해당 wave responses row 존재(불변·완료컬럼 없음). `previewCohortByCode`(코드·미가입자)와 목적 분리. directive 2026-06-29 승인 |
| ADR-30 | Q4 확정 — 멤버 리포트 = **순화 뷰**(갈망 거울 재사용), 코치 = **리얼 리포트**(measurement) | 멤버 본인 열람은 `participantMirror`(ADR-27) 산출을 공용 `MirrorView`로 렌더(②방향·③갈망·⑤믿음) — severity·점수·버킷·돌봄 0. 코치 `ReportScreen`(measurement 전체)와 **시각·경로 분리**(/my/cohorts/[id]/report vs /coach/cohort/[id]/report/[responseId]). 멤버 self-read는 `responses_select`(user_id=auth.uid()) 직접 — RPC 불요. scores 미저장(재채점, ADR-09). 계약·DB·RLS 무변경(G1=0). directive 2026-06-29 승인 |
| ADR-31 | `setName` 추가(본인 표시 이름 수정) | `/account` 프로필의 이름 수정 = `users.name` 본인 행 update. **본인 전용**(requireUser→id=auth.uid()) — userId 인자 없음(타인 수정 불요). **role 미포함**(2.S2로 role 컬럼 권한 봉쇄·set_user_role 전용) — RLS(본인 행) + 컬럼권한(name=true) 이중 보장. 전화는 기존 `setPhone`/`getPhone` 재사용(계약 +0). 실패는 정제(raw 비노출·내부 로그, ADR 흡수). 계약 +1 메서드만. directive 2026-06-29 승인 |
| ADR-32 | 참여 프로필 수집(생년·성별 필수, 종교·신앙연수 선택) + 러너 프로필 운반 | 제출 실패 수정: 러너가 `subjectProfile:{}` 를 보내는데 `futurenowProfileSchema` 가 필수 필드를 요구해 전 제출 실패(러너 빈스냅샷 의도 ↔ 스키마 필수 충돌, stub validators 라 단위테스트 미포착). 지휘부 확정 — 프로필 **실수집**: `futurenowProfileSchema` = `birthYear`(int 1900~2100·필수)·`gender`(필수)·`religion`(선택)·`faithYears`(선택). 수집 UI = `ProfileForm`(참여자 — 의미색·구인 어휘 0, §0.4·§7), `/join` 흐름에 `start→profile→runner` 삽입. **계약 +1(선택 필드)**: `ResponseRunnerProps.subjectProfile?`(미전달 시 `{}`) — 러너가 `saveResponse` 로 운반(견고화: 러너가 스냅샷 운반). 채점·리포트·거울은 `answers`만 사용 → 프로필 다운스트림 영향 0. 마이그레이션 0. directive 2026-06-29 승인 |
| ADR-33 | 재진단 **허용**(최신 유효) + 집계·열람 경로 dedup(`latestPerUser`) | 같은 user·차수·wave 에 응답이 다중 행 쌓일 수 있음(`responses` 유니크 제약 없음 — 재진단과 양립). 무결성 취약 경로 둘을 앱층에서 user별 `created_at` 최신 1건으로 접음: 그룹 평균(`/coach/cohort/[id]/group` — 평균 오염 방지)·개인 리포트 재방문(`/my/cohorts/[id]/report` — 무순서 `[0]` staleness 방지). 코치 콘솔 카운트(`buildCohortRoster`)는 이미 user별 최신 — 무변경. `listResponses` 시그니처·DB·계약 무변경(G1=0, 진단-1A). directive 2026-06-30 승인 |
| ADR-34 | 중간저장 = 별도 테이블 `response_drafts` + `CoreContext.saveDraft/getDraft/clearDraft` | 진단을 도중 잃지 않게 2층 보존: localStorage 자동(투명·디바운스) + [중간 저장] 버튼 서버 보존. **answers만 저장·step 미저장**(블록 내 셔플과 무관 — 재개 시 `draftLocation`으로 안 푼 첫 필수 문항 블록 재계산). `responses`(불변·정식 제출)와 **분리** — 회귀면 0, 제출 성공 시 draft 정리. PK(user,cohort,wave) upsert=최신 1개. **RPC 대신 RLS 직접 I/O**(self-scoped CRUD라 `user_id=auth.uid()` USING+WITH CHECK로 완전 표현, `saveResponse` 선례 동형 — 함수 DDL 0). INSERT/UPDATE는 `is_cohort_member` 부기(`responses_insert` 선례). 마이그 `20260630134334`(테이블+RLS) + 계약 +3. 러너(코어, §10 "진행 저장/재개")는 토스트 대신 인라인 확인(코어→앱 의존 회피). directive 2026-06-30 승인 |
| ADR-35 | 코어 AI 게이트웨이 통로 `CoreContext.aiChat`(범용·서버 전용) | B③-0(3bc4788) 코드-only 도입 §11 보강. 프롬프트·진단 어휘는 인스트루먼트 소유·코어는 통로만(진단어휘 0) → 코어 '채점 안 함'(CLAUDE §2) 유지. `src/core/ai/gateway.ts`(API Key 불요·provider fallback). 서버 전용(키·provider 비노출) |
| ADR-36 | 코치 리포트 해석 그릇 `report_interpretations` + 계약 4메서드(get/save/setCoach/clearCoach) | B③-1(ca51840) 보강. 유효=coach_content ?? ai_content. RLS 코치(is_cohort_coach)·운영자만 **참여자 분기 없음**(코치 리포트 전용). ai_content 앱 규약 불변(감사). 구조화 형상 진단 소유(계약 unknown·경계 zod는 인스트루먼트). 마이그 `20260630160857` |
| ADR-37 | 참여 프로필 `user_profiles` 코어 별도 테이블 분리(CoreUser 무변경·getProfile/setProfile 게터) | S2(2026-07-01). users 본체 미오염(SAIL 형제 영향 0·ADR-04 격리 선례). RLS 본인 CRUD+운영자 SELECT·코치 직접열람 없음. 전 컬럼 nullable(필수성=폼/IdentityPolicy). role 등 민감 컬럼 부재. 마이그 `20260701061118` |
| ADR-38 | 코치 조원 프로필 열람 DEFINER RPC `cohort_member_profiles`(RLS 확대 회피) | ADR-24 패턴. 내부 `is_cohort_coach OR is_admin` + `user_id IN (차수 enrollments)` 이중 스코프. REVOKE anon/GRANT authenticated·DEFINER·search_path=public. 코치는 자기 차수 조원만. 마이그 `20260701061156` |
| ADR-39 | 자가 코치 신청 `createCoachApplication`=self-scoped DEFINER `create_coach_application`(status='pending' 고정·upsert) | 신청 자격 `user_role='user'`. 재신청 ON CONFLICT(user_id)→status pending·motivation/kpc 덮어씀·reviewer 초기화. 견고화: coach_apps_insert RLS에 `status='pending'` 부기(직접 approved 차단). 클라 metadata 신뢰 폐기(ADR-41). 마이그 `20260701070126`. 그릇=backfill(`20260701061038`)+UNIQUE·kpc_number(`20260701061054`) |
| ADR-40 | 코치 본인 KPC 보완 `setMyCoachKpc`=self-scoped DEFINER `set_my_coach_kpc`(role·status 불변 — 권한상승 아님) | 지정제 코치(role=coach)는 ADR-39(role=user 게이트) 재사용 불가 → 별도. 불변식: 자격 role=coach·**role 미접근**(coach_applications만 upsert)·status 신규만 'approved'·기존 무변경·형식 `^KPC\d{5}$` RPC+CHECK 이중. 실측 status·role 무오염·pending 큐 오염 0. getMyCoachKpc=coach_apps_select 본인 self-read. 마이그 `20260701080201` |
| ADR-41 | `handle_new_user` name 키 버그 수정(full_name 단독 → COALESCE('name','full_name')) + user_profiles INSERT(방어 sanitize)·coach 신청 INSERT 제거 | 버그: name을 full_name 단독으로 채워 futurenow 가입(키='name')에서 users.name 항상 NULL → 신원 필수성 무력화. COALESCE라 SAIL(full_name) 비파괴(ADR-15). coach 신청 트리거 미INSERT(client metadata 신뢰 폐기·신청은 ADR-39). sanitize: CHECK 위반→NULL(가입 안 깨짐). search_path 보존. 마이그 `20260701061220` |
| ADR-42 | futurenow IdentityPolicy 강화 — 참여자 `user.name='required'`(코어 ADR-03 반전 아님) | 코치 명단 식별. **ADR-03 불변** — CoreUser.name nullable·DB NOT NULL 미검. 필수성은 정책 데이터(`identityPolicy.byRole.user.name='required'`)+폼 게이트로만. 진단 소유 정책이라 futurenow 한정(SAIL 미영향). 성별·생년 필수도 폼 게이트(DB nullable) |
| ADR-43 | 코치 정보 게이트 — role=coach·전화/KPC 미완 시 `/coach`가 콘솔 대신 보완 화면(강등 아님·참여자 폴백) | S4. 판정 `/coach/page.tsx`(role='coach' && (!phone‖!kpc)→CoachInfoGate). **강등 아님**(role 변경 0·loginOutcome 무변경). 운영자 면제. [나중에]→/home(코치 권한 유지·콘솔 접근만 유예·완비 시 refresh 개방). 저장=setPhone(ADR-04)+setMyCoachKpc(ADR-40) 재사용 — 새 권한 경로 0. 부분저장 허용·KPC 형식 클라+CHECK 이중 |
| ADR-44 | `motivation`(참여 계기) = 응답 전용 선택 필드(계정 아님) 부활 | ADR-32는 계정 복사 4필드만 확정 — motivation 미포함. 참여 계기는 시점 종속이라 계정(`user_profiles`) 아닌 응답 스냅샷 소유. 사전 wave `subjectProfile` 선택 필드(`futurenowProfileSchema.motivation?`)·`ProfileForm`이 프리필/스킵과 무관하게 수집. `profileFieldsByWave`(계정 4필드)엔 미포함. 채점·리포트·거울은 answers만 → 다운스트림 0. 계약·DB 무변경(G1=0) |
| ADR-45 | 셸 홈 복귀 어포던스 = `HeaderActions.homeHref`(root 노출·자기참조 생략, sub 는 AppHeader 홈 아이콘 재사용) · **A′-1·ADR-51 이 역할별 거점 전제 대체(어포던스 구조는 계승)** | 트랙 A1(항목5). root 화면(내 정보·내 차수 등)에서 홈 복귀가 로고 링크뿐이라 비발견적 → 우측 액션에 홈 아이콘 링크. `usePathname`으로 **현재=홈이면 생략**(자기참조 방지 — /home·/coach·/admin). sub 는 `AppHeader`(variant='sub')가 이미 홈 아이콘 → homeHref 미전달(중복 회피). 역할 거점(참여자/home·코치/coach·운영자/admin). `HomeIcon` export 재사용. 계약·DB·마이그 0. directive 2026-07-02 승인 |
| ADR-46 | 프로필 허용값 소유 계층 — 성별=전 서비스 공통 상수(`contracts/vocab.ts`) / 종교·KPC형식·생년상한=퓨처나우(`instruments/futurenow/profileVocab.ts`) + /account 프로필·KPC 편집 완결 | 트랙 A2(항목6). 성별 표기는 전 서비스 일관(지휘부 확정) → 계약 인접 런타임 상수로 원천화. 배럴은 `export type *`(타입 전용)이라 사용처는 `@/contracts/vocab` **직접 import**(척추 성격 보존). 종교 목록은 진단 고유라 인스트루먼트 소유(둘의 소유 계층 분리). **TS 상수 ↔ SQL CHECK 이원**(SQL은 상수 미참조) — 값 변경 시 마이그(CHECK+`handle_new_user` sanitize) 동반 의무(A4). /account 는 `getProfile`/`setProfile`·(코치)`getMyCoachKpc`/`setMyCoachKpc` 재사용(계약 +0). AuthGate·ProfileForm 로컬 상수 → 공유 import(중복 제거·렌더 무변경). 마이그 0. directive 2026-07-02 승인 |
| ADR-47 | 본부 코치 신청 큐(승인 대기) 구분 + 운영자 로그인 알림(/coach 배너) | 트랙 A3(항목4). /admin 을 두 섹션으로 구분 — ① 승인 대기(`listCoachApplications('pending')`→`decide_coach_application` 승인/거절, 승인 시 user→coach 원자 승격) ② 멤버 관리(기존 `setUserRole`). 운영자는 로그인 시 /coach 로 착지(loginOutcome 무변경)하므로 pending>0 이면 콘솔 상단에 '승인 대기 N건·본부에서 확인' 배너(→/admin). 계약·DB·마이그 0(기존 메서드·RPC·RLS admin 게이트 재사용). **로그인 착지 전제는 ADR-51(A′-1)이 대체 — admin도 /home 착지**; 승인 대기는 /home '본부' 카드 건수 + /coach 배너 이원(ADR-59). directive 2026-07-02 승인 |
| ADR-48 | 성별 허용값 '남'/'여' 2값으로 축소('남성/여성/기타' 폐기) | 트랙 A4(항목3). 성별=남/여 2값을 전 서비스 일관 규약으로 확정(지휘부). ADR-46 이원 동기화 의무 이행 — TS 상수(`GENDERS=['남','여']`) + SQL 마이그(`20260702002311`: 구 CHECK **DROP → 데이터 변환**(남성→남·여성→여·그 외 NULL) **→ 새 CHECK ADD** IN('남','여') → `handle_new_user` sanitize 교체) 동시 변경. 순서 필수(DROP 먼저 — 구 CHECK 살아있으면 변환값 '남'이 구 값집합에 걸림). 실측 기존 '남성' 1행→'남'. AuthGate·ProfileForm·AccountForm 은 공유 상수 참조라 자동 반영(재수정 0). 마이그 1. directive 2026-07-02 승인 |
| ADR-49 | 코드 전달 배선 — 코드 복사(clipboard) + 초대 링크 공유(Web Share·폴백) + /join `?code=` deep-link | 트랙 A5(항목: 코드 전달). CreateCohort 완료 스텝 placeholder 두 버튼 실배선 — 코드 복사=`navigator.clipboard`, 초대 공유=`navigator.share`(미지원 시 링크 clipboard 폴백). 피드백은 로컬 상태(토스트 미의존 — 미리보기 안전·비보안 컨텍스트 try/catch). 초대 링크=`${origin}/join?code=<코드>`; JoinClient 가 `initialCode` 로 받아 미리보기 자동 deep-link(코드 입력 생략, `cohort=` 재진입 우선). 계약·DB·마이그 0. directive 2026-07-02 승인 |
| ADR-50 | 빈/로딩/에러 상태 감사 + ConsoleHome '진행 중 차수' 빈 상태 보강 | 트랙 A6(항목: 상태 완결). 감사 결과 대부분 기존 처리 확인 — 빈 상태(MyCohorts·AllCohorts·AdminMembers 승인대기·MemberHome), 로딩(Next 서버 로드 + 버튼 busy + JoinClient resolving), 에러(`error.tsx` 경계 + 액션 토스트). 조용한 catch 들은 의도된 폴백(null→빈 폼·`notFound`·우아한 저하 ADR-27·재시도 상태)이라 보존(무분별 제거 금지). 유일 갭=ConsoleHome '진행 중 차수' 빈 목록 → 안내 문구 추가. 계약·DB·마이그 0. directive 2026-07-02 승인 |
| ADR-51 | 통합 홈 — 역할 감금 해제(비대칭 개방) | 트랙 A′-1. ADR-45 의 **역할별 거점 홈 전제를 대체**(홈 어포던스 메커니즘 `HomeIcon`·`HeaderActions.homeHref` 는 계승). `/home`·`/my/cohorts` 역할 리다이렉트 제거(전 로그인 사용자 개방) + `loginOutcome` 전원 `/home` + `MemberHome` '운영' 진입 카드(코치·운영자만 →/coach·/admin, 중립 팔레트·참여자 미노출·§0.4). **비대칭 개방**: 홈은 전원, 콘솔(`/coach` role=user→/home)·본부(`/admin`) 게이트 유지 — 데이터는 RLS 불변이라 홈 개방=UX 이득(보안 무관, A′-1 워크스루로 URL 직접접근 차단 실증). 계약·DB·마이그 0(`MemberHome` role prop=컴포넌트 시그니처). directive 2026-07-02 승인 |
| ADR-52 | 인증 영역 네비 정비(A′-3) — 현관(`/`) 복귀 + 상호 전환, 경량 인라인(AppHeader auth variant 미신설) | login·signup·reset·reset/confirm 에 `/`(현관) 출구 + 상호 링크(로그인↔가입↔재설정). signup 막다른 상태 해소(→/login·→/). **§5 Q2 판단**: 인증 페이지가 AppHeader 를 안 씀(독립 폼) 실측 → 경량 인라인 채택(root/sub/flow 에 variant 추가 안 함 — 모드 파급 0). 루트 `/` 링크는 `no-html-link-for-pages` 룰로 `next/link`, 하위 라우트는 기존 `<a>` 유지. **[ADR-120 정정 주: 이 결정은 뒤집혔다.** 당시에는 하위 라우트를 `<a>` 로 두는 대가가 보이지 않았다 — 그러나 그 결과 **클라이언트 라우팅이 앱에서 사라졌고**, 그래서 `src/app/loading.tsx` 가 만들어진 뒤로 **한 번도 발동하지 못했다**(발동 통로가 클라이언트 전환뿐이다). 참여자가 메뉴를 눌러도 화면이 그대로 멈춰 "안 눌렸다"고 판단하고 다시 누르는 실사용 보고(2026-08-25)가 그 대가였다. ADR-120 이 57 곳을 `<Link>` 로 전수 전환했다. **원 결정을 지우지 않는 이유는 ADR-86·94 때와 같다** — 지우면 `왜 `<a>` 로 두기로 했는가`가 사라져 다음 사람이 같은 판단을 반복한다.**]** 계약·DB·마이그 0. directive 2026-07-02 승인 |
| ADR-53 | 차수 상세 뒤로가기 = 진입 출처 기반(`?from=`) | 트랙 A′-4. `/coach/cohort/[id]` backHref 고정(`/coach`) → 진입 출처 분기: 콘솔 경유(`?from=console`)→`/coach`, 목록 경유(`?from=cohorts`)→`/coach/cohorts`, 출처 없음(직접)→목록 기본. push 지점(ConsoleHomeClient·AllCohortsClient)이 `from` 부여, 서버 page 가 읽어 backHref 산출. `?from=` 은 `/coach/cohort/[id]` 전용 — A5 `/join?code=`(ADR-49) 와 라우트·파라미터 무충돌 확인. 계약·DB·마이그 0. directive 2026-07-02 승인 |
| ADR-54 | root 홈 인지성 — 라벨드 홈 컨트롤 + 로고=서비스 역할 분리 | 트랙 A′-5. 우측 홈 어포던스를 아이콘 단독→**아이콘 + '홈' 텍스트 라벨**(인지성 강화). root 로고는 서비스 정체성(제목=접근성 이름·`aria-label="홈"` 제거)이되 홈으로도 링크(브랜드 관례) — "로고=서비스 / 우측=홈 복귀" 역할 명료화. ADR-45·51 어포던스 계승·구체화. 계약·DB·마이그 0. directive 2026-07-02 승인 |
| ADR-55 | 사후 진단 인프라·코치 개시 — `cohorts.post_opened_at` + `open_post_wave` DEFINER RPC(트랙 B-1) | 차수는 wave 중립(사전=개설=개방). 사후는 코치 수동 개시 = `post_opened_at`(nullable, NULL=미개시). `open_post_wave(p_cohort_id)` self-scoped DEFINER — `is_cohort_coach OR is_admin` 게이트, NULL→now() **단방향·멱등**, `post_opened_at`만 세팅(role/status/기타 불건드림 — 권한 상승 아님, 라이브 실증). 계약 +`openPostWave` + `my_cohorts`·`listCohortsByCoach` 반환에 `post_opened`(형상 변경·G1 명시). **`responses` UNIQUE 미추가**(ADR-33 재진단 허용 유지 — wave 컬럼+latestPerUser dedup으로 분리·페어링, 지시서 개정 반영). 마이그 `20260702051200`. directive 2026-07-02 승인 |
| ADR-56 | 참여자 사후 진입 — JoinClient wave 파라미터화(`?wave=post`) + MemberHome/MyCohorts '사후 진단하기'(트랙 B-2) | JoinClient `wave="pre"` 하드코딩을 `initialWave`(page `?wave=`)로 파라미터화 — 사후 진입 시 `getSchema('post')`·`wave='post'`로 saveResponse·채점(기본 'pre'·기존 불변). 홈/내차수: `post_opened && !post_done` → '사후 진단하기'(→`/join?cohort=&wave=post`), 사전 미완 시 pre 우선. `finalizeResponse` 는 `resp.wave` 재사용(무변경). 사후 dedup=pre의 `latestPerUser`(ADR-33) wave만 교체 — 하부 재사용. 재진입 UX=pre 동형. `?wave=`↔`?from=`(A′-4)·`?code=`(A5) 무충돌. 계약·DB·마이그 0. directive 2026-07-02 승인 |
| ADR-57 | 사전↔사후 비교 리포트 — 응답 존재 자동 감지(트랙 B-3) | 참여자 개인(`/my/cohorts/[id]/report`)·코치 그룹(`/coach/cohort/[id]/group`)의 `wave='pre'` 고정 해제 — 사전·사후를 각각 `latestPerUser`(ADR-33)로 최신 1건 페어링(다중 행이어도 최신끼리). 둘 다 있으면 비교, 사전만이면 사전 뷰 폴백(**자동 감지**·쿼리 불요). **이원 원칙 불변**(ADR-30): 참여자=순화(`participantMirror` 미러 2개 '세미나 전/후' 나란히·측정 0), 코치=리얼(`GroupView` 사전/사후 그룹 평균 2개 라벨). 계약·DB·마이그 0(기존 부품 재사용). directive 2026-07-02 승인 |
| ADR-58 | 차수 라이프사이클 — 마감=**진입 봉인(데이터 미봉인)** + CreateCohort 죽은 wave UI 제거(트랙 B-4) | 마감(`status='archived'`)은 **신규 가입만 차단**(`resolve_cohort_by_code`·`enrollByCode`가 이미 `status='active'` 필터 → 미가입자 preview/enroll 불가), **리포트·데이터는 상시 열람**(리포트 경로 status 미검), **가입자는 진단 완료 가능**(재진입 `?cohort=` 허용). expires_at 만료도 동일 차단(resolve 필터). 마감/재개 컨트롤은 CohortDetail(`updateCohort{status}`)에 기구현. CreateCohort의 사전/종료 wave 선택(저장 안 되던 죽은 UI) 제거 — 차수는 wave 중립, 사후는 개설 아닌 개시(ADR-55). **§5 판단**: '진입 봉인 not 데이터 봉인' 채택 — draft 유무 구분 없이 가입자 완료 허용(단순·저위험, '세미나에 있던 사람은 마칠 수 있다'). 계약·DB·마이그 0. directive 2026-07-02 승인 |
| ADR-59 | A·B 정합 마감 — 재점검(17 에이전트) 확정 이슈 수정(문서-코드 드리프트=작업 미완, CLAUDE §11) | **med**: (1) 내 리포트 라우트 `role!=='user'→/coach` 게이트 제거 → 전 로그인 사용자가 본인 참여분 순화 뷰 열람(A′-1 개방 정합·RLS 본인 스코프); (2) admin 로그인 알림 — admin은 loginOutcome로 /home 착지하므로 `/home` '본부' 카드에 승인 대기 건수 노출(/coach 배너와 이원); (3) `CohortDetail` '다시 공유' 실배선(onClick 부재 죽은 컨트롤 → 재공유, A5·ADR-49 로직 동형). **low**: `loginOutcome` role 인자·호출부 `currentUser()` 왕복·도달불가 폴백 제거; `copy.ts` 미사용 export(`notices`·`profileLabels`) 제거(`profileFieldsByWave`는 ADR-32/44 규범 참조라 유지); 스테일 주석 동기화(MyCohorts·HeaderActions·AppHeader·coach/page). 계약·DB·마이그 0. directive 2026-07-02 승인 |
| ADR-60 | `currentUser` 요청 단위 메모이즈(트랙 C-2 성능) | `SupabaseCoreContext` 인스턴스(요청마다 생성)에 currentUser Promise 캐시 — getPhone/requireUser/requireRole/setProfile 등이 내부 재호출해도 `getUser`(Auth 검증)+users SELECT 는 **요청당 1회**. **검증 우회 아님**(최초 1회 getUser JWT 검증 그대로·이후 검증된 결과만 공유). 특히 /coach 게이트(page currentUser + getPhone + getMyCoachKpc = 3× → 1×)·/account 유사 절감. **이중 getUser(proxy↔page 별 실행맥락)·`getClaims` 검증 교체는 위조 세션 검증 직결 보안 사안이라 미착수**(별도 보고·승인 후). 계약·DB·마이그 0. directive 2026-07-02 승인 |
| ADR-61 | 콘솔 차수 루프 병렬화(트랙 C-3 성능) | `/coach` 콘솔의 차수 집계 `for` 루프(차수 **간** 순차 await = wall-clock ∝ N)를 `Promise.all(cohorts.map(...))`로 병렬화 — 차수 **내** 4쿼리(enrollments/responses/alerts/members)는 기존 Promise.all 유지, 차수 간 순차만 제거. `map` 결과 배열의 입력 순서 보존으로 `summaries`·`careMembers`(flatMap) 순서 불변, 예외 전파는 for와 동일(첫 reject→페이지 error·조용한 삼킴 없음). 동일 Supabase·동일 데이터·동일 URL 실측(전=fa1e6f3 순차·후=636b18b 병렬): **차수당 한계비용 순차 +854ms(warm-min +437) → 병렬 +289ms(warm-min +253), N=3 wall-clock 3457→2619ms(median −24%·warm-min 2411→2034 −16%)**. N=1은 순차==병렬(warm-min 1538 vs 1528 — 단일 차수 sanity). 데이터 무손상: 콘솔 렌더 본문(스크립트 제거) N=1 4405B·N=3 5769B가 전후 **바이트 동일**(명단·돌봄·응답 순서·집계 불변). 잔여 스케일링(+253ms/차수 warm-min)은 Supabase 커넥션 풀링(N차수×4쿼리 동시 일부 큐잉)+RSC payload 증가에서 옴 — 현실적 차수 수(소수)엔 충분히 완만. **(A) 집계 DEFINER RPC(왕복 1회)는 buildCohortRoster 로직/원시행 + RLS 동등 게이트 복제 복잡도 대비 이득 제한적이라 미착수**(대량 차수 코치가 실병목일 때 재검토). 계약·DB·마이그 0. directive 2026-07-02 승인 |
| ADR-62 | 낙관적 UI 시범 — 차수 이름 변경(트랙 C-4 성능·봉인) | 액션 성공 후 `router.refresh()` 전체 재렌더(액션마다 바닥 ~1.5s 재발생)를 낙관적 전이로 대체. 시범=**차수 이름 변경**(저위험: 자기 소유 설정·완전 가역·RLS 코치스코프, 서버가 `name.trim()` 그대로 저장 → **낙관적 값 == 서버 값·드리프트 0**). `CohortDetailClient` 가 `optimisticName` 오버레이(`effectiveName = optimisticName ?? summary.name`)를 헤더에 즉시 반영(refresh 없음), 실패·예외 시 이전 이름 롤백 + 에러 토스트. 오케스트레이션 = `applyOptimistic`(cohortAdmin 순수 헬퍼·단위테스트 3): 성공→onCommit, 실패/throw→onRollback(error) — **조용한 삼킴 금지**(실패든 예외든 사용자에 롤백/에러 노출). **고위험 제외**: decide(승인/거절)·역할 승격 등 person-affecting 비가역은 낙관적 미적용(directive). 나머지 5개 관리 액션(정원·소개·마감·재개·사후개시)은 현재 `run()`+refresh 유지 — 확대 시 동일 패턴 재사용. force-dynamic 재평가는 대부분 per-user 저이득이라 최소. 계약·DB·마이그 0. directive 2026-07-02 승인 |
| ADR-63 | general 공개 체험 진단 — 예약 코드 차수 인프라(트랙 D-1) | 세미나 코드 없이 **로그인** 사용자가 체험하는 general 진단 = **예약 차수** 방식(진단 파이프라인 재사용, 예약 진입로만 얹음). 마이그 `20260702131316`: `cohorts_code_check` 를 `~ '^[…]{5}$' OR code='JOINF'` 로 확장(5자 랜덤 생성/검증 무변경 — 예약어 1개만 예외) + general 차수 1건 시드(운영자 소유·상시 active·무기한 `expires_at NULL`·`max_members=1000000` 사실상 무제한[enrollByCode 정원검사 회피]·`instrument=futurenow`·`code='JOINF'`). `coach_id`=최초 admin 서브쿼리(생성 UUID 하드코딩 회피), `ON CONFLICT(code) DO NOTHING` 멱등. **보안**: 코치 코드 생성은 랜덤 5자만(`context.ts newCode`) → `JOINF` 위조 불가; general 진입도 로그인 필수(익명 아님). `resolve_cohort_by_code(UPPER)` 가 `JOINF`/`joinf` 모두 반환 → `/join?code=JOINF` 딥링크가 기존 preview→가입→진단 흐름에 합류(D-2). 예약 코드=앱 상수 1곳 격리(`general.ts GENERAL_CODE`) + DB 마이그 하드코딩. **D-2**(진입 UI): CodeInput 하단 '체험 진단 시작하기'(prop 미전달 시 미노출 — 세미나 코드용 무변경) → `onCode('JOINF')` → `previewCohort` → CohortPreview `isGeneral`(인도자·인원 숨김·체험 문구·'체험 시작하기'). **D-3**(정책·리포트): general 은 **사전 wave 고정**(사후 개시 없음), 참여자 본인 순화 거울은 기존 `/my/cohorts/[id]/report` 그대로(RLS 본인 스코프·측정/신호 0 유지), 운영자는 소유자라 기존 코치 콘솔 재사용으로 general 열람(별도 배선 0·Q2 노출). **워크스루 실측**(throwaway 참여자→삭제·general 보존): /join 체험 진입 노출·`?code=JOINF` 200·`/my/cohorts` general 열람·본인 리포트 200 미러 렌더·**신호 0**. 계약 0·마이그 +1. directive 2026-07-02 확정(코드=`JOINF`·운영자 콘솔 노출·CodeInput 하단 진입). |
| ADR-64 | 코치 리포트 해석 **비차단 생성**(B③-A) | 코치 리포트 page(`/coach/cohort/[id]/report/[responseId]`)가 `generateInterpretation`(aiChat ≤28s)을 렌더 경로에서 **동기 await** → **첫 열람 블로킹(실측 34,892ms)**. 분리: 서버 컴포넌트는 `getInterpretation`(existing·빠름)만 조회 → 있으면 즉시 표시, 없으면 클라이언트 `InterpretationPanel`(신규)이 마운트 후 `ensureInterpretationAction`(**유일 신규 표면** — 생성 트리거 서버 액션)으로 백그라운드 생성. **리포트 시각화(ReportScreen)는 항상 즉시 렌더**(해석 무관). 실패·타임아웃 → 재시도 안내(조용한 실패 금지). **비차단 방식 (A)클라 트리거 채택**(B③-B 검수 컨트롤이 어차피 클라 컴포넌트 필요 → 통합; (B)Suspense 스트리밍은 응답 26s 개방+throw 시 error boundary 위험·재시도 어색). **중복 생성**(동시 열람): `generateInterpretation` existing 선확인 + `saveInterpretation` '없을 때만' + `aiChat useCache:true`(동일 입력 캐시)로 완화 — 락(DB 커넥션 26s 점유)은 기각, 드문 중복 aiChat 은 낭비이나 정합 안전. 참여자 리포트는 해석 대상 아님(무변경). **측정 전후(동일 Supabase·동일 응답)**: 신선 리포트(해석 없음) 열람 TTFB **블로킹 34,892ms → 비차단 2,466ms(~14×·93%↓, 리포트 시각화+플레이스홀더 즉시)**; existing 해석은 5,082ms 즉시 표시(재생성 0). 게이트웨이 정상 작동 확인(claude-sonnet-4-6·~35s 느림 → 비차단 정당성 실증). 계약 0(aiChat·getInterpretation·saveInterpretation 재사용)·마이그 0. directive 2026-07-02 승인 |
| ADR-65 | 코치 리포트 해석 **검수 UI**(B③-B·Q5) | `InterpretationPanel` 에 검수 컨트롤 배선 — **'다듬기'**(구조화 편집기: 전체 인상·축별 읽기·돌봄 안부(선택)·성장 여지 textarea → `setCoachInterpretation`·edited_by 본인·edited_at now) · (코치 수정본일 때만)**'AI 원문으로'**(`clearCoachInterpretation` → coach_content=null → effective=AI 원문). `effective`=coach본 우선(InterpretationView coalesce), 출처 배지('코치가 다듬어 확정' vs 'AI 초안'). 저장/되돌리기 실패는 토스트(조용한 삼킴 금지)·제목/성장 빈값 가드. 신규 액션 `saveCoachInterpretationAction`·`clearCoachInterpretationAction`(계약 setCoachInterpretation·clearCoachInterpretation 래퍼 — 계약 델타 0). **권한 격리 실측**: `report_interpretations` RLS 4정책(SELECT·UPDATE·INSERT·DELETE) 전부 `is_cohort_coach(cohort_id, auth.uid()) OR is_admin(auth.uid())` → **참여자·비소유 코치 열람·수정 불가**(소유 코치+운영자만); page role 게이트(role=user→/home) 이중. 계약 0·마이그 0. directive 2026-07-02 확정(Q5 검수 단계 둔다). |
| ADR-66 | 이중 getUser dedup — page 검증 왕복 제거(S-1 보안·성능) | 요청당 Auth 왕복 2(proxy `getUser` + page `currentUser`의 `getUser`)를 **1로** 줄임. proxy가 검증한 신원을 헤더로 page에 전달 → page는 재검증 없이 `users` SELECT만. **신뢰 경계(핵심)**: proxy가 인입 `x-futurenow-verified-uid` 를 먼저 **strip** 하고 자기 검증값만 세팅(미들웨어 matcher가 정적 자산 외 **전 경로 커버** → 전수 strip) → 클라이언트 위조 무효(proxy만 세팅 가능). page `loadCurrentUser`: 헤더 있으면 `getUser` 생략·id로 SELECT, 없으면 `getUser` fallback. **role·email 미탑재**(민감·스테일 회피 — role은 SELECT 유지, 클레임화 배제 §1). 서버 팩토리 `createServerContext`(헤더 리더+주입)로 18 서버 호출부 마이그레이션(클라이언트 3곳은 브라우저 getUser fallback 유지). **proxy 세션 갱신 유지**(`getUser` 무변경 → 만료 토큰 refresh + refresh 쿠키 최종 response 보존). **보안 실증(로컬 next start)**: 위조 헤더 단독(쿠키 없음)→/home·/account **307 /login**(strip·위장 불가), 세션+위조 헤더→**실제 세션 신원 판정**(위조 무시), 미인증→/login(차단 유지). **측정 전후**: /home median 1573ms → 1596ms(warm-min 1141→899). page getUser 제거의 in-region 왕복 절감은 SSR 바닥(~1.5s) 대비 작아 median 노이즈 내이나, **왕복 수 2→1 결정적**(page getUser 소거·코드 명시). **프로덕션 위조-거부 재실증 통과**(Vercel 미들웨어도 strip 동작 확인 — 로컬과 동일). 계약 0·마이그 0. directive 2026-07-02 승인 |
| ADR-67 | 차수 하드삭제 + '인도자' 용어 통일 | **삭제**(`deleteCohort` 계약+코어+`deleteCohortAction`+UI 2단계 컨펌·위험색). 규칙: **예약 general 차수(JOINF)는 운영자 포함 삭제 금지**(앱 액션 가드·인프라 보호) · **운영자=임의 차수**(데이터 있어도·컨펌에 참여/응답 영향 규모 표시) · **코치(소유)=빈 차수만**(참여·응답 0 — 데이터 있는 차수를 코치가 지워 응답이 SET NULL 고아화되는 파괴를 코드 경계에서 차단, 데이터 있으면 마감 유도). RLS `cohorts_delete`=`coach_id=auth.uid() OR is_admin` 가 **소유**를 강제(하드 경계=소유권; 빈-판정·예약가드는 앱 레벨 product 가드 — 소유자 자기 데이터). 성공 시 차수 소멸 → 목록 이동. FK: enrollments/response_drafts CASCADE·responses/alerts/report_interpretations SET NULL. **마이그 0**(cohorts_delete 정책 기존). 초기 정리: loginheaven 빈 테스트 차수 2개 삭제(JOINF·타 계정 데이터 차수 보존). **용어 통일**: 사용자-노출 '코치'→'인도자'(인도자 콘솔·본부 인도자 신청/승격·로그인 안내·검수 출처 배지·AuthGate 신청·승격/승인 토스트·ROLE_LABEL). **유지(#3·#4 확정)**: 코드/DB 식별자(`role='coach'`·`coach_id`·`is_cohort_coach`·`listCohortsByCoach`)·`/coach` URL·KPC(외부 자격명 원문)·AI 시스템 프롬프트(★최박사 고정)·내부 주석. 계약 +`deleteCohort`. directive 2026-07-03 승인. |
| ADR-68 | 진입 플로우 출구 보완 — `/join` 전진밖에 없는 화면(A′ 정합) | A′-3(현관 복귀)이 auth 영역(login/signup/reset)만 덮어, **`/join` 진입 플로우 5개**(`CodeInput`·`CohortPreview`·`AuthGate`·`StartGuide`·`ProfileForm`)가 `flow` variant(back·홈·로고 전무·"진단 선형성")로 남아 출구가 없었음(전수조사). 진입 스텝은 **응답 전**이라 홈 이탈이 안전(가입·차수·draft 유지·재개 가능) → `sub` 헤더로 전환(우상단 **홈 아이콘=/home** + 가능한 곳 **‹뒤로=이전 스텝**). `AuthGate` 는 /join·/signup 공유라 **`onBack` 유무로 분기**(/join=sub 뒤로+홈, /signup=flow 유지 — SignupClient 의 A′-3 현관 링크가 출구). **`ResponseRunner`(응답 중)는 제외**(흐름상 홈 이탈 어색 — 문항 이전/다음+자동 저장으로 후진·보존). `Completion`(→/home)·auth 영역·그 외(sub ‹뒤로+홈 / root 로고=홈)는 기존 충족. 계약·마이그 0. directive 2026-07-03 승인. |
| ADR-69 | 개인 리포트 PDF 저장(A4) | 코치 개인 리포트(`/coach/cohort/[id]/report/[responseId]`)에 **'PDF로 저장'** 배선 — `window.print()` + `@media print`(`@page{size:A4}`·`print-color-adjust:exact`)로 브라우저 "PDF로 저장". **무거운 라이브러리·서버 헤드리스 없이** 벡터 텍스트·SVG 차트 그대로(래스터화 0·선명). `.no-print`(앱 크롬·검수 버튼·재시도 제외) / `.print-only`(브랜드 문서 헤더 `ReportPrintHeader`: 퓨처나우·"개인 진단 리포트"·대상·차수·회차·날짜)로 화면↔인쇄 분리. 패널 `break-inside:avoid`(카드 페이지 분할 방지)·`.report-print-root` max-width 해제(A4 폭). page 가 getCohort·listCohortMembers 로 헤더 메타 조회(소유 코치 RLS 통과·실패 폴백). 기존 디자인 토큰만(디자인시스템 §8 준수). 신규 파일 `ReportPrintButton`(client)·`ReportPrintHeader`. **인쇄 페이지 순서**(2026-07-03): 차트(1p) → 해석·코칭(2p) — 화면은 해석이 위(코치 편집 편의) 유지, **인쇄에서만 flex `order` 로 재배치**(`.report-charts-block` order 1·`.report-interp-block` order 2·DOM/화면 무변경). 계약·마이그 0. directive 2026-07-03 승인. |
| ADR-70 | 멤버 하드삭제(운영자 임의) — 본부 멤버 관리 | 운영자가 **임의 멤버**를 계정째 삭제(`deleteMember` 계약+코어+`deleteMemberAction`+MemberRow 세부 내 2단계 컨펌·위험색). `delete_user`(DEFINER, 마이그 `20260704120000`): `is_admin` 게이트 + **자기삭제 금지**, `DELETE FROM auth.users` 1회 → `users_id_fkey ON DELETE CASCADE` 로 public.users·하위 전량 연쇄. **연쇄 범위**: CASCADE=cohorts(소유)·enrollments·user_profiles·user_contacts·coach_applications·**groups/group_members(SAIL 공유)**; SET NULL=responses·results·report_interpretations.edited_by(응답 고아 보존·불변 유지). 즉 **코치 삭제 시 소유 차수·그 참여가 함께 소멸·응답 고아화** — 파괴적이라 세부(#2 활동)에 소유 차수·응답 수 표시 + 컨펌에 '인도 차수 N개 함께 삭제' 고지(**인지 삭제**). SAIL 공유 영향(groups cascade)은 지휘부 승인(2026-07-03). **라이브 검증(throwaway admin/victim코치/enrollee, `set_config` 임퍼소네이트)**: 가드 3종 PASS(non-admin→'admin only'·self→'cannot delete self'), happy-path 삭제 후 victim auth/public/cohort/enroll/profile/contact **0**·응답 user_id/cohort_id **SET NULL 고아 보존**·enrollee 계정 무영향. 잔여 0 정리. 계약 +`deleteMember`·마이그 +1. directive 2026-07-03 승인. |
| ADR-71 | 멤버 세부정보(신원+활동) — 본부 멤버 관리 | 본부 멤버 리스트에서 **이름 클릭 → 세부 펼침**(신원+활동). `MemberRow`(신규 client): 펼칠 때 `memberDetailAction`(신규 액션) 1회 조회·캐시 — **신원**=전화(`getPhone` 게이트)·프로필(`getProfile`: 성별·출생연도·종교·신앙연수), **활동**=`getMemberActivity`(계약+코어→`admin_member_activity` DEFINER, 마이그 `20260704120000`): 소유(인도) 차수·참여 차수·응답 수(운영자 `is_admin` 게이트로 users RLS 밖 enrollments/responses 안전 집계). 세부 펼침이 삭제(ADR-70)의 자리(영향 고지원). `gender` 표시값은 저장값 `'남'/'여'`→'남성'/'여성' 매핑(gender_two_values 정합 — 검증 중 'male'/'female' 오매핑 발견·수정). **라이브 검증**: `admin_member_activity(victim)` = owned[`MV검증차수`]·enrolled[`MV검증차수`]·responses 1·non-admin→'admin only'. 계약 +`getMemberActivity`·+`MemberActivity`·마이그 공유(ADR-70). directive 2026-07-03 승인. |
| ADR-72 | 개인 리포트 해석 **자동 사전생성**(진단 종료 시 · 비차단) | 참여자가 진단을 마치면 `finalizeResponse` 가 `after()`(next/server)로 코치 해석 초안을 **배경 생성·저장** → 코치가 이름 클릭 시 **즉시 열람**(첫 열람 ~수십초 지연 제거, ADR-64 지연생성을 사전화). after 는 참여자 응답 반환 **뒤** 실행이라 완료 UX 지연 0; /join `maxDuration=60` 으로 배경 aiChat 예산 확보. ctx 의 supabase 클라이언트는 생성 시 캡처된 `cookieStore`(클로저)라 after 에서도 참여자 JWT 유지. **저장 경로**: 참여자는 `report_interpretations` INSERT RLS(코치·운영자) 밖 → `save_report_interpretation`(DEFINER, 마이그 `20260704130000`) 신설: 자격=**응답 소유자 OR 차수 코치 OR 운영자**, 없을 때만(ON CONFLICT DO NOTHING·멱등), 저장/기존 **행 반환**(DEFINER 라 RLS 우회 반환 → 참여자 경로도 재조회 없이 뷰 구성). `saveInterpretation`(코어)를 이 RPC 로 전환 — 코치 지연생성 경로도 동형(자격에 코치 포함). **§7·§9.4 분리 유지**: 참여자는 '저장'만, **열람(getInterpretation SELECT RLS)은 여전히 코치·운영자 전용** → 참여자는 자기 임상 해석을 못 본다. **트레이드오프**: authenticated EXECUTE 라 참여자가 자기 응답 해석을 직접 RPC 로 1회 선점 가능(자기범위·코치 '다듬기'로 복구·타인 무영향) — 서비스롤 클라이언트(앱 전면 RLS 우회 신설) 회피한 최소 확대. 실패는 무해(코치 첫 열람의 지연 생성이 폴백). **라이브 검증(throwaway coach/P/P2·set_config·SET ROLE)**: 소유자 저장 성공(H1)·멱등(재저장 H1 보존)·비소유 차단('not authorized')·코치 생성(C2)·**참여자 열람 0/코치 열람 1**(§7). 잔여 0. 계약 0(saveInterpretation 재사용)·마이그 +1. directive 2026-07-03 승인. |
| ADR-73 | 참여자 휴지통 — 차수에서 제거(오염·오등록 정리) | 코치 콘솔 차수 상세 명단(먼저 챙길 분·응답 완료·아직 안 함)의 각 행에 **휴지통**(🗑) — 눌러 **2단계 컨펌** 후 그 참여자를 **이 차수에서** 제거. `remove_cohort_member`(DEFINER, 마이그 `20260709120000`): 자격=**해당 차수 코치 OR 운영자**(is_cohort_coach OR is_admin), 삭제=responses(→alerts·report_interpretations `response_id` ON DELETE CASCADE 자동 정리)·response_drafts·enrollments **이 차수 한정**. 계정·타 차수 데이터는 불변. **불변식 예외(§10)**: responses·alerts 는 일반 경로 불변이나, 오염·오등록 정리는 관리 작업이라 코치(자기 차수)·운영자 게이트 DEFINER 로만 삭제 허용(참여자·직접 테이블 경로 불변 유지). UI: `RosterRow`(신규 — ListRow + 휴지통), `RosterMember.userId` 추가(응답자 행 id=responseId 와 별도 참여자 식별), 서버 `canManageMembers`(운영자 OR 소유 코치)로 휴지통 노출 이중 게이트. 계약 `+removeCohortMember`. **라이브 검증(throwaway coach/P/X·set_config 임퍼소네이트)**: 비권한→'not authorized', 코치가 A에서 P 제거→응답·alert·해석(cascade)·draft·참여 0·**타 차수 B 무영향(1/1)**·**P 계정 보존**·운영자 경로 정상. 잔여 0. **초기 정리**: 실제 과제 '퓨처나우2026예봄'에 오염된 테스트 계정 '통통이'(0@0.com) 계정·응답·참여 완전 삭제(응답 10→9·고아 0). 마이그 +1. directive 2026-07-09 승인. |
| ADR-74 | 운영자 콘솔 = 전체 인도자 차수 감독(수퍼바이저 뷰) | 인도자 콘솔(`/coach`·`/coach/cohorts`)이 `listCohortsByCoach(me.id)`로 **본인 소유 차수만** 조회 → 소유 차수 없는 운영자에게 '차수 없음'으로 보이던 문제. 운영자는 인도자의 수퍼바이저이므로 **모든 인도자 차수**를 봐야 함. `listAllCohorts()`(계약+코어) 신설 — coach 필터 없는 cohorts SELECT, **RLS(cohorts_select `is_admin`)가 운영자=전체·그 외=자기 스코프**로 이중 강제(비운영자 호출도 안전). 두 콘솔이 `me.role==='admin' ? listAllCohorts() : listCohortsByCoach(me.id)`. 운영자 뷰는 각 차수 **소유 인도자 이름**(`CohortSummary.coachName`, `listUsers` id→name 맵)을 카드에 표시('인도자 …') + 헤딩('모든 인도자 차수'). 운영자는 어느 차수든 들어가 **참여자·진단내역 열람**(RLS 전부 `is_admin` — 검증 9/9)·**차수 삭제**(ADR-67 운영자=임의, 예약 체험 제외)·**참여자 제거**(ADR-73) 가능 → 감독 권한 완성. **라이브 검증(set_config 임퍼소네이트)**: 이승은(운영자)이 보는 차수 3/실제 3. 계약 +`listAllCohorts`·마이그 0. directive 2026-07-09 승인. |
| ADR-75 | 차수 멤버 신상정보 — 코치=자기 조원(§10 완화)·리포트 패널 + 참여자 전화 필수 수집 | 운영자·인도자가 회원 인적사항(이름·전화·이메일·성별·나이·종교·신앙연수·참여이력)을 열람. **(1) 수집**: 참여자 전화 0건이던 원인=가입 폼이 코치 신청 시에만 전화를 받음 → `AuthGate` 전화를 **전 참여자 필수**로 승격(JoinClient 가입 직후 `setPhone` 저장, SignupClient 는 기존 저장). **(2) 접근(§10·ADR-04 완화)**: 전화(`user_contacts`)는 본인·운영자만이었으나 `cohort_member_detail`(DEFINER, 마이그 `20260709150000`) 신설 — 자격=**운영자(전체) OR 해당 차수 코치 + 대상이 그 차수 실제 구성원(참여/응답)**. 코치가 남의 user_id 를 주입해도 구성원 검사로 차단. 참여 이력은 호출자 가시 범위 스코프(운영자=전체·코치=자기 차수). `getPhone` 자체는 무변경(본인·운영자) — 완화는 이 RPC 한정. **(3) 표시**: 개인 리포트 상단 `MemberProfilePanel`(화면 전용 `.no-print` — 연락처는 공유 PDF 미포함) — 전화 `tel:`·이메일 `mailto:` 링크. 본부 멤버 상세(ADR-71)는 이미 전화 표시(수집되면 채워짐). 계약 +`getCohortMemberDetail`·+`CohortMemberDetail`. **라이브 검증(set_config 임퍼소네이트)**: 코치가 자기 조원 열람 성공(전화 010-…·이름·이메일·참여이력)·비구성원 'not authorized'·운영자 성공·외부인 'not authorized'. 잔여 0. **전화 필수화는 신규 가입부터** — 기존 참여자는 다음 `/account` 보완 전까지 공백(백필 갭). 마이그 +1. directive 2026-07-09 승인. |
| ADR-76 | 개인정보 동의(멤버 수집·이용 + 인도자 보호 서약) + 주소·계좌 수집 | 개인정보보호법 정합. **`user_consents`**(마이그 `20260709160000`, `(user_id,type)` PK · RLS 본인 기록/조회 + 운영자 조회) — type: `privacy_use`(멤버 필수)·`sensitive_use`(민감 선택)·`coach_pledge`(인도자 서약). `CONSENT_VERSION`(앱 상수)으로 약관 개정 시 재동의 판정. **① 멤버 수집·이용(필수)**: 가입 폼(AuthGate) 필수 체크 — 미체크 시 가입 버튼 비활성. 보유·이용 **1년**. **② 민감정보(종교·신앙) 선택 동의**: PIPA 민감정보라 별도 동의 — 체크해야 종교·신앙 입력란 노출·수집(미동의 시 미수집). **③ 인도자 서약**: `CoachInfoGate` 서약 체크(needPledge) — 서약 전엔 콘솔·조원 신상 접근 불가(/coach 게이트가 phone·KPC·pledge 완비 요구). **소급(기존 회원)**: `/home` `ConsentGate`(privacy_use 최신 버전 미동의 시 홈 대신 동의 화면 렌더 → 기록 후 통과). **수집 확장**: 전화 필수(ADR-75)에 더해 **주소·계좌(개근장학금)** 선택 입력 → `user_contacts`(운영자·본인 격리, contacts_self/admin_read) — **인도자 비노출**(운영 목적, `cohort_member_detail` 은 phone 만), 본부 멤버 상세(운영자)만 표시. 계약 +`recordConsent`·`listMyConsents`·`getContactDetail`·`setContact` · 타입 `ContactDetail`·`ConsentType`·`ConsentRecord`. **라이브 검증(set_config·SET ROLE)**: 본인 동의 기록·조회 성공·**타인 기록 RLS 차단**('violates row-level security')·운영자 조회 성공. 잔여 0. **1년 자동 파기 잡은 후속**(이번 범위=동의·기간 기록까지). 마이그 +1. directive 2026-07-09 확정(문안·소급 승인). |
| ADR-77 | 코치 리포트 상세화 — 인도자 전용 숨은 층(함정·믿음) + 참여자 원응답(운영자 포함) | ORDER `CC_ORDER_coach_report_detail_v2`(2026-07-04) 실행. 두 공백을 **표시만**으로 닫음(채점·AI 입력·참여자 경로 불변). **2면 함정·믿음**(Phase 1): `labels.ts` +`TRAP_AXES`(D1 관성·D2 준비·D3 안주)·`FAITH_LABELS`(F1 의미·F2 실행). `FacilitatorPanel`(신규) — scoring 이 이미 낸 `trap`(주 함정 강조 + D1~3 원점수)·`faith`(값 또는 '무응답') 표시, ReportScreen 하단(주관식 다음), 중립 톤(care 금지 §2-4). **3면 참여자 원응답**(Phase 2): `resp.answers`(추가 조회 0)를 `copy.ts`(`itemPrompts`·`bipolarLabels`·`askPrompts`·`likertLabels`·`blockIntros`) 원문으로 섹션 렌더(들어가며·나침반·지금의 나·믿음·간격·주관식), ReportScreen 다음 접이식. **열람 범위(§4.3 최박사 확인=운영자 포함, 2026-07-13)**: 원응답=리포트 경로 권한(차수 코치+운영자) 재사용 — ADR-74 수퍼바이저·동의서(440f985) 열람주체 정합. 이에 진단지 약속을 정직하게 갱신: `StartGuide`·`ResponseRunner` '인도자 한 사람만 봅니다' → '인도자와 운영자만 봅니다'. **경계 준수**: `scoring.ts`·`interpretation.ts`(AI 입력 — 함정·믿음·원응답 전량 미입력, E1~E3 200자 클립만) 불변, 참여자 `MirrorView`/`participantMirror` 불변(정성 '믿음 한 줄'은 F1/F2 값·D코드 아님 → 유지). 테스트(§5): 패널 렌더·배치 순서·미러 회귀가드(D/F 코드·트랩 라벨·원응답 문항 부재)·AI 입력 가드·null 처리. **Phase 3(로스터 주 함정 태그) 완료**: 차수 상세 명단(RosterRow)에 주 함정 라벨(관성/준비/안주) 중립 pill — 소그룹 편성 참고. 채점은 futurenow-aware 페이지(`/coach/cohort/[id]`)가 이미 로드된 답안으로 사용자별 최신 응답 score(추가 쿼리 0·in-memory), `trap.primary`→라벨을 `trapByUserId` 로 제네릭 `buildCohortRoster` 에 주입(rosterModel 인스트루먼트 비결합 유지 — 문서의 'rosterModel이 채점' 대신 경계 보존형 채택). futurenow 차수만·응답자만·미주입 시 태그 0. 계약 0·마이그 0. directive 2026-07-04(v2 ORDER) 실행. |
| ADR-78 | 리포트 신상정보 버튼→팝업 + 주소 코치 열람 | 코치가 진단결과를 보다 조원 인적사항을 바로 확인(홈→멤버관리 왕복 제거). 개인 리포트 툴바 **'신상정보 보기'** 버튼 → 모달(`MemberProfileButton` 신규·client, ESC·배경·✕ 닫기)에 `MemberProfilePanel`(이름·전화·이메일·**주소**·성별·나이·종교·신앙연수·참여이력). 화면 전용(연락처 PDF 미포함). **주소 열람 확대(ADR-76 완화)**: `cohort_member_detail`(마이그 `20260713120000`, DROP+재생성으로 컬럼 추가)에 `address` 추가 — 목양 방문 목적상 자기 차수 코치·운영자에게 개방(동의서 440f985 열람주체=본인·인도자·운영자 정합·전화와 동일 스코프). **계좌(운영·장학금)는 미포함** — 본부 멤버상세(운영자·`getContactDetail`)만 유지. 기존 인라인 패널(ADR-75)을 버튼+팝업으로 전환. 계약 `CohortMemberDetail` +`address`. **라이브 검증(set_config 임퍼소네이트)**: `cohort_member_detail` 이 (name·phone·address) 반환·에러 0. 마이그 +1. directive 2026-07-13 승인. |
| ADR-79 | 운영자 임시 비밀번호 리셋 UI(계정 복구) | 이메일 발송(Resend 도메인 미검증)으로 자가 비번 재설정이 막힌 회원을 운영자가 직접 복구. 본부 멤버 상세(ADR-71 펼침) **신상정보 맨 아래**에 '비밀번호 리셋 (임시)' 입력칸+`[비번 리셋]` → **1단계 확인**(대상·설정값 명시) → '변경'. `admin_set_temp_password`(DEFINER, 마이그 `20260713140000`): `is_admin(auth.uid())` 게이트·최소 8자·`auth.users.encrypted_password = extensions.crypt(pw, gen_salt('bf',10))`(pgcrypto bcrypt). **auth.users 쓰기(SAIL 공유 테이블)**: 기존 비번 컬럼만 갱신(스키마 무변경)이라 SAIL 무영향 — 운영자 게이트 DEFINER 로만. 성공 시 설정 비번을 **토스트로 1회 표기**(운영자가 사용자에게 전달·저장 안 함) + 입력칸 비움, 로그인 후 변경 안내. **강제 변경(다음 로그인)은 후속** — 이번 범위=복구 경로 확립. 배선: 계약 +`setMemberPassword`·코어·`setMemberPasswordAction`·`MemberRow`(입력·확인 UI)·`AdminMembers`/`AdminClient`(`onSetPassword` 스레드, 토스트는 provider 있는 AdminClient 소유 — useToast 무프로바이더 예외 회피 부모핸들러 패턴). **라이브 검증(set_config·SET ROLE 임퍼소네이트·throwaway)**: 운영자 설정→비번 매칭 true·비운영자→'admin only'·8자 미만→'password must be at least 8 chars'. 잔여 0. 계약 +`setMemberPassword`·마이그 +1. directive 2026-07-13 승인. |
| ADR-80 | 회차 갈무리는 `responses` 밖에 둔다 — `checkins`·`cohort_sessions` 신설 | 7주 세미나 매 회차 종료 후 참여자가 스스로 정리하는 **갈무리 카드**. 진단이 아니라 **수정 가능한 문서**(채점·리포트·AI 입력 미배선). `responses`에 넣지 않는 3근거: (1) `responses`는 불변(UPDATE/DELETE 정책 없음), (2) `wave`가 pre/post로 잠겨 페어링 인덱스·채점·비교 파이프라인이 전체를 훑음 → 회차 값 혼입 시 사전-사후 비교 오염, (3) `cohorts.instrument_id` 단일값이라 별도 인스트루먼트 등록 불가. **테이블(마이그 `20260727100000`)**: `cohort_sessions`(차수별 회차 일정 — 날짜만, 편성은 코드 상수)·`checkins`(`UNIQUE(cohort,user,session)`+upsert — ADR-33 append 와 다른 규약, 성격=되돌아와 고쳐 쓰는 문서). **쓰기 전량 DEFINER RPC**(`checkin_save`/`checkin_submit`/`checkin_mark`·`seed_cohort_sessions`) — `is_cohort_member` 게이트. **D1(권한 격리)**: 이 프로젝트는 default privileges 로 신규 테이블에 authenticated 전체 write 를 자동 부여하므로, `checkins` write 를 **명시 REVOKE**(마이그 `20260727110000`) — 참여자의 `submitted_at` 위조·`edit_count`/`prompt_count`(1기 보정 근거) 조작 차단. SELECT 만 GRANT + DEFINER RPC 로만 쓰기(테이블 REVOKE 는 유효, 컬럼 REVOKE 무효와 다름). `has_content`는 checkin_save 가 쓰기 시점 계산(빈 행 존재≠작성 중 — 안내만 받은 사람 오분류 방지). `is_late`는 미저장·`submitted_at>closes_at` 파생. **일정 시드는 개설이 아니라 인도자가 날짜 아는 시점**(콘솔 회차 일정 UI) — `cohorts` 에 시작일 두면 `cohort_sessions` 와 이중 진실·플레이스홀더 날짜는 게이트 오작동. '일정 미등록'은 정상 상태(참여자=담담 안내·콘솔=조치 항목). **`my_cohorts` 8→11필드**(`open_session_no`·`open_session_submitted`·`open_session_has_content` 추가, `post_opened`·`joined_at` 보존 — DROP+CREATE 회귀 주의). **배포 순서(D2)**: `RETURNS TABLE` 변경이라 마이그→원격검증→앱 배포(역순 시 `openSessionNo=undefined`→`/checkin/undefined` 파손), 매퍼 `?? null`/`?? false` 방어. 계약 +`CohortSession`·`CheckinRecord`·`MyCohortSummary`+3필드·`CoreContext`+9메서드. **경계 zod**(§9·S4): 코어는 인스트루먼트 어휘 무지 — 일반 구조 한계(문자열 2000자·배열 8·32KB)만. **라이브 실증(set_config·SET ROLE 임퍼소네이트·throwaway 2차수)**: seed 7행·has_content 공백false/내용true·submit·**직접 INSERT/UPDATE 거부(permission denied)**·member 본인 SELECT 1·sessions INSERT RLS 거부·prompt 상한 2·타차수코치 0·담당코치 1·코치 UPDATE 거부·운영자 1·seed 멱등·잔여 0. `has_column_privilege` INSERT/UPDATE=false. 마이그 +2. directive 2026-07-27 승인. |
| ADR-81 | 회차 갈무리 화면 — 차수 홈 신설 + 1회차 카드 + 노출 정책(상시 진입로·모달 비강제) | 갈무리 진입로를 **세 층**으로: 현장 QR(주력)·인도자 카톡 링크(결석자)·앱 내 안내(부수). 앱 내 안내는 회차당 두 번 상한(`prompt_count` 서버 판정)·'나중에'에 비용 0. **차수 홈 신설**(`/my/cohorts/[cohortId]`) — 카드 하나에 진단 둘+갈무리 일곱 못 담음. **시각 위계 3단**: 이번 주 갈무리(accent·primary)·진단(중립·ghost)·지난 회차(접힌 줄) — 진단과 갈무리 동급 배치 금지(“매주 진단받는다” 오인→사회적 바람직성 방어 유입 차단). **1회차 카드**(`/checkin/[session]`): `session1.ts` 원문 상수, `ResponseRunner` 미재사용(순서 고정·제출 후 열림), 자동저장(디바운스 2s/blur)·**단일 버튼 save→submit**(R2, 한 서버액션 순차)·이탈경고 없음·완료 상태(자기 문장 되돌려주기). 필수 4칸 카운터(`checkinFilledCount`), confidence 슬라이더 미선택(`—`)→`null` 시작. **상태 판정**: 미작성(행없음 or `hasContent===false`)·작성중(`hasContent && !submitted`)·제출(`submitted`) — 빈 행 오염 방지(S2). **일정 미등록='준비 중'(정상)**: 참여자 담담 안내·인도자 콘솔이 조치 항목(회차 일정 UI: `seedCohortSessions` 시작일→7행·미등록 경고). **QR 짧은 경로**(`/c/[code]/[session]`): 코드→차수 해석→미인증 `/login?returnTo`→비멤버 `/join`→멤버 카드. **returnTo 배선 신설**(B3): `loginOutcome`+`safeReturnTo`(화이트리스트 상대경로만·절대URL/`//`/`\\`/스킴 거부→`/home`), 단위테스트로 오픈리다이렉트 방어 고정. 신규 위젯 `MultiChoiceChips`(인스트루먼트 중립·배타·max 축출). 목록 카드(`MyCohorts`) 버튼 우선순위에 '이번 주 갈무리' 2순위 삽입(사전진단 우선 유지). **범위**: 1회차만(2~7회차 문항·자동리마인드·조공개화면·AI요약·계측조회화면 제외). **전면 안내(Phase 6) 배너 UI 는 후속**(prompt_count 인프라·`markCheckinPrompted` 배선은 데이터계층서 완비). 테스트: `safeReturnTo`·`nextChipSelection`·`loginOutcome` returnTo·`interpretation` 어휘 회귀. 계약 0(ADR-80 재사용)·마이그 0. directive 2026-07-27 승인. |
| ADR-82 | 1회차 문안 v2 + 시드 카운트 반환 + 공유동의 인도자 대면 이관(라이브 후 개정) | 최박사 실화면 검토 3건을 한 배치로. **F1(시드 결함)**: `seed_cohort_sessions`가 `ON CONFLICT DO NOTHING`인데 액션이 무조건 `ok:true`라 재시드가 조용한 무효 → 반환형 **void→integer(삽입 행 수)**(마이그 `20260728100000`, DROP+재생성·REVOKE/GRANT 재부여·anon EXECUTE 없음 확인). 계약 `seedCohortSessions`만 `Promise<number>`(그 외 시그니처 불변)·액션 `{ok,inserted}`·0건 문구. 날짜 수정은 이미 회차별 편집 UI(`upsertCohortSessions`, ADR-81 후속 보완분)로 가능 — 편집기가 재시드를 대체. **F6(입력칸 미표시)**: `ui-input` 클래스 미정의로 필수 포함 6칸 안 보이던 것 — inline 박스 스타일(AuthGate `inputStyle`과 동일 토큰)로 이미 수정(ui- 전수 감사 미정의 0). Field 라벨을 `t-body-lg`→필드라벨(15/500)로 낮춰 '선택' 상자가 필수 구역보다 커 보이던 위계 역전 교정. **C2(문안 v2)**: 책 진행(재해석→존재가치) 순서로 재배치 — **바꿔 쓴 문장 쌍**(`desire_from`/`desire_to`, 신규 필수)·존재가치 선언문 틀·`scene` 은유 제거(카드는 혼자 열림)·`reframe`→`letter_line`(응답 0건이라 이관 없음)·2면 '한 걸음' 뜻 소개·3면 인도자 상자 라벨 질문형·**공유 동의 UI 삭제**. 필수 4→**5칸**(`checkinFilledCount`). 책 페이지 좌표는 **문장 끝 괄호**(책 배포됨·워크북 미배포라 '워크북' 금지). **공유 동의 → 인도자 개별 대면 이관**: 카드는 혼자·밤에 열려 자기개시에 최악, 쓰이지 않는 칸은 비용. `share_consent` **컬럼은 남기고 미사용 주석**(v1.2 여지). 인도자 현황 '나눔'(익명)→**'문장 모아 보기'**(실명+갈망·존재가치·기억) — 코치는 이미 갈무리 전량 열람(RLS)이라 새 노출 아님, 동의는 대면으로 질 높임(가이드 문서는 지휘부 몫). 테스트: `checkinFilledCount` 5칸·갈망쌍·금지어·`share_target` 부재. 마이그 +1(seed 반환형)·계약 1줄. directive 2026-07-28 승인. |
| ADR-83 | 편지 사진 첨부 — 참여자 기록 보관(비공개 Storage·경로 RLS·EXIF 제거) | 참여자가 종이에 쓴 '과거의 나에게 편지'를 촬영·첨부해 보관. **저장소 결정: Supabase Storage**(Cloudflare R2 대비 — 기존 RLS 헬퍼 재사용·같은 auth·최소 코드·소규모라 egress 차이 무의미. 대용량·고트래픽 되면 R2 재검토). **비공개 버킷 `checkin-photos`**(마이그 `20260728120000`) + **경로 규약** `{cohort_id}/{user_id}/{session_no}/{uuid}.jpg`. **Storage RLS**(`storage.objects`, `storage.foldername` 파싱): INSERT=본인(경로 user=uid)+차수 멤버 / SELECT=본인 OR 차수 코치 OR 운영자 / DELETE=본인 OR 운영자(인도자 삭제 불가) / UPDATE 없음. **열람 본인·인도자·운영자 · 삭제 본인·운영자 · 보관 본인 삭제 전까지 영구**(ADR-76 1년 파기의 명시적 예외). **업로드 바이트는 클라이언트 직접**(`LetterPhotos` — 브라우저 supabase): `createImageBitmap`+canvas 재인코딩으로 **EXIF(GPS 등 위치) 제거**·리사이즈(장변 2000px·jpeg 0.85)·장당 상한, **최대 3장**. 서빙은 **만료 signed URL**(1h). 코어 `+listCheckinPhotos`(list+sign, 코치 현황용 서버 경로)·`+deleteCheckinPhoto`(운영자). 인도자 '문장 모아 보기'에 사진 썸네일(운영자에게만 삭제 ×). 편지 문안을 현재형("촬영해 첨부하셔도 됩니다")으로 교체. **동의**: 업로드 지점 **고지 안내**(누가 보는지·삭제 전 보관·위치정보 제거)로 informed consent — 형식적 `photo_archive` 동의기록은 후속(user_consents CHECK 확장 필요). **고아 정리 gotcha**: 참여자/차수 하드삭제(CASCADE) 시 Storage 객체는 자동 삭제 안 됨 → 삭제 경로는 즉시 제거하나 **주기 sweep 백스톱은 후속**. 라이브 RLS 실증(본인/코치/타차수코치/운영자/외부인 업로드·열람·삭제). 마이그 +1·계약 +`CheckinPhoto`·+2메서드. directive 2026-07-28 승인. |
| ADR-84 | 참여자 이동/삭제(운영자) — 등록만 이동·소프트 삭제(휴지통)·집계 등록 기준 | 운영자가 참여자를 **A차수→B차수 / 미배정(체험 진단 JOINF) / 휴지통(삭제)** 로 옮긴다. **핵심 원칙: 등록(enrollment)만 이동, 응답·갈무리는 생성 차수에 남긴다(불변 보존).** `move_cohort_member(user, from, to)`(DEFINER·`is_admin` 게이트, 마이그 `20260728140000`): enrollment.cohort_id 이동, 대상에 이미 있으면 원본만 제거(병합), from 미등록이면 예외. **삭제 = 휴지통 차수로 이동**(소프트·복원 가능) — 휴지통은 시드된 특수 차수(코드 `TRASH`·운영자 소유). **복원 = 휴지통→미배정/차수로 이동**(응답이 원 차수에 그대로 있어 무손실). **집계 clean(#4)**: 이동/삭제된 사람의 응답이 DB에 남아도(불변) `buildCohortRoster`가 **등록 기준 필터**(등록된 멤버의 응답만 집계)로 원 차수 명단·통계에서 제외 — 인도자 '문장 모아 보기'도 등록 멤버만. **`my_cohorts`에서 휴지통 제외**(소프트 삭제된 사람이 '내 세미나'에서 휴지통을 보지 않도록). **`remove_cohort_member` 보강**(영구삭제=휴지통 비우기): ADR-80 이후 생긴 **checkins 미삭제 구멍**을 메움(checkins도 삭제). **UI**: 차수 상세(운영자)에 '참여자 이동' 섹션 — 이름별 대상 드롭다운(같은 진단 차수+미배정+휴지통, 현재 제외)+[이동]. 휴지통 차수를 열면 같은 UI로 복원(미배정 등으로 이동)·영구삭제(🗑). 권한 운영자 전용. **라이브**: 휴지통 존재·비운영자 차단('admin only')·이동·병합 검증. **후속**: 그룹 리포트 집계도 등록 기준 필터·Storage 사진 고아 sweep. 마이그 +1·계약 +`moveCohortMember`. directive 2026-07-28 승인. |
| ADR-85 | 회차 갈무리 세션 레지스트리 + 2회차 카드 + 되비추기 + 1회차 안전 보완 | 3~7회차를 **파일 하나 + 레지스트리 한 줄**로 늘리기 위한 일반화 + 2회차 신규. **레지스트리**(`instruments/futurenow/checkin/index.ts`): `CheckinSession` 타입 + `getCheckinSession(n)`(미등록 회차 `null`). `session1`을 이 형태로 통일 — 필드명 `identitySentence`→`identity`(문안·응답키 `identity_sentence` 불변), 판정 `checkinFilledCount`/`CHECKIN_REQUIRED_TOTAL`을 객체 내부(`filledCount`/`requiredTotal`)로 이관, `summaryFields` 추가(구조 통일이며 렌더 문자열 델타 0 — §7-2). **카드 일반화**: `CheckinCardClient`가 회차번호로 분기하지 않고 **블록 존재**로 렌더(`copy.today.desire` vs `futureArea`, `copy.step.lastStep`/`share` 유무). **지시서 §4-3 이탈(견고화·지휘부 보고)**: ORDER는 "`copy`를 prop으로 받는다"였으나 `copy`에 함수(`filledCount`·`cover.counter`)가 있어 서버→클라 prop 직렬화가 깨진다(digest 3203392472, 3bf0025에서 이미 겪은 함정) → 카드가 `getCheckinSession(sessionNo)`를 **직접 import**(레지스트리는 순수 모듈이라 클라 import 안전). §7-5 목표(`CHECKIN_SESSION_1` import 부재·블록 존재 렌더)는 그대로 충족. `page.tsx` 가드 `sessionNo!==1`→`getCheckinSession()===null`. 인도자 '문장 모아 보기' 열을 `summaryFields`로(회차별 열 전환 — 1회차 갈망/존재가치/기억, 2회차 영역/인생의 한 문장/장면). `SummaryField`는 단일키 또는 **한 쌍**(갈망 A→B) union으로 확장(ORDER의 `{key,label}[]` 힌트를 1회차 쌍 표현 위해 견고화). **2회차 문안**(`session2.ts`): ① 가슴 뛴 영역 — 기존 `MultiChoiceChips`를 `max=1`로 재사용해 **단일 문자열**(`future_area`) 저장(배열 아님·§7-8)+`future_line`, ② `identity_statement`(1회차 존재가치와 키 구분), ③ 마음(설렘·막막함…), ④ 심화(`future_scene`·`letter_line`—키·형식을 1회차와 같게 거울 구조·사진경로 회차별 분리), ⑤ 지난 한 걸음 결산 `last_step_result`(칩 단일선택=필수 1칸)+`last_step_note`(선택), ⑥ 다음 한 걸음(1회차 동일)+**공개 토글**(`step_private` 컬럼·`checkin_save`의 `p_flags->>'stepPrivate'` 기존 배선·양방향·기본 공개), self_note. **필수 6칸**. **되비추기(Phase 4·§6)**: `page`가 `getMyCheckin(cohortId, sessionNo-1)`(신규 코어 메서드 0)로 지난 존재가치 문장(그 회차 레지스트리 `identity.key`로 추출)·지난 한 걸음을 읽기전용 회색으로 카드에 전달 — ② 위 문장 되비추기(값 없으면 블록 생략), ⑤ 위 한 걸음 되비추기(없으면 대체 문구), 미제출이어도 값 있으면 표시·조회 실패 무해(`catch→null`). **1회차 안전 보완 6건(§3)**: self_note 보조문구 통일('꼭 칭찬이 아니어도…'—전 회차 공통), **사진 서버 상한**(마이그 `20260729100000` — `storage.buckets` file_size_limit 3MB·allowed_mime_types jpeg, 클라 리사이즈를 서버가 이중 방어), 사진 열람 고지+오류 분기(형식/크기), desire 보조문구·placeholder 교정, 슬라이더 미선택 흐림(opacity), confidence leftLabel '아직 자신 없음'. **§5-3 stale(보고)**: ORDER가 "그대로 둔다"던 1회차 공개 안내('나와 인도자만 봅니다…')는 ADR-82(`de1c840`)에서 이미 삭제됨 → 없는 채로 둠(§2-4 미지정 문자열 불변). **§9 일반화 경계 보고**: 3~7회차가 기존/이번 블록형(문장·칩단일·심화·한걸음·토글)만 쓰면 `sessionN.ts`+레지스트리 한 줄로 끝. 완전 새 입력 위젯이 나오면 그때 카드에 블록 분기 하나 추가 필요. **경미 보고**: 스텝 되비추기 caption('지난 시간의 한 걸음')은 §6가 스텝용 caption 미지정이라 서술형으로 신설. 사진 고지에 위치정보 제거 안심 문구를 꼬리로 보존(ORDER 문구+1절). **검증**: tsc 0·eslint 0(내 파일; `contracts/instrument.ts` 기존 경고 무관)·vitest 348(session2 신규 12)·build 성공. 버킷 상한 원격 실조회(3145728·`{image/jpeg}`). 계약 0(ADR-80 재사용)·마이그 +1(버킷). directive 2026-07-29(CC_ORDER_checkin_session2) 승인. |
| ADR-86 | 회차 갈무리 **전체 열람** — 본인 열람(read 모드) + 인도자 명단 행 펼침 | 두 공백을 함께 닫는다. **(1) 작성자 본인이 자기 갈무리 전 항목을 볼 방법이 0**(제출 후 done 화면은 `self_note`·`step_what`·`step_when` 3줄뿐 — 나머지는 '고쳐 쓰기'로 편집 폼을 열어야 보였고, 차수 홈의 '지난 회차 N개'는 링크가 아닌 회색 캡션이라 지난 회차 진입로 자체가 없었다). **(2) 인도자도 전 항목을 볼 방법이 0**(명단·한 걸음·문장 모아 보기 3섹션뿐 — `mood`·`mood_custom`·`letter_line`·`step_blocker`·`confidence`·`need`·`suggestion`·`future_line`·`last_step_*` 는 표시 경로가 없었다). **성격 규정: 새 노출이 아니라 이미 가진 권한(RLS)의 표면화** — `checkins_select` 가 이미 `user_id=auth.uid() OR is_cohort_coach OR is_admin` 이고, 저장 버튼 위 고지 `save.notice2`('적으신 내용은 인도자와 운영자가 읽습니다.' session1.ts:116·session2.ts:131)가 1·2회차 공통으로 이미 존재한다 → **문안(sessionN.ts) 델타 0**. **데이터는 이미 와 있었다**: `listCohortCheckins` 가 `CHECKIN_COLS`(answers jsonb 포함)를 `cohort_id`·`session_no` 두 조건으로만 select 한다(core/context.ts:1002-1010) → **계약 0·코어 0·마이그 0·신설 라우트 0·safeReturn 0**. **읽기모델**(`instruments/futurenow/checkin/readModel.ts` 신설): `buildCheckinRead(sessionNo, answers, flags, audience)` 가 레지스트리를 카드 순서로 순회해 `ReadBlock[]`(pair·text·list·scale·flag·note·group·hidden)를 만든다. 규율 셋 — ① 회차번호로 분기하지 않고 **블록 존재**로(ADR-85 동일 원칙) ② **응답 키 리터럴 0**(전부 `copy.*.key` 경유) ③ **한국어 문자열 리터럴 0**(라벨·보조문구는 전부 레지스트리 원문 — 문안 이중진실 금지. 소스 정규식 검사로 회귀 고정). 렌더러 `CheckinReadView.tsx` 를 참여자·인도자가 **공유**한다. **참여자(모드 둘·URL 하나)**: `done`→**`read`** 로 재정의. 초기 모드는 순수 함수 `resolveCheckinMode`(`checkin/[session]/mode.ts` 신설 · 단위테스트 7)가 정한다 — ① `?edit=1` → edit ② 행 없음 → edit(현장 QR 첫 진입) ③ **`!closed && 미제출` → edit**(열린 회차를 쓰다 만 사람은 이어 쓰러 온 것 — ADR-81 주력 경로 보존) ④ 마감된 빈 행 → edit(뒤늦게 쓰러 갈 길) ⑤ 그 밖 → read. **`closed` 조건이 두 요구를 동시에 만족시킨다** — '쓰다 만 지난 회차'는 읽기로, '쓰다 만 이번 주 회차'는 쓰기로 열린다(초안 검토에서 QR·목록 버튼이 읽기 화면에 착지하던 회귀를 이 규칙으로 닫았다). 작성 의도가 확정인 진입로(차수 홈 accent 버튼 미제출분 · `MyCohorts` '이번 주 갈무리')는 `?edit=1` 을 명시한다. 완료 문구(`done.title`)는 **`justSubmitted` 일 때만**(3주 전 갈무리에 '저장했습니다'는 거짓말). 전체 항목은 접힌 줄로 펼친다. 차수 홈 '지난 회차 N개' 캡션 → **접힘 목록 링크**(`PastSessionsClient` 신설 · 시각 위계 3단 최하위 유지 · 서버 왕복 0), 대상은 마감된 회차 중 **레지스트리 등록분만**(미등록 회차를 링크로 내보내면 '준비 중'에 부딪힌다. 캡션 문자열 불변, N 의미만 좁아짐). 차수 홈 accent 버튼은 **문구와 목적지를 일치**시킨다: 쓰러/이어 쓰기 → `?edit=1`, 제출됨 → '적으신 것 보기'(read). **계측 무오염(제1원칙)**: read 경로에서 `checkin_save`·`checkin_submit`·`markCheckinOpened` **미호출**, `deep_opened` 미갱신(심화를 처음부터 펼쳐 토글 자체를 없앤다 — `checkin_save` 의 `deep_opened` 는 단방향 OR 이라 한 번 true 면 영구). `markCheckinOpened` 는 **edit 모드 진입 시점**으로 좁힌다(ref 가드·1회) — 지난 회차를 '읽으러' 들어온 진입이 섞이면 `first_opened_at` 이 영구 오염된다(1회 고정·되돌릴 수 없음. ADR-80 1기 보정 근거). **되비추기(ADR-85 §6)는 read 에 싣지 않는다** — '지금 쓰는 것을 돕는' 작성 보조이지 기록이 아니고, 지난 회차 열람이 생겨 중복이다. `getMyCheckin(sessionNo-1)` **조회 자체를 edit 일 때만** 수행(왕복 감소). 그래서 read 의 '고쳐 쓰기'는 **상태 토글이 아니라 `?edit=1` 로의 실제 이동**이어야 한다 — `setMode('edit')` 로 두면 서버가 `prior` 를 다시 계산하지 않아 2회차 편집 폼이 1회차 기록이 있는데도 `mirrorEmpty`('지난 한 걸음이 남아 있지 않아요')라는 **거짓 문구**를 띄운다(초안 검토가 잡아낸 ADR-85 §6 회귀). **인도자**: 명단 행을 **그 자리에서 펼친다**(`RosterDetail` 신설 · 라우트 이동 0 · **서버 왕복 0** — 데이터가 이미 서버 메모리에 있다). 사람 축은 `?open={userId}`, 회차 축은 기존 `?session=N` 탭이 `open` 을 물고 간다. 펼친 내용은 **열었을 때만 렌더**한다(접힌 12명 전문이 DOM 에 상주하면 어깨너머 노출·Ctrl+F 면적이 커진다). **펼침 가능 여부는 '갈무리 행 존재'로만 판정한다(`hasRow`) — 내용 유무로 갈리면 익명 부채널이 된다**: 익명 '바라는 점'만 쓴 사람은 개인 상세가 비므로, 내용 기준으로 화살표를 감추면 명단만 훑어도 그가 익명 제보자임이 드러난다(초안 검토 지적). prop 은 **순수 데이터만** — `copy` 객체·함수를 경계 너머로 넘기지 않는다(ADR-85 digest 3203392472 재발 방지). 섹션 2개 신설: **부탁**(`need` · 명단 **위**, 돌봄 우선 · 수신자가 문안에 인도자로 명시된 유일한 자유서술이라 실명 — 지금까지 읽는 경로가 0이어서 **약속 불이행 상태였다**), **이름 없이 온 말**(익명 `suggestion` · 화면 최하단, 명단과 최대 이격 · 캡션은 참여자가 읽고 켠 고지 원문 그대로 · 정렬키 `checkin.id`(gen_random_uuid)라 명단순·`userId`·작성 시각과 무관하고 회차마다 순열이 달라진다). **'문장 모아 보기'는 덮어쓰지 않고 나란히 둔다** — ADR-82가 준 성격(나눔 도구·개별 대면 동의)과 펼침(목양 도구)은 목적이 다르다. `self_note` 는 펼침에만 나오고 `summaryFields`(낭독 후보)에는 넣지 않는다(코드 델타 0 — 지금도 없다). 펼침에 나눔 CTA·복사·내보내기·PDF 를 두지 않는다. **지휘부 확정 프라이버시 3건(2026-08-02)**: ① `self_note` 인도자 **실명 표시**(개별 비공개 약속 문구 없음 · notice2 포괄. 단 본인 화면 accent 인용구 장치는 유지) ② `suggestion` **참여자 토글 존중**(`suggestion_anon=true`→익명 섹션 / `false`→실명. 무조건 익명화하지 않는다. **익명분은 이름이 붙는 자리에 절대 두지 않는다**) ③ `step_private=true` → 인도자에게 **⑥ 블록 전체**(`step_what`·`step_when`·`step_blocker`) 제외 — 토글 문안이 '이번 한 걸음은 나만 볼게요'로 필드가 아니라 **블록**을 가리키므로, 표시 경로가 없던 `step_blocker` 만 새로 노출하면 참여자가 켠 토글을 우회하는 셈이다. 자리 표시는 **참여자가 켠 토글 원문 그대로**(신규 문안 0) — 인도자가 '안 썼다'와 '비공개로 썼다'를 구분해야 이탈 조기경보에 거짓 음성이 없다. ⑤ '지난 한 걸음 결산'(`last_step_result`·`last_step_note`)과 `confidence`(구분선 아래 3면 마무리)는 **사정권 밖** → 표시. `need` 는 익명화 대상이 아니다(익명 부탁은 성립하지 않는다 — 누구의 부탁인지 알아야 들어줄 수 있다). **권한 등급으로 뚫지 않는다** — 운영자도 코치와 동일(참여자에게 한 약속이지 역할 통제가 아니다). **표시 규율**: `confidence` 는 숫자+양끝 라벨만(막대·게이지·색·백분위·평균·정렬키 금지 — 문안이 '낮게 답하셔도 아무 일 없습니다'라 약속했고 ADR-80이 '채점 대상 아님'을 못박았다) **[ADR-94 정정 주: 근거 둘 중 앞엣것은 죽었다 — ADR-91 D 가 그 문안을 '낮게 적힌 숫자가 인도자에게는 가장 쓸모 있습니다…'로 교체했다. 그러나 뒤엣것(ADR-80 '채점 대상 아님')은 문안과 무관하게 살아 있고, 금지 항목 여섯이 공유하는 성질(숫자를 등급으로 바꾸는 표현)도 그쪽을 가리킨다. 따라서 **이 규율은 유지된다.** 원 인용을 지우지 않고 정정 주로 남기는 이유는, 지우면 '근거 하나가 죽었다'는 사실 자체가 문서에서 사라져 다음 사람이 같은 부분 인용을 반복하기 때문이다.]**, `mood` 는 집계·분포·추이 금지(ADR-81 '매주 진단받는다' 오인 차단), `last_step_result` 는 경고색·판정·정렬키 금지(ADR-85 '네 칸 무게 동일'), `contact_request` 는 `help`('짧은 안부 연락입니다. 코칭 세션이 아닙니다.')를 함께 실어 '코칭 신청' 오독을 막는다. 계측 원값(`edit_count`·`first_opened_at`·`prompt_count`·`has_content`)은 어느 화면에도 노출하지 않는다. **ADR-84 필터 보정(범위 밖 1줄 · 승인)**: `steps` 에만 빠져 있던 등록멤버 필터를 채워 신설 섹션들과 기준을 맞춘다(이동·삭제된 참여자의 `need` 가 실명으로 되살아나지 않게). **`viewed_at` 계측 컬럼 신설은 명시적으로 기각한다** — 제1원칙이 '열람은 계측을 오염시키지 않는다'인데 열람용 계측을 새로 만드는 것은 자기모순이다. 필요해지면 별건으로 올린다. **회차 확장**: 3~7회차는 `sessionN.ts` + 레지스트리 한 줄이면 열람 화면 **0줄 수정**(완전히 새로운 입력 위젯이 나올 때만 `ReadBlock` kind 하나 증가 — ADR-85 §9가 예고한 경계). **신규 문안 4건**(전부 degrade 경로 있음): 참여자 '적으신 것 모두 보기'·'적으신 것 보기', 인도자 섹션 제목 '부탁'·'이름 없이 온 말'. **§6.2 가시성 매트릭스에 갈무리 4행 추가**(코드에만 있는 프라이버시 규칙을 남기지 않는다). **사진은 '적은 것'으로 친다** — 글은 한 줄도 없이 종이 편지만 촬영해 붙인 갈무리가 빈 화면이 되지 않도록 열람 게이트를 `blocks.length > 0 || photos.length > 0` 로 둔다(초안 검토가 잡은 사진 삼킴). **구현 후 다중 에이전트 검토(4렌즈 19에이전트)에서 확인된 결함 5건을 푸시 전 전부 수정**: ① 사진 삼킴(뷰·카드 두 게이트) ② `prior` 미재계산(고쳐 쓰기를 앵커 이동으로) ③ QR·목록 진입이 읽기에 착지(모드 규칙에 `closed` 도입) ④ 익명 부채널(`hasRow` 판정) ⑤ 테스트 공백(`mode.test.ts` 7 · `CheckinReadView.test.tsx` 9 신설). 기각 5건. **검증**: tsc 0 · eslint 0(내 파일. `contracts/instrument.ts` 기존 경고는 무관) · vitest **380**(`readModel.test.ts` 26 — 20키 전량 노출·라벨 문자단위 일치·리터럴 0 소스검사·가시성 3정책·회차 확장 / `mode.test.ts` 7 / `CheckinReadView.test.tsx` 9) · `next build` 성공. 신설 7 · 수정 6 · 문서 1. 계약 0 · 코어 0 · 마이그 0. directive 2026-08-02 승인. |
| ADR-87 | 갈무리도 참여자를 따라 이동 (ADR-84 개정) + 편지 사진 귀속을 경로에서 갈무리로 이관 | **지휘부 결정 2026-08-02: 갈무리는 사람에게 귀속된다.** ADR-84 의 '등록만 이동, 응답·갈무리는 생성 차수에 남긴다'를 갈무리에 한해 뒤집는다 — 갈무리는 ADR-80이 규정한 **수정 가능한 문서**이고, 진단 응답(불변·사전사후 페어링·채점 파이프라인 대상)과 성격이 다르다. `responses`·`response_drafts` 는 종전대로 불변·잔류. **이동**(`move_cohort_member`): enrollment 에 더해 `checkins` 도 옮긴다. 대상에 같은 회차가 이미 있으면 그 행이 참여자의 현재 기록이므로 원본을 버린다(enrollment 병합 규약과 동일 · 이중 등록 상태에서 양쪽에 쓴 드문 경우에만 발생). 부수 효과로 **휴지통 이동·복원이 무손실**이 된다(갈무리가 함께 다녀온다). ADR-84 의 등록 기준 집계 필터는 그대로 두되, 이제 고아 갈무리가 생기지 않으므로 이중 방어가 된다. **라이브 실증에서 잡은 결함 둘을 후속 마이그레이션으로 고쳤다(적용 이력 보존 · CLAUDE §5)**: **(F1)** `checkins` 가 `(cohort_id, session_no)` 로 `cohort_sessions` 를 참조하는 FK 를 가져, **일정이 없는 차수로는 이동이 통째로 실패**한다 — 휴지통·미배정(JOINF)·미시드 차수 모두 `cohort_sessions` 0행이라 **갈무리 보유자의 삭제가 막히는 프로덕션 파손**이었다(마이그 `…100100`). 해법: 옮길 회차의 일정이 대상에 없으면 **원 차수 일정 행을 복사**해 FK 를 충족시킨다(있으면 대상 것을 존중). 잔여 주의 — 미시드 차수로 옮기면 원 차수 날짜가 먼저 박히고 이후 `seed_cohort_sessions`(ON CONFLICT DO NOTHING)가 그 행을 건너뛰므로, 인도자가 회차별 날짜 편집기(ADR-82)로 바로잡는다. **운영 권고: 참여자를 옮기기 전에 대상 차수 일정을 먼저 등록한다.** **(F2)** Supabase 는 `storage.protect_delete()` 트리거로 **`storage.objects` 직접 DELETE 를 금지**한다('Use the Storage API instead') → DB 레벨 고아 sweep 은 성립하지 않을 뿐 아니라, 그 트리거가 붙어 있는 동안 **차수 삭제가 통째로 막혔다**(마이그 `…100200` 로 제거·`remove_cohort_member` 를 ADR-84 형태로 원복). **사진**: 물리 경로를 옮기지 않는다 — Storage 는 객체의 실제 저장 키에 `name` 을 포함하므로 `storage.objects.name` 만 UPDATE 하면 DB 와 파일이 어긋나 다운로드가 깨진다(진짜 이동은 Storage API 의 copy+delete). 대신 **접근 판정을 경로의 차수에서 `checkins` 로 이관**한다: SELECT 정책이 `path[2]=user_id`·`path[3]=session_no` 로 갈무리 행을 찾아 `is_cohort_coach(k.cohort_id)` 를 본다(uuid/int 캐스팅 없이 text 비교 — 형식이 어긋난 경로에서 정책이 예외를 던지면 버킷 전체가 막힌다). 그래서 **갈무리가 이동하면 열람 권한이 자동으로 따라가고 떠난 차수 인도자는 자동으로 접근을 잃는다.** 경로가 옛 차수 접두어에 남아 prefix list 로는 못 찾으므로 **`checkin_photo_paths(cohort,user,session)` RPC 신설**('그 회차 갈무리가 지금 이 차수에 있는가'로 게이트 후 이름 반환 · 서명은 앱). `listCheckinPhotos` 구현만 교체(**계약 시그니처 불변**). INSERT·DELETE 정책 불변(업로드 시점엔 경로 차수=현재 차수라 정확하고, DELETE 는 본인 OR 운영자라 차수 무관). **고아 사진 회수(ADR-83 미해결분)는 전부 앱 Storage API 경로로**: `removeCohortMemberAction` 이 RPC 호출 **전에** 회차별 사진을 지운다(갈무리를 먼저 지우면 어느 회차였는지 알 수 없어진다). 사진 회수 실패가 참여자 삭제를 막지 않게 감싼다. **라이브 실증(더미 차수 3개·실행 후 전량 정리)**: 일정 0인 대상으로 이동 성공·일정 2행 자동 충족·갈무리 2건 이동·enrollment 이동 / **떠난 차수의 비운영자 코치: 갈무리 0 · storage SELECT 0 · RPC 0**(경로는 옛 차수 그대로인데 접근만 끊김 — 이 ADR 의 핵심 명제) / 갈무리 보유 코치: RPC 1 / 프로덕션 사진 1장 회귀 확인(소유·담당 코치 1, 비담당 코치 0). 정리 후 프로덕션 무결성 재확인(1기 12명·14건 불변 · 사진 1 · 잔여 더미 0 · 문제 트리거 0). **ADR-86 확대 노출 역할별 실측(선행)**: 담당코치/운영자 14건 전량 · 비담당 코치 본인 1건(타인 0) · 참여자 본인 2건(전 차수 통틀어 2건) · 비담당 코치 사진 0. 마이그 +3 · 계약 0 · 코어 1메서드 구현 교체. directive 2026-08-02 승인. |
| ADR-88 | 접힘 블록 공용 부품(`Disclosure`) + 요약 줄 + 목적을 찾는 세 질문(2회차) | **문제**: 심화 헤더가 `d11d3cb`·`bf86b13` 두 차례에 걸쳐 화살표를 우측방향으로 돌리고 두껍게 했는데도 실사용 검토에서 **여전히 '누를 수 있는 줄'로 분간되지 않았다.** 아이콘 하나로 풀리지 않는 문제라 구조를 바꾼다. **`core/ui/Disclosure`(신설)**: 줄 전체가 버튼(`space-between`) · 왼쪽 제목+뱃지+요약 줄 · **오른쪽에 '펼치기'/'접기' 글자**+꺾쇠(열리면 180° 회전) · 최소 52px(`--tap-min` 44 보다 크게 잡아 몸으로 알리는 값) · 배경 `--color-surface-2`(hover `--color-surface-sunken`) · focus `2px --color-accent` · 열렸을 때만 헤더 아래 hairline · `prefers-reduced-motion` 존중 · **여백 위아래 `--space-6` 대칭**(초안에서 `margin-top` 만 주고 아래를 빠뜨려 다음 문항이 상자에 달라붙었다 — 비대칭은 눈에 안 띄면서 화면을 답답하게 만든다). **핵심은 아이콘이 아니라 글자다** — '펼치기'/'접기'가 지금 열려 있는지까지 말해 준다. 영문·마우스 은유를 쓰지 않는다(대부분 폰에서 탭한다). **부품 규율 둘**: ① **계측을 모른다**(ADR-86 제1원칙) — `onToggle` 콜백만 노출하고 `deep_opened` 기록은 심화 호출부에만 남긴다(단방향 OR 라 한 번 true 면 영구). 기본 펼침 블록·인도자 상자는 열림을 기록하지 않는다. ② **문안을 갖지 않는다** — 제목·요약·뱃지 문구는 전부 레지스트리(`sessionN.ts`)가 준다. **적용 범위(축소 판단·승인)**: 저장소의 `aria-expanded` 보유 파일은 다섯(`MemberRow`·`RawAnswers`·`PastSessionsClient`·`RosterDetail`·`CheckinCardClient`)이나, 갈아탄 것은 **문항 블록뿐**이다. 나머지는 규격이 맞지 않아 대상 밖 — `PastSessionsClient` 에 52px·surface-2·hairline 을 입히면 ADR-81 이 정한 시각 위계 3단 최하위(캡션·muted·카드 없음)가 깨지고, `RosterDetail` 은 명단 행 자체가 컨트롤이라 구조가 다르다. **'선택' 뱃지 — ADR-82 의 반전이 아니라 조건 추가**: ADR-82 가 뱃지를 지운 대상은 **접힌** 블록이었고 그 판단은 유효하다(접혀 있다는 사실 자체가 건너뛸 수 있음을 말하므로 뱃지가 군더더기). 그러나 **기본 펼침 블록은 ADR-82 가 검토한 적 없는 경우**다 — 빈 입력칸 셋이 펼쳐져 있으면 필수로 읽히고, 하단 '필수 N칸 남음'은 저장 시점의 신호인데 불안은 읽는 시점에 생긴다. → **접힘 블록은 뱃지 없음(ADR-82 유지), 기본 펼침 블록에 한해 표기.** 부수로 뱃지가 선택 prop 이 되어 부품이 단순해진다. **요약 줄(`deepen.summary`·`facilitatorBox.summary`)**: 접힌 상태에서 안에 무엇이 있는지 한 줄 — 열기 전에 알아야 여는 판단이 선다. 1회차 '집에 남은 기억 · 과거에게 쓴 편지' / 2회차 '5년 뒤의 한 장면 · 미래에게서 온 편지(인터스텔라 편지)' / 인도자 상자 공통 '부탁 · 세미나 제안 · 연락 요청'. (2회차 요약 줄의 괄호 부기와 `future_scene` 보조문구 '어떤 방'→'어떤 장소'는 지휘부 문안 교정 2026-08-03.) **인도자 상자에서는 안전장치다** — 그 안의 '연락 요청'은 의견함이 아니라 **돌봄 채널**이라, 제목만 달면 거기 있는 줄 모른 채 지나간다. **`facilitatorBox.defaultOpen` 회차별 설정**: 기본 접힘, **7회차만 `true`**(마지막 회차의 세미나 제안은 다음 기수 설계의 최대 수확처다). 지금 구조를 만들지 않으면 7회차에서 하드코딩하게 된다. **목적을 찾는 세 질문(2회차 전용·신규 3키)**: `purpose_alive`·`purpose_ache`·`purpose_fit`. 책의 논리 순서(세 질문 → 교차점 → 한 문장)대로 ① 뒤 ② 앞. **기본 펼침**(수업 중 시간이 부족해 숙제로 나갈 가능성이 커, 접어 두면 존재를 모른 채 넘어간다) + '선택' 뱃지. **필수는 6칸 그대로**(세 칸을 비워도 저장·제출된다 — 테스트로 고정). 은사·부르심 해설은 카드에 넣지 않는다(그 대목은 책과 인도자 스크립트가 맡는다. 갈무리는 성찰 카드이지 묵상집이 아니고, 이 카드는 이틀 뒤 혼자 열린다). **책 페이지 참조는 미확정이라 붙이지 않는다.** **ADR-86 열람 파급**: `readModel` 에 목적 묶음 블록 추가 → **노출 정책 20키 → 23키**. 본인·인도자 모두 표시(`save.notice2` 고지 근거), **`summaryFields`(나눔 후보)에는 넣지 않는다** — 재료이지 대표문장이 아니다(테스트로 고정). **문안 델타**: 1회차는 **기존 문자열 diff 0**(요약 줄 1건 추가만) — 헤더 형태만 바뀐다. 2회차 `facilitatorBox.title` 은 '선택 · 하고 싶은 말이 있을 때만' → '**인도자에게 하고 싶은 말**'(1회차는 불변). 상단 카운터(`cover.counter`)는 부활시키지 않는다(`d11d3cb` 결정 유지 — 렌더 경로 0건 확인). **회귀 게이트(지휘부 요구·D1 직후)**: `session1.ts` 문안 diff 0 · 1회차 필수 5칸 유지 · 1회차 심화·인도자 상자에 뱃지 없음 · 실제 CSS 로 렌더한 1회차 확인 파일 제출 후 승인. **선행 문서 정정 기록**: `CC_ORDER_checkin_session2`(v1)·`CC_GUIDE_checkin_session2_spec` 의 착수 순서(Phase 1~4)는 **폐기**한다 — ADR-85·86·87 로 이미 완료된 범위를 덮어쓴다. 원인은 클론 한 벌(`6dc85d3`, 7-28)로 세 문서를 나흘에 걸쳐 발행한 것이며, '감사 전 fresh clone' 규율을 발행에는 적용하지 않았다. **지시서는 틀리면 이미 있는 것을 덮어쓰므로 감사보다 위험하다 — 앞으로 지시·가이드·메모도 발행 직전 clone 을 기준선으로 명시한다.** 문안 원문·필드 키·계약 규격은 유효하므로 폐기하지 않는다. **검증**: tsc 0 · eslint 0(내 파일) · vitest **392**(`Disclosure.test.tsx` 신규 8 · `readModel` 목적 4 추가) · `next build` 성공. 신설 2 · 수정 5 · 계약 0 · 코어 +1부품 · 마이그 0. directive 2026-08-03(CC_MEMO 정정 3건) 승인. |
| ADR-89 | 갈무리 QR 비멤버 처리 — `/join`(사전진단) 대신 담담한 안내로 정지 (ADR-81 개정) | 2회차 배포 직전 실사용에서 발견. `https://future.yebom.org/c/HMT7Z/2` 를 **그 차수 소속이 아닌 사람이 열면 사전진단 화면이 떴다.** 원인은 ADR-81 이 설계한 흐름의 마지막 분기 — 비멤버를 `/join?cohort={id}` 로 보내는데, 그 라우트는 `initialCohortId` 를 받으면 **가입자 러너로 재진입**해 진단이 시작된다(`join/page.tsx` §7). **두 가지가 틀렸다.** ① **목적지-의도 불일치**: 이 링크의 목적지는 '그 회차 갈무리'다. 갈무리를 쓰러 온 사람 앞에 수십 분짜리 사전진단이 열리는 것은 비용이 크고, 자기가 무엇을 누른 것인지 알 수 없게 만든다. ② **무단 자기등록 표면**: 갈무리 링크는 현장 QR·인도자 카톡으로 옮겨 다니는데(ADR-81 이 규정한 세 진입로), 링크만 가지면 **누구나 실명제 차수에 스스로 등록**되고 진단까지 시작됐다. 차수는 실명제이고 인도자가 명단을 관리한다는 전제(§10·ADR-75)와 어긋난다. **바꾼 것**: 비멤버는 리다이렉트하지 않고 **그 자리에서 멈춘다** — 차수 이름 + '참여자 명단에 없어요' + '인도자에게 문의해 주세요' + [홈으로]. 같은 파일의 '코드를 찾을 수 없어요' 안내와 동일한 형태다(담담한 안내·비난 없음·§0.4). 차수 이름 노출은 새 누출이 아니다 — 코드 보유자에게는 `/join?code=` 미리보기가 이미 이름을 보여 준다. **잃는 것과 대안**: 2회차부터 합류하는 신규 참여자가 QR 로 자기가입하는 경로가 사라진다. 인도자가 명단에 등록하거나, 참여자가 기존 코드 참여 경로(홈 → 코드로 참여 → `/join`)를 쓴다 — 그 경로는 그대로 살아 있으므로 기능 손실이 아니라 **자동 유발이 사라진 것**이다. 실명제 차수 운영에는 오히려 맞다. **불변**: 미인증 시 `/login?returnTo=/c/{code}/{n}` 은 그대로다(`safeReturnTo` 화이트리스트 1번 정규식이 이 형태를 이미 허용 — 로그인 후 원래 링크로 복귀). 코드 해석 실패 안내·멤버의 카드 리다이렉트도 불변. `/join` 라우트 자체는 손대지 않았다. **신규 문안 2줄**(차수명 + '참여자 명단에 없어요' / '인도자에게 문의해 주세요') — **지휘부 승인 2026-08-03**. (ADR-90 이 이 판단을 정책으로 승격 — '미확정 참조는 넣지 않는다'와 같은 계열.) QR 자기가입 경로를 닫는 트레이드오프도 함께 승인(늦은 합류는 인도자 등록 또는 기존 코드 참여 경로). 계약 0 · 코어 0 · 마이그 0 · 라우트 0. 검증: tsc 0 · eslint 0 · vitest 392. directive 2026-08-03 승인. |
| ADR-90 | 블록 타입 일반화(모양 슬롯·`order`·`Mirror`·`group`) + 3회차 갈무리 | **문제**: `CheckinSession` 이 슬롯을 **회차 이름**으로 나눠 둬(`desire`·`futureArea`·필수 `identity`) 3회차가 세 군데서 걸렸다 — 3회차엔 정체성 문장이 **없고**, ERRC 짝은 `desire` 와 같은 모양이며, 되비추기가 **세 곳**(①·심화①·⑤)이고, 블록 순서가 2회차와 다르다. 슬롯을 회차마다 늘리면 7회차에 여덟 개가 되어 '회차 추가 = 파일 하나 + 한 줄' 목표가 무너진다. **해법 — 슬롯 이름을 회차가 아니라 '모양'으로**: `desire`→**`pairText`**(+`connector?`) · `futureArea`→**`areaPick`** · `identity` **선택 슬롯화** · **`question`** 신설(한 칸 서술 + `badge`). **`today.order: SlotName[]` 로 렌더 순서를 데이터화** — 지금까지 클라이언트 선언 순서가 곧 화면 순서라 회차마다 다른 순서를 표현할 수 없었다. **카드와 `readModel` 이 같은 `order` 를 훑는다**(열람 순서가 작성 순서와 어긋나면 안 된다). `mood` 도 `order` 에 넣는다 — '언제나 마지막' 특례를 두면 묶음의 끝을 데이터로 말할 수 없다. **되비추기를 자리에서 블록 속성으로**: `identity.mirror: boolean`·`lastStep.mirrorEmpty: string` → `Mirror{label, keys[], empty?}` 를 `areaPick`·`identity`·`question`·`pairText`·`deepen.fields[]`·`lastStep` 어디에나 붙인다. **`keys[0]` 은 앵커다 — 앵커가 비면 나머지 값이 있어도 되비추지 않는다.** 이 규칙이 없으면 `step_what` 은 비고 `step_when` 만 저장된 초안(자동저장이 부분 저장하므로 실제 도달 가능)에서 '무엇을' 없이 '언제'만 한 걸음으로 되비춰져 화면이 달라진다 — **초안 검토 20 에이전트 중 4개 렌즈가 독립적으로 잡은 회귀**이며, 판정을 `slots.ts`(순수 함수)로 빼 9 케이스로 고정했다(출력은 원문, 존재 판정에만 trim). 되비추기 재료가 3필드 다이제스트에서 **지난 회차 `answers` 전체**로 바뀌었다(`Mirror.keys` 가 임의 키를 지목하므로) — 본인 자신의 지난 답이라 노출 범위 변화 없음, **조회는 edit 모드에서만**(ADR-86 유지). **묶음 이정표 `group`**: 이중 STEP 회차(3~5)는 1면 안에서 화제가 한 번 바뀌어, 표시가 없으면 참여자가 세 번째 문항에서 걸린다. 표지 부제를 쪼개 쓰므로 **새 어휘도 STEP 용어 노출도 없다**. 뜻은 '이 블록**부터**'가 아니라 '이 블록이 **속한** 묶음'이다. **렌더 규칙 한 문장이 네 전이를 덮는다 — 현재 블록의 `group` 이 직전과 다르면 경계를 그린다(값 있으면 hairline+캡션, 없으면 hairline만). 면의 첫 블록이면 hairline 을 생략하고 캡션만.** 없음→A(캡션) · A→A(없음) · A→B(선+캡션) · **A→없음(선만 — 묶음이 닫힌다)**. 이 마지막 경우가 없으면 `mood`(묶음 밖)가 습관 묶음에 딸린 것처럼 읽힌다. 캡션은 제목이 아니라 이정표라 `.t-micro`(12px·`--color-text-muted`) — 라벨 15·보조 13 아래 한 칸. **단일 STEP 회차(1·2·6·7)는 `group` 이 없어 경계가 생기지 않는다 → 신규 hairline 0건**(테스트로 고정). **부품 적용 범위**: 공용 `Disclosure`(ADR-88) 규격은 건드리지 않았다. **1·2회차 문안 회귀 증명**: 슬롯 이름과 배치가 바뀌어 파일 diff 로는 증명할 수 없으므로, **변경 전 한국어 리터럴 집합을 스냅샷(`copyBaseline.json`)으로 박고 삭제·변경 0 을 테스트로 잠갔다**(추가는 허용·열거). 1회차 **증감 0**, 2회차는 되비추기 캡션 2건이 클라이언트 하드코딩에서 레지스트리로 이관된 것뿐이며 **바이트 동일 확인**. **망라성 가드**: `SlotName` 이 늘면 카드 `renderSlot`·`readModel` switch·`ALL_SLOTS` 세 곳이 동시에 컴파일 실패한다 — 없으면 새 슬롯이 조용히 누락돼 참여자가 쓴 답이 열람에서 사라진다(신호 0). **3회차 문안**(`session3.ts` · STEP 3 현재 직면 + STEP 4 습관 재구성): ① 간절한 영역(`gap_area`+`gap_want`) — **`future_area` 를 재사용하지 않는다**(2회차 '가슴 뛴 영역' ≠ 3회차 '간절한 영역'. 둘이 다를 때가 인도자에게 가장 값진 정보인데 같은 키를 쓰면 그 차이가 덮인다) ② 오늘의 질문(`stuck_named` · 선택 · 접힘으로 감싸지 않음 — 그 회차의 핵심 경험을 받는 자리) ③ 습관 짝(`habit_stop`→`habit_start` · connector '↓ 그 자리에' · **둘 다 채워야 1칸** — 제거는 창조와 짝을 이룬다는 원칙을 구조로 강제) ④ 마음(묶음 밖). 심화 `identity_gap`·`speech_habit`. **필수 6칸 유지** — 이중 STEP 이라 옮겨 적기가 둘이어서 필수가 하나 늘 자리인데 **그 증가분을 서술 질문에서 회수**한다(옮겨 적기는 이관이라 값이 싸고 서술은 비싸다). `'함정'` 등 **사람을 판정하는 낱말을 카드에 쓰지 않는다** — 카드는 이틀 뒤 혼자 열리고 그때 판정어는 자기 낙인이 된다. `mood` 의 '오기'를 '해볼 만함'으로 교체(자기를 낮게 보는 작업이 세 번 걸리는 주간이라 부정 낱말 과반이면 골라 놓고 더 가라앉는다). **책 페이지 참조는 미확정이라 붙이지 않았다**(ADR-89 정책 승격: **미확정 참조는 넣지 않는다 — 확정된 값만 카드에 오른다**. 틀린 페이지는 없는 것보다 나쁘다). **나눔 후보 열 기준을 한 문장으로 확정**: `summaryFields` 에는 **나눌 수 있는 문장만** 넣는다 — 범주 낱말(`gap_area`)과 자기개시가 깊은 문항(`stuck_named`·`identity_gap`)은 제외하고, **비어 있을 가능성은 배제 사유가 아니다**(인도자가 훑어 고르는 도구이므로 빈 칸은 그냥 빈 칸이다). 3회차 열 = `gap_want` · `habit_stop→habit_start` · `speech_habit`. 쌍(`from→to`)은 **교체·변환**을 뜻하므로 습관 짝에는 참이고 영역+바람에는 거짓이라 쓰지 않는다. `speech_habit` 은 관찰 가능한 습관이라 얕고, 세 번 자기를 낮게 보고 나온 주간의 다음 회차를 가볍게 여는 재료가 된다. **`readModel` 노출 정책 23키 → 30키**(신규 7: `gap_area`·`gap_want`·`stuck_named`·`habit_stop`·`habit_start`·`identity_gap`·`speech_habit`. 전부 본인·인도자 표시 — `save.notice2` 근거). **선행 지시서 기준선 사고 재발 방지**: `CC_ORDER_checkin_session3` 는 발행 직전 fresh clone(`78b47b0`)으로 대조해 발행됐다(ADR-88 이 기록한 교훈의 첫 적용). **검증**: tsc 0 · eslint 0(내 파일) · vitest **442**(`slots.test.ts` 신규 21 · `session3.test.ts` 신규 19 · `copyRegression.test.ts` 4) · `next build` 성공 · 지시서 §4 문안 17건 문자열 대조 일치 · 금지어 0 · `함정` 0 · 책 참조 0. 신설 5 · 수정 7. **계약 0 · 코어 0 · DB·마이그레이션 0 · 라우트 0**. 게이트: Phase 1 승인 후 Phase 2 착수(지휘부 2026-08-03). **배포 후 1·2·3회차 실기기 확인 필요** — 렌더 HTML 은 ADR-82 `ui-input` 종류의 사고를 잡지 못한다. directive 2026-08-03(CC_ORDER_checkin_session3 + 정정 메모) 승인. |
| ADR-91 | 1기 실측 보정 4건 — 필수 공란 제출 차단 · 복귀 안내 구현 · 마음 낱말 · 완충 문구 | **1기(11명) 1·2회차 실측이 두 구조 결함을 드러냈다.** ① **1회차 제출 8건 중 3건이 2·3면을 통째로 건너뛴 채 제출**됐다(`step_what`·`confidence`·`self_note` 동시 결측 — 2회차 1건 포함 총 4건). 현장 클로징에서 QR 로 열어 1면을 채운 뒤, 맨 아래 버튼에 경고가 없으니 그 자리에서 눌러 끝내 버린 것이다. ② **`prompt_count > 0` 인 행이 0건** — `markCheckinPrompted` 는 ADR-80 이 코어에 만들어 뒀는데 **호출하는 화면이 없었다.** 돌아오게 만들 장치가 미구현이라 두 결함이 겹쳐 절반 가까이가 미완성으로 굳었다. **실측 수치 정정(지시서 §1 오기)**: 세 칸 동시 결측은 1회차 3건(37.5%)이지 4건이 아니며, 4건은 두 회차 합산이다. `아직 모르겠음`은 **제출된 1회차 6/7(86%)**이다(각주: **미제출 초안 1건을 포함하면 7/8**, 전 회차 mood 응답 기준으로는 7/10). 갈무리는 제출을 완결로 보는 문서이고 초안은 아직 마음이 정해지지 않은 상태일 수 있어 제출분을 기준으로 삼는다 — 지휘부 집계 6은 제출분, 클코1 집계 7은 초안 포함이었고, 지시서의 `6/10`은 분자(제출분)와 분모(전체)가 섞인 값이었다. 어느 기준이든 지시서가 적은 55%보다 높다. **관찰**: 필수는 절반이 비는데 선택은 6~7할이 찬다(심화 펼침 8/11 · 못 하게 될 때 7/11 · 편지 한 줄 6/11 · 목적 세 질문 2/2). **부담을 피하는 집단이 아니라 마음이 가는 곳에 쓰는 집단**이다. 그래서 A·B(구조)를 고치고 C·D(문안)는 되돌릴 수 있게 둔다. **A — 필수 공란 제출 차단**: 원칙은 '**막는 것은 제출이지 저장이 아니다**'. 갈무리는 한 번에 끝내는 서식이 아니라 며칠에 걸쳐 고쳐 쓰는 문서라, 벽을 세우되 가둬서는 안 된다. 저장 버튼은 **언제나 눌린다**(비활성 버튼은 고장으로 읽힌다) — 누른 뒤가 갈린다. 빈 필수가 있으면 **저장 버튼 위 인라인 패널**(모달 아님 — 3분짜리 카드에 포커스 트랩·스크롤 락을 들이는 것은 과하고, 저장소에 쓸 만한 모달도 없다)이 뜨고 **빈 칸의 라벨을 그대로** 나열한다(숫자만으로는 어디로 갈지 모른다). `채우러 가기`→첫 빈 칸으로 스크롤+포커스 · `나중에 이어 쓰기`→차수 홈(제출하지 않는다). **판정 일원화가 이 조치의 핵심 구조다** — `filledCount`·`missingLabels`·`missingKeys` 를 손으로 각각 쓰면 세 회차 × 세 함수가 어긋날 수 있고, 어긋나면 '제출을 막았는데 무엇이 비었는지는 못 알려 주는' 상태가 된다. **하나의 `required` 선언에서 전부 파생**시켜(`required.ts` 신설) 그 가능성을 구조로 없앴다. 기존 `filledCount` 테스트가 그대로 통과하는 것이 파생 구현의 동치 증명이다. 결측 라벨은 **레지스트리 원문**이다 — 처음엔 `…(책 49~52쪽) — 바꾸기 전`처럼 합성했다가 **ADR-90 리터럴 잠금이 새 문안 2건을 잡아내** 원문(`바꾸기 전`)으로 되돌렸다(지시서 §3-2 예시는 축약형이나 §8-5 가 '라벨 원문'을 요구하므로 후자를 따른다). **다만 `required` 선언에는 라벨이 문자열로 다시 적히므로**, 문안 쪽만 고치고 선언을 안 고치면 두 곳이 어긋난다 — 리터럴 잠금은 baseline 대비 '변경'만 보므로 이 어긋남은 못 잡는 사각지대다. 지휘부 감사가 **2회차 대조 누락**을 짚었고(슬롯 이름을 열거하는 방식이라 회차가 늘면 반복될 누락이었다), **'결측 라벨이 그 회차 문안 어딘가에 실제로 있는가'로 일반화**해 세 회차를 `ALL` 순회로 덮었다 — 4~7회차가 자동으로 포함된다. 음성 대조로 이빨을 확인했다(라벨 한 글자만 흐트러뜨려도 세 회차 모두 검출 · 대조 라벨 7·8·9개). **`confidence` 논리 구멍 보정**: 필수가 아니라 `filledCount` 가 세지 않으므로 그것만 빈 사람에겐 패널이 영영 안 뜬다(실측 `●——` 2건이 그 경우) → 조건을 넓히되 **필수가 다 찼으면 주 버튼이 `이대로 제출`**이 되어 막지 않는다. **빈 칸 표식** 조건은 `hasContent || gateSeen` 이고 둘 다 필요하다 — `hasContent`(서버)는 다음 진입에도 살아 있어 주 조건이고, `gateSeen`(클라)은 `hasContent` 가 **로드 시점 스냅샷**이라 edit 중 갱신되지 않는 한 세션을 덮는다. 처음 여는 사람은 둘 다 거짓이라 표식이 없다(시작이 무거워지지 않게). 버튼 위 문구는 `필수 N칸 남음`→**`아직 {n}칸이 비어 있어요`**('필수·남음'은 서식 어휘고 갈무리는 서식이 아니다). **B — 복귀 안내 구현**: `/home` 에 배너 신설(`CheckinPrompt`). 노출 조건 `prompt_count === 0`(첫 진입) 또는 `=== 1 && 마감 6시간 전 이후`. 상한 2는 RPC 가 강제한다. `나중에`에 **어떤 비용도 붙이지 않는다**(죄책감 문구·재확인·카운트다운 금지). `/my/cohorts` 에는 두지 않았다 — 차수가 하나면 차수 홈으로 리다이렉트하므로 실효 지점이 `/home` 이다. **조회 비용**: `my_cohorts` 가 `prompt_count` 를 반환하지 않아 대상이 있을 때 `getMyCheckin`·`listCohortSessions` 각 1회가 는다. 11명 규모라 감수하고, 기수가 늘면 RPC 에 얹는다(계약·마이그 변경을 피하려는 의도적 선택). 차수 홈 '작성 중' 줄에 **남은 칸 수**를 더한다(미완성 제출은 '돌아올 이유'가 없어서 생겼다). **B4**: `checkin_mark('prompt')` 는 행이 없으면 INSERT 하므로 배너만 본 '미작성' 참여자에게 인도자 명단의 빈 펼침 화살표가 생긴다 → `hasRow` 를 `hasContent || submittedAt` 으로 좁혔다. (`first_opened_at` 은 'prompt' 종류가 건드리지 않으므로 ADR-86 계측은 안전하다 — RPC 설계가 옳았다.) **C — 마음 낱말**: `아직 모르겠음`이 '내 마음을 모르겠다'와 '이 목록에 맞는 게 없다'를 한데 담아 **가장 싼 탈출구**로 쓰였다(선택자 중 직접 쓰기 0명·필수 결측 다수). 뒤엣것으로 좁혀 **`딱 맞는 말이 없음`**으로 바꾸고, 고르면 직접 쓰기 placeholder 가 안내로 바뀐다(강제하지 않는다). 배타는 유지(목록이 안 맞는다면서 다른 낱말을 함께 고르는 것은 모순). **3회차부터만 교체한다** — `MultiChoiceChips` 는 `options` 에 있는 값만 그리고 `nextChipSelection` 의 배타 필터는 `exclusive` 문자열만 걷어내므로, 1·2회차 옵션을 바꾸면 이미 그 낱말을 고른 **7명의 답이 화면에서 사라지고** 저장값에 유령 값이 남는다. ADR-86 이 지난 회차 링크를 만들어 이제 실제로 열리는 경로다. 2기 시작 전에 바꾸면 저장값이 없어 안전하다 **[ADR-109 정정 주: 이 근거는 틀렸다.** 1·2회차 마음 낱말 통일은 `아직 모르겠음` → `딱 맞는 말이 없음` **교체**이고, ADR-91 R1 이 실제로 금지하는 경우다 — 저장된 값이 옵션에서 사라지면 그 답이 화면에서 지워진다. **1기 실측**(2026-08-24): 1회차 `mood` 응답 8행 중 **7행**이 `아직 모르겠음` 이다(2회차는 0행). 바꾸면 일곱 사람의 답이 사라진다. 2기 시작 여부와 무관하며 **안전한 시점이 없다.** 통일하려면 옛 값을 새 값으로 옮기는 마이그레이션이 선행돼야 하고, 그 비용이 통일의 편익보다 큰지 별도로 판단한다. ADR-108 이월(결산 선택지 다섯째)과 성격이 다르다 — 그쪽은 **추가**라 기존 값을 하나도 건드리지 않는다. 원 문장을 지우지 않고 정정 주로 남기는 이유는 ADR-86·94 때와 같다: 지우면 `근거가 틀렸는데 결론이 맞아 아무도 다시 안 봤다`는 사실이 사라진다.**]** . **D — 완충 문구를 '허락'에서 '용도'로**: 실측이 근거를 지웠다(자신감 값 2·5·7·8 평균 5.5 로 부풀림 없음 · placeholder 를 베낀 사람 0명 · 평균 18.6자로 각자 자기 문장). 없애지 않고 문법을 바꾼다 — 정직성 확보 효과는 같고 자세가 반대다(능동적인 사람은 안전보다 쓸모에 반응한다). 실행 자신감 `help` → '낮게 적힌 숫자가 인도자에게는 가장 쓸모 있습니다…'(1·2·3), 3회차 지난 걸음 `note.help` 에서 허락절 제거, `self_note.placeholder` 를 **회차별로**(label 은 이미 회차별인데 예시만 고정이던 설계 누락). **§6-2 는 손대지 않는다** — 연락 요청·익명 안내·'꼭 칭찬이 아니어도 됩니다'는 5주차에 실제로 무너진 사람이 여는 문이라 거기까지 딱딱해지면 소수를 잃는다. 분량 면제·오해 방지 문구도 정서 완충이 아니라 지시라 그대로 둔다. **D 가 ADR-90 리터럴 잠금을 깨뜨려 `copyBaseline.json` 을 재생성했고**(session2 83→85), 교체가 되돌아가지 않도록 `copyRegression.test.ts` 에 명시 단언을 더했다(옛 문구 부재 + 새 문구 존재 + §6-2 보존 + 1·2회차 `아직 모르겠음` 유지). **검증**: tsc 0 · eslint 0(내 파일) · vitest **471**(`required.test.ts` 신규 20 · session3 C·D 5 · copyRegression 재작성) · `next build` 성공. 신설 4 · 수정 8. **계약 0 · 코어 0 · DB·마이그레이션 0**. A 는 게이트로 승인 후 B·C·D 착수(지휘부 2026-08-06). **배포 후 실기기 확인 필요** — 게이트 패널·복귀 배너·`채우러 가기` 스크롤은 정적 검증이 닫지 못한다. directive 2026-08-06(CC_ORDER 실측 보정 4건 + 정정 메모) 승인. **후속(같은 날)**: 지휘부 감사가 결측 라벨 대조에서 **2회차 누락**을 짚었다 — `required` 선언에 라벨이 문자열로 다시 적히므로 문안 쪽만 고치고 선언을 안 고치면 이중 진실이 되는데, 리터럴 잠금은 baseline 대비 '변경'만 보므로 이 어긋남은 못 잡는 **사각지대**다. 슬롯 이름을 열거하는 방식이라 회차가 늘 때마다 손으로 추가해야 했고 그래서 빠졌다 → **'결측 라벨이 그 회차 문안 어딘가에 실제로 있는가'로 일반화**해 `ALL` 순회로 바꿨다(4~7회차 자동 포함). 음성 대조로 이빨 확인(라벨 한 글자 훼손 시 세 회차 모두 검출 · 대조 라벨 7·8·9개). **미리보기 라우트**(→ ADR-92 로 공식화·이전). |
| ADR-92 | 갈무리 카드 미리보기 — 인도자 콘솔 정식 기능 | **문제**: 카드 라우트의 `opens_at` 게이트에는 **역할 우회가 없다**(설계대로). 그래서 아직 열리지 않은 회차를 운영자도 열 수 없는데, 인도자는 클로징 스크립트를 짜기 위해 카드가 무엇을 묻는지 알아야 한다. 회차마다(4~7) 반복되는 수요다. **일정을 임시로 당기는 방법은 쓰지 않는다** — 되돌리기 전에 참여자가 열면 미공개 회차가 그대로 노출된다. **처음엔 `/preview/checkin`(개발용)으로 냈다가 공식화하면서 폐기하고 콘솔 보호 라우트로 옮겼다**(`/coach/cohort/[cohortId]/checkin/preview`). 근거: `/preview` 계열은 파일 첫 줄에 스스로 '운영 라우트 아님'이라 선언한 자리이고 `PROTECTED_PREFIXES` 밖이라 **인증이 없다**. 공식 UI 가 그것을 링크하면 (a) 선언과 모순되고 (b) **아직 확정 전인 4~7회차 문안이 링크만 알면 열린다** — ADR-89 가 세운 '미확정 참조는 넣지 않는다'와 같은 계열의 문제다. 진입로를 둘로 두지 않기 위해 개발용 라우트는 삭제했다. **접근 범위 — 운영자 전용으로 좁히지 않는다(지휘부 승인)**: 실제로 필요한 사람은 인도자이고, 회차 일정을 정하는 것도 인도자다. 게이트는 회차 현황 화면과 **동일**하게 둔다(`role === 'user'` → `/home`). 노출도 늘지 않는다 — 코치는 이미 그 차수 참여자의 **답 전체**를 본다(ADR-86). 문항 문안은 그보다 훨씬 덜 민감하다. **구현**: 복제본이 아니라 **실제 `CheckinCardClient` 를 그대로 렌더**한다(회람용 HTML 처럼 따로 그리면 드리프트가 생긴다). `preview` 플래그가 **서버 쓰기를 네 곳에서 막는다** — 자동저장·제출·`markCheckinOpened`·사진 위젯(브라우저 Storage 직접 호출). 미리보기가 `checkins` 행을 만들거나 계측을 오염시키면 안 된다(ADR-86 제1원칙). 제출을 눌러도 게이트 패널과 완료 화면까지 흐름은 다 보이되 서버에는 아무것도 남지 않는다. 되비추기는 토글로 두 경우(지난 회차를 쓴 사람 / 안 쓴 사람)를 다 보여 준다. **고지를 두 곳에** 둔다 — 상단 배너는 스크롤하면 사라지므로 저장 버튼 곁에 '미리보기 — 저장되지 않습니다'를 한 번 더(적은 것이 남는 줄 알고 쓰다가 잃는 일이 없게). **자리**: 회차 일정 편집 행의 '링크 복사' 옆 눈 아이콘 — 인도자가 날짜를 정하는 자리라 맥락이 맞고 회차별이라 대상이 분명하다. **회차 확장**: 목록을 레지스트리에서 뽑으므로 4~7회차가 등록되면 이 화면은 **0줄 수정**으로 따라온다. 계약 0 · 코어 0 · DB 0. 검증: tsc 0 · eslint 0 · vitest 473 · build 성공. directive 2026-08-06 승인. |
| ADR-93 | 개발용 미리보기 4종 공개 노출 차단 — /preview 게이트 | **결함**: `/preview` · `/preview/console` · `/preview/entry` · `/preview/report` 넷이 `PROTECTED_PREFIXES` 밖이고 페이지 게이트도 없어 **인증 없이 운영 도메인에서 열렸다.** 파일 첫 줄에 스스로 `운영 라우트 아님`이라 선언해 놓고 실제로는 공개돼 있었다(ADR-92 가 `/preview/checkin` 을 콘솔로 옮긴 것과 **같은 판단을 나머지 넷에 적용**). 신규 작업의 결함이 아니라 디자인 시스템 확인용으로 만든 초기 라우트가 그대로 남은 것이다. **실측한 노출 규모 — 우려보다 한 단계 크다**: 사용자 데이터는 없지만(stub 컨텍스트) 나가는 것이 사업의 핵심 자산이다. `/preview` 는 `futurenowFlow` 를 **클라이언트로** import 하므로 러너 첫 화면만이 아니라 **`copy.ts` 의 `itemPrompts` 27개 전량**이 정적 청크에 실려 나갔다(프리렌더 `preview.html` 13.5KB + 청크). `/preview/report` 는 **104.7KB** 프리렌더 본문에 리포트 구조와 명명(§9.4 `시들음`·`원씽`·`번성`·`나침반`)을 그대로 담았다. `/preview/entry` 26.5KB(진입 퍼널 문안) · `/preview/console` 24.3KB(콘솔 레이아웃·돌봄 신호). §7 이 참여자 렌더 경로에서 구인명·강의 어휘를 막는 동안 이 경로가 인증 없이 전부 내보내고 있었다. **판단 — 지우지 않고 게이트한다**: 넷 다 복제본이 아니라 **실물 컴포넌트를 그대로 렌더**하므로(`ResponseRunner`·`ReportScreen`·`GroupView`·`_screens/*`) 드리프트가 0이고, 지우면 §4·§5·§7·§8 의 디자인 확인 경로를 잃는다. **구현 — 페이지마다 넷 다는 대신 `src/app/preview/layout.tsx` 하나**. 세션은 미들웨어(`PROTECTED_PREFIXES` 에 `/preview` 추가), 역할은 레이아웃(`role === 'user'` → `/home`, 게이트 기준은 ADR-92 인도자 콘솔과 동일)이 막는 **이중 방어**다. 레이아웃 한 자리로 둔 것은 `PROXY_MATCHER` 불변식과 같은 사고 — **신규 미리보기 라우트가 opt-in 없이 기본 보호되고, 뚫으려면 명시적으로 손대야 한다.** `dynamic = 'force-dynamic'` 으로 프리렌더를 금지한다(게이트를 걸었는데 정적 HTML 이 남으면 의미가 없다). 회귀 잠금: `proxy.guard.test.ts` 가 네 라우트를 **개별로** 못 박고 `/previewer` 접두 오매칭도 함께 막는다. **검증(CLAUDE §9 규율 — 200 을 통과로 보지 않는다)**: build 에서 넷 다 `○`(static) → `ƒ`(dynamic) 전환 · 프리렌더 HTML 4개 **소멸** · 실서버 미인증 요청 넷 다 **307 → /login**, 본문 6바이트, 문항·명명 0 · 정적 프리렌더 HTML(`/`·`_not-found`·`_global-error`)이 로드하는 청크 중 문항 원문 포함 **0**. **잔여 — `/join`(별건, 미조치)**: 공개 라우트 전수 조사에서 `/join` 만이 미인증 상태로 문항 원문 청크를 내보낸다(`JoinClient.tsx` 가 `futurenowFlow` 를 클라이언트로 import). `/`·`/login`·`/signup`·`/reset`·`/reset/confirm`·`/c/[code]/[session]`·404 는 모두 깨끗하다. 이 건은 참여자 진입 퍼널을 건드리므로 판단을 올렸고 **→ ADR-95 에서 `감수`로 종결**했다. 계약 0 · 코어 0 · DB 0. 검증: tsc 0 · eslint 0(기존 경고 1, 미접촉 `contracts/instrument.ts`) · vitest **474** · build 성공. directive 2026-08-06(보정 지시 — /preview 공개 노출). |
| ADR-94 | 3회차 책 페이지 확정 부착 + 실행 자신감 명단 노출 **기각** | **A — 책 페이지 다섯이 최종 조판 확정치로 붙었다**(지휘부 선언 2026-08-07). ADR-88·89 의 `확정된 값만 카드에 오른다` 조건이 이로써 해소된다. `gap_area` 94~95 · `stuck_named` **96~104** · `identity_gap` 108~111 · `pairText`(습관 짝) **117~118** · `speech_habit` 126~133. `stuck_named` 는 초안(96~103)에서 한 쪽 늘었다 — 참조는 `거기 쓴 것을 옮겨라`가 아니라 **`기억이 안 나면 여기를 펴 보라`는 좌표**이고 카드 문항이 겨누는 문장이 104쪽 상단에 있다. 반 쪽이 다음 활동에 걸치는 대가가 문장을 잘라 내는 대가보다 싸다. `pairText` 는 117~118 을 **나누지 않는다** — 사분면은 117쪽 펼침면 오른쪽 끝, 짝 원칙은 118쪽 다음 펼침면 왼쪽이라 현장에서 왼쪽 두 칸만 채우는 일이 여기서 생기고, 카드가 두 칸을 한 칸으로 세는 설계가 그 다리다. **부착 자리는 다섯이 아니라 여섯** — `gap_area` 만 `REQUIRED_3`(필수 선언)과 `areaPick.label`(렌더)에 같은 문자열로 두 벌 있다. 양쪽 다 붙인다(1·2회차 관례 동일). **결측 패널에 책 쪽수가 딸려 나오는 것은 의도로 확정** — `어디를 펴 보면 되는지`가 함께 나오는 편이 ADR-91 A `채우러 가기`의 취지에 맞다. 표기는 **문장부호 뒤 + 반각 공백 1칸 + 문자열 맨 끝**(마침표·물음표 동일), 1·2회차 10건과 코드포인트 단위 일치. **금지 테스트를 조용히 지우지 않고 뒤집었다** — `session3.test.ts` 의 `not.toMatch(/책 d+/)` 는 ADR-88·89 가 세운 의도된 문이라, 삭제하면 승인된 정책을 코드에서 몰래 되돌리는 것이 된다. 2회차 선례대로 `toContain` 다섯 열거 + 표기 규칙 단언 + `gap_area` 양쪽 일치 단언으로 교체했다. **`copyBaseline` 에 `session3` 을 넣었다** — `3회차는 잠금 대상이 아니므로 갱신이 필요 없다`는 사실이지만 **필요 없다는 것과 안 하는 게 옳다는 것은 다르다**(그때까지 3회차 문안 전체가 회귀 보호 밖이었다). 스냅샷은 참조를 **붙인 뒤** 뽑았다(먼저 뽑으면 참조 없는 상태를 잠그고 즉시 깨진다). `copyRegression` 순회를 세 회차로 넓히고, 회차 간 마음 낱말 차이(1·2회차 `아직 모르겠음` / 3회차 `딱 맞는 말이 없음`, ADR-91 C 의 의도된 차이)를 양방향으로 못 박았다. 음성 대조로 이빨 확인 — `96~104` 를 초안값 `96~103` 으로 한 글자 되돌리자 **세 테스트가 검출**. **B — 실행 자신감을 인도자 명단에 올리는 안은 기각**(지휘부 2026-08-07). 클코1 이 `ADR-86 금지의 근거가 ADR-91 D 로 무너졌다`고 반박했으나 **부분 인용이었다** — ADR-86 의 근거는 둘이고(문안 약속 + ADR-80 `채점 대상 아님`), 앞엣것만 죽었다. 금지 항목 여섯이 공유하는 성질(숫자를 등급으로 바꾸는 표현)도 뒤엣것을 가리키며 임계 강조도 같은 계열이다. **규율 명문화: 승인된 ADR 의 근거를 다룰 때는 전부 인용한 뒤 각각을 따로 판정한다.** 기각의 실질 근거는 셋 — ① **도구가 이미 있다**(ADR-86 펼침의 `scale` 블록, 서버 왕복 0)이고 인도자는 갈무리를 읽으려 어차피 전원을 펼치므로 추가 비용이 없다, ② 실측이 작다(2회차 제출 2건 · 전 DB `confidence` 실측값 6개 · 3 이하 실참여자 제출분 1건 — **8/9 에 펼칠 대상은 두 명**), ③ 문안의 약속은 인도자의 펼침과 4회차 블록 0 절차가 이행한다. **ADR-86 표시 규율을 개정하지 않는다. `RosterEntry` 에 `confidence` 를 더하지 않았다.** **재고 조건을 못박는다** — 한 회차 제출이 **8건 이상**이고 그중 **3 이하 응답이 3건 이상**일 때 다시 올린다(둘 중 하나라도 미달이면 올리지 않는다). 조건 판정에 B 가 필요해지지 않도록 **판정 SQL 을 여기 둔다**: `select session_no, count(*) filter (where submitted_at is not null) as 제출, count(*) filter (where submitted_at is not null and (answers->>'confidence')::int <= 3) as 삼이하 from checkins where cohort_id = :id group by 1 order by 1;`. **대체 소항목 2건**(B 기각과 무관하게 8/9 에 인도자가 실제로 겪을 것): ① **회차 기본 선택** — `sessions[0]`(=1회차) 고정이라 8/9 에 열면 1회차를 보면서 3회차를 준비하게 된다. `defaultSessionNo` 신설(순수·단위테스트 8) — 열린 회차 → 없으면 마지막으로 마감된 회차 → 둘 다 없으면 첫 회차. 열린 것이 여럿이면 **회차 번호가 큰 쪽**(현 일정은 창이 안 겹치지만 일정 편집이 임의 날짜를 허용한다). ② **`--color-care` 두 줄 제거** — 지각과 연락 요청이 같은 앰버로 뭉개져 인도자가 두 신호를 구별하지 못했다. 토큰 참조를 걷어내 fallback 이 살아나면 **지각=muted / 연락 요청=navy** 로 갈린다(연락 요청은 돌봄 채널, 지각은 단순 사실). **문안 드리프트 정정** — `readModel.ts` 주석의 옛 문구 인용을 현행으로 고치고, ADR-86 본문에는 **원 인용을 지우지 않고 정정 주**를 달았다(지우면 `근거 하나가 죽었다`는 사실이 사라져 다음 사람이 같은 부분 인용을 반복한다). 회람용 `futurenow_checkin_session3_review.html` 은 **검토 시점 스냅샷**임을 상단에 명기하고 동기화 대상에서 제외했다(정본이 둘이면 그것이 드리프트의 원인 — 최신 문안은 ADR-92 미리보기로 본다). **[ADR-99 정정 주: 이 파일은 삭제됐다.** 동기화 대상에서 뺀 순간부터 그것은 낡아 갈 문서였고, ADR-98 개정까지 반영되지 않아 책 참조 다섯도 없는 8/5 스냅샷이 됐다. 인도자 콘솔 미리보기(ADR-92)가 **정본을 그대로 렌더**하므로 대체물이 이미 있다. 원 기록을 지우지 않고 정정 주로 남기는 이유는 ADR-86 때와 같다 — 지우면 `왜 스냅샷으로 두기로 했는가`가 사라져 다음 사람이 같은 회람본을 또 만든다.**]** **이월**: `copy.wrap.confidence.min/max` 죽은 선언(카드가 0·10 을 하드코딩, 전역 grep 0건)은 B 가 없어져 당장 문제가 아니다 — 4회차 작업 때 되살리거나 걷어낸다. 1·2회차 마음 낱말 통일도 2기 시작 전(저장값이 없을 때). **실측 기준 명시**: `실기수 제출분 10건(1·2회차 합산) 중 6건 미응답, 2026-08-07 기준`(1회차 단독은 8건 중 5건 · 전체 코호트는 11건 중 6건 · 초안 포함은 22건 중 16건 — 기준을 안 적으면 과거의 6 vs 7 혼선이 재발한다). **관찰(범위 밖·지휘부 처리)**: 1회차 11명 개시·8명 제출 → 2회차 6명 개시·2명 제출. 마지막 초안 진척이 61시간 전이라 이탈이 `confidence` 미응답보다 큰 신호다. 계약 0 · 코어 0 · DB·마이그레이션 0. 검증: tsc 0 · eslint 0 · vitest **487**(474→487) · build 성공. directive 2026-08-07(CC ORDER + CC MEMO 정정) 승인. |
| ADR-95 | `/join` 문항 원문 노출 — 감수(코드 소지자와 동등한 노출로 본다) | **사실**: `/join` 은 공개 라우트인데 `JoinClient.tsx` 가 `futurenowFlow` 를 **클라이언트로** import 해, 사전진단 문항 원문 전량(`copy.ts` `itemPrompts` 27개)이 미인증 방문자에게도 내려가는 청크에 실린다. 러너는 인증 뒤(`step==='runner'`)에야 그려지지만 **번들은 첫 로드에 함께 나간다.** ADR-93 이 `/preview` 를 닫으며 발견했고, 공개 라우트 전수 조사(`/`·`/login`·`/signup`·`/reset`·`/reset/confirm`·`/c/[code]/[session]`·404)에서 **유일하게 남은 자리**다. **결정 — 고치지 않는다(지휘부 2026-08-07).** 근거: 차수 코드를 가진 사람은 어차피 문항을 전부 보므로, 노출의 실질 증분은 `코드 없이 누구나`이고 그 차이가 이 사업에서 감수할 수 없을 만큼 크지 않다. 사용자 데이터·리포트 구조·채점 가중치는 이 경로로 나가지 않는다(그것이 ADR-93 이 닫은 `/preview` 와 다른 점이다). **세 선택지를 놓고 고른 것이다** — ① `next/dynamic` 지연 로드: 초기 HTML 에서 청크가 빠지지만 이름이 loadable 매니페스트에 남아 **진짜 게이트가 아니다**(문턱만 올림). ② 스키마를 인가 후 서버에서 내려주기: 실제로 닫히지만 `ResponseRunner` 의 `schema` prop 계약에 닿는 구조 변경이라 §0.3 승인 사항이고 진입 퍼널 회귀 검증이 따른다. ③ 감수. **①을 쓰지 않은 이유가 중요하다** — 닫히지 않았는데 닫힌 것처럼 보이는 상태가 감수보다 나쁘다. 감수는 기록으로 남지만 반쪽 조치는 다음 감사에서 `이미 처리됨`으로 읽힌다. **코드에 표식을 남긴다** — `JoinClient.tsx` 의 해당 import 위에 `의도적으로 남겨 둔 것이며 되돌리려 하지 말 것` 을 근거와 함께 적었다. 결정을 기록만 하고 코드에 남기지 않으면, 다음 사람이 `실수`로 보고 고치려다 진입 퍼널을 건드린다(ADR-93 이 `/preview` 를 지우지 않고 게이트한 것과 같은 사고 — 값싸고 명백한 것은 닫고, 값이 비싼 것은 **명시적으로** 감수한다). **재검토 조건(지휘부 확정 2026-08-07)**: `/join` 에 인스트루먼트 모듈을 새로 `import` 하는 변경은 이 ADR 을 다시 읽고 한다. 리포트 해석 문안·채점 가중치·기준표가 이 경로로 나가면 감수의 전제가 무너진다. **ADR-93 이 `/preview` 에서 닫은 것이 정확히 그것이다.** 초안에 있던 `문항 세트 상품화` 조건은 **뺐다** — 재검토 조건은 **코드에서 관찰 가능해야** 한다. `import` 한 줄은 diff 에 드러나지만 사업 가정의 변화는 드러나지 않고, 조건을 가정에 걸면 그 가정이 바뀔 때 아무도 이 ADR 을 다시 읽지 않는다. 코드 0줄 · 계약 0 · 코어 0 · DB 0(주석과 문서만). directive 2026-08-07. |
| ADR-96 | 참여자 원문을 코드 저장소에 두지 않는다 — `docs/` 유입 차단 | **결함**: 로컬 `docs/` 에 62분 참여자 세션 녹취록(실명 포함·110KB)과 워크북·진행자 스크립트북이 있었는데 `.gitignore` 가 막지 않았다. 저장소는 **public** 이다(`loginheaven-jpg/futurenow-platform`). 이력 유입은 0건이라 아직 막을 수 있는 상태였으나 **무방비**였다 — 다음 커밋에서 `git add .` 나 `git add -A` 가 한 번 나오면 들어가고, **그러면 되돌릴 방법이 없다.** ADR-90 이래 여러 차례 `docs/ 는 커밋에서 제외했다`고 보고했으나 그것은 매번 손으로 뺀 것이었고 구조적 방어가 아니었다. **판단 착오의 기록**: 클코1 은 이 건을 `처분 결정 대기`로 묶어 미뤘다. 처분(별도 private 저장소 / 저장소 private 전환 / 유지)은 결정이 필요했지만 **보호는 결정이 필요 없었다.** 급한 것과 결정이 필요한 것을 한 덩어리로 보면 급한 것이 결정 속도에 묶인다 — 되돌릴 수 없는 위험은 처분과 분리해 **먼저** 막는다. **조치**: ① `.gitignore` 에 `/docs/` — 앞 슬래시로 저장소 루트에 앵커한다(없으면 하위 디렉터리의 다른 `docs` 까지 함께 막힌다). ② 로컬 `docs/` 를 저장소 밖으로 **이동**(삭제 아님) — `C:devjindan-private-docs`, 5개 파일 전량. ③ §6.0 규율 명문화. **private 전환은 하지 않는다(지휘부)**: 필요한 것은 저장소 성격을 바꾸는 것이 아니라 **그 파일을 저장소에 두지 않는 것**이다. private 으로 바꿔도 협업자가 늘면 접근이 함께 넓어지고 git 이력은 여전히 지우기 어렵다. **녹취록은 버전 관리가 필요한 물건이 아니다.** **실측**: `git log --all -- docs/` 빈 출력 · `--diff-filter=A` 에서 `docs/` 파일 추가 **0건**(이력 무오염 증명) · `git check-ignore -v` 가 `docs/` 와 녹취록 파일 모두 무시 판정 · `git add docs/` **거부**됨(`-f` 없이는 불가) · 이동 후 저장소 내 `docs` 부재. 코드 0줄 · 계약 0 · 코어 0 · DB 0. directive 2026-08-07(최우선 지시). |
| ADR-97 | 지시서·보고서를 저장소에서 유통한다 — `docs/` 를 allowlist 로 뒤집음 | **지시**(2026-08-07): 작업지시서는 `docs/tasks/`, 검토·완료 보고서는 `docs/reports/` 에 `.md` 로 둔다. **취지 수용** — 채팅은 휘발되고 지휘부는 fresh clone 으로 감리하는데(ADR-88), 지시서와 보고서가 저장소에 없으면 **감리 대상과 감리 근거가 따로 논다.** 저장소 루트에 흩어져 있던 `CC_ORDER_*`·`CC_PLAN_*`·`CC_REPORT_*` 4건도 이 폴더로 모을 자리가 생긴다(이관은 지시 대기 — 옮기면 기존 ADR 의 경로 인용을 함께 갱신해야 한다). **충돌**: 30분 전 ADR-96 이 `/docs/` 를 통째로 막아, 도착한 지시서가 실제로 차단돼 있었다. **해법은 되돌리기가 아니라 뒤집기** — `/docs/*` + `!/docs/tasks/` + `!/docs/reports/`. 종전이 `docs 를 막는다` 였다면 이제는 `docs 는 전부 막되 두 자리만 연다` 이므로 **ADR-96 보다 강한 자세**다: 녹취록·상담 기록이 `docs/` 에 다시 떨어져도, **새 하위 폴더를 만들어 떨어져도** 자동으로 걸린다. **문법 함정 — 단순화 금지**: git 은 디렉터리 자체가 제외되면 그 안의 negation 을 보지 않으므로 `/docs/` + `!/docs/tasks/` 는 **동작하지 않는다.** 반드시 `/docs/*`(한 겹만 제외)여야 하며, 이 사실을 `.gitignore` 주석에 남겼다. **양방향 실측**: 허용 3(`docs/tasks`·`docs/reports`·도착한 지시서 파일) 전부 추적 가능 · 차단 3(`docs/녹취록-테스트.txt`·**새 하위 폴더** `docs/원문/상담기록.md`·`docs/무엇이든.md`) 전부 `check-ignore` 판정. **두 폴더의 내용에도 §6.0 이 걸린다** — 공개 저장소이고 그 사실은 바뀌지 않았다. 보고서는 실측 수치·집계·문안만(수치는 **분자·분모 기준과 기준 시점**을 함께 — ADR-94), 지시서는 더 위험하다(도착분에도 녹취 인용 2건이 있었다. 이번엔 이름이 없어 통과했으나 같은 형식으로 한 번 섞이면 그대로 공개된다). **규율**: 지시서에 실명·연락처·식별 가능한 정황이 섞여 오면 **커밋하지 않고 먼저 보고한다.** 두 폴더의 `README.md` 에 규율·파일명(`YYYY-MM-DD-<슬러그>-검토/완료.md`)·§11 보고 규격을 적었다. **보고 채널은 병행으로 잡았다** — 채팅에 요약, 파일에 전문(즉시 읽을 것과 나중에 감리할 것은 분량이 다르다). 코드 0줄 · 계약 0 · 코어 0 · DB 0. directive 2026-08-07. |
| ADR-98 | 3회차 갈무리 개정 1차 — 우당탕탕 신설 · 습관 짝 순서 반전 · 문안 4 | **근거**: 3회차 실강(2026-08-09·143분) 녹취와 코치 검토. **개정 1 — `rough_project` 신설**(1면 `areaPick` 둘째 줄·**선택**·필수 6칸 불변). 인도자가 카드에 적으라고 지정한 항목이 카드에 없었다. 필수로 올리지 않은 것은 **과제 시점이 어긋나기** 때문이다(갈무리 제출은 이틀 안, 명명은 다음 주까지) — 필수면 아직 정하지 못한 사람이 카드를 닫지 못한다. 책 참조도 붙이지 않는다(98쪽은 바로 아래 `stuck_named` 가 이미 참조하는 96~104 안이고, 참조가 여섯이 되면 카드가 색인처럼 읽힌다). **타입: `areaPick.line` → `lines: readonly CheckinField[]`.** `line2` 같은 임시 필드를 더하지 않은 이유는 ADR-90 과 같다 — 슬롯을 회차마다 늘리면 7회차에 무너진다. 1·2회차는 한 줄짜리 배열이 될 뿐 **문안이 한 글자도 바뀌지 않아** 리터럴 잠금에 무영향(양방향 단언으로 확인). 렌더·`readModel` 두 곳이 순회로 바뀌었다. **개정 2 — 습관 짝 기입 순서 반전**(시작할 것 위 · 없앨 것 아래). 사분면 좌측 상단이 Eliminate 이고 사람은 위에서부터 채우므로, 짝 맞추기를 나중에 요구하면 **이미 자책 목록이 완성된 뒤**다. 배치가 아니라 **순서**가 만든 결과다. **키는 바꾸지 않는다** — 저장값·결측 판정·나눔 열이 그대로 산다. `to.help` 삭제(연결선 `↓ 그 자리를 만들려면` 이 같은 논리를 진다). **`REQUIRED_3` 도 순서와 라벨 문자열을 함께 고쳤다** — 지시서는 순서만 말했으나 라벨도 바뀌므로, 한쪽만 고치면 결측 안내가 화면과 다른 문안을 읽어 준다(`gap_area` 와 같은 이중 기재. ADR-91 일반화 테스트가 잡는다). 즉 개정 2 의 실제 수정 지점은 다섯이 아니라 **일곱**이었다. **개정 3·4·5 — 문안**: 심화① 부정 서술(`~이 아닙니다`)을 긍정형으로(삭제 의견은 미수용 — 심화 블록의 유일한 자책 방지 장치이고 실강에서 그 위험이 발현됐다) · 심화② 축약 · 한 걸음 `help` 가 우당탕탕을 잇는다(**대체는 미수용** — 4회차 결산이 `step_what`·`step_when` 을 되비추므로 대체하면 회차 간 사슬이 끊기고, 층위도 다르다). **개정 1·5 는 묶음**이다(5의 문구가 1의 칸을 가리킨다). **`summaryFields` 넷째로 우당탕탕 — 자리는 가운데가 아니라 마지막**(클코1 판단): 선택 항목이고 과제 시점이 `다음 주까지`라 **빈 칸이 가장 많이 날 항목**이라, 가운데면 빈 칸이 목록 한복판에 생겨 읽는 흐름이 끊기고 끝이면 있는 사람만 덧붙는 모양이 된다. 짝의 `{from:habit_stop, to:habit_start}` 는 **의미 방향**이라 화면 기입 순서와 별개로 유지. 열람(`readModel`)은 반대로 **카드 순서를 따른다** — 열람은 카드를 되비추는 것이다. **배포 시점이 이 건의 실제 쟁점이었다**(지시서·초판 검토서 모두 놓쳤다): 3회차는 **당일 00:00 에 이미 열렸고** 실강이 오전이라 참여자가 쓰는 중일 수 있는데, 개정 2 는 값을 보존하면서도 **화면에서 두 칸의 자리를 바꾼다** — 이미 위 칸에 적은 사람이 다시 열면 자기 문장이 아래로 내려가 있다(ADR-91 C 와 같은 계열, 값은 살지만 눈에 더 띈다). 지휘부가 **감수하고 당일 배포**를 택했다 — 개정 2 의 목적이 `자책 목록이 완성되기 전에 짝을 맞추게 한다` 인데 미루면 이번 주 참여자가 정확히 그 상태로 한 주를 보낸다. 자리 이동은 한 번 놀라고 끝나지만 자책은 남는다. **`copyBaseline` session3 재생성**(85→88) — ADR-94 가 방금 잠근 자리라 문안 일곱 곳 변경이 곧바로 걸렸고, 이는 사고가 아니라 **잠금이 작동한 것**이다. 재생성 뒤에는 무엇이 왜 바뀌었는지가 스냅샷에서 사라지므로 ADR-91 D 선례대로 **명시 단언**(옛 문안 부재 + 새 문안 존재 + 1·2회차 무영향)을 함께 달았다. 음성 대조: `connector` 와 `REQUIRED_3` 순서를 되돌리면 **네 테스트가 검출**. **미결(지시서 §4·이월)**: 여섯 번째 영역(신앙 축) 6축 승인 대기 · 마음 낱말 `조급함` 은 3회차 제출 데이터로 4회차에 판단 · 한 걸음 동질화는 스크립트북 사안 · 워크북 개정안 승인 시 `identity_gap` 108~111 → 121~123(그때 `session3.test.ts` 열거 단언과 baseline 을 **함께** 고쳐야 한다). 계약 0 · 코어 0 · DB·마이그레이션 0. 검증: tsc 0 · eslint 0 · vitest **496**(489→496) · build 성공. directive 2026-08-09(GALMURI_ORDER_S3_rev1). |
| ADR-99 | §6.0 문언 정정 · 나눔 열 자리 · 저장소 정리 | **① §6.0 문언 정정 — 규율 완화가 아니라 오독 바로잡기.** 후속 지시서 1판이 `docs/tasks/GALMURI_ORDER_S3_rev1.md` 의 발언 원문 두 건을 요지로 바꾸라고 지시했다가 **2판에서 스스로 철회**했다. 철회 사유: §6.0 의 금지 대상은 **개인을 지목할 수 있는 형태**이며 실명이나 식별 가능한 속성과 **결합된** 발언 원문을 말한다. 이름과 분리된 원문 자체는 대상이 아니다. **클코1 의 최초 판정(`이름이 없어 통과`)이 옳았다** — 이 사실을 기록해 두지 않으면 다음에 같은 오독이 났을 때 판례가 없다. 1판 논거 셋이 왜 성립하지 않는지도 함께 남긴다: ⓐ `열 명 남짓이라 재식별된다` → 그 방에 있던 사람은 **이미 그 말을 직접 들었다**. 아는 사람이 계속 아는 것은 공개로 인한 손실이 아니고, 저장소 방문자에게 그 문장은 이름 없는 한 줄이다. ⓑ `되돌릴 수 없으므로 막아야 한다` → **되돌릴 수 없다는 것은 위험의 크기가 아니라 되돌리는 비용이다.** ADR-96 의 녹취록은 실명이 든 전문이라 위험이 실재했고, 그 논리를 위험 없는 대상에 그대로 옮긴 것이 오류다. ⓒ `판정이 필요 없는 규칙이 강하다` → 실명 결합 금지는 판정이 아니라 **관찰**이다(이름이 붙어 있는가만 본다). **그리고 잃는 것이 있다 — 요지 서술은 인과를 지운다.** ADR-98 의 습관 짝 순서 반전은 `잘못된 부분에 집중하니 불편했다`는 **인과**에 걸려 있어, 요지로 바꾸면 뒷사람이 그 결정을 감리할 근거가 얇아진다. 없는 위험을 막느라 치를 비용이 아니다. **문언은 그래도 고친다** — 현행 `실명·발언 원문은 안 되고` 가 **두 개의 병렬 금지로도 한 덩어리로도 읽혀** 실제로 잘못된 시정 지시가 나갔기 때문이다. §6.0 과 두 README 를 `실명·연락처, 그리고 실명이나 식별 가능한 속성과 결합된 발언 원문` 으로 통일하고, **결합 여부가 애매하면 커밋하지 않고 먼저 보고한다**(관찰로 끝나지 않는 경우만 보고 대상)를 덧붙였다. **미조치 보안 취약점 보고서는 `docs/reports/` 에 두지 않는다**(공격 지도 공개가 된다 — 비공개 폐에 두고 조치 후 요약본만 옮긴다)도 함께 명문화. **② 나눔 열 자리 — 클코1 판단 철회.** ADR-98 이 `rough_project` 를 마지막에 둔 근거(`빈 칸이 목록 한복판에 생겨 읽는 흐름이 끊긴다`)가 **코드와 어긋났다** — 회차 현황은 `{s.cells.map((c,j) => (c.has ? … : null))}` 로 **빈 칸을 아예 렌더하지 않는다**. 그 상황은 발생하지 않는다. **자리는 카드 순서를 따른다** — 클코1 이 `readModel` 에 세운 원칙(`열람은 카드를 되비추는 것`)을 나눔 열에도 같이 적용한 것이며, 카드에서 `rough_project` 는 `gap_want` 바로 아래이고 4회차 오프닝에서 프로젝트 선정의 직접 재료다. 두 목록이 손으로 맞춘 것이라 어긋나지 않도록 **나눔 열 첫 두 자리가 카드 1면 줄 순서와 같은지**를 구조로 단언했다. **③ 저장소 정리.** 루트 `CC_*` 4건을 `docs/tasks/` 로 `git mv`(이력 보존). `architecture.md` 의 `CC_*` 언급은 전부 **문서명 참조이지 경로가 아니어서** 갱신할 인용이 0건이었다(지시서는 갱신을 예상했으나 실측 결과 불필요). 회람용 `futurenow_checkin_session3_review.html` **삭제** — 동기화 대상에서 뺀 순간부터 낡아 갈 문서였고 ADR-98 개정도 반영되지 않은 8/5 스냅샷이 됐다. 인도자 콘솔 미리보기(ADR-92)가 정본을 그대로 렌더하므로 대체물이 있다. ADR-94 본문에는 **정정 주**를 달았다(원 기록을 지우면 `왜 스냅샷으로 두기로 했는가` 가 사라져 다음 사람이 같은 회람본을 또 만든다 — ADR-86 과 같은 처리). **④ 미결 단일화** — `docs/tasks/OPEN_ITEMS.md` 신설. ADR 은 결정의 기록이고 미결은 결정되지 않은 것이라 자리를 나눈다. 11건을 한 표로 모았고 **이 파일이 미결의 단일 진실**이다. **⑤ 완료 보고서에 커밋 해시** — `해시는 채팅 보고에 포함` 은 자기모순이었다(보고서 폴더의 존재 이유가 **채팅과 무관하게 감리 가능해야 한다**는 것이다). **⑥ 3회차 실측 — 지금은 잴 수 없다.** Supabase 권한은 복구됐으나(앞선 `-32600` 은 일시적) **표본이 1건**이다(2026-08-09 23:38 KST 기준 내용있는행 1 · 제출 0). 개정 2 영향분 1건만 확정됐고 마음 낱말·우당탕탕·6번째 낱말은 **마감(8/15 23:59) 후**에 재야 한다 — 제약은 권한이 아니라 표본이었다. 계약 0 · 코어 0 · DB 0. 검증: tsc 0 · eslint 0 · vitest **497**(496→497) · build 성공. directive 2026-08-09(TASK_S3_followup 2판). |
| ADR-100 | 3회차 심화 개정 — 심화① 삭제 · 심화② 문항 교체 | **지시**(2026-08-10): 심화 `정체성과 어긋난 하루` 줄과 그 보조 문구를 삭제하고, `요즘 내 입에서 가장 자주 나오는 말 한마디는 무엇인가요?` 를 **`요즘 자주 하는 말 중에 꼭 바꾸고 싶은 말은 무엇이고, 어떻게 바꿀 건가요?`** 로 바꾼다. **저장값 손실 0** — 착수 전 실측(2026-08-10 00:18 KST)에서 3회차 `identity_gap`·`speech_habit` 기입이 각 **0건**이라 화면에서 사라질 답이 없다. **①** `identity_gap` 필드를 통째로 삭제했다. **잃은 것 셋을 명시로 남긴다** — ⓐ **되비추기 세 곳 → 두 곳**: 2회차 정체성 문장(`identity_statement`)을 3회차에서 되비추던 **유일한 지점**이고, 갈망(1회차) → 원씽(4회차) → 정체성 선언(7회차) 종단 축의 중간 고리였다. ⓑ **책 참조 다섯 → 넷**(`108~111` 소멸). 이로써 `OPEN_ITEMS` #4(워크북 개정 승인 시 108~111 → 121~123)가 **대상 소멸로 종결**된다 — 미결이 하나 줄었다. ⓒ ADR-98 개정 3(부정 서술 → 긍정형)이 **대상 자체와 함께 사라졌다**. **②** `speech_habit` 라벨 교체 + `help` 삭제. **ADR-98 이 미수용했던 코치 의견을 지휘부가 채택한 것**이다. 미수용 사유 둘 중 **`1면 습관 짝과 구조가 겹친다`는 여전히 유효하다**(짝도 `바꿀 것`을 묻고 새 문항도 `바꾸고 싶은 말 → 어떻게`로 같은 모양이다) — 판단이 뒤집혔다는 사실과 그 잔여 우려를 함께 기록한다. 나머지 사유(`이 칸은 의도적으로 얕다 · 4회차를 가볍게 여는 재료`)는 **성립하지 않게 됐다** — 유일한 칸이 되면서 심화 전체를 대표하고, 새 문항은 무엇을·어떻게를 함께 물어 더 무겁다. **help 를 두지 않은 것은 지휘부 선택(다)** — 라벨이 이미 두 가지를 묻고 있어 보조 문구가 군더더기가 되고, 남기면 옛 문구(`좋은 말이든 아니든, 그냥 자주 나오는 말이면`)가 **선별하지 말라`고 해 새 문항과 정면으로 어긋난다.** 착수 전 이 모순을 보고해 문안 결정을 받았다(문안은 지휘부 소관이라 클코1 이 지어 넣지 않는다). 라벨의 겹친 어절(`꼭 바꾸고 싶은 것은 말은`)도 확인받아 바로잡았고, 책 참조 `(책 126~133쪽)` 은 ADR-94 확정 좌표라 **유지**했다. **타입**: `deepen.fields` 의 `help: string` → **`help?: string`**. 카드 렌더도 `f.help ? … : null` 로 바꿨다 — 빈 줄이 남으면 간격이 어긋난다. `mirror` 부품은 남긴다(4~7회차가 다시 쓸 수 있다). **주석 드리프트 다섯 곳**을 함께 고쳤다(되비추기 세 곳 · 책 참조 다섯 · 심화 두 칸 · 요약 줄 두 갈래 · 나눔 열 제외 목록) — 코드만 고치고 주석을 두면 다음 사람이 없는 칸을 찾는다. **검증**: 음성 대조로 옛 문항·옛 help 를 되돌리면 **다섯 테스트가 검출**(baseline 2 + ADR-98 절 1 + ADR-100 절 1 + 심화 구조 1). `copyBaseline` session3 재생성(88→84). 계약 0 · 코어 0 · DB 0. tsc 0 · eslint 0 · vitest **505**(497→505) · build 성공. directive 2026-08-10. |
| ADR-101 | 마음 — 직접 쓰기로 커서 이동 + 직접 쓰기만으로도 필수 충족 | **문제**: 화면에 `딱 맞는 말이 없음` 칩과 `직접 쓰기 (선택)` 상자가 나란히 있어 **모순처럼 보인다**(지휘부 지적). 실제로는 모순이 아니라 **역할이 다르다** — 필수 판정은 `mood`(칩 배열)만 보고 `mood_custom`(직접 쓰기)은 세지 않으므로, 그 칩이 목록에 없는 감정을 느낀 사람의 **유일한 통과 수단**이었다. 그런데 화면이 그 분담을 말해 주지 않는다. **실측이 문제를 확정한다**: 1회차에서 여섯 번째 낱말을 고른 **7명 중 직접 쓰기를 함께 쓴 사람 0명**. ADR-91 C 가 `아직 모르겠음`→`딱 맞는 말이 없음` 으로 좁혀 `목록의 한계를 가리켜 자연히 직접 쓰기로 눈이 가게` 한 의도가 **닿지 않았다.** 상자가 처음부터 떠 있어 다리가 아니라 두 갈래로 읽힌다. 그리고 그 1건의 직접 쓰기는 여섯 번째를 **고르지 않은** 사람의 것이었다 — 칩 없이 상자에만 쓰면 `5/6` 으로 제출이 막힌다(실증: 임시 테스트로 `filledCount 5` · 결측 `이 시간을 마치고 나온 지금, 마음은 어떤가요?` 확인). **본인은 방금 적었는데 마음이 비었다는 안내를 받는다** — ADR-91 A 가 없애려던 그 막힘이다. **검토했으나 쓰지 않은 두 안**: ⓐ **칩 제거** — 빼면 그 사람의 통과 수단이 사라져 함정이 커진다. 빼려면 필수 판정을 함께 고쳐야 하므로 `칩 하나만 빼는` 일이 아니고, 3회차만 칩이 다섯이 되어 회차 간 차이가 낱말에 더해 **개수**까지 벌어진다(1·2회차는 저장값 7건 때문에 못 바꾼다 — OPEN_ITEMS #6). ⓑ **상자를 칩 선택 뒤에만 노출** — 다른 칩을 고른 사람이 뉘앙스를 덧붙일 자리를 잃는다(실측 1건이 정확히 그 경우). ⓒ **칩을 자동으로 눌러 주기**(지휘부 제안) — **채택하지 않았다.** 칩을 안 고르고 상자에 쓴 사람이 반드시 `목록에 딱 맞는 말이 없다`고 생각한 것은 아닌데, 자동 선택은 **그 사람 이름으로 그 말을 저장**한다. 그 값은 인도자 화면에 남고 4회차 낱말 교체 판단의 실측(OPEN_ITEMS #2)에까지 들어간다 — **참여자가 하지 않은 말이 데이터가 된다.** ADR-86·91 의 기조(참여자가 적은 것을 그대로 되비춘다)와 어긋난다. 부수 문제도 셋 붙는다(상자를 비우면 자동 해제할 것인가 · 매 글자냐 blur 냐 · 배타라 이미 고른 칩이 지워짐). **채택한 것 둘 — 예방과 안전망**: **① 커서 이동**(지휘부 안): `딱 맞는 말이 없음` 을 **새로 고른 순간에만** 직접 쓰기로 포커스를 옮긴다. 해제·재선택·다른 칩에는 반응하지 않는다(이미 쓰고 있는 사람의 커서를 빼앗지 않는다). 이미 있는 `promptPlaceholder` 와 신호가 겹쳐 같은 방향을 가리킨다. 칩이 다리라는 것을 **문구가 아니라 동작이 말한다.** 포커스는 화면 동작이라 미리보기에서도 막지 않는다. **② 판정에서 인정**: `RequiredField.altKey` 신설 — `mood` 가 비어도 `mood_custom` 에 값이 있으면 충족. **저장값은 만들지 않는다.** 열람은 이미 안전하다(`readModel` 이 칩과 직접 쓰기를 한 줄로 합쳐 보여 준다). 결측 안내는 칩 라벨 **하나만** 가리킨다(상자가 바로 아래 있어 둘을 나열하면 오히려 헷갈린다). **3회차부터만** — 1·2회차는 마감된 회차라 판정을 바꾸면 이미 저장된 행의 `남은 칸 수`가 소급해서 달라진다(ADR-91 §2-4). ①이 대부분을 예방하고 ②가 빠져나간 사람을 받는다. **어느 쪽도 참여자 대신 말하지 않는다.** `required.ts` 는 ADR-91 이 판정 어긋남을 없애려 일원화한 모듈이라 변경을 `altKey` 한 갈래로 좁혔고, 느슨해진 것이 아님을 양방향으로 단언했다(둘 다 비면 여전히 결측 · 공백만은 빈 것 · 1·2회차 무영향). 음성 대조: `altKey` 를 떼면 **두 테스트가 검출**. 계약 0 · 코어 0 · DB 0. tsc 0 · eslint 0 · vitest **510**(505→510) · build 성공. directive 2026-08-10. |
| ADR-102 | 갈무리 1~3회차 문안 진취 전환 — Phase 1(전 회차 공통 넷 + 심화 격상) | **상위 규범 신설**: `futurenow_copy_principles.md`(저장소 루트) — 갈무리·워크북·스크립트북·인도자 가이드에 걸치는 **문안 최상위 규범**이며 회차별 지시서와 충돌하면 이 문서가 이긴다. 지시서는 `docs/tasks/CC_ORDER_checkin_tone_overhaul.md`. **근거**: 1기 1~3회차 실측이 문안 설계의 어긋남을 드러냈다 — 선택은 6~7할이 차는데(심화 8/11 · 못 하게 될 때 7/11) **필수인 selfNote 는 4/8 이 비었다.** 그리고 완충이 막으려던 문제는 **하나도 관측되지 않았다**(실행 자신감 2·5·7·8 로 부풀림 0 · 예시 베낌 0명). 참여자는 시간과 돈을 내고 온 사람인데 문안 스물다섯 곳이 `안 해도 된다`고 말하고 있었다 — **없는 위험에 대비하느라 있는 기회를 잃었다.** **번호·기준선 정정**: 지시서가 스스로를 ADR-97 이라 불렀으나 97 은 선점됐고(docs allowlist), 기준 커밋 `164af43` 은 발행 후 10 커밋 뒤처졌다 — §4-3 이 인용한 `rough_project` help 는 그 시점 트리에 **0건**이고 ADR-98 이 신설한 것이다. **원인은 규율 위반이 아니라 규율의 공백** — `발행 직전 clone` 은 발행 시점만 보장하고 그 이후를 못 막는다. 규율 신설: **같은 파일을 건드리는 작업이 진행 중이면 지시서를 발행하지 않는다.** (다만 클코1 이 사전 경고한 `ADR-98~101 이 이 문안들을 이미 바꿨다`는 **과장이었다** — 대조 14항목 중 실제로 바뀐 문안은 **0건**이고 session1 은 완전 무변경이었다. 표의 오류는 표류가 아니라 작성 시점의 오기·위치 지정 오류였다.) **Phase 1 — 전 회차 공통 넷 + 심화**: ① `selfNote` help 를 값 문장으로(`오늘의 나에게 지금 필요한 말을 적으십시오. 이 한 줄이 회차마다 쌓입니다.`) — **ADR-91 D 판례를 부분 파기했다.** §6-2 가 셋을 묶어 남겼으나 이 하나는 성격이 다르다: `self_note` 는 자기에게 쓰는 말이지 도움을 청하는 통로가 아니라 **무너진 사람이 여는 문이 아니고**(그 문은 연락 요청·익명 안내다), 남겨 둔 완충 넷 중 **유일하게 결측이 일어난 자리**다. 새 문안도 칭찬을 요구하지 않는다 — 허락이 값으로 바뀔 뿐이다. 나머지 둘(`짧은 안부 연락입니다`·익명 안내)은 테스트에서 **그대로 지킨다.** ② `identity` help 완충 제거 — **여기서 회차별로 갈렸다.** 새 값 문장(`이 문장이 다음 회차의 출발점이 됩니다`)은 1회차에서는 참이지만(2회차가 `identity_sentence` 를 되비춘다) **2회차에서는 거짓**이다 — `identity_statement` 를 읽는 자리가 ADR-100 이후 **0곳**이고 `session3.test.ts` 가 그 부재를 단언 중이다. 그래서 **1회차는 전문·2회차는 앞 문장만** 넣었다. **값 문장을 억지로 채우지 않는다** — 지금 넣을 수 있는 참인 문장(`회차마다 쌓입니다`)은 selfNote 와 같은 말이라 원칙 §2-6 에 걸린다. 비워 두는 편이 거짓보다 낫다. **문안이 거짓이 된 것은 증상이고 원인은 종단 축 사슬이 2회차에서 끊긴 것**이다 — 복구는 **4회차 발주에 `identity_statement` 되비추기를 필수 항목으로** 넣어 하고(4회차는 원씽을 고르는 회차이고 그 준거가 2회차의 한 문장이다), 배포 후 2회차 값 문장을 넣는다. 톤 개편에 회차 구조를 섞지 않는다. ③ `lastStep.mirror.empty` 를 누적으로(`이번 회차부터 한 걸음이 쌓입니다.`) ④ 심화 `title` 격상(`여기서부터가 진짜입니다`). **⑤ 심화 기본 펼침 — `deep_opened` 계측 폐기와 한 몸이다.** `Disclosure` 가 마운트 시 콜백을 부르지 않아 기본 펼침이면 지표가 **자동으로 거짓**이 되고, `defaultOpen` 만 뒤집고 `onToggle` 을 남기면 **접었다 다시 편 사람만** 집계돼 0 이 아닌 작은 수가 찍혀 옛 지표와 구별되지 않는다. 그래서 **`onToggle` 을 뗐다.** **`deep_opened` 는 1기 지표로 동결**하고 컬럼은 보존한다(1기 실측이 그 안에 있다). 앞으로의 지표는 **심화 칸 실제 작성률** — 펼침은 의도이고 작성은 행동이다. 부수 효과: 접은 상태가 저장되지 않아 다시 열면 늘 펼쳐져 있다(기본 펼침의 취지와 맞다). **`선택` 뱃지는 이번 범위에서 뺐다**(지휘부 철회) — 심화에 뱃지가 애초에 없고(ADR-82 가 지웠고 ADR-88 이 접힘 블록에 대해 유지) 되살리면 타입·문안 3건·baseline 이 따라온다. ADR-88 의 근거(`빈 칸이 펼쳐져 있으면 필수로 읽힌다`)를 건드린다는 것은 **알고 뺐다** — 이 개편이 요구를 올리는 방향이라 그 오독이 이번엔 손해가 아니다. **새 규범(원칙 문서 반영)**: **값을 말할 때는 제품이 지금 이행할 수 있는 것만 말한다. 미래 기능을 근거로 든 문안은 약속이 아니라 부채다.** 초판이 예시로 든 `7주 뒤, 일곱 마디가 나란히 남습니다` 가 그 사고였다 — 여러 회차를 한 화면에 나란히 보는 기능이 **없다**(`getMyCheckin` 은 단일 회차 전용, ADR-86 이 만든 것은 회차 링크 목록. 1·2회차 selfNote 비교에 탭 5회·페이지 이동 3회가 들고 두 문장이 동시에 보이는 순간은 0). **ADR-94 B 가 세운 기준을 지휘부 스스로 어긴 것**이라 문안을 낮추고 화면은 만들지 않는다(종단 비교는 종료 리포트 v1.2 의 몫 — 카드 한 줄 때문에 새 화면을 세우지 않는다). **Phase 3 문안도 함께 확정**: `완성` 을 버리고 **`완료`** 로 통일한다(`완성` 은 품질 판정처럼 읽히고 우리가 세는 것은 칸이 다 찼는지다). `{n}칸 더 채우면 완료` — **`더 채우면` 이 방향을 주므로** 잔여인지 누적인지 묻지 않아도 되고 `남음` 처럼 결핍을 가리키지도 않는다. `아직 완성되지 않았습니다` 는 **폐기** — `미제출 ≡ 아직 완성되지 않음` 이라 참여자 판정 금지에 저촉하며, 원칙 축3 이 같은 문서 §4 를 어긴 **내부 충돌**이었다. 일곱 칸 표시는 **`AppHeader` 바로 아래 고정**(상태 무관 규칙 — `이번 주 갈무리` 블록은 사전진단 미완이면 밀리고 `openSessionNo` 가 없으면 안 그려져 앵커가 못 된다) · **캡션급 위계**(ADR-81 3단에 넷째가 끼는 것이라 강조하면 accent CTA 가 밀린다) · 계약 0(`getMyCheckin` 팬아웃, `opensAt <= now` 만 질의하면 추가 왕복 2회). **회귀**: `copyBaseline` 세 회차 재생성(62·85·84 — 문안 교체라 총량은 같다) + ADR-102 명시 단언 5건(허락 문구 넷 부재 · selfNote 값 문장 · 심화 제목 · 누적 안내 · **identity 회차별 분기**). `BANNED` 일괄 단언은 **Phase 2 이후에** 넣는다 — 지금 넣으면 아직 안 고친 Phase 2 문안이 즉시 걸린다. 음성 대조: 완충 문구를 되돌리면 **다섯 테스트가 검출**. **문서 배치**: 지시서 `docs/tasks/` · 규범 **저장소 루트**(`design_system.md` 옆 — 워크북·스크립트북에도 걸리는 문서를 갈무리 지시서 폴더에 두면 다음 사람이 못 찾는다). 둘 다 `docs/` 루트에 도착해 `.gitignore` 에 막혀 있던 것을 옮겼다. **스크린샷은 렌더된 문자열 대조로 갈음**(승인) — 실기기 확인은 지휘부 몫. 계약 0 · 코어 0 · DB 0. tsc 0 · eslint 0 · vitest **515**(510→515) · build 성공. **후속(같은 날) — `충분해요` 계열 3곳 삭제.** `BANNED` 에 `충분` 을 넣으려면 함께 처리해야 할 자리로 클코1 이 올렸고 지휘부가 **어미 교체가 아니라 삭제**를 택했다(`사족`). 한 걸음 help 는 `하나면 충분해요` 와 `두 개를 적으면 대개 둘 다 하지 않거든요` 를 **한 묶음으로** 지웠다 — 앞엣것은 허락이고 뒤엣것은 그 허락의 이유라, 앞을 지우면 뒤가 가리킬 데가 없어진다. 남는 것은 용어 안내 한 줄(`이 세미나에서는 이것을 한 걸음이라고 부릅니다.`)이고 이 칸에 필요한 전부다. 목적 세 질문은 앞의 허락만 지우고 뒤 문장(`인생을 이끌어갈 하나의 문장의 재료가 됩니다`)은 값이라 남겼다. baseline 총량은 불변(문자열 개수가 아니라 내용이 줄었다). **그런데 두 문장 삭제는 과했다 — 뒷문장을 되살렸다(지휘부 정정).** `두 개를 적으면 대개 둘 다 하지 않거든요` 는 허락의 이유가 아니라 **실행에 관한 사실**이고, 하나만 고르라는 **요구의 근거**다. **앞을 어떻게 쓰느냐가 뒤의 성격을 정한다** — 허락 뒤에 붙으면 허락의 근거로 읽히고 요구 뒤에 붙으면 요구의 근거로 읽힌다. 클코1 은 그때의 읽힘만 보고 문장 자체를 허락의 부속으로 판정했다(목적 세 질문에서는 같은 실수를 안 했으면서 한 걸음에서만 어긋난 것이 그 증거다). 지우자 **왜 하나인지가 사라져** 참여자가 칸이 하나인 것만 보고 이유를 모르게 됐고, 다음 회차에 둘을 적는 사람이 생긴다. **축 1 은 허락을 지우라 했지 요구를 지우라 하지 않았다.** 교체: `이 세미나에서는 이것을 한 걸음이라고 부릅니다. **하나만 정하십시오.** / 두 개를 적으면 대개 둘 다 하지 않거든요.` — 허락이 요구로 바뀌고 뒷문장이 그 요구의 근거로 제자리를 찾는다. **규칙으로 남긴다(원칙 문서 §1 축1)**: 허락을 지울 때 그 문장이 딸고 있던 **요구와 근거까지 함께 지우지 않는다. 허락을 요구로 교체하면 근거는 제자리에 남는다.** **3회차는 대상이 아니다** — `세 회차 모두 같다`는 지시였으나 3회차 `step.help` 는 그 문자열을 **가진 적이 없고**(git -S 실측 0건) ADR-98 이 우당탕탕 연결로 교체한 다른 문안이다. 그 끝의 `그 첫 걸음이어도 좋습니다` 는 허락 계열이나 정서 완충이 아니라 **경로 안내**(우당탕탕을 한 걸음으로 써도 된다)라 지우면 ADR-98 이 만든 두 칸의 연결이 끊긴다 — **유지**(지휘부 확정). **주석 자기모순 정정**: 카드 렌더 주석이 `컬럼(deep_opened)은 지운다` 로 적혀 있었다(착수 조건은 보존이고 뒷절이 그 근거인데 앞절이 정반대였다) → `보존한다`. 테스트도 `충분해요 부재` 만이 아니라 **요구·근거의 존재**를 함께 단언하도록 고쳤다. 잔여 `충분합니다` 둘(2회차 areaPick · 3회차 pairText)은 Phase 2 대상이라 그대로다. vitest **516**. **Phase 2 완료 — 회차별 문안 여덟 자리.** 1회차: 표지 띠(`오늘 꺼낸 것을, 흐려지기 전에 붙잡아 둡니다. 7주 기록의 첫 장입니다.`) · 편지 첨부 말미를 명령형으로. 2회차: `areaPick`(`다섯 중 가장 가슴이 뛴 하나만 고르십시오. 이 선택이 4회차 원씽의 재료가 됩니다.` — 분량 지시를 명령형으로 바꾸는 것이 값이 크다. `안 해도 된다`로 읽히던 것이 실제로는 **하나만 골라라는 더 어려운 요구**였다) · 지난 걸음 메모(`정직한 기록만이 다음 한 주를 바꿉니다.`) · 편지 첨부. 3회차: 우당탕탕(`완성이 아니라 시작이 목적입니다. 이름을 붙이는 순간 프로젝트가 됩니다.`) · 오늘의 질문(문장 셋을 둘로 — `정확한 분석이 아니어도 됩니다`는 오해 방지처럼 보이나 실은 완충이다. 무엇을 적으라는 지시가 이미 앞에 있어 오답이 생기지 않는다) · 습관 짝(`짝이 맞는 한 쌍만 고르십시오. 없앨 것과 들일 것이 함께 있어야 습관이 바뀝니다.` — 이 회차의 핵심 원칙이 문안에 드러나야 구조가 설명된다). **3회차 `lastStep note.help` 는 건드리지 않았다**(지시서 정정 — 지울 완충이 애초에 없다). **`BANNED` 일괄 잠금을 Phase 2 끝에 넣었다** — 여섯 어휘(`하셔도 됩니다`·`않으셔도 됩니다`·`괜찮습니다`·`아니어도 됩니다`·`충분합니다`·`충분해요`)가 세 회차 문안에서 **0건**임을 단언한다. Phase 1 시점에 넣었다면 아직 안 고친 Phase 2 문안이 자기 테스트에 걸렸을 것이다. **검사 범위는 `session*.ts` 로 한정하고, 원칙 §3 의 다섯 자리는 별도 `describe` 로 존재를 단언한다** — 그 자리들은 허락 어휘를 그대로 쓰지만 지우면 안 되는 것들이고(5주차에 무너진 사람이 여는 문·오답을 막는 지시·프라이버시 고지·채우게 만드는 장치), **BANNED 에 안 걸린다는 것은 지워도 안 잡힌다는 뜻**이기 때문이다. 셋(연락 요청·익명 안내·오해 방지)은 레지스트리, 둘(사진 첨부 고지 `LetterPhotos.tsx` · 결측 패널 `CheckinCardClient.tsx`)은 **컴포넌트 파일에서 문자열 존재**로 지킨다. 압박 어휘(`반드시`·`절대`·`놓치지`)와 판정어(`지각`·`미제출`·`함정`) 0건도 함께 단언. 음성 대조: 허락 어휘 1건을 되돌리고 사진 고지를 지우면 **다섯 테스트가 검출**(컴포넌트 존재 단언 포함). baseline 세 회차 재생성(62·85·84 — 총량 불변). vitest **525**(516→525). **Phase 3 미착수** — §5 문안은 지휘부가 확정해 지시서 §0 에 실었다. directive 2026-08-08 발행 · 2026-08-14 정정·착수. |
| ADR-103 | 되비추기 깊이 — `Mirror.back` | **무엇**: 되비추기가 직전 회차만 읽던 것을 **몇 회차 전이든** 읽게 연다. `Mirror.back`(기본 1) · `Priors = Record<number, answers|null>` · `neededBacks(copy)` · `priorSessionNos(sessionNo, mode, backs)`. **왜 지금 하는가 — 값을 과장하지 않는다.** `back: 2` 가 실제로 겨누는 것은 4회차 심화의 2회차 `identity_statement` 인데 1기 실측 기준 **2회차 제출은 2건**이고 `empty` 를 두지 않아 대다수 참여자에게 그 상자는 **그려지지 않는다.** 타입·순수함수·컴포넌트·페이지 넷을 고치고 조회 왕복을 늘리는 대가로 얻는 것이 소수에게만 보이는 회색 상자 하나라면 ADR-94 가 세운 기준(도구가 있는가를 먼저 묻는다)에 걸린다. **그럼에도 진행하는 근거는 일반화 시점이다** — `back` 을 지금 도입하는 이유는 4회차 심화의 값이 아니라 **5·6·7회차에서 두 회차 전을 되비출 일이 반복되기 때문**이다. 회차별 특례로 처리하면 그때 하드코딩하게 되고, ADR-90 이 슬롯을 모양으로 일반화한 판단과 어긋난다. **이 기능의 값은 남은 세 회차에서 회수된다.**(지시서 §4-0 원문 — 값을 과장한 채 승인하면 나중에 `그때 왜 만들었지`를 다시 묻게 된다.) **상한을 두지 않는다 — 지시서가 자기 논리를 어긴 자리를 클코1 이 잡았다.** §4-0 이 `회차별 특례는 하드코딩`이라 써 놓고 §4-2 에서 `back?: 1 | 2` 로 상한을 박았다. 종단 축(갈망1 → 한 문장2 → 영역3 → 원씽4 → 환경5 → 남은 시간6 → 정체성 선언7)에서 **7회차가 2회차를 되비추면 `back: 5`** 이고 그것은 축의 끝점이라 반드시 온다. `1|2` 로 두면 그때 타입·봉투·페이지를 또 고친다 → **`back?: number`**. 지휘부가 전량 수용(정정 ⑤ · `가장 큼`). **병합하지 않는다**: `step_what`·`step_when`·`mood`·`self_note` 는 회차 공용 키라 한 봉투에 합치면 지난 걸음 되비추기가 다른 회차 값을 읽고 참여자에게 **틀린 문장**이 보인다. **어느 깊이를 부를지는 회차 번호가 아니라 문안이 정한다**(`neededBacks`) — 번호로 분기하면 5·6·7회차에서 다시 고쳐야 한다. **1회차는 되비추기 선언이 0개라 깊이도 0**(첫 회차라 되비출 지난 회차가 없다) · 2·3회차 `[1]` · 4회차 `[1,2]`. 지시서 메모는 셋 다 `[1]` 이라 적었으나 실측이 다르다 — 코드가 옳고 테스트를 사실에 맞췄다. **판정 로직(앵커 규칙·trim·원문 출력)을 한 글자도 바꾸지 않았다.** **하위호환을 사람 눈이 아니라 테스트가 본다**(지시서 §4-7 — 중간 게이트를 자동 검증으로 옮겼다): ⓐ 다른 깊이(`{2:…, 5:…}`)를 채워도 1~3회차 출력 불변 **← 봉투 분리 증명** ⓑ `back:2` 는 두 회차 전만 읽고 직전은 읽지 않는다 ⓒ `neededBacks`·`priorSessionNos` 순수함수 — **3회차 왕복 1회로 이전과 동일** ⓓ 열람(read)은 조회 자체를 건너뛴다(ADR-86) ⓔ **가상 `back:5` 에서 `priorSessionNos(7,'edit',[1,5]) === [6,2]`** — 상한 없음을 잠근다. 미리보기 표본도 깊이별로 나눴고 `back2` 에 **다른 값**을 넣어 봉투 분리를 눈으로도 보게 했다. 문안 파일(`session1·2·3.ts`) diff **0** — 렌더 경로만 건드린다. 계약 0 · 코어 0 · DB 0. tsc 0 · vitest **540**. directive 2026-08-16(CC_ORDER_checkin_session4 §4 + CC_MEMO 정정 ④⑤). |
| ADR-104 | 4회차 갈무리 문안 — STEP 5·6 | **두 번째 이중 STEP 회차**(옵션을 펼쳐라 + 단 한 가지를 선택하라). 3회차가 만든 구조(`group`·`order`·`mirror`)를 그대로 쓰고 **새 입력 형태를 만들지 않는다.** 레지스트리 한 줄로 등록 — 회차 추가 = `sessionN.ts` + 한 줄이라는 ADR-85 의 약속이 네 번째 회차에서 지켜졌다. **1면 순서가 3회차와 다르다** — `areaPick → pairText → question → mood`(3회차는 `question` 이 `pairText` 앞). 두 옮겨 적기(펼친 것 하나 · 고른 것 하나)가 붙어 있어야 묶음 경계가 화제 전환과 일치한다. **필수 6칸이되 1번이 세 필드**(`project_area`·`project_name`·`project_due`) — STEP 5 의 유일한 판별 기준이 **끝나는 날**이라, 마감일을 선택으로 내리면 이 회차의 원리가 카드에서 빠지고 이름과 한 칸에 합치면 한 문항이 두 가지를 묻는다. 참여자가 워크북 정의서에 이미 써 둔 것이라 옮겨 적기이고 값이 싸다. `RequiredGroup.fields` 가 배열이라 세 필드도 그대로 동작한다. 이중 STEP 이라 필수가 하나 늘 자리인데 **그 증가분을 서술 질문(`parked`)에서 회수한다** — 3회차 `stuck_named` 와 같은 처리다. **신규 키 일곱**: `project_area`·`project_name`·`project_due`·`domino_what`·`domino_effect`·`parked`·`onething`. **영역 키를 재사용하지 않는다** — 2회차는 가장 가슴 뛴 영역, 3회차는 가장 간절한 영역, 4회차는 **실제로 손대는** 영역이고, **세 값이 어긋날 때가 인도자에게 가장 값진 정보**인데 같은 키를 쓰면 그 차이가 덮인다. **첫 도미노 짝은 구조 강제 장치다** — 아래 칸(`함께 넘어갈 것은`)이 **검산기** 노릇을 한다: 못 적으면 그것은 첫 도미노가 아니라 그냥 해야 할 일 중 하나다. **인도자가 판정하지 않고 참여자가 스스로 안다.** `from → to` 가 **인과**라 화살표가 참이다(3회차 습관 짝은 교체이나 방향성이 있다는 점에서 같은 자격). **되비추기 네 곳** — ①3회차 영역+바람 · ②3회차 우당탕탕(선택이라 빌 수 있어 `empty` 없음 → 그때는 상자를 안 그린다) · **심화 2회차 한 문장(`back: 2` · ADR-103 이 이 자리를 위해 났다)** · ⑤3회차 한 걸음. **4회차의 아픔은 직면이 아니라 상실**이다 — 스무 개를 펼쳐 놓고 하나만 남기는 날이라 카드가 이틀 뒤 열릴 때 그 아쉬움이 남아 있다. 그래서 마음 낱말이 앞 세 회차와 결이 다르다(`선명함·아쉬움·두근거림·망설임·든든함`). **`망설임` 을 넣은 이유는 첫 도미노를 아직 못 고른 사람이 반드시 나오기 때문**이고, 그 상태에 이름이 없으면 카드 앞에서 자기를 탓한다. 앞 다섯 낱말이 1~3회차와 겹치지 않음을 **주장이 아니라 잠금으로** 뒀다(교집합 공집합 · 20낱말 — 낱말은 회차마다 사람이 짜므로 5·6·7회차에서 겹칠 수 있다). 여섯 번째(`딱 맞는 말이 없음`)는 공유가 의도다. **`대기열` 은 4회차 신규 금지어** — 세션 안에서만 통하는 내부 용어이고, 강의 어휘를 못 들은 사람도 문장만으로 무엇을 쓸지 알아야 한다(코드 주석과 지시서에는 써도 된다). **`step_blocker.help` 를 3회차의 위로에서 5회차 인계로 바꿨다** — STEP 7 이 환경 설계이고 여기 적힌 방해 요인이 그대로 그 시간의 재료가 된다. **이 회차에서는 방해에 대한 답을 주지 않는 것이 설계다.** `what.help` 를 새로 붙였다(계획이 아니라 동작) — 워크북이 이 회차에서 그것을 강제하는데 카드에도 같은 지시가 있어야 이틀 뒤에 큰 것을 적지 않는다. **첫 도미노와 한 걸음은 대체가 아니라 층 관계**다(이번 달 / 이번 주 한 동작). **심화는 한 칸**(3회차 선례) — 인생의 원씽은 오늘 정하는 것이 아니라 **가설**이라 필수로 두지 않는다. 사진 첨부 없음(`letter_line` 키 미사용 → 위젯이 자동으로 안 그려진다). 어조는 ADR-102 진취 전환 — 허락 어휘 0건이 `BANNED` 로 잠긴다. **`아니어도 좋습니다` 는 허락이 아니다** — `~여도 좋습니다` 가 **범위를 넓히는 예시/오답 방지**로 쓰인 경우이며(원칙 §1 축1 네 번째 갈래) 뒷문장이 명령형으로 값을 회수한다. **책 페이지 참조를 붙이지 않는다** — STEP 5·6 구간의 조판 확정본 대조가 끝나지 않았다(ADR-88·89: 확정된 값만 카드에 오른다). 붙일 자리 셋을 파일 상단에 적어 뒀다. **미등록 회차 가드 셋을 지우지 않고 이어 썼다**(`session2.test.ts`·`session3.test.ts`·`readModel.test.ts` → `getCheckinSession(5)`) — 그 단언은 4회차 부재의 기록이 아니라 **미등록 회차가 링크로 새어 나가지 않는다**는 가드다. 지시서 §6-4 에 없던 항목으로 클코1 이 잡았다(정정 ③). **`project_due` placeholder 를 `10월 31일` 로**(정정 ⑥) — 원안 `6월 30일` 은 4회차 개시(8/16)보다 과거라 `끝나는 날이 붙으면 프로젝트가 된다`는 그 칸의 `help` 를 배반한다. 1기 종료(9/13) 이후라 `세미나가 끝나도 이어지는 프로젝트`라는 결도 맞는다. `copyBaseline` 에 `session4` 89건 편입 · `copyRegression`·`required.test` 순회 확장 · ADR-104 명시 잠금 4건. 음성 대조: 허락 어휘·`back:2`·`대기열`·5회차 인계 넷을 되돌리면 **아홉 테스트가 검출**. 계약 0 · 코어 0 · DB·마이그레이션 0. tsc 0 · eslint 0 · vitest **591**(547→591) · build 성공. directive 2026-08-16(CC_ORDER_checkin_session4_final + CC_MEMO 정정 7건 전량 수용). |
| ADR-105 | 사전진단 이행 약속 체크(`PLEDGE`) | **무엇**: `마지막 한 걸음` 블록에 다짐 체크 한 줄 추가 — `매주 모임시간을 잘 지켜 참석하고, 사전 과제와 사후 갈무리를 충실히 하겠습니다.` **두 줄의 층이 다르다**: `COMMIT` 은 마음의 다짐(이 시간을 끝까지 걷겠다)이고 `PLEDGE` 는 그 다짐이 실제로 무엇을 뜻하는지를 행동으로 적은 약속이다. 큰 다짐이 먼저, 구체가 뒤 — 순서를 뒤집으면 약속만 하고 이유를 못 읽는다. **사전 wave 전용**(`Record<WaveKey, string>` 로 두지 않았다) — 이 문장은 **앞으로의 5주**를 두고 하는 약속이라 세미나가 끝난 자리에서는 성립하지 않는다. 사후에 과거형을 지어 넣으면 지휘부가 주지 않은 문안을 코드가 만들어 내는 셈이라, 회차가 아니라 **wave 로 가른다.** **`required: false`** — `COMMIT`·`CARE` 와 같다. 체크를 강제하면 그것은 더 이상 다짐이 아니고 제출까지 막힌다. **채점 0**: `polarity: neutral` · §9.1 스펙 31코드 밖 · `scoring.ts` 미참조(`CARE` 만 Red Flag 를 건드린다) · 리포트 미노출(`COMMIT` 도 같다 — 인도자 화면에 띄우려면 별건이다). zod `optional()` — **이미 제출된 사전 응답에는 이 키가 없다.** 필수로 두면 그 행들이 경계에서 전부 튕긴다(ADR-91 §2-4: 제출된 행을 소급 처리하지 않는다). **기존 단언 하나를 지우지 않고 좁혔다** — `flow.test.ts` 의 `postCodes === preCodes` 가 즉시 레드가 됐는데, 그 단언이 지키던 것은 `사전·사후가 같은 자를 써야 변화를 잰다`이고 그것은 **채점 코드**에 대한 말이다. 채점 코드는 한 자도 안 갈렸다. 넓은 단언을 통째로 버리면 내일 누가 `A1` 을 사후에서 빼도 아무도 못 잡으므로, 채점 코드 완전 일치를 그대로 두고 **갈라진 자리를 이름(`['PLEDGE']`)으로 못 박아** 여기가 늘어나는 순간 테스트가 먼저 말하게 했다(ADR-104 의 미등록 회차 가드 처리와 같은 방식). 잠금 7건 · 음성 대조: 사후 누출·문안 변조를 넣으면 **3건 검출**. 계약 0 · 코어 0 · DB·마이그레이션 0. tsc 0 · eslint 0 · vitest **598**(591→598) · build 성공. |
| ADR-106 | 공용 기수 모집 랜딩 `/recruit` | **무엇**: 카드뉴스 13장의 웹 판을 신규 라우트 `/recruit` 에 세운다. **`/join` 과 다른 물건이다** — 지휘부가 착수 중 두 번 바로잡았다. 발주서(2026-08-18) §3.1 은 랜딩 경로를 `/join` 으로 확정하고 §3.7 은 `QR 은 이 랜딩을 가리킨다·사전 체크로 직행시키지 않는다` 고 썼는데, **둘 다 폐기**됐다: `https://future.yebom.org/join?code=ZR4KB` 는 **그 기수의 가입 + 사전체크 링크**이고 이미 살아 있다(cohorts.code='ZR4KB' → '퓨처나우2026예봄2기', 2026-07-27 생성 — 실측). 랜딩은 **기수마다 가입링크만 갈아 끼워 재사용하는 공용 페이지**이고 카톡에 링크로 돌리는 물건이다. 카드뉴스 QR 은 가입링크를 직접 가리킨다(렌더된 카드 13 에서 디코딩 확인). **`src/app/join/*` 은 한 줄도 건드리지 않았다**(git status 로 증명) — 처음에 `/join` 을 분기점으로 만들어 `?start=1` 로 가르는 안을 짰다가 지휘부 정정으로 버렸다. 그 안은 재진입(`?cohort=`)·마무리 체크(`?wave=post`) 와 한 라우트를 공유해 위험이 있었고, 경로를 나누니 그 위험이 사라졌다. **문구의 최종 기준은 원고 `.md` 가 아니라 시안 HTML 이다**(지휘부 확정 — `html이 최종기준이다`). 그래서 회귀 테스트가 스냅샷 사본을 만들지 않고 **시안 파일을 직접 읽어 대조**한다(갈무리 `copyRegression` 이 baseline JSON 을 쓰는 것과 다른 선택 — 거기는 원본이 코드 안이고 여기는 코드 밖 문서라, 사본을 만들면 진실이 셋이 된다). 시안이 고쳐지면 이 테스트가 먼저 레드가 된다. **기수 교체는 `intake.ts` 상수 하나다.** 링크만 상수로 빼면 일정·참가비·마감일이 본문에 박혀 3기 때 문안을 헤집으므로 **기수 가변값 전부**(코드·기수명·상태·일정 6행·참가비·장학금·개근기준·계좌·마감일)를 거기 모았고, `copy.ts` 에는 3·4기에도 그대로 서는 문장만 남겼다. 그 약속을 말이 아니라 **테스트로** 잠갔다 — 3기를 가정한 상수로 갈아 끼워 CTA 가 따라 바뀌는지 보고, 기수 고유값이 문구 상수에 새어 들어갔는지 전수 검사한다. **마감 상태 3종은 상수 표**(`STATUS_COPY`)다 — 발주서 §3.5 의 문구가 미확정이라 자리만 만들고 `DB 마이그레이션은 만들지 않았다`. 운영 데이터(S-3)가 붙으면 타입 그대로 넘겨받는다. 자동 마감 없음(인도자 수동). **공개 라우트다** — `PROTECTED_PREFIXES`(src/proxy.guard.ts)에 없어 설정 없이 비인증 열람되고, 그 사실을 테스트로 못 박았다(로그인 벽 뒤에 갇히면 모집이 통째로 죽는다). **OG 이미지는 Next 파일 관례**(`opengraph-image.png` = 카드 1, 1080×1080)를 쓴다. `metadataBase` 를 세그먼트 `layout.tsx` 에 둔 이유: 그 이미지 라우트는 **레이아웃 체인의 metadata 만** 보므로 page 에만 두면 매 빌드가 localhost 경고를 낸다. 루트 layout 은 전 라우트 공유라 버렸다. 실측 og:image = `https://future.yebom.org/recruit/opengraph-image.png`. **시각 언어는 시안에서, 색은 앱 토큰에서.** 가져온 것: 네이비 바탕·골드 강조선·도면 격자(60px/1080 → 28px 환산)·눈썹문(letter-spacing .22em)·인용 좌측 골드 세로선·표 구조. 버린 것: 카드번호·1:1 비율·카드11 전용 축소(셋 다 카드뉴스 매체 전용). 골드 면 위 글자는 `--color-text-on-gold`(네이비) — 흰 글자는 대비 2.79:1 미달. 앱이 라이트 고정이라(layout `colorScheme:'only light'`) 이 페이지만 네이비 바탕이므로 글자색을 역할 토큰이 아니라 지역 변수로 정했다. **실측 검증**(발주서 §5: `fetch 200 은 근거로 인정하지 않는다`): 390px 실제 브라우저 — 문서폭 390 = 창폭 390 **가로 스크롤 0** · 넘친 요소 0 · 탭 타깃 51px·44px(전부 ≥44) · 하단 고정 바가 마지막 문장을 가리지 않음(padding-bottom + `env(safe-area-inset-bottom)`) · 스크린샷 전체 확인. 계좌 복사는 `navigator.clipboard` 실패 시 침묵하지 않고 `길게 눌러 선택`을 안내한다(인앱 브라우저 차단 대비, 경고색 0). 복사값은 번호만 — 이체 화면에 은행명·예금주가 딸려 가면 다시 지워야 한다. **범위 밖**: S-1(기존 화면 용어 통일)·S-3(운영 데이터·마감 스위치·확정자 목록). 계약 0 · 코어 0 · DB·마이그레이션 0 · `/join` 0 · 루트(/) 0. tsc 0 · eslint 0 · vitest **626**(598→626, 신규 28) · build 성공(`/recruit` 정적 ○). directive 2026-08-19. **셋을 한자리에 묶었다(2026-08-19 추가)** — 지휘부 요구 `이미지, 원고, 웹 모두 일치되도록 하세요`. 원고 `.md` 는 머리말에 `이 파일이 최신이며, 시안을 고칠 때 이 파일도 함께 고친다` 고 적혀 있었으나 `html 이 최종기준` 확정으로 **서열이 뒤집혔다.** 카드 1~13 전문을 시안 기준으로 동기화하고(11개 항목이 뒤처져 있었다) 머리말을 현재 서열로 고쳤다 — 문서가 자기 위치를 알아야 다음 사람이 어느 쪽을 믿을지 묻지 않는다. **일치를 말이 아니라 테스트로 잠갔다**: 시안↔웹은 시안 파일을 직접 읽어 대조하고, 원고↔웹은 같은 방식으로 원고를 읽어 대조한다. `copy.ts` 를 가운데 두는 이유 — 그것이 이미 시안과 동일함이 증명됐으므로 `원고 ⊇ copy.ts` 이면 `원고 == 시안` 이고, 대조를 두 번 쓰지 않아도 된다. 옛 판본의 흔적 16개(`의지가 약해서가 아니다`·`방향이 흐린 분`·`한국상담대학원대학교` 등)를 이름으로 금지해 되살아나지 못하게 했다. 음성 대조: 원고만 옛 문구로 되돌리면 **2건 검출**. 이후 문구 수정 순서는 **시안 → 웹·원고**이며, 하나만 고치면 반드시 레드가 난다. |
| ADR-107 | 용어 통일 — `진단` → `체크`(S-1) | **무엇**: 발주서(2026-08-18) §2 의 명칭 원장을 화면 전체에 적용한다. 시작 시점 도구 = **사전 체크** · 종료 시점 = **마무리 체크** · 인도자용 산출물 = **사전/마무리 체크 리포트**. **왜 지금**: 카드뉴스 13장이 이미 `사전 체크`로 조판돼 배포를 기다린다. 카드에서 그 말을 보고 온 사람이 가입 첫 화면에서 `진단`을 만나면 첫 접촉에서 어긋난다. **상수 한 곳** — `src/app/_vocab/tool.ts` 의 `TOOL` · `toolName(wave)` · `reportName(wave)`(발주서 §2.3 이 지정한 구현 방식). 앱 프레젠테이션 계층에 둔 이유: 참여자 화면과 인도자 콘솔이 **함께** 쓰는 표시 어휘라, `/contracts` 에 두면 계약 변경이고 `/core` 에 두면 코어가 인스트루먼트 어휘를 갖는다(CLAUDE §2). `_screens/types.ts` 의 `instrumentDisplay()` 가 같은 성격이라 그것도 여기를 바라보게 했다. **다음 개명은 이 파일 한 곳이다** — 이번에 서른 자리를 뒤진 일을 되풀이하지 않는다. **코드 식별자는 한 글자도 안 바꿨다**(발주서 §1): 라우트 `/join` · 컬럼 `responses.wave` · 타입 `Wave` · API 필드 그대로. 참여자는 식별자를 보지 않는다. **`5주` → `6주` 도 함께**(§2.4 — 명칭과 무관하나 같은 표면) · 참여자 표면의 `문항` → `질문`. **동의 문안은 낱말만 바꾸고 `CONSENT_VERSION` 은 올리지 않았다** — 수집 항목·이용 목적·보유 기간·열람 주체가 한 글자도 달라지지 않았고, 버전을 올리면 `home/page.tsx` 의 버전 일치 판정 때문에 **진행 중인 1기 참여자 전원이 다음 방문에 재동의 화면을 만난다**. 동의의 내용이 바뀔 때만 올린다(판단 필요 — 지휘부가 뒤집으면 한 줄이다). **`copydeck.md` 도 함께 맞췄다** — 원고 `.md` 때와 같은 이유(사본이 뒤처지면 3기 때 어느 쪽이 맞는지 매번 대조한다). **잔존 10건과 사유**(§5.2 요구): ①`copy.ts` C8 문항 원문의 `점검` — **§1 불변식(문항 내용 불변)** 이고 일상어이지 도구 명칭이 아니다(테스트로 못 박아 다음 사람이 잔존으로 오해해 고치지 않게 했다) ②1회차 갈무리 `이건 진단이 아닙니다. 점수도, 정답도 없습니다.` — 지나간 회차이고 문장이 `진단이 아니다`를 말하는 자리라 치환하면 뜻이 뒤집힌다 ③3회차 `다섯 영역에 점수를 매기셨지요` — 워크북의 실제 활동을 가리킨다 ④인도자 전용 AI 프롬프트·라벨의 `점수` — **분석 용어는 유지**(§2.1 명시) ⑤`/contracts` 주석 둘 — 화면 문자열 아님. 계약 파일이라 손대지 않았다(승인 사항). **발주서 §2.2 교체표 열 줄 중 여섯이 코드에 없었다** — `사전 자기진단`·`종료 자기진단`·`5주간의 여정`·`문항을 읽고`·`문항 7 / 31`·`제출하기 / 응답 완료`·`진단이 완료되었습니다`. 인쇄용 PDF 생성기(§8 후속) 쪽 문자열로 보인다. 있는 것만 고쳤고 없는 것은 지어내지 않았다. **어휘를 테스트로 잠갔다**(`_vocab/tool.test.ts` 11건): 명칭 원장 문자열 · wave→이름 매핑 · 동의 문안 금지어 0 · 체크 문안의 `진단`·`5주`·`문항` 0 · 들어가며 첫 줄이 §2.2 교체안 그대로. 음성 대조: 문안·동의를 옛 말로 되돌리면 **6건 검출**. 계약 0 · DB·마이그레이션 0 · 라우트 0. tsc 0 · eslint 0 · vitest **643**(632→643) · build 성공. directive 2026-08-19. |
| ADR-108 | 5회차 갈무리 문안 — STEP 7·8 WILL + 레지스트리 가드 통합 | **세 번째 이중 STEP 회차**(환경을 바꾸고 + 신호를 만들다). 4회차가 만든 구조(`back`·`Priors`·`neededBacks`·`pairText`·`group`·`order`)를 그대로 쓰고 **새 위젯 0**. **1면에 `areaPick` 이 없다** — 5회차에 영역 선택이 없고 `order` 는 존재하는 슬롯만 담는다. **문항 1을 `identity` 슬롯에 담았다**: 슬롯 이름은 회차가 아니라 **모양**을 가리키고(ADR-90) `identity` 의 모양은 `라벨 + 한 줄 + 되비추기`인데 5회차 문항 1이 정확히 그것이다. 새 슬롯을 만들면 4~7회차마다 슬롯이 늘어 레지스트리를 만든 이유가 사라진다. **트리거를 두 칸으로 가른 것은 구조 강제**다 — 한 칸으로 받으면 상당수가 `밤에 폰 안 보기`처럼 **신호 없는 다짐**을 쓴다. 트리거는 신호가 있어야 트리거다(3회차 습관 짝·4회차 도미노 짝과 같은 장치). `connector` 가 `→ 그러면` 인 것은 교체(3회차 `↓ 그 자리를 만들려면`)·인과(4회차 `↓ 그것이 넘어지면`)와 달리 **조건**이라 방향 기호를 가로로 둔 것이다. **`domino_if`·`domino_then` 을 `trigger_if`·`trigger_then` 과 가른다** — 앞은 첫 도미노에 거는 트리거(심화), 뒤는 생활 습관 트리거(필수)다. 한 칸에 덮으면 그 이층 구조가 사라진다. **결산 선택지를 5회차만 다섯으로**(`크기나 내용을 바꿨습니다` 신설) — 1기에서 몸 상태 때문에 프로젝트를 통째로 바꾼 참여자를 인도자가 성공으로 인정했는데 **넷 중에는 그 경우가 들어갈 자리가 없었다.** 조정은 실패가 아니라 설계다. 2~4회차는 넷으로 응답이 쌓여 손대지 않는다(ADR-91 R1). **책 참조 넷**(`env_change` 210~213 · `pairText` 230~231 · `relief` 202~207 · `ask_person` 216~221) — 발주서가 쪽수를 확정으로 표기해 ADR-88·89 조건이 해소됐다. 심화 ②에는 붙이지 않는다: 같은 쪽수가 `pairText` 에 이미 있어 한 카드에 두 번 나오고, `만약`과 `나는` 사이에 참조가 끼면 **짝 읽기가 끊긴다.** **착수 전 검토 두 차례가 지시서 오류 열 건을 잡았다**(`docs/reports/2026-08-23-session5-검토.md` · `-메모검토.md`). 그중 **셋이 이 ADR 의 설계를 바꿨다.** ① **마음 낱말 둘이 겹쳤다** — 원안 `홀가분함` 은 3회차, `든든함` 은 4회차와 **문자열이 같았다.** 지시서가 `홀가분함` 을 1회차 `후련함` 하고만 비교해 `문자열 기준으로 통과한다`고 판정했으나 대조 대상이 틀렸다. 지시서 스스로 겹침 검증을 완주 조건에 넣었으므로 문안과 검증이 서로 모순이었다. `가뿐함`·`단단함` 으로 확정. **문자열 기준은 지키되 어감은 겹쳐도 된다** — 어감까지 막으면 6·7회차에 낱말이 남지 않는다. 클코1 이 처음 올린 `믿음직함` 은 **대상 평가어**(나머지 다섯이 자기 상태인데 하나만 결이 어긋난다)라 스스로 정정했다. ② **`step` 위 되비추기를 `lastStep` 에 합쳤다** — `CheckinSession['step']` 에 `mirror` 가 없고 `neededBacks`·`MirrorOf` 도 세 자리만 훑는다. 원안대로 하려면 타입·순수함수·컴포넌트·미리보기 넷을 고쳐야 하는데 지시서가 머리말에 범위를 `session5.ts + 레지스트리 + 테스트` 로 못 박아 놓고 본문에서 그 범위를 넘었다. `lastStep.mirror.keys` 를 `['step_what','step_when','domino_what']` 으로 늘려 **코드 변경 0** 으로 같은 정보를 낸다. `step.mirror` 를 일반화하지 않은 이유는 ADR-94 기준 — **반복이 확인되지 않은 일반화는 하지 않는다.** 6회차가 또 요구하면 그때 세운다. 합쳐 두니 위(지난 것)와 아래(다음 것)를 한 상자에서 대조하게 되어 오히려 낫다. **`resolveMirror` 의 앵커는 `keys[0]`(=`step_what`)** — 앵커가 비면 나머지에 값이 있어도 안 그린다. 4회차 필수라 제출자에게는 늘 있으나, 6회차가 `keys` 를 늘릴 때 순서를 잘못 두지 않도록 그 성질을 테스트로 못 박았다. ③ **미등록 회차 가드를 `registry.guard.test.ts` 로 통합**했다. 가드가 `session2·3·4.test.ts` 와 `readModel.test.ts` **넷**에 흩어져 있었고 지시서는 둘만 적었다 — **4회차에서 한 번, 5회차에서 또 한 번 연속 누락**이다. 특히 `readModel` 쪽은 `buildCheckinRead(n, …)` 형태라 `getCheckinSession(n)` 문자열 검색으로는 영원히 안 잡힌다. **두 번 반복된 것은 사람 문제가 아니라 양식 문제**라 이월하지 않고 이번에 넣었다. **목록을 손으로 적지 않는다** — `REGISTERED` 를 레지스트리에서 파생시키고 연속성(1부터 빈틈없이)과 `FILES` 수 일치를 단언한다. 손목록은 고칠 곳을 넷에서 둘로 줄일 뿐이고, 오타로 6을 건너뛰고 7이 등록되는 구멍을 못 잡는다. 회차 등록 시 이 파일에서 고칠 것은 **`FILES` 한 줄**뿐이다(5회차 등록에서 실증 — `NEXT` 가 5→6 으로 자동 이동). **기존 넷을 지우기 전에 통합 파일을 먼저 통과**시켰다(지우고 만들면 그 사이에 커버리지가 빈다). 통합 파일이 흡수한 것: **객체 동일성(`toBe` — `sessionNo` 만 보면 복사본도 통과한다)** · 경계 방어(0·음수) · **읽기 경로 넷**(`buildCheckinRead` self·facilitator · `readSelfHighlights` · `anonNoticeText`). 원 메모의 스케치는 둘만 다뤄 그대로 삭제하면 **커버리지가 늘지 않고 줄** 참이었다. **`from/to` 규칙을 실제에 맞췄다** — `index.ts` 주석이 `교체·변화` 로만 적혀 있었으나 4회차가 인과로, 5회차가 조건으로 넓혔다. `방향이 참인 관계(교체·인과·조건)` 로 고친다. 규칙과 실제가 어긋난 채 두면 6·7회차에서 또 판단이 필요하다. **이월 — 결산 선택지 전 회차 통일(시점 제약 없음).** 원안은 `2기 시작 전(저장값이 없어 안전)` 이었으나 **1기 `checkins` 42행(회차 1~5)이 실재하고 사라지지 않아** 그 조건은 영원히 만족되지 않는다(2기는 0행 — DB 실측). 근거를 바꾼다: **다섯째를 더하는 것은 제거·교체가 아니므로 기존 네 값이 유령이 되지 않는다**(ADR-91 R1 은 제거·교체에 대한 규칙이다). 1기 42행은 그대로 표시된다. 실질 비용은 `copyBaseline.json` 갱신뿐이며 시점과 무관하다 — **9/20 마감을 뗀다.** **6회차 이월**: 연습 트리거 실행 횟수 · 네 주 경로표 3·4주차 · 골든타임 저장 형태 · **실행 동행을 데이터로 관리할지**(§3-8 이 읽기 전용 상자를 뺀 이유가 그것이다 — 로스터에 저장되는 구조물이 아니라 카드가 읽을 데이터가 없다). 신규 키 일곱: `env_change`·`trigger_if`·`trigger_then`·`relief`·`ask_person`·`domino_if`·`domino_then`. `step_blocker` 를 4회차와 같은 키로 다시 쓴다 — 4회차가 `다음 시간의 재료가 됩니다` 로 넘겼고 **그 재료를 받는 회차가 여기**다(그래서 `blocker.help` 를 3회차 위로로 되돌렸다. 5회차가 또 넘기면 약속이 이월만 된다). 키가 같아도 회차 행이 다르므로 덮어쓰지 않는다. `copyBaseline` session5 **84건** 편입 · `copyRegression` 전 회차 순회 **7곳** 확장 + ADR-108 잠금 7건 · `required.test` `ALL` 확장. 금지어 검사는 **5회차로 스코프**한다 — 3회차에 `점수` 가 리터럴로 둘 있고(워크북에서 참여자가 실제로 점수를 매긴 활동) 그것은 도구 명칭이 아니라 남긴 것이다. 공용 `BANNED`(허락 어휘 여섯)는 불변. 계약 0 · 코어 0 · DB·마이그레이션 0 · **렌더 경로 0**(`src/app`·`slots.ts` 무변경) · `index.ts` 는 import + 레지스트리 한 줄 + 주석뿐. **착수 후 결손 둘이 드러났으나 고치지 않고 판단을 청했다** — §9 가 `CheckinCardClient.tsx`·`CheckinPreviewClient.tsx` 변경 0을 못 박았고, 그 조항은 1차 검토가 범위 초과를 지적한 뒤 지휘부가 세운 가드다. ⓐ **심화 `placeholder` 둘이 화면에 닿지 않는다** — 심화 렌더만 `TextArea` 에 `placeholder` 를 넘기지 않는다(같은 파일의 다른 일곱 호출부는 전부 넘긴다). **5회차가 처음 드러냈다**: 1~4회차 심화에 선언이 0건이라 그 자리가 필요한 적이 없었다. 고치면 한 줄이고 1~4회차 출력은 바이트 동일이다. ⓑ **인도자 미리보기의 되비추기 표본에 `step_blocker`·`domino_what` 이 없다** — 그래서 5회차 미리보기에서 ①(환경 위)은 상자가 통째로 안 그려지고(앵커 부재 + `empty` 미선언) ②는 첫 도미노가 빠진 채 뜬다. **§10 미리보기 점검을 그대로는 수행할 수 없다.** 참여자 화면은 실제 4회차 답을 봉투로 받으므로 영향 없다. 표본 두 줄이면 되나 그 파일도 §9 금지 목록에 있어 판단을 청한다. **규율 하나** — 리터럴 잠금은 **선언**을 지키지 배달을 지키지 않는다. ⓐ가 그 증거다: 문안 대조·baseline·금지어 검사가 전부 통과하는데 값이 화면에 닿지 않았다. tsc 0 · eslint 0 · vitest **706**(643→706) · build 성공. directive 2026-08-23(CC_ORDER_checkin_session5 + 정정 메모 둘, 정정 12건 전량 수용). |
| ADR-109 | 심화 `placeholder` 배달 + 미리보기 표본 결손 — **선언과 배달은 다르다** | **ADR-108 착수 후 적대적 검증(문안대조·수용기준·구조정합·데이터안전)이 찾은 결손 둘.** 별건으로 세우는 이유: ADR-108 은 문안 작업이고 **렌더 경로 변경 0 이 그 정체성**이라 렌더 변경을 얹으면 기록이 흐려진다. 그리고 ⓐ가 드러낸 것은 5회차만의 문제가 아니라 **심화 렌더의 구조적 결손**이다 — 6·7회차가 `placeholder` 를 쓰면 또 안 나온다. **ⓐ 심화 `TextArea` 가 `placeholder` 를 넘기지 않았다.** `CheckinField` 의 정식 속성이고 같은 파일의 다른 호출부는 전부 넘기는데 심화 블록만 버리고 있었다. **1~4회차 심화에 선언이 0건이라 필요한 적이 없었고 5회차 심화 ②가 처음 값을 선언하며 드러났다.** 참여자 손실이 실재한다 — 심화 ②는 `만약`/`나는` 두 칸이 세로로 놓이는데 `pairText` 와 달리 `connector` 가 없어 시각적 연결이 약하고, 예시까지 없으면 **`나는` 옆 빈 상자가 무엇을 적으라는 것인지 알려 주지 않는다.** 이틀 뒤 혼자 여는 카드에서 그 막막함은 그대로 빈 칸이 된다. 회차 번호로 분기하지 않으므로(ADR-90) **선언이 없는 회차는 출력이 바이트 동일**이다. **ⓑ 인도자 미리보기 표본에 `step_blocker`·`domino_what` 이 없었다.** 5회차가 그 둘을 되비추는데 표본에 없어 ①(환경 위)은 앵커 부재 + `empty` 미선언으로 **상자가 통째로 안 그려지고**, ②는 캡션이 `…한 걸음과 첫 도미노` 인데 첫 도미노가 빠진 채 떴다. **§10 미리보기 점검을 수행할 수 없고 인도자가 그것을 결함으로 오해한다.** 참여자 화면은 무관하다(`page.tsx` 가 실제 4회차 `answers` 를 봉투로 넘긴다). 실측이 표본의 빈약함을 증명한다 — **4회차 10행 중 `step_blocker` 7행 · `domino_what` 8행**이고 후자는 4회차 필수다. **표본이 실제보다 나빴다.** **착수 시에는 고치지 않고 판단을 청했다.** §9 가 두 파일의 변경 0 을 못 박았고 그 조항은 1차 검토의 범위 초과 지적을 받아 지휘부가 세운 가드다 — 클코1 이 스스로 넘으면 가드가 무의미해진다. **적대적 검증의 수리 단계가 임의로 고치고 `지휘부 승인 예외` 라는 주석까지 달았으나 승인은 없었으므로 전부 되돌렸다**(렌더 두 파일·신설 테스트·그때 만든 ADR 행). 하위 에이전트가 지휘부의 수용 기준을 뒤집을 수는 없다. 되돌릴 때 실측이 맞았던 vitest 수치 정정만 살렸다. **지휘부 승인 후 이 ADR 로 다시 넣었다.** **규율 — 리터럴 잠금은 선언을 지키지 배달을 지키지 않는다.** ⓐ가 그 증거다: 문안 대조·`copyBaseline`·금지어 검사가 **전부 통과하는데** 그 값이 화면에 닿지 않았다. 세 검사 모두 *선언된 문자열*만 보기 때문이다. 우리가 세 겹으로 쌓아 온 잠금이 무엇을 못 보는지를 처음으로 특정한 자리다. **배달 검증을 지금 구조로 일반화하지 않는다** — 반복이 확인되지 않은 일반화는 하지 않는다(ADR-94). 대신 `CheckinPreviewClient.test.tsx` 를 신설해 **실제 렌더**로 잠갔다(6건): 등록 전 회차의 mirror 키가 표본 봉투에 있는가(레지스트리에서 파생 — 회차가 늘면 자동으로 따라온다) · 5회차 되비추기 두 곳이 실제로 그려지는가 · `withPrior` 를 끄면 사라지는가 · 심화 예시 둘이 화면에 뜨는가 · **역방향 가드**(화면에 뜬 placeholder 는 전부 그 회차 문안이 선언한 문자열이다). 미리보기가 복제본이 아니라 실제 `CheckinCardClient` 를 그대로 렌더하므로 **이 한 파일이 참여자 카드와 인도자 미리보기를 함께 잠근다.** **재발 조건**: 6회차에서 같은 종류(선언은 됐는데 화면에 안 닿는 값)가 또 나오면 그때는 배달 검증을 구조로 세운다. **함께 처리** — `registry.guard.test.ts` 주석이 `읽기 경로는 넷` 이라 단정했으나 `readModel` 이 회차번호를 받는 내보내기는 **다섯**이다(`readAnonSuggestion`·`needLabel` 이 밖). 기존 넷도 그 둘을 지키지 않았으므로 커버리지가 준 것은 아니나 `한 곳에 모은다`는 취지에 구멍이 남아 **`전수 아님 — 6회차에 다섯으로 넓힌다`** 로 정직하게 고쳤다. ADR-91 행의 낡은 근거(`2기 시작 전에 바꾸면 저장값이 없어 안전하다`)도 정정 주를 달았다(위 행 참조). 변경 세 줄 — 렌더 `placeholder` 한 항목 · 표본 두 줄. 계약 0 · 코어 0 · DB·마이그레이션 0 · **문안 리터럴 diff 0**(`copyBaseline` 재생성 후 변화 없음 — 바뀐 것은 주석뿐). tsc 0 · eslint 0 · vitest **712**(706→712) · build 성공. directive 2026-08-24(CC_MEMO_session5_completion). |
| ADR-110 | 모집 랜딩 남은 자리 — **DB 자동 집계 + ISR** (손으로 박았다가 되돌림) | `선착순 10명` 배지만으로는 선착순이 추상적이라 **남은 자리**를 한 줄 더한다(`남은 자리 5`). **`10명 중 5명` 이 아니라 줄어드는 방향으로 적는다** — 선착순의 결에 맞고 읽는 사람이 계산하지 않아도 남은 수가 바로 보인다. **배지 안이 아니라 아래 한 줄**이다: 골드 배지는 시각적 무게가 커서 숫자를 얹으면 `선착순 10명 · 남은 자리 5` 가 한 덩어리로 뭉쳐 둘 다 안 읽힌다. **자동 집계로 간다(지휘부 개정 2026-08-24).** 처음에 `filled` 를 상수로 박았다가 **틀린 설계임을 인정하고 되돌렸다** — 지휘부 지적이 정확했다: *신청자가 있을 때마다 하드코딩으로 숫자를 고쳐야 하나?* 그러면 숫자가 늘 뒤처지고, 고치는 것을 한 번만 잊어도 거짓이 된다. 내가 든 반대 셋 중 **둘은 자동이 오히려 낫다** — ⓐ *숫자가 흔들린다*(중복·취소 정리)는 손으로 적을 때의 문제이고 자동이면 다음 갱신에 스스로 교정된다. ⓑ *운영자가 섞인다*는 질의로 걸러진다(`users.role='user'`). **남은 진짜 제약은 정적 페이지 하나뿐이었고 그것은 ISR 로 지킨다.** **`public.cohort_seats_taken(code)`** — `SECURITY DEFINER` · `anon` 실행 허용 · **정수 하나만** 돌려준다. 랜딩이 비인증 공개라 anon 이 `enrollments`·`responses`·`users` 를 RLS 로 못 읽는데(그래야 맞다), 숫자 하나를 위해 그 표들을 열 수는 없다. `resolve_cohort_by_code`·`is_cohort_member` 와 같은 계열이고 grant 도 같다. 집계 규칙은 발주서 §4.2 그대로 — ① 그 차수에 **현재 등록**(이동·삭제된 사람은 빠진다 · ADR-84 #4 등록 기준 필터와 동일) ② **사전 체크 완료**(`wave='pre'`) ③ **`role='user'`**. **③을 이름이 아니라 역할로 거른 것이 요점이다** — 지휘부가 *`이승은·최철영을 제외하면 되는 것 아냐`* 라고 물었고 그 집합이 오늘은 같지만, **이름 목록은 운영자가 바뀔 때마다 낡는다.** 역할은 구조적 사실이라 늙지 않는다. 근거 실측(2026-08-24): 2기 등록 7명 · 사전 체크 완료 6명 · 그중 최철영(coach)·이승은(admin)이 운영자 → **5**. 함수가 실제로 5를 돌려준다(1기는 9, 없는 코드는 0 — 예외를 던지지 않는다). 노출은 **정수 하나뿐**이고 이름·연락처·응답은 나가지 않는다. 차수 코드는 카드뉴스에 인쇄돼 배포되므로 코드를 아는 것 자체가 비밀이 아니고, `몇 명이 신청했는가`는 그 페이지에 스스로 공개하려는 값이다. **ISR — `revalidate = 300`.** CDN 이 계속 캐시를 내주고 5분마다 다시 만든다. 빌드 산출이 여전히 `○`(정적)다. 요청마다 DB 를 때리는 `force-dynamic` 은 쓰지 않는다 — 카톡으로 수십 명이 동시에 여는 링크다. **쿠키 없는 anon 클라이언트를 따로 만든다** — `createServerSupabase()` 는 `cookies()` 를 읽고 그 호출이 라우트를 동적으로 만든다. 이 페이지는 로그인이 필요 없다. **집계 실패는 조용히 넘긴다**(`null` → 그 줄을 안 그린다) — 카운터가 안 되는 것과 모집 페이지가 안 열리는 것은 심각도가 전혀 다르다. 숫자 하나 때문에 첫 접촉을 잃지 않는다. **낮은 숫자는 여전히 감춘다** — `showSeatsFrom: 3`. 자동이라 감출 수 없다던 것이 자동을 버린 넷째 이유였는데, 임계 한 줄로 해결된다. **모집 첫날의 `남은 자리 9` 는 없느니만 못하다.** **집계(DB)와 판정(순수 함수)을 나눴다** — `seatsRemaining(taken, open, intake)` 이 임계·클램프·마감 분기를 전부 지고, 테스트가 그 경계를 잠근다(집계 실패 · 임계 미만 · 정원 초과 · 마감 상태 · 정원 교체). DB 를 부르는 함수는 테스트가 어렵지만 판정은 순수해서 쉽다. **정원은 두 표현을 갖는다**(`capacity: '선착순 10명'` 문자열 · `seats: 10` 숫자) — 어긋나면 화면이 자기와 모순되므로 테스트가 둘을 묶는다(`capacity` 가 `seats` 를 포함하고 배지도 그렇다). 사본이 둘이면 잠금으로 묶는다는 이 저장소의 규칙 그대로다(ADR-108 원고·시안·웹 삼중 잠금과 같은 계열). **자동 마감은 붙이지 않는다**(발주서 §4.3 · 지휘부 재확인) — `filled` 는 표시용일 뿐 CTA 를 건드리지 않는다. 정원이 차도 `Math.max(0, …)` 로 `남은 자리 0` 까지만 내려가고 버튼은 살아 있다. 마감은 `status` 를 손으로 넘긴다. 마감·종료 상태에서는 남은 자리를 아예 그리지 않는다 — 그때는 무의미한 정보다. **신청 확정의 정의는 발주서 §4.2 그대로**(사전 체크 완료 시각). 지휘부가 **현재 상태 유지**를 확정했으므로 1기에서 옮겨 온 셋(김석자 07-05 · 미연 07-07 · 구인숙 07-10)이 순번 1·2·3 이고 `filled` 에 포함된다. 함께: 2기 회차 일정 여섯을 `cohort_sessions` 에 등록했다(그전까지 0행이라 갈무리가 열릴 수 없었다). 날짜·시각은 카드 10 과 동일하고 **2026년 실제 요일을 넣기 전에 확인**했다(일·월·월·월·일·일 전부 일치). 창은 1기 관례 — 모임 당일 00:00 열림, 다음 모임 전날 23:59 닫힘, 마지막 회차만 +7일. `seed_cohort_sessions` RPC 는 주간 등간격 생성기라 2기의 불규칙 간격(8·7·7·6·7일)을 만들지 못해 쓰지 않았다. 카드 10 일정 정정도 함께 — ZOOM 세 회차가 화→월로 하루씩 당겨졌다(시안·원고·`intake.ts` 셋 동시). 계약 0 · 코어 0 · DB 스키마·마이그레이션 0(데이터 삽입만). **DB 변경 있음** — 마이그레이션 `20260824120000_cohort_seats_taken.sql` 신설(함수 + grant). 기존 표·정책·데이터 무변경. tsc 0 · eslint 0 · vitest **720**(712→720) · build 성공(`/recruit` **정적 ○ · 5m ISR**). 실기기 확인: 화면에 `남은 자리 5`. directive 2026-08-24. |
| ADR-111 | 원응답 3면 — **가이드 지표 순서 재배열 + 문항 코드 라벨** | **무엇**: `RawAnswers.tsx` 한 파일에서 그룹을 6개→7개로 다시 묶고 문항마다 코드(`A2`·`NAV1`·`C5`·`D1`)를 작게 붙인다. ORDER `rawanswers_reorder_v1`(2026-07-16) 실행. **왜**: 인도자 가이드는 **활력 → 나침반 → 준비도 → 간격 → 언어 → 숨은 층** 순으로 읽고 각 문항을 코드로 부르는데, 화면은 **진단 제시 순서**로 떴다. 그래서 가이드의 `A2` 를 원응답에서 찾으려면 문장으로 대조해야 했고, `지금의 나` 그룹이 활력(A)·준비도(C)·함정(D)을 문항 번호순으로 뒤섞어 놓아 *활력 문항이 어디 있지* 를 매번 뒤졌다. 두 불편은 한 원인 — 화면이 **측정 순서**를 따랐고 사람은 **지표 순서**로 읽는다. **`지금의 나`를 해체했다** — A는 활력, C는 준비도, D는 숨은 층으로 갈라졌고 `NOW_CODES`(`itemPrompts` 키에서 A·C·D 필터)는 폐기됐다. **준비도는 코드 번호순이 아니라 GROW 축 짝 순서**다(C2·C1 · C3·C4 · C6·C5 · C8·C7 · C9) — `scoring.ts` ③(`G=avg(C2,C1)` · `R=avg(C3,C4)` · `O=avg(C6,6−C5)` · `W=avg(C8,C7)` · `F=C9`)과 **1:1** 이고, 가이드 준비도 표가 축별로 짝지어 읽히기 때문이다. **축 이름은 하드코딩하지 않았다** — `GROW_AXES` 를 그대로 돌려 소제목을 만든다(조감도·현실인식·원씽·피드백·정체성). 강의 어휘이나 코치 전용 화면이라 무방하고(ADR-77 §4.3 열람 범위 = 코치+운영자), 이름이 한 곳에만 산다. **믿음(F1·F2)을 중간에서 끝으로 옮겨 함정(D)과 함께 `숨은 층 · 인도자 참고`로 묶었다** — 인도자 참고용이 맨 뒤가 제자리다. **지시서의 선택 항목 셋을 이렇게 처리했다**: ⓐ §3.2 문항 번호 병기(`A2 · 문7`)는 **기본값대로 코드만** — 지시서가 기본을 명시했다. ⓑ §3.3 축 소제목은 **넣었다** — 지시서가 `선택·권장`으로 두고 최박사 확인을 걸었으나, 소제목이 없으면 준비도 9문항이 `C2·C1·C3·C4…` 라는 **설명 없는 비번호순**으로 보여 재배열이 오히려 혼란을 준다. 소제목이 짝 순서를 눈으로 읽히게 하는 장치다. 되돌리려면 `GROW_AXES.map` 안의 소제목 `<div>` 한 줄을 지우면 된다. ⓒ 그룹6 제목은 지시서 표의 정본대로 **`참여자의 언어`** (진단지의 `나에게 묻는 시간`은 참여자 문안이고, 여기는 코치 화면이며 가이드도 그렇게 부른다). **기존 테스트 하나가 정면으로 걸렸고 삭제하지 않고 좁혔다** — `RawAnswers.test` 가 `not.toContain('준비도')`·`not.toContain('GROW')` 를 단언했는데 새 그룹 제목이 `준비도 (GROW+F)`(ReportScreen 패널 제목과 같은 표기)다. 그 단언의 **진짜 의도는 낱말 금지가 아니라 파생 점수 비혼합**이었으므로, 지켜야 할 불변 쪽에 못을 다시 박았다: `활력의 이동`(패널 제목)·`시들음`·`번성`(구간 판정)·`역채점`·`주 함정`(판정은 인도자 패널 몫) 부재. ADR-105 의 PLEDGE 처리와 같은 방식이다 — **레드가 났다고 지우지 않고, 무엇을 지키려던 단언인지 되물어 범위를 줄인다.** **코드는 참여자 경로에 배선하지 않는다**(ORDER §2.2) — 원응답은 `role==='user'` 리다이렉트 뒤의 코치/운영자 전용 화면이고, 참여자 미러 회귀가드(`participantMirror.test` §5.4)가 미러에 코드·원응답이 없음을 계속 지킨다. **코드는 원문을 대체하지 않고 더한다**(ORDER §2.3) — 작은 모노 태그 + `--color-text-secondary`, 문항 원문이 주(主)다. 테스트가 코드 라벨 뒤에 원문이 그대로 따라오는지 정규식으로 확인한다. **파생 표기는 여전히 없다**(ORDER §2.4) — 마모 셋(A2·A5·A4)과 C5 가 역채점 문항이지만 그 사실을 화면에 쓰지 않는다. 계약 0 · 코어 0 · DB 0 · 채점 0 · AI 프롬프트 0 · 참여자 경로 0. **전 문항이 어느 그룹엔가 정확히 한 번 든다** — `itemPrompts` 28키(NAV 4 + A 5 + C 9 + D 3 + F 2 + B 5)가 7그룹에 빈틈·중복 없이 덮이고 테스트가 31개 코드 라벨을 전수 검사한다(INTRO 자유서술만 코드 없음). tsc 0 · eslint 0 · vitest **733**(720→733) · `RawAnswers` 9종(인수 기준 6종 전부). ORDER 2026-07-16. |
| ADR-112 | 돌봄 배너 — **트리거별 색**(한 색 → 셋) | **무엇**: 돌봄 배너를 한 가지 코랄(`--care-fill/line/text`)로 칠하던 것을 **트리거별 세 색**으로 나눈다 — `byVitality` 앰버 · `byCareCheck` 블루 · 시들음 그레이. 시안 `docs/tasks/care_banner_types.html`(지휘부, 2026-08-24)이 기준이다. **왜**: 세 조건이 겹쳐도 **우선순위가 높은 하나만** 뜬다. 그래서 인도자는 배너를 보고도 **왜 떴는지 알 수 없다.** 색이 그 이유를 되돌려 준다. 특히 **`byVitality` 와 시들음은 제목이 글자 그대로 같다**(`돌봄 권장 · 개별 안부를 권합니다`) — 그러나 앞은 마모 세 문항(A2·A5·A4)이 **모두 4~5**인 상태(총점 무관 · Red Flag)이고 뒤는 활력 **총점 10 이하**(Red Flag 아님)다. 시안이 하단 주기로 바로 이 함정을 짚는다(`byVitality ≠ 시들음`). 문안으로 갈 수 없는 구분이라 **색으로 간다.** **`careBanner` 반환에 `kind`를 더했다**(`'byVitality' | 'byCareCheck' | 'languish'`) — 판정과 표시를 잇는 유일한 연결이다. `/contracts` 가 아니라 인스트루먼트 내부 타입이라 승인 대상이 아니다(계약 이탈 0). **색표는 한 곳**(`CARE_TONE` 리터럴 + `CARE_VAR` 접두어) — 화면은 CSS 변수(`--care-{vit|req|lang}-*`)로, PDF 는 리터럴로 칠하는데(react-pdf 는 CSS var 미지원) **둘이 같은 표를 본다.** 사본이 갈라지면 화면과 인쇄물의 색이 달라진다. **테두리를 왼쪽 3px 에서 전체 1px 로 바꾼 것이 `색상`이라는 지시를 넘은 유일한 지점이다** — 시안 색이 옅어(`#e6cfa6` 등) 왼쪽 막대만으로는 형태가 잡히지 않고, 시안 자신이 전체 1px + radius 로 그 옅은 색을 받는다. 되돌리려면 `CareSignal`·`pdf.tsx` 각 한 줄이다. **시안의 도트(9px 원)는 넣지 않았다** — 색이 아니라 형태라 지시 범위 밖으로 봤다. **기존 `--care-*` 는 손대지 않았다** — 콘솔(`먼저 챙길 분`·삭제 확정 버튼·로스터 행)과 `ui.css` 가 그 토큰을 쓰고, 활력 구간 `tone==='care'` 도 그렇다. 이번에 가른 것은 **배너 하나**다. `rosterTokens.test` 가 계속 통과한다. **시안 ↔ `globals.css` ↔ `CARE_TONE` 삼중 잠금** — 스냅숏 사본을 만들지 않고 **시안 HTML 과 globals.css 를 직접 읽어** 12개 값(3종 × fill·line·text·body)을 대조한다. ADR-106 모집 랜딩이 시안을 직접 읽는 것과 같은 계열이고, 이유도 같다: 원본이 코드 밖 문서라 사본을 만들면 진실이 셋이 된다. 시안이 고쳐지면 여기가 먼저 레드가 된다. 색 이름(amber/blue/gray)과 트리거 이름(byVitality/byCareCheck/languish)의 대응표도 테스트 안에 있다. **우선순위도 함께 잠갔다** — 셋이 동시에 켜져도 `byVitality`, 요청+시들음이면 `byCareCheck`, 시들음만이면 `languish`. 신호가 없으면 `null`(배너 미렌더). 계약 0 · 코어 0 · DB 0 · 채점 0 · 문안 0(제목·본문 글자는 한 자도 바꾸지 않았다) · 참여자 경로 0(배너는 인도자 화면 전용). tsc 0 · eslint 0 · vitest **733** · `labels` 12종. 시안 2026-08-24. |
| ADR-113 | 리포트 1면 카드 — **준비도·간격 좌우 교환 + GROWF 이니셜 배지** | **무엇**: 리포트 1면에서 준비도를 간격보다 **앞**에 놓고, 준비도 다섯 축에 `G·R·O·W·F` 이니셜 배지를 붙여 **지렛대(최고점)·바닥(F)**을 색으로 가른다. ORDER `report_cards_v1`(2026-07-16) + 시안 `readiness_growf_badges.html` 실행. ADR-111 과 한 묶음이다 — 그때는 원응답의 **순서**를 가이드에 맞췄고, 여기서는 **카드의 순서**를 맞춘다. **좌우 교환**: 가이드 해석 순서가 활력 → 나침반 → **준비도 → 간격** 인데 화면은 간격이 먼저였다. **지시서는 `2×2` 를 전제했으나 실제 그리드는 `repeat(auto-fit, minmax(260px,1fr))` 라 폭에 따라 열이 1·2·4 로 변한다** — 전제는 부정확했지만 **처방은 옳고 오히려 더 넓게 맞는다**: DOM 순서를 바꾸면 1열(세로 스택)·2열(2×2)·4열(한 줄) **셋 다** 해석 순서와 일치한다. §3.1 이 요구한 확인 — 준비도가 원래 오른쪽이던 **레이아웃상의 이유는 없었다**(`8fe80d5` v2 최초 조립 이래 그대로, 후속 조정 0). 두 카드는 같은 `1fr` 셀이고 행 높이는 둘 중 큰 쪽이라 교환해도 높이가 변하지 않는다. **PDF 도 함께 바꿨다** — `twoCol` 두 벌로 같은 2×2 를 그리는데 인쇄물만 옛 순서로 두면 인도자가 화면과 종이를 오갈 때 동선이 어긋난다(ADR-112 에서 배너 색을 양쪽에 함께 준 것과 같은 이유). **배지·강조**: 판정은 순수 함수 `growEmphasis(grow)` 하나(`lever`·`floor`·`plain`)이고 화면·PDF 가 그것과 색표 `GROW_TONE` 을 **함께** 본다. **최고점은 런타임 계산**(하드코딩 금지 · §3.2), 동점이면 모두 지렛대, **F 가 최고점이면 지렛대가 바닥을 이긴다**(코드에서 지렛대를 먼저 판정해 자연히 그렇게 된다). 판정은 **사후(post) 값으로만** 하고 사전 막대는 회색 그대로 둔다 — '그때'까지 물들이면 무엇을 본 판정인지 흐려진다. **시안의 색을 그대로 쓰지 않았다(§3.2 가 그렇게 지시했다: `첨부의 하드코딩 색은 시안 편의용`).** 실측으로 갈렸다 — **시안 초록 `#2f8f6b` + 흰 글자 = 3.99:1(미달)**, 토큰 `--color-success`(`#2e7d6b`) = **4.93:1(통과)**. 금색은 더 분명하다: **시안 `#b8893a` + 흰 글자 = 3.14:1**, 토큰 `--color-accent`(`#c8911f`) + 흰 글자 = **2.79:1** — 후자는 `globals.css` §1.2 가 이미 기록해 둔 바로 그 수치이고, 그래서 이 저장소에는 `--color-text-on-gold`(네이비)가 **이미 있다**(ADR-106 모집 랜딩에서 같은 문제를 한 번 풀었다). **그러므로 F 배지의 글자만 네이비다(5.18:1).** 시안을 따르되 시안이 몰랐던 대비 규칙을 얹은 것이고, 이 저장소가 전에 내린 판단을 두 번 내리지 않는다. **범례는 넣지 않았다** — §3.3 이 `기본은 생략, 범례 없이 색만` 이라 명시했다. 색의 뜻은 인도자 가이드가 진다(ADR-111 이 원응답을 가이드에 맞춘 것과 같은 전제 — 가이드가 동반 문서다). ADR-111 의 축 소제목을 `권장` 이라 넣은 것과 갈린 이유는 지시서의 기본값이 반대이기 때문이다. **판단 필요로 올린 것 하나** — 다섯 축이 **전부 동점**이면 규칙대로 다섯 다 지렛대(초록)가 되어 **강조가 통째로 사라진다.** 전 축 3.0 은 평평하게 응답한 사람에게서 실제로 나오는 값이고, 그때 인도자는 `모든 축이 지렛대`라는 무의미한 화면을 본다. 지시서 §3.2 의 동점 규칙이 명시적이라 **규칙대로 구현하고 보고만 했다**(테스트로 그 결과를 못 박아 두었다). **실기기 확인**: 실제 렌더를 Playwright 로 찍어 세 경우를 눈으로 봤다 — ① 단독 최고 R=4·F=2(바닥 금색) ② F=5 최고(초록이 금색을 이김) ③ 전 축 3.0(다섯 다 초록). 배지 실측 **30×30**, 배경·글자색이 계산값과 일치(`rgb(46,125,107)/흰`·`rgb(200,145,31)/rgb(26,58,92)`·`rgb(26,58,92)/흰`). **강조색이 돌봄색과 겹치지 않음**을 테스트가 지킨다 — 준비도 초록·금색이 돌봄 신호(앰버·블루·그레이)로 오독되면 안 된다. 함께: `MONO_STACK` 을 `labels.ts` 로 올려 ADR-111 의 코드 라벨과 배지가 **한 스택**을 쓰게 했다(사본 둘을 만들지 않는다). 계약 0 · 코어 0 · DB 0 · **채점 0**(`scoring.ts` 무변경 — 준비도 값·순서·산식 그대로) · 참여자 경로 0(배지·강조는 코치 리포트에만, 참여자 미러 회귀가드 유지). tsc 0 · eslint 0 · vitest **746**(733→746) · build 성공. ORDER 2026-07-16 · 시안 2026-08-24. |

| ADR-114 | 인도자 박스 — **프로파일 특징 플래그**(원형 재료 · 자동판정 없음) | **무엇**: `FacilitatorPanel`(2면 인도자 전용)에 `프로파일 특징 (원형 참고)` 구획을 더한다. 인도자 가이드의 `여섯 원형`·`원형 빠른 식별표`와 대조할 **재료**를 리포트가 짚어 준다. ORDER `facilitator_profile_flags_v1`(2026-08-24) 실행. **원형을 판정하지 않는 것이 이 기능의 전부다.** 원형은 극단을 그린 초상이라 대부분의 사람은 여섯 중 어디에도 딱 맞지 않고 자동 규칙은 오분류하거나 아무것도 못 고른다. 더 무거운 이유는 **닻내림**이다 — 박스에 `명료한 정체형`이 떠 있으면 인도자가 그 판정에 묶여, 가이드의 `원형은 가설이지 서랍이 아니다` 가 무너진다. 그래서 **코드는 사실(어느 축이 두드러진다)만 말하고 판정(어느 원형이다)은 인도자가 한다.** 화면 캡션이 그 분리를 못 박는다(`원형은 코드가 판정하지 않습니다 — 확정은 인도자의 몫입니다`). **지시서의 사실 오류 하나**: §3 이 `vitality: { total; band; low }` 라 적었으나 실제 계약은 **`{ score; low }`** 다 — `total`·`band` 필드는 없고 구간명은 `vitalityZone(score)` 가 준다. 조건 자체(≤10·≥18)는 옳아서 값 접근만 고쳐 실행했다. **반박 하나 — 활력 임계를 상수표에 넣지 않았다.** 지시서 §4.1 은 임계 열 개를 `상수로 한 곳에 모아 두면 최박사가 조정하기 쉽다`고 했고 그 취지는 맞지만, 활력의 `≤10`·`≥18` 은 **이미 확정된 경계**다: `VITALITY_ZONES`(시들음 ≤10 · 중간 11~17 · 번성 18~25 · ADR 확정 2026-06-28, `labels.test` 가 잠금)와 `scoring.ts` 의 `vitality.low` 가 같은 수를 쓴다. 여기 사본을 두면 시들음 경계가 **세 곳**이 되고, 최박사가 하나만 고쳤을 때 **화면의 구간명과 플래그가 어긋난다** — 조정하기 쉬우라고 만든 상수가 정반대로 작동한다. 그래서 활력만은 `vitalityZone()` 을 **그대로 부르고**, 나머지 여덟 임계는 지시대로 `FLAG_THRESHOLDS` 한 표에 모았다. 테스트가 상수표에 활력 키가 **없음**을 단언해 되살아나지 않게 한다. **보완수용 — 문구 둘의 방향이 거꾸로 읽힌다.** `gap.B1~B5` 는 `5년 후 완성된 나를 10점이라 할 때 지금의 나는 몇 점` 이라 **높을수록 이상에 가깝다.** 그런데 지시서 문구 `간격 관계·기여 높음` 은 `간격이 크다(약점)` 로 읽히는데 조건(B3·B5 ≥ 7)이 뜻하는 것은 **강점**이고 관련 원형(따뜻한 표류형 — 관계·기여가 강점)과도 그렇다. **정반대로 오독되면 인도자가 반대편 원형 가설을 세운다** — 이 기능의 목적이 바로 무너진다. 그래서 `관계·기여 점수 높음` 으로, 짝이 되는 `간격 한 축만 깊이 함몰` 은 `한 영역만 깊이 함몰` 로 다듬었다(`함몰` 이 이미 방향을 말하므로 `간격` 을 앞에 붙이면 이중이다). 상수표 옆 한 줄이라 되돌리기 쉽다. **경계**: 헬퍼는 `report/profileFlags.ts` 신설 — **표시 층의 파생값**이고 `careBanner` 가 `labels.ts` 에 사는 것과 같은 성격이다. `scoring.ts` **불변**, `interpretation.ts`(AI 입력) **불변**, 참여자 미러 **불변**. 주관식(E1~E3)은 쓰지 않는다 — **감정을 자동 판정하지 않는다**(정서·기대는 인도자가 원응답에서 직접 읽는다 · ADR-111 이 그 원응답을 가이드 순서로 세워 둔 이유이기도 하다). 톤은 기존 패널과 같은 중립 회색 칩 — care 톤·경보색을 쓰지 않는다(경보가 아니라 참고다). **선택 항목 둘**: ⓐ 소프트 캡은 **걸지 않았다**(지시서 기본 = 뜬 것 모두). 최대로 흔든 프로파일에서 여덟 개가 떴고 두 줄로 읽힌다 — 실렌더로 확인했다. 잡음이라 판단되시면 상위 N 자르기는 한 줄이다. ⓑ 빈 경우는 **`두드러진 특징 없음` 표기**를 골랐다(구획 숨김 대신) — 계산이 돌았다는 사실 자체가 정보이고, 구획이 사라지면 인도자가 고장으로 읽는다. 전 축 중립(모두 3·5·3)이면 실제로 하나도 안 뜬다. **원형명 부재를 세 겹으로 잠갔다**: ① `profileFlags.ts`·`FacilitatorPanel.tsx` **소스 전문**에 원형 다섯 이름이 없음 ② 조건을 다 켠 프로파일의 **플래그 문구**에 없음 ③ `ReportScreen` **최종 렌더 HTML**에 없음(가드가 헛돌지 않았음을 `활력 시들음 구간` 존재로 함께 확인). 참여자 미러 회귀가드도 플래그 문구 전수로 확장했다. **실렌더 확인**(Playwright): 대표 프로파일 3칩(편차·현실인식·원씽) · 최대 8칩 · 중립 0칩(`두드러진 특징 없음`), 배치는 믿음의 자리 → 프로파일 특징 → 주의 문구, 원형명 노출 0. 계약 0 · 코어 0 · DB 0 · 채점 0 · AI 0 · 참여자 경로 0. tsc 0 · eslint 0 · vitest **762**(746→762) · build 성공. ORDER 2026-08-24. |
| ADR-115~117 | *(예약 — 6회차 갈무리)* | `CC_ORDER_checkin_session6.md` 가 선점했다: **115** 다중 되비추기(`MirrorSet`) · **116** 마지막 회차 구조(`step.companion`·선택 필드들) · **117** 6회차 문안. ADR-118 이 먼저 착수돼 번호가 비었다. **결번이 아니라 예약이다** — CLAUDE §11.3 의 `결번 확인` 을 번호 재배치(6회차 지시서까지 고쳐야 한다) 대신 **이유를 표에 남기는 것**으로 푼다. 6회차 착수 시 이 줄을 세 행으로 채운다. |
| ADR-118 | 갈무리 누적 조회 — **인도자 세로 보기**(갈래 A) · 종단 축 조립 · 규칙 신호 | **무엇**: 한 사람의 전 회차를 한 화면에 세우는 `/coach/cohort/[cohortId]/member/[userId]` 를 세우고, 그 조립·판정을 순수 함수(`checkin/journey.ts`)로 뽑는다. ORDER `checkin_journey`(2026-08-24) + 정정 메모 셋 실행. **왜**: 인도자가 한 사람의 5주를 보려면 회차 탭을 다섯 번 눌러야 했고 **두 회차를 나란히 놓는 순간이 0이었다.** 리포트와 갈무리가 서로를 몰라(리포트 경로에 `checkin` 참조 0건 — 실측) 나침반 점수를 보다 *이 사람이 실제로 무엇을 하고 있지* 를 물으면 화면을 나가야 했다. **종단 축을 설계해 놓고 그 축을 볼 화면을 만들지 않았다.** **계약 둘을 넓혔다.** ⓐ `listCohortCheckins(cohortId, sessionNo?)` — 회차를 생략하면 차수 전체(세로 보기가 회차 수만큼 왕복하지 않는다). ⓑ `listCohortMembers(cohortId, onlyParticipants?)` — 뒤엣것이 이 작업의 가장 까다로운 자리였다. **명단에 운영자가 섞여 있었다**(1기 등록 11 = 참여자 9 + admin 1 + coach 1). 그들이 제출하지 않으므로 격자·신호가 그 명단으로 서면 **인도자가 `오늘 연락할 사람` 목록에서 자기 자신을 본다.** ADR-110 이 `cohort_seats_taken` 에서 같은 문제를 `u.role='user'` 로 풀었고 *"이름이 아니라 역할로 거른다"* 를 적었다 — 같은 원칙이다. **다만 RPC 에 필터를 박지 않고 선택 인자로 뺐다.** `cohort_member_directory` 가 **두 목적을 겸하고 있었기 때문**이다: ⓐ 코칭 대상 명단(회차 현황·격자) ⓑ `userId → name` 이름 조회(리포트 PDF 헤더 `report/[responseId]/page.tsx:46` · `rosterModel.ts:31`). 호출부가 **다섯**인데(회차 현황·차수 상세·리포트 헤더·차수 목록·코치 홈) 기본값을 바꾸면 ⓑ가 `'참여자'` 로 폴백되고, **실측상 `role<>'user'` 계정이 1기 사전 체크 응답을 1건 갖고 있어 즉시 발생한다.** 그래서 `p_only_participants boolean DEFAULT false` — **기본값이 기존 동작**이라 호출부 넷이 무변경으로 살고 회차 현황·세로 보기만 `true` 로 부른다. 회차 현황 명단이 11 → 9 로 주는 것은 **의도된 회귀**이고 테스트가 기본값을 못 박는다. 마이그레이션은 **구 시그니처를 먼저 DROP** 한다(인자 개수가 다르면 오버로드로 둘이 공존한다 — 적용 후 `pg_proc` 에 하나만 남는 것을 확인했다). **종단 축은 `summaryFields[0]` 으로 조립되지 않는다.** 발주서가 그렇게 지시했고 두 라운드에 걸쳐 정정됐다 — 확정 축(`갈망1 → 한 문장2 → 바라는 것3 → 원씽4 → 환경5`)을 인덱스로 옮기면 **`{1:0, 2:1, 3:0, 4:3, 5:0}`** 이라 다섯 중 둘이 벗어난다(2회차 `[0]` 은 `영역`, 4회차 `[0]` 은 `이번에 시작할 프로젝트` 라 정작 `인생의 원씽` 이 빠진다). 그래서 `AXIS_INDEX` 다섯 줄이 **`어느 것`만** 정하고 라벨·키·값은 전부 문안이 준다(새 문안 0). 테스트가 그 표가 가리키는 라벨 다섯을 문안 원문과 대조한다. **3회차만 `gap_want` 인 이유**: 칩으로 고른 `gap_area` 는 `summaryFields` 에 **없고**(3회차가 낱말 하나를 나눔 열에 안 넣은 것은 옳다), 종단 축의 나머지 넷이 전부 문장이다. 그에 맞춰 **`index.ts` 축 주석의 `영역3` 을 `바라는 것3` 으로 고쳤다** — 주석과 코드가 어긋난 채 두면 다음 사람이 `gap_area` 를 축으로 되살린다. 같은 주석의 **옛 배속**(`남은 시간6 → 정체성 선언7`)도 함께 정정했다(확정 배속 = 6회차 STEP 9+10 · **7회차 인도자 세션, 참여자 카드 없음**). `facilitatorBox.defaultOpen` 주석의 `7회차만` 도 같이 고쳤다. **`CC_ORDER_checkin_session6.md` §7 이 같은 주석을 다른 이유로 고치라 하는데 이 작업이 먼저 반영했다** — 6회차 착수 시 그 항목을 건너뛴다(파일도 `slots.ts` 가 아니라 `index.ts` 다). **지시서 파일은 고치지 않았다** — 발주 문서가 수신자 손에 바뀌면 지휘부가 클론해 대조할 기준이 사라진다. **신호는 축이 셋이다.** `연락 요청` 을 주/보조와 **같은 축에 두지 않는다** — 넷은 코드의 추론이고 그것만 **참여자가 직접 손을 든 것**이라, 빈도가 아니라 확실성으로 자리를 정하는 곳이다. 급을 빈도로 매기면 희귀하지만 중대한 신호가 가장 낮은 급이 된다. 주 신호는 셋(`작성 중 방치` · `연속 미착수` · `자신감 급락`)이고 `결산 악화` 가 보조다. **`작성 중 방치` 가 실측이 찾아낸 신호다** — 1기에 마감 지난 작성 중이 **10건**이고, 그들은 미착수한 사람과 다르다(시작했고 막혔다). **`자신감 급락` 은 `인접 두 회차` 가 아니라 `직전 응답 회차` 대비다** — 응답자가 9명 중 5명이라 인접 쌍이 자주 끊기고, 인접만 보면 재료의 절반을 버린다. 1기 실측 2건: `1→2`(8→3, 인접) · **`1→4`(5→2, 세 회차 건너뜀)**. 뒤엣것이 **자신감이 낮은 채로 침묵한 사람**이라 더 중요한데 인접 조건으로는 영영 안 잡혔다. **임계 3은 새로 정한 값이다** — 10점 척도에서 구간 하나를 건너뛴 크기이고 1기에 낙폭 5·3이 각 1건, 1~2가 3건이라 3이 둘을 가른다. **ADR-94 의 `3 이하`(절대값 임계)를 근거로 삼지 않는다** — `8→5` 는 낙폭 3이나 5는 낮은 값이 아니고 `3→2` 는 낙폭 1이나 둘 다 위험 구간이라, **절대 임계에서 낙폭 임계를 도출할 수 없다.** **ADR-94 B 판정을 함께 싣는다**(그 ADR 이 *"승인된 ADR 의 근거를 다룰 때는 전부 인용한 뒤 각각을 따로 판정한다"* 를 세웠다): ADR-94 가 기각한 것은 **`RosterEntry` 에 `confidence` 값을 싣는 것**이고 근거가 셋이다 — ⓐ 펼침에 도구가 있다 ⓑ 실측이 작다 ⓒ 문안의 약속을 펼침이 이행한다. `자신감 급락` 은 **값이 아니라 변화 사실**을 표시하므로 ⓐ·ⓒ와 충돌하지 않고, ADR-86 이 금지한 `숫자를 등급으로 바꾸는 표현`(막대·게이지·색·백분위·평균·정렬키)에도 해당하지 않는다 — **순위를 만들지 않고 한 사람의 시간 축 위 변화만 가리킨다.** ⓑ의 재고 조건(한 회차 제출 8건 이상 · `3 이하` 3건 이상)은 **2026-08-25 기준 여전히 미충족**이나(최대 5건 · 최대 1건) 그것은 **값을 명단에 올리는 조건**이지 신호를 켜는 조건이 아니다. **익명 제안은 세로 보기에 싣지 않는다.** 발주서가 *"한 사람 화면이라 익명이 성립하지 않으니 이름을 가리지 않는다"* 고 했으나 **논거가 거꾸로였다** — 그러므로 거기 실으면 안 되는 것이다. `readModel.ts:195` 가 이미 `isSelf || !suggestionAnon` 로 거르고 그 주석이 **지휘부 결정 2026-08-02** 를 명시하며, 무기명 수집 경로(`readAnonSuggestion`)도 회차 현황이 쓰고 있다. 참여자가 읽고 체크한 문안은 `이름 없이 전달합니다 … 글의 결로 짐작될 수 있습니다` 로 **짐작은 인정해도 시스템이 이름을 붙인다고 약속하지 않았다.** 그리고 ⑦(전문)이 `buildCheckinRead` 를 재사용하므로, ⑤에 실으면 **같은 화면에서 한 문장이 위에는 있고 아래는 없게** 된다. `facilitatorNotes()` 가 같은 판정을 쓴다. **`cellState` 는 네 갈래다** — `행 없음`과 `아직 안 열림`이 다르다(전자는 연락할 사람, 후자는 아니다. 1기 5회차가 창이 열린 채 제출 0 이라 그 구별이 실제로 필요했다). `hasContent` 는 실제 컬럼이라 **행 존재 ≠ 작성 중**이다 — 전면 안내가 만든 빈 행은 미착수로 본다. **상태 문구를 순수 함수가 만들지 않는다** — 인도자에게는 사실이고 참여자에게는 판정이라(`미착수` vs `아직 비어 있어요`) 낱말은 화면이 고른다. 화면 C 가 같은 함수를 쓰게 하려는 설계다. `journey.ts` 를 인스트루먼트에 둔 이유: 종단 축 조립이 `summaryFields` 를 읽어 **문안 구조를 안다.** 코어는 회차를 몰라야 한다(ADR-90). 계약만 바라보고 코어를 import 하지 않는다. **표시 규율 유지** — 막대·게이지·색·정렬키 0, 자신감은 숫자만(미응답 `—`, 빈칸은 0으로 오해된다), 의미색은 연락 요청에만, 주 신호는 굵기·테두리로 가른다. **화면 C(참여자 세로)는 같은 데이터·다른 편집**이다. `journey.ts` 를 그대로 쓰되 **자신감·인도자에게 남긴 말·신호 셋을 뺀다.** 자신감을 빼는 이유가 실측으로 강해졌다 — 제출 24건 중 16건(67%)이 채워져 있어 **자기 숫자 다섯이 세로로 서면 진짜 추이 그래프가 되고**, 그것이 ADR-86 이 `막대·게이지·색·백분위·평균·정렬키 금지`로 막은 효과다. 상태 문구도 다르다(`작성 중`→`이어서 쓰기` · `미착수`→`아직 비어 있어요` · `아직 열리지 않음`→`곧 열립니다`) — 인도자에게는 사실이고 참여자에게는 판정이 되기 때문이다. **빈 칸은 링크다** — 자기 점검의 결과가 행동으로 이어지지 않으면 점검이 아니다(열린 자리만. 아직 안 열린 회차는 갈 곳이 없다). 맨 아래 한 줄(`여기 쌓인 것이 마지막 시간의 재료가 됩니다`)이 자기 점검을 넘어 **왜 계속 쓰는가**를 말한다(ADR-102 축2). **제외 셋을 배달 검증으로 잠갔다**(ADR-109 교훈) — 순수 함수가 옳은 값을 내도 컴포넌트가 다른 경로로 그릴 수 있고, 화면 분기라 타입이 안 잡는다. `renderToStaticMarkup` 산출물을 직접 단언하고, **같은 자료로 화면 A 는 셋을 전부 그리는지**까지 함께 확인해 가드가 헛돌지 않음을 증명한다. **그 검증이 첫 실행에서 경계를 짚어 줬다** — ⑦ 회차별 전문은 `audience='self'` 라 참여자가 **자기 글을 되읽는** 것이고(기존 열람 화면과 같은 렌더, 새로 노출되는 정보 0), 발주서 §5-3 표가 이미 `self` 로 명시했다. 그래서 단언을 **편집 블록**으로 좁혔다 — ADR-86 이 막은 것은 자기 숫자 다섯이 세로로 서는 것이지 접힌 회차 안에 자기가 적은 한 값이 있는 것이 아니다. **화면 B(격자)** 는 명단 × 회차다. **색을 뿌리지 않는다**(성적표가 된다) — 상태는 기호(`●◐○·`), 신호는 행 왼쪽 **세로 막대**. **신호로 정렬하지 않는다**(순위가 된다) — 행은 명단 순서 그대로. 열 머리의 마감·열림은 머리 색이 아니라 **아래 캡션**으로 구분한다. 칸을 누르면 기존 회차 현황(`?session=N&open=userId`), 이름을 누르면 화면 A. 의미색 토큰 부재를 테스트가 전수로 단언한다. 1기 실측 격자는 **9행 중 5행에 표식**(작성 중 방치 5명 · 연속 미착수 0명) — 과탐지가 아니라 그 코호트의 실제 상태다(§1-1 이 `작성 중 12건이 가장 손이 필요한 사람들` 이라 한 그대로). **화면 D(PDF)는 A·C 각각**이고 격자는 만들지 않는다 — 눌러 들어가는 것이 용도라 종이에서 죽는다. `ReportPrintButton`·`ReportPrintHeader` 를 **그대로 재사용**한다(사본을 만들지 않으려고 헤더에 선택 `title` 만 더했다). **접힘을 `<details>` 로 두지 않았다** — 브라우저가 비열림 자식을 UA 규칙으로 감춰 `@media print` 강제 펼침이 기기마다 다르게 동작한다. ADR-77 의 `report-raw-content` 가 검증한 **display 토글**을 그대로 쓴다(`JourneyCollapsible` + `.journey-full-content { display: block !important }`). 인도자 PDF 에 신호가 포함되고(사실 요약이지 해석이 아니다) **AI 자리는 비워 두지도 않는다** — 빈 자리가 남으면 다음 사람이 채워야 할 결손으로 읽는다(§9 이월). **왕복 진입 셋을 이 배치에서 닫았다**: 리포트 → 갈무리 기록 · 갈무리 → 사전 체크 리포트 · 차수 홈 → 나의 기록. 회차 현황에는 `격자로 보기` 를 달았다. 계약 **2**(위 ⓐⓑ) · 코어 조건부 필터 2줄 · **DB 마이그레이션 1**(`20260825213000_cohort_member_directory_participants.sql` — 함수 시그니처만, 표·정책·데이터 무변경) · 문안 파일 **0** · 채점 0 · AI 0. tsc 0 · eslint 0 · vitest **812**(762→812) · build 성공(세 라우트 전부 ƒ). ORDER 2026-08-24 · 정정 3회 · **A·C·B·D 완주.** |
| ADR-119 | 모집 자료 열람 범위 고지 — **앱 문안에 맞춘다**(네 번 보고된 드리프트) | **무엇**: 모집 카드 8 하단을 `적으신 내용은 세미나 인도자만 봅니다.` → **`… 세미나 인도자와 운영자만 봅니다.`** 로 고치고, 그 어긋남이 재발하지 않게 **모집 문안과 앱 문안을 테스트로 묶는다.** **왜**: 앱은 일관되게 운영자를 명시한다 — `StartGuide`(`여기 적는 모든 것은 세미나 인도자와 운영자만 봅니다`) · `ResponseRunner` · 갈무리 1~5회차 `save.notice2`(`적으신 내용은 인도자와 운영자가 읽습니다`) · `LetterPhotos` · 개인정보 동의(`열람 주체: 본인, 소속 그룹 인도자, 운영자`). 그 문장들은 **ADR-77 §4.3 이 열람 범위를 운영자 포함으로 확정하며 일부러 정직하게 갱신한 것**이다(`인도자 한 사람만 봅니다` → `인도자와 운영자만 봅니다`). 그런데 모집 자료는 옛 형태로 남아 **가입 전에 본 약속이 가입 시 동의서보다 좁았다.** 원고가 ADR-77 이후인 8/18 에 쓰이며 옛 진단지 문구를 가져온 것으로 보이고, 8/24 지휘부 지시(`인도자 한 사람만` → `인도자만`)는 `한 사람`을 뺀 것이지 운영자를 뺀 판단이 아니었다 — 애초에 원고에 운영자가 없었다. **네 번 보고된 뒤 지휘부가 `모집 자료를 앱 문안에 맞추는 것이 맞다` 로 확정했다.** **원인은 둘을 묶는 것이 없었다는 데 있다** — 모집 삼중 잠금(시안 HTML ↔ `copy.ts` ↔ 원고 `.md`)은 **서로만** 봤고 앱 문안은 그 바깥이었다. 그래서 `recruit.test.ts` 가 이제 **`CHECKIN_SESSION_1.save.notice2` 를 직접 읽어** 대조한다: 앱이 운영자를 명시하는지 먼저 확인하고(기준이 실재하는지), 모집 고지도 그러한지 보고, 옛 문구가 시안·원고·웹 어디에도 없는지 본다. 한쪽이 바뀌면 다른 쪽을 함께 옮기라고 테스트가 먼저 말한다. **카드 8 PNG 를 재렌더했고 `card_08.png` 하나만 바뀌었다** — 렌더가 결정적이고 변경이 그 카드에 격리됐다는 확인이다. **함께 고친 버그 하나(줄끝)**: `recruit.test.ts` 가 원고·시안을 `readFileSync` 로 읽어 **여러 줄 문구**(`title.join('<br>')` 이 만드는 개행)를 대조하는데, 이 저장소는 `core.autocrlf=true` 라 **Windows 에서 클론하면 문서가 CRLF 로 풀린다.** Node 는 원본 바이트를 그대로 읽으므로 그 상태에서는 **문구가 맞는데도 레드가 난다** — 지휘부가 매 단계 클론해 감리하므로 그 자리에서 먼저 터지는 종류의 함정이다(이번에 실제로 재현됐다). 읽는 자리에서 줄끝을 정규화해 막았다. 계약 0 · 코어 0 · DB 0 · 채점 0 · **앱 문안 0**(모집 자료만 앱에 맞췄다). tsc 0 · eslint 0 · vitest **815**(812→815) · build 성공. 지휘부 확정 2026-08-25. |
| ADR-120 | 내비게이션 `<Link>` 전수 전환 + 액션 pending + 눌림 피드백 (성능 트랙 P2) | **무엇**: 내부 이동 앵커 **57 곳을 `next/link` 로 전수 전환**하고, 액션 버튼의 pending·잠김 결함을 닫고, 모바일 눌림 피드백을 되살린다. ORDER `nav_link_pending_v1`(2026-08-25) 실행 — **다만 그 지시서를 그대로 따르지 않았다**(아래 정정 넷). **왜**: 사용자 보고가 둘이었다 — *유독 느리다* · *로딩 사인도 없이 멈춰 있어서 다시 누른다*. **뿌리가 다르다.** 앞엣것은 **Vercel 함수 리전**이었고(P1 에서 최박사가 서울로 전환 — 실측 `X-Vercel-Id: icn1::iad1` → `icn1::icn1`, `/login` TTFB **349~452ms → 150~236ms**, DB 호출 0 인 페이지라 순수 위치 비용이다), 뒤엣것은 **클라이언트 라우팅 부재**다. **`<a href>` 로 이동하면 App Router 의 전환·프리페치·`loading.tsx` 가 통째로 작동하지 않는다** — 그래서 `src/app/loading.tsx`(`불러오는 중…`)가 만들어진 뒤 **한 번도 뜬 적이 없었다.** 이번에 실브라우저로 **처음 발동을 실증했다**(§검증). **ADR-52 를 뒤집는다.** 그 결정(`하위 라우트는 기존 `<a>` 유지`, directive 2026-07-02 승인)이 `<Link>` 4 곳의 원인이었고, 발주서는 그것을 **결함처럼 서술하며 ADR-52 를 한 번도 인용하지 않았다.** ADR-94 가 세운 규율(`승인된 ADR 의 근거를 다룰 때는 전부 인용한 뒤 각각을 따로 판정한다`)에 걸려 **착수 전 지휘부 승인을 받고**(2026-08-26) ADR-52 에 정정 주를 달았다. **발주서 정정 넷**: ⑴ **§1-3 `JoinClient 만 busy 가드 보유` 는 사실이 아니다** — `busy` 가 **34 개 파일**에 있고 `router.refresh` 를 쓰는 **8 개 파일 전부**가 갖고 있다. 발주서가 `우선하라`고 지목한 `CheckinCardClient` 는 이미 `disabled={busy}` 완비였다. 실제로 무가드인 것은 **`CohortDetailClient.run()` 하나**였다(관리 액션 다섯). ⑵ **§3.2 `프리페치 서버 부하 우려 없음` 은 이 앱에서 성립하지 않는다** — Next 16 문서가 *`많은 링크 목록을 렌더할 때는 자원 사용을 피하려 prefetch 를 끄라`*(`prefetching.md:308`) 하고, 동적 라우트도 **`loading.tsx` 가 있으면 부분 프리페치**된다(`linking-and-navigating.md:88`). 루트 `loading.tsx` 가 **하위 전부를 감싸므로**(`loading.md:78·88`) 28/32 동적 라우트가 전부 대상이 되고, **프리페치 RSC 요청도 `proxy.ts` matcher 를 지나 `getUser()` 를 부른다.** 격자는 9 명 × 7 회차 = **한 화면에 링크 72 개**다. 그래서 **격자·로스터만 `prefetch={false}`**, 나머지는 기본값이다. ⑶ **§3.1 대조 기준의 짝이 어긋났다** — 발주서는 `median 1573ms(warm-min 899)` 라 썼으나 ADR-66 실물은 `1573 → 1596(warm-min 1141 → 899)` 로, **개선 전 median 과 개선 후 warm-min 을 한 쌍으로 묶은 것**이다. 정지 조건이라 짝을 맞춰야 한다. ⑷ **`force-dynamic` 정리는 하지 않는다** — 25 곳 선언 중 **22 곳이 no-op** 이다(`cookies()`/`searchParams` 가 이미 동적으로 만든다). 지워도 1ms 도 안 바뀐다. **발주서에 없던 항목 하나를 더했다 — 눌림 피드백.** Tailwind Preflight 가 `-webkit-tap-highlight-color: transparent`(`preflight.css:45`) 로 **모바일 기본 탭 하이라이트를 꺼 버렸고**, `src/` 에 되돌리는 선언이 0 건이며, 저장소 전체 `:active` 규칙 3 개가 전부 `.ui-btn--*`·`.ui-listrow--tappable` 전용인데 **헤더와 홈 카드 행은 인라인 스타일이라 그 클래스가 안 붙어 있었다.** → **탭한 사실 자체가 화면에 남지 않았다.** 이건 `<Link>` 로 안 고쳐지는 별개 원인이다. **새 색을 고르지 않았다** — `.ui-btn--primary:active` 가 이미 쓰는 `brightness(0.94)` 를 그대로 재사용하고 탭 하이라이트를 `initial` 로 되돌렸다(§8 임의 디자인 아님). **잠김 버그 넷을 함께 닫았다**(같은 계열): `CheckinCardClient.doSubmit`(**참여자 제출** — `finally` 가 없어 액션이 throw 하면 새로고침 전까지 제출 불가) · `LoginClient.onSubmit`(**현관** — async 이벤트 핸들러의 예외는 에러 경계로 가지 않고 unhandled rejection 이 되므로 catch 가 없으면 버튼이 영구히 잠긴다) · `CoachPhotos` · `ScheduleSeedClient`. `CohortDetailClient.run()` 은 `useTransition` 으로 **refresh 구간까지** 덮고, `CohortDetail` 에 선택 prop `actionPending` 을 더해 그 구간에도 버튼이 비활성이게 했다(이름 변경은 ADR-62 대로 무변경). **함께 넣은 것**: `vercel.json` 에 `regions: ["icn1"]` — P1 은 대시보드 설정이라 **코드에 고정돼 있지 않았고**, 프로젝트 재연결·설정 되돌림으로 조용히 회귀한다. 홈 `내 세미나` 를 차수 홈으로 직결(차수가 하나면 `/my/cohorts` 가 즉시 리다이렉트하므로 목록 경유는 **서버 렌더를 한 번 더** 돌린다 — 같은 파일 `reportHref` 와 같은 판정이다). **남긴 예외 3 곳**: `MemberProfilePanel` 의 `tel:`·`mailto:` 둘, `error.tsx` 의 홈 링크 하나(에러 경계에서는 전체 새로고침이 의도다 — 상태 초기화). 잔존 `<a>` 가 이 셋과 정확히 일치함을 grep 으로 증명했다. **검증은 실브라우저로 했다**(`fetch 200 ≠ browser normal`) — `/login` → `/signup` 전환에서 `document` 재요청 **0건**(클라이언트 전환 확인) · 전환 중 `main[aria-busy="true"]` 가 **보이고** 문구가 `불러오는 중…` · 전환 뒤 사라짐. **중간에 한 번 위양성을 잡았다** — 첫 시도는 포트 3000 에 남아 있던 **이전 빌드 좀비 프로세스**에 붙어 청크가 500 이 나고 하이드레이션이 죽어 하드 내비게이션으로 보였다. 서버를 정리하고 다시 재니 전부 통과했다. 눌림 피드백도 실브라우저 computed style 로 확인했다(평상시 `filter: none` → 누르는 중 `brightness(0.94)`). 계약 0 · 코어 **1**(`context.ts` 무변경, `ui.css` 에 규칙 2 개 추가) · DB 0 · 마이그레이션 0 · 문안 **0**(pending 은 비활성화로만 알린다). tsc 0 · eslint 0 · vitest **816**(812→816) · build 성공. **질의 1건**: `LoginClient` 예외 시 문구 — 기존 `이메일 또는 비밀번호를 확인해 주세요` 는 네트워크 오류에 **틀린 안내**라 쓰지 않았고, 신설은 §2-1 금지라 **잠김만 풀고 문구는 비웠다.** 지휘부 판단 대기. ORDER 2026-08-25 · 승인 2026-08-26. |
| ADR-121 | 가치 카드 이식 — 전 슬라이스 완주(데이터·계약·저장·화면·진입·열람) | **무엇**: 이식 원본 `loginheaven-jpg/value`(Manus·MySQL·tRPC)의 72장 카드 소팅을 퓨처나우 인스트루먼트 자산으로 세운다. 지시서 v1→v2→v3 로 세 판을 거쳤고 검토서 셋이 `docs/reports/` 에 있다. 이번 커밋은 **골격과 데이터까지**이고 DB·계약·화면은 후속 슬라이스다. **배치**: 순수 로직·데이터·문안은 `src/instruments/futurenow/values/`, 라우트는 `src/app/my/cohorts/[cohortId]/values/`. **DB 접근은 이 폴더에 두지 않는다** — 이 저장소는 `/app`·`/instruments` 의 테이블 접근이 **0건**이고 갈무리조차 인스트루먼트 전용 기능이면서 `CoreContext` 계약 메서드를 지난다(ADR-80). v2 지시서가 `코어를 경유하지 않는다`고 쓴 것을 검토서 B-2 가 뒤집었고 v3 가 수용했다. **카테고리를 참여자 렌더 타입에서 뺐다** — 원본 `ValueCard.tsx` 는 카드마다 `#{category}` 를 출력했다. 그러면 화면 제목에서 주제어를 걷어내도 카드마다 **72번 되살아나** `이 영역에서 무엇을 중시해야 하는가` 라는 사회적 프레임이 생긴다. 원본 핸드오버가 못 박은 제1원칙(`사회적으로 중요해야 한다고 생각하는 항목이 아니라 마음이 끌리는 값`)과 정면으로 어긋난다. 렌더 금지를 규칙으로 두면 지켜지는지 매번 사람이 봐야 하므로 **타입에서 뺐다** — `ValueCard` 에 category 가 없으니 화면이 참조할 경로가 없다. 카테고리는 `CARD_CATEGORY` 별도 맵으로 남겨 배열 분산과 그 검증에만 쓴다(CLAUDE §7 어휘 분리와 같은 사고). **노출 배열은 보폭(stride) 분배다.** greedy(남은 수 최다 우선)를 먼저 짰다가 버렸다 — **인접 제약은 만족하지만 화면 단위로 쏠린다.** 큰 카테고리가 앞 화면으로 몰려 1화면에 관계 8장(전체 14장의 절반 이상)이 들어갔다. 인접만 보는 제약으로는 화면이 주제 화면처럼 읽히는 것을 못 막는다. 보폭 분배는 각 카테고리의 k번째 카드에 `(k+0.5)/크기` 좌표를 주어 전 구간에 균등히 펼치므로 화면당 몫이 자동으로 맞는다. 실측: **인접 동일 카테고리 0 · 화면당 최다 4장**(관계 3/4/1/4/2). 파이썬으로 알고리즘을 독립 재현해 교차 검증했다. **난수를 쓰지 않는다** — 1차는 집에서 중단·재개하고 2차가 1차 결과를 되비추므로 같은 사람이 같은 순서를 다시 봐야 하고, 참여자 간 비교(어떤 카드가 몇 번 뽑혔는가)도 노출 순서가 같아야 뜻이 있다. v3 §4-1 이 선례로 든 `core/response/ordering.ts` 는 **런타임 rng 셔플**이라 하는 일이 반대여서, 사고(제약을 걸어 쏠림을 푼다)만 계승했다. **판본은 카드 상수가 정한다** — `CARD_SET_VERSION` 을 스키마 DEFAULT 가 아니라 `cards.ts` 에 두었다. 버전은 기록자가 아는 값이다. **원본이 잠그던 보장을 승계**: id 1..72 연속·중복 0·id 73(사문 커스텀 카드) 부재·한글명 중복 0. 원본 문서가 `id 재번호화 금지`를 명시했고, 우리 쪽은 `candidates`·`value*_id` 가 정수라 재번호화가 곧 과거 결과의 오독이 된다. **카드 72장 문안은 한 자도 고치지 않았다**(v3 §14) — 대신 전수 금지어·허락어휘·압박어휘 **0건**을 테스트로 못 박았다. 통과하지 못하는 날이 오면 그때는 지휘부 결정 사안이 된다. **`safeReturnTo` 에 두 형태를 더했다**(`/c/{code}/values` · `/my/cohorts/{uuid}/values`). 실제로 왕복이 도는 것은 짧은 경로 쪽이고 (`/my/…` 는 proxy 가 미인증을 걸 때 search 를 비운다) 목적지가 같은 화면이라 짝을 맞춰 두었다. **미결 2건을 코드에 명시했다**(`stages.ts` 주석): 후보 상한 12 가 화면에서는 소프트인데 v3 §3-4 는 서버에서 하드로 강제한다(13장이면 화면 통과 후 저장 예외) · 정리 화면 하한이 8 로 내려가 다음 화면과 목표 구간이 같아졌다. 지휘부 확정 전까지 v3 표의 값을 그대로 싣고, 확정되면 그 파일 한 곳만 고친다. **클코1 자기 오류 2건 정정**: `has_column_privilege` 지적 철회(저장소 관용이고 기능상 더 넓게 잡는다) · jsdom 지적 철회(`vitest.config.mts` 가 `jsdom 불요`를 명문으로 선언하고 `renderToStaticMarkup` 으로 화면을 검증한다 — 설치는 회귀다). 계약 0 · 코어 0 · DB·마이그레이션 0. tsc 0 · eslint 0 error · vitest **661**(643→661, 신규 18) · build 성공 — **검증 수치 정정(2026-08-26)**: 이 `661` 은 **리베이스 전 기준 `4e4d759` 위**의 값이다(같은 표의 ADR-119 는 815, ADR-120 은 816 이라 어긋나 보인다). **원값을 지우지 않는다** — 검증 수치는 기준 커밋과 짝일 때만 의미가 있고, 661 은 그 시점에서 참이었다. 지우면 왜 어긋났는지가 사라진다(ADR-94 의 정신). **리베이스 후 실측 = `9664f9a` 위에서 vitest 860**(78 파일 통과 · 13 skip = RLS 통합 옵트인 · tsc 0). 착수 보고 때 든 `≈834` 는 **추정이었고 틀렸다** — 그래서 추정치를 문서에 박지 않고 실제로 돌려 확정값을 넣는다(`ƒ /my/cohorts/[cohortId]/values` 등록 확인). directive 2026-08-19(v3). **채번 정정**: 착수 시점 원격 최대가 107 이라 108 로 적었는데, 밀어 올리기 직전 원격이 18 커밋 앞서 있었고 108~120 이 이미 쓰여 있었다(5회차 갈무리~내비 전환). 기준 커밋 `4e4d759` 위에서 검토·구현했으므로 `origin/main` 에 리베이스하고 **121** 로 옮겼다. 리베이스 후 전수 재검증했다. **완주분(2026-08-26)**: 지시서 v3 의 V-3~V-13 을 이어서 마쳤고, 3차 검토가 낸 미결을 클코1 이 판단해 반영했다. **저장 계층**(마이그 `20260826120000`) — 쓰기를 테이블에서 회수하고 DEFINER RPC 셋(`value_save_progress`·`value_finalize`·`value_patch`)으로 좁혔다(D1·ADR-80 선례). 이 프로젝트는 default privileges 로 신규 public 테이블에 `authenticated` 전권을 자동 부여하므로 **GRANT 가 아니라 REVOKE 가 본체**다. `TRUNCATE` 를 반드시 회수한다 — RLS 대상이 아니라 남기면 로그인 사용자가 테이블을 비운다. **N-2 교정**: '역행 금지'만으로는 `exploring`→`final` 도약을 못 막아 v2 B-1 이 지목한 '탐색·비교 건너뛰기'가 그대로 살아 있었다. `value_stage_ok` 허용 전이 목록으로 바꾸고 `value_finalize` 에 `stage='finalists'` 전제를 걸었다. **N-1 교정**: 후보 8~12 를 화면과 서버가 **같은 경계**로 강제한다(v3 는 화면 소프트·서버 하드라 13장이면 화면 통과 후 저장 예외였다). **N-3 교정**: v3 가 든 `participant_move` 는 함수가 아니라 **파일명**이었다(실제 `move_cohort_member`·ADR-84, '삭제'는 휴지통 차수로의 이동). `unique(user_id,cohort_id)` 때문에 곧이곧대로 UPDATE 하면 23505 로 이동 RPC 전체가 실패하므로 갈무리와 동형의 충돌 제거 절을 넣었다(라이브 복수 차수 참여자 3명 — 가공의 시나리오가 아니다). **N-4**: `progress`·`candidates`·라벨·대조를 코어 경계에서 zod 로 파싱하고 직렬화 8KB 상한을 건다(CLAUDE §9·갈무리 32KB 선례의 축소판). DB 에도 길이 CHECK 를 이중으로. **계약 델타 5종**(Q8 승인은 '3종'이었다 — RPC 가 셋이라 쓰기 진입점이 셋 필요하고 본인 조회·인도자 열람이 더해졌다): `getMyValueAssessment`·`saveMyValueProgress`·`finalizeMyValue`·`patchMyValue`·`listCohortValueAssessments`. **저장 지점을 늘렸다** — v3 는 1차 전체와 2차 중반이 휘발성이었다. 화면 이동마다 `progress` 를 증분 저장하고 `stage` 4값으로 이어하기를 지탱한다. **문안**: 어투를 실측에 맞췄다 — 참여자 표면에 `주십시오` 0 · `주세요` 47 · `습니까?` 0 이고 질문형 53건은 전부 해요체 `~요?` 다. v3 정본은 통째로 합쇼체라 같은 회차에 갈무리와 나란히 놓이면 한 제품으로 읽히지 않았다. **시점 지시를 걷어냈다**(`오늘`·`지난 시간` 0) — 게이트가 없어 1차 직후에 2차를 할 수도 있고 1차 미완료자는 다음 회차로 밀리므로, 축2 제약('참·거짓이 회차마다 갈리는 문장을 공통 문구로 넣지 않는다')에 걸린다. 대신 대상의 이름을 부른다. **성찰 질문은 전원 공통 3개로 통일**(Q10 재산정) — 전용 80문항의 금지어 적중이 **0건**이라 '금지어 제거만'은 무작업이었고, 79%가 범용 틀이라 '버리면 하향 평준화'가 실측으로 부정됐다. 방향이 아래인 문항(§2-4)과 예/아니오 문항(§2-3)은 다시 썼다. **진입 두 갈래**: 차수 홈 카드(상태 4종) + 짧은 공유 경로 `/c/[code]/values`. 짧은 경로는 **비멤버 처리가 갈린다** — 인도자 차수 코드는 정지시키고(ADR-89: 링크가 카톡으로 옮겨 다니므로 실명제 차수 자기등록 표면을 열지 않는다), `GENERAL_CODE` 만 가입시킨다(체험은 자기등록이 목적). 분기를 코드로 못 박아 이 관문이 ADR-89 가 막으려던 구멍이 되지 않게 했다. **가시성**(지휘부 확인 2026-08-26): 인도자 차수는 본인·그 차수 인도자·운영자, **체험 차수는 본인·운영자** — `is_cohort_coach` 가 `coach_id` 단일 비교이고 체험 차수의 coach_id 가 운영자라(ADR-63) 정책 추가 없이 성립한다. 다만 그 보장이 **데이터에 얹혀 있다** — 체험 차수 코치를 일반 인도자로 바꾸면 깨진다. 검증 축으로 남긴다. **인도자 열람 화면을 함께 만들었다** — RLS 로 열기만 하고 화면을 안 만들면 순수한 노출면 증가다. **역할별 실측(라이브·전량 롤백)**: 16/16 통과 — 전진 도약 거부 · 역행 거부 · 후보 7장·13장 거부 · 후보 밖·중복 id 거부 · 재확정 거부 · 확정 후 되돌아쓰기 거부 · 부분 갱신 보존 · 비멤버 주입 거부 · 이중 등록자 이동 무충돌 · 영구삭제 연쇄. 권한 카탈로그도 실측: `authenticated` UPDATE=false · TRUNCATE=false · `anon` SELECT=false · RPC EXECUTE=false. 같은 매트릭스를 `tests/rls.integration.test.ts` 에 8건으로 남겨 반복 검증이 가능하다(옵트인). tsc 0 · eslint 0 error · vitest **860**(816→860, 신규 44) · build 성공(`/my/cohorts/[cohortId]/values` · `/c/[code]/values` · `/coach/cohort/[cohortId]/values`). |
| ADR-122 | 회원 상태·승인 + 가치 카드 개인 응시 (S-1 완주 · S-2) | **무엇**: 역할과 **별도 축**인 회원 상태 다섯을 세우고, 그 판정이 **새 응시 하나**를 가르게 한다. 함께 가치 카드를 차수 밖으로 연다. 지시 `CC_ORDER_site_v2.md` + 판정 메모 `CC_MEMO_site_v2_S1.md`, 상위 기준 IA v2.1. 보고서 8편이 `docs/reports/` 에 있다. **① 저장과 산출을 상태마다 나눈다.** `cohort` 는 `enrollments ⋈ cohorts` 가 이미 아는 사실이라 **저장하지 않는다.** 나머지 넷(pending·individual·expired·held)은 운영자의 결정이라 저장 외에 방법이 없다. `memberships.status` CHECK 에 `cohort` 가 **없어 문법이 이중 기록을 막는다**(ADR-121 이 `ValueCard` 에서 category 를 뺀 것과 같은 사고). **② `cohorts.kind` 는 4값이다**(설계 초안의 3값에서 정정). 발주서는 *"차수 회원 여부는 `enrollments` 가 이미 안다"* 고 썼으나 **그것은 틀렸다** — 실측 5개 차수 중 셋(체험 `JOINF`·휴지통 `TRASH`·test)이 세미나가 아닌데 전부 `status='active'` 라, `EXISTS(enrollments)` 로 판정하면 **휴지통에 버린 사람이 차수 회원이 된다.** 그리고 `test` 를 빼면 test 차수가 DEFAULT `seminar` 를 받아 거기 등록된 운영자가 `cohort` 로 판정돼 백필 확정과 어긋난다. 코드 문자열로 거르는 것은 **일회성 시딩 한 번뿐**이고 런타임은 `kind` 만 본다(ADR-110 "이름이 아니라 역할로 거른다"). **③ 판정 우선순위 `held > cohort > 저장 > pending`.** `held` 가 `cohort` 를 이기지 않으면 차수에 등록되는 것만으로 보류가 무력화된다 — 코드 한 줄로 게이트를 뚫는 길이 생긴다. 참여자·인도자·운영자 셋 다에서 실측했다. **④ 만료는 산출한다.** 이 저장소에 크론이 없어 저장값만 보면 자격이 영원히 산다. `individual` + 지난 `valid_until` → `expired`. 저장된 `expired` 는 운영자가 손으로 끊은 경우다. 만료 시계는 **KST 고정**(`membership_today()`) — DB TimeZone 이 UTC 라 `current_date` 를 쓰면 자격이 한국 시각 오전 9시에 꺾인다. **⑤ 열람은 상태와 무관하다.** 발주서 §4.1 표가 `pending`·`held` 의 열람을 `—` 로 적은 것은 *"가입 직후라 결과가 없다"* 는 서술이었으나 문언상 금지로 읽혔고, **기수 마감 시 수료자 전원이 자기 리포트를 잃는 절벽**을 만들고 있었다. IA v2.1 §5.4 가 `○` 로 정정했다. **그래서 열람 게이트 함수를 아예 만들지 않았다** — 없는 것이 곧 규율이고, 진실표 픽스처에 열람 열이 없음을 테스트가 단언한다. **⑥ 자동 전이는 트리거다.** `updateCohort` 가 앱측 평범한 UPDATE 라(ADR-26) 원자성을 얻는 자리가 트리거뿐이다. 마감 시 `individual` 생성 · 기존 `individual` **불연장**(연장은 운영자 결정) · `held` **불변**(트리거가 운영자 판단을 뒤집지 않는다) · 재실행 멱등. **role 필터를 걸지 않았다** — ADR-110 의 role 필터는 정원 집계 규칙이지 자격 부여 규칙이 아니고, 거르면 세미나에 등록된 인도자·운영자가 마감과 함께 잠기는데 `decide_membership` 이 자기 자신을 막으므로 스스로 풀 수 없다. **⑦ `cohorts` 테이블 UPDATE 권한을 회수했다.** 실측상 `authenticated` **와 `anon` 둘 다** 테이블 레벨 UPDATE 를 갖고 있어, `kind` 를 그냥 더하면 인도자가 자기 차수를 `trash` 로 뒤집어 **조원 전원의 응시 자격을 조용히 없앨 수 있었다.** `20260629100002` 이 `users.role` 에서 겪은 그대로 — 테이블 레벨 권한 앞에서 컬럼 REVOKE 는 no-op 이라 테이블 권한을 회수하고 `updateCohort` 가 실제로 쓰는 5컬럼만 재부여한다. **⑧ 알려진 한계 — 복수 차수 수료자의 기간이 두 번째 마감에서 갱신되지 않는다**(`ON CONFLICT … WHERE status IN ('pending','expired')`). 대상 3명. **로직은 그대로 둔다**(지휘부 확정) — '연장은 운영자 결정'이라는 멱등 규칙이 본체이고, 만료 임박 목록이 안전망이다. 다음 사람이 이 `WHERE` 를 버그로 보고 열지 않도록 해당 절에 남겼다. **(⑧ 은 ADR-147·148 로 소멸했다 — 한계를 만들던 마감 트리거가 폐지됐고, 안전망이던 임박 목록도 함께 걷혔다.)** **⑨ 파일명 타임스탬프 ≠ 적용 버전.** `apply_migration` 이 적용 시각으로 자체 채번한다 — 이번 발주에서만 넷(`membership` 20260826160000→**20260826092730** · `assessment_gate` 190000→**20260826141012** · `signup_intake` 200000→**20260826142335** · `value_personal` 20260827100000→**20260826150546**), 앞서 `value_assessments` 20260826120000→20260826005825 까지 다섯 사례다. **파일명을 사후에 맞추지 않는다** — 만드는 시점에 그 값을 알 수 없어 규율로 성립하지 않고, 이미 push 된 커밋의 경로가 깨진다. **감수할 수 있는 조건은 상대 순서 보존 하나**이고 다섯 사례 모두 유지된다. 대신 파일 머리에 적용 버전을 적어 대조가 파일 안에서 끝나게 한다. **⑩ RPC 4 → 계약 3.** `member_state`→`getMyMemberState` · `list_membership_queue`→`listMembershipQueue`(대기+만료임박 한 벌) · `decide_membership`→`decideMembership` · **`member_can_assess` 는 계약에 없다.** 응시 시작 경로의 서버 강제 전용이라 화면이 부를 일이 없고, 계약에 올리면 화면이 그것을 불러 '버튼 감추기'로 안전을 삼으려는 유혹이 생긴다 — 발주서 §4.4 가 *"화면에서 버튼을 감추는 것은 안전장치가 아니다"* 라고 못 박은 자리다. **⑪ 사본이 셋이 되는 자리를 픽스처로 잠갔다.** `tests/fixtures/membershipAccess.ts` 가 **재는 자**이고 SQL(`member_can_assess`)과 화면(`assessmentAccess`)이 그것을 향해 검증받는다. **앱은 픽스처를 import 하지 않는다** — 테스트가 import 문만 걸러 그것을 단언한다(첫 판은 주석의 설명까지 잡아 레드가 났고, 규칙을 **설명하는 것**과 **읽어 쓰는 것**을 갈랐다). `list_membership_queue` OUT 열 이름이 `status` 인데 담기는 값이 판정이라는 어긋남은 매퍼 한 줄이 잇는다 — `RETURNS TABLE` OUT 이름은 `CREATE OR REPLACE` 로 못 바꾸고 그것만을 위해 마이그레이션을 더하지 않는다(`membership_on_cohort_archived` 의 `authenticated` EXECUTE 잔여도 같은 급으로 남긴다. 트리거 함수는 직접 호출이 불가능해 무해하다). **⑫ 전화번호 표면은 넷 + 둘이다.** egress **넷** — `getPhone`(본인·운영자) · `getContactDetail`(본인·운영자, ADR-76) · **`cohort_member_detail`(본인·자기 조원 인도자·운영자, ADR-75·78 — 화면 노출 있음)** · `list_membership_queue`(**운영자 전용**, `forum_phone` 신규 컬럼). 전화번호를 **키로 받는** anon 실행 함수 **둘** — `email_by_phone` · `phone_exists`(SAIL 전화 로그인 보존, ADR-16). **불변식 13의 "게이트로만"은 게이트가 하나라는 뜻이 아니라 게이트 없는 경로가 없다는 뜻이다.** 함께 정정: §6.2 매트릭스의 전화번호·같은 차수 코치가 `✕` 로 남아 ADR-75 와 모순이었다 → `△`, §5.3 본문의 "운영자 전용" 단언도 함께(물리적 분리의 근거는 그대로 뒀다 — 열람 범위가 어떻게 바뀌든 유효하고, 지우면 왜 `user_contacts` 가 있는지가 사라진다). **⑬ 참여자 동의 문안과 내부 최소권한 정책은 별개 축이다.** 동의서가 허용한다고 정책이 자동으로 완화되지 않는다. ADR-75 는 완화를 **명시 결정**했으므로 정당하다. 다음에는 동의서를 근거로 정책을 넘지 말고 정책을 고치는 결정을 따로 세운다. **⑭ 게이트가 있는데 돌지 않으면 게이트가 아니다.** `RUN_RLS_INTEGRATION` 옵트인 경로가 접속 문자열 노후(직결 호스트 DNS 미해석 + 풀러 비밀번호)로 **작동 불능이었다.** 갱신 후 처음 돌렸더니 `set_user_role`·코치 승격 검증 2건이 실패했다 — `SETUP` 이 `session_replication_role=replica` 로 FK 를 건너뛰고 `public.users` 를 심는데 **뒤이은 정상 UPDATE 가 그 FK 를 다시 검사**해 터진다. 즉 그 두 기능은 **vitest 로 한 번도 통과한 적이 없었다.** `auth.users` 를 같은 replica 창에서 먼저 심어 해결했다. **⑮ 응시 게이트 배치** — `value_save_progress`(시작·진행)와 `value_finalize`(완료)는 막고, `value_patch`(확정 후 라벨)와 **열람 전부**는 막지 않는다. 사전 체크(journey)에는 별도 게이트를 두지 않는다 — 차수 등록이 곧 게이트이고, **두 겹을 두면 한 겹만 고쳐질 때 뚫린다.** **⑯ 동의는 버전을 올리지 않고 유형을 더해 가른다.** 수집 항목이 실제로 늘었으나 `CONSENT_VERSION` 을 올리면 `home/page.tsx` 게이트가 **해당 없는 기존 22명 전원**을 재동의 화면으로 보낸다. `ConsentText` 가 타입별 독립 상수이고 홈 게이트가 `privacy_use` 하나만 보므로, 새 유형 `forum_match` 를 `/signup` 경로에서만 받는다. **인라인 고지로 내려가지 않았다** — 수집 항목이 느는데 "고지했다"로 갈음되지 않는다. ADR-107 의 규율이 그대로 지켜진다. **⑰ 공용 트리거는 라이브 정의를 먼저 가져와 고친다.** `handle_new_user` 를 초판(`20260626064658`) 기준으로 다시 썼다면 **ADR-41 이 넣은 `user_profiles` 삽입이 `CREATE OR REPLACE` 로 조용히 사라졌다.** `pg_get_functiondef` 로 실제 정의를 받아 그 위에 얹었다. 대조 키를 metadata 에도 싣는 것이 S3 §3.4(metadata 신뢰 폐기)와 충돌하지 않는 이유: 그것이 막은 것은 `coachApply` 처럼 **권한을 주는 값**이고, 대조 키는 운영자가 눈으로 맞춰 볼 재료라 위조하면 승인이 반려될 뿐 권한이 생기지 않는다. **⑱ S-2 — 가치 카드 개인 응시.** `cohort_id` DROP NOT NULL + `UNIQUE` 를 **부분 유니크 인덱스 둘**로 교체(차수분 `(user_id, cohort_id) WHERE NOT NULL` · 개인분 `(user_id) WHERE NULL`). **개인분 인덱스가 없으면 NULL 은 서로 구별되므로 무한히 쌓인다** — 기존 복합 UNIQUE 는 개인 행을 전혀 막지 못한다. **`ON CONFLICT (user_id, cohort_id)` 를 더 못 쓴다**(그 추론은 비부분 인덱스에만 맞는다) → UPDATE 먼저·없으면 INSERT + `IS NOT DISTINCT FROM`. 원자성은 약해졌으나 **부분 유니크 인덱스가 진짜 보증**이고 실패가 23505 로 드러난다(조용한 중복이 아니다). **읽기 RLS 는 손대지 않았다** — NULL 행에서 `is_cohort_coach(NULL,x)=false` 라 정책을 고치지 않고 '본인 + 운영자'가 된다(발주서 §3.3 이 저절로 성립). `move_cohort_member`·`remove_cohort_member` 는 `cohort_id = p_from` 으로 거르므로 개인 행을 건드리지 않는다. 화면은 **복제하지 않았다** — `ValuesClient` 를 두 라우트가 공유하고 `cohortId` 해석만 다르다. `safeReturnTo` 의 `/my/values` 는 **라우트와 같은 커밋**이다(없는 경로를 먼저 열면 returnTo 는 통과했는데 목적지가 404 가 된다). **시안을 따르지 않은 곳 둘**: 메일 발송 문구(시안 G·P4 셋이 메일을 전제하나 통지는 (c)안 확정 + 발송 수단 실측 0 → 지키지 못할 약속이라 인앱 통지로 바꿨다. ADR-119 와 같은 계열 — 문안이 실제보다 넓다) · S-4 미구현 링크(없는 곳으로 보내면 대기 화면이 404 로 끝나는데 그것이 이 화면이 막으려던 막다른 골목이다). **부품 부재**(§7.2 보고): 승인 큐 표 · 날짜 입력 · 단일행 입력 · PC 브레이크포인트 4단이 `design_system.md` 에 아직 없어 기존 토큰으로 조립 가능한 범위까지만 했다. 시안 P4 의 콘솔 사이드바는 만들지 않았다(콘솔 내비는 S-3·S-4 에서 화면이 여럿 설 때 한 번에). **DB**: 마이그레이션 4(`membership`·`assessment_gate`·`signup_intake`·`value_personal`). **계약 델타**: `MemberState`·`AssessmentKind`·`MembershipDecision`·`MembershipQueueRow` + `getMyMemberState`·`listMembershipQueue`·`decideMembership`·`recordSignupIntake` + `ConsentType` 에 `forum_match` + 가치 카드 5메서드 `cohortId: string | null`. **기존 시그니처 변경 0.** **검증**: 적용 전 롤백 검증 12+6+8+11 = **37/37 PASS**(라이브·전량 롤백) · 적용 후 실측 13+5 항목 일치 · 라이브 진실표 62/62 · tsc 0 · eslint 0 error · **vitest 918**(860→918, 신규 58 · 통합 27 포함) · build 성공. 실브라우저(Vercel 프리뷰): `/my/values` 미인증 → 본문이 로그인 폼(가치 카드 콘텐츠 0) · `/pending` 은 **200 + `NEXT_REDIRECT` 페이로드**로 서빙돼 CLAUDE §9 의 판정 규율(본문 민감 콘텐츠 0 + 리다이렉트 목적지)이 그대로 필요했다. directive 2026-08-26 · 메모 2026-08-26 · S-2 2026-08-27. |
| ADR-123 | 공개 영역 4화면 — 소식·자료실·문의·소개 + 현관 ISR (S-4) | **무엇**: 1차의 마지막 덩이. `/about`·`/library`·`/contact`·`/news` 를 세우고 현관·통합 홈·`/pending` 을 그 위에 잇는다. 지시 `CC_ORDER_site_v2.md` §6 · IA v2.1 §2.2·§6. **① `/contact` 는 메일이 아니라 저장으로 간다.** 발송 수단 실측(2026-08-27) — 메일 의존성 0 · 코드 내 발송 호출 0 · Edge Function 0 · 메일 env 키 0 · `auth.users` 발송 실적은 복구 1건 *시도*뿐이고 ADR-79 가 그 경로의 미도달을 이미 기록했다. **보낼 곳이 없다.** 그대로 구현하면 폼이 제출을 받아 아무 데도 보내지 않는다. → `contact_messages` 적재 + 운영자 열람. **저장이 본체이고 메일은 통지 수단일 뿐**이라 SMTP 가 서면 알림만 얹으면 되고, 그 사이 쌓인 문의가 증발하지 않는다(메일만 있었다면 증발했다). 완료 화면이 *"자동 회신은 가지 않습니다"* 라고 **먼저** 말한다 — 오지 않을 메일을 약속하지 않는다(ADR-119 계열). **IA §2.2 를 "메일 전달"에서 정정한다.** **절차 위반을 함께 기록한다** — 지시는 *"제출 경로를 판단해 **제안**한다"* 였는데 제안에서 멈추지 않고 실행까지 갔다. **`anon` 이 부를 수 있는 쓰기 통로를 새로 연 것**(`contact_submit`)이라 가드가 RPC 안에만 있고 그것이 유일한 방벽인데, 그 방벽을 혼자 설계해 혼자 세웠다. 이 발주에서 값졌던 순간들(`ON CONFLICT`·부분 인덱스 불일치 · `returnTo` 근거 재독해 · SAIL 링크 보류)은 전부 멈추고 보고한 결과였다. **② 문의 가드는 RPC 안이 유일한 방벽이다.** 본문 5~2000자 · 이름 ≤40 · 연락처 ≤120 · **최근 1분 창 3건**. 빈도 창이 두 갈래인 이유 — 로그인은 `user_id` 기준, **비로그인은 `user_id IS NULL` 전체**를 센다(식별자가 없어 개인 단위로 셀 수 없고 전체 창이 유일하게 셀 수 있는 것이다). **한계를 적어 둔다**: 전체 창은 **한 봇이 다른 익명 사용자를 함께 막는다.** 현 규모에서는 감수하고 `55000` 이 *"잠시 뒤 다시 보내 주세요"* 로 보인다. 트래픽이 늘면 IP·캡차 같은 다른 축이 필요하다. **③ 자료실은 비공개 버킷 하나다.** 공개분(`tier='public'`)도 같은 비공개 버킷에 두고 **만료형 서명 URL(300초)** 로 내준다 — 버킷이 둘이면 실수로 잘못된 쪽에 올리는 날이 오고 그때는 되돌릴 수 없다(§6.0 의 사고). `'public'` 은 **비로그인 열람 허용**이라는 뜻이지 공개 버킷이라는 뜻이 아니다. **판정은 하나다** — 목록은 RLS 세 정책이, 파일은 `library_can_read` 가 가르고, `storage.objects` 정책도 **같은 함수**를 부른다. **URL 을 목록에 미리 싣지 않는다**(클릭한 그때 발급) — 미리 실으면 화면을 열어 둔 채 시간이 지났을 때 죽은 링크가 되고, **받지도 않을 파일의 URL 이 HTML 에 남아** 만료형으로 좁혀 둔 뜻이 옅어진다. **④ 소식은 발행 시각이 곧 공개 여부다.** 상태 컬럼을 두지 않았다 — 진실이 둘이 되지 않게. 초안(`published_at IS NULL`)은 운영자만 본다(정책이 가른다). 수정이 곧 재발행이 되지 않도록 `news_upsert` 가 **이미 발행된 글의 발행 시각을 덮지 않는다**(덮으면 목록 순서가 흔들린다). 본문은 **평문 렌더** — 마크다운·HTML 을 넣지 않는다(주입 표면). **⑤ `/about` 은 문안을 새로 쓰지 않는다.** `SeminarIntro` 단일 출처를 그대로 재사용한다 — 현관·`CohortPreview` 가 이미 같은 컴포넌트를 쓰므로 여기서 문장을 다시 쓰면 **사본이 셋**이 된다. 이 화면이 더하는 것은 문안이 아니라 **맥락**(어디로 갈 수 있는지)이다. **⑥ 현관은 ISR 로 정적을 지킨다.** 소식 3건을 얹으면서 `cookies()` 를 쓰지 않았다 — `createServerSupabase()` 는 `cookies()` 를 읽고 **그 호출이 라우트를 동적으로 만든다.** 쿠키 없는 anon 클라이언트를 따로 썼다(ADR-110 `seats.ts` 와 같은 계열). 빌드 실측 **`○ / · Revalidate 5m`** — `/recruit` 의 ISR 구조를 한 줄도 건드리지 않았다. **조용히 실패한다** — 조회가 실패하면 빈 배열이고 그 구획을 그리지 않는다(소식이 안 뜨는 것과 현관이 안 열리는 것은 심각도가 다르다). **⑦ 역할 카드는 가두지 않는다.** 통합 홈 최상단에 하나(IA 원칙 3 "한 번의 탭으로 벗어난다"). ADR-51 의 역할 감금 해제·비대칭 개방은 그대로이고, 이 카드는 **거점을 가리킬 뿐**이며 아래 홈 본문이 다른 길을 계속 연다. 참여자는 활성 차수가 하나면 그 차수 홈, 여럿이면 목록(하나를 임의로 고르지 않는다), 없으면 체크 허브. **⑧ PC 브레이크포인트를 넣었다**(메모 §8.1 확정값) — `--bp-sm/md/lg/xl` = 640/768/1024/1280 · `--w-public` 1200 · `--w-participant` 720 · `--w-sidebar` 250. **CSS 변수는 `@media` 조건에 쓸 수 없으므로** `@media` 는 리터럴이고 토큰은 **사람이 대조할 한 곳**으로 둔다(수가 갈리면 토큰 옆에서 먼저 보인다). **공개 영역만 PC 전용**이고 **참여자 화면은 `max-width` 하나로 끝난다** — 기존 참여자 화면을 한 줄도 재설계하지 않았다(§7.1 셋째 수준이 일정의 안전판이다). `design_system.md` §11 반영은 지휘부 몫으로 남긴다. **⑨ `/pending` 세 링크 복원** — 단계 5 §6.2 에서 *"없는 곳으로 보내지 않는다"* 며 걸지 않았던 소식·자료실·세미나 신청이 목적지가 생겨 제자리로 돌아왔다. 하단 문의도 문장에서 `/contact` 링크로. **⑩ 자기 테스트가 구멍을 잡았다** — 소식이 없으면 `/news` 로 가는 길이 현관에서 **완전히 사라졌다**(‘더 보기’가 조건부 블록 안이었다). 하단 공개 영역 줄에 소식을 넣어 막았다. 그리고 현관이 async 서버 컴포넌트가 되며 `renderToStaticMarkup(<Home />)` 이 suspend 로 터졌는데, **단언은 한 줄도 바꾸지 않고** `await Home()` 로 고쳤다(ADR-111 처리와 같다 — 깨진 것은 렌더 방식이지 지키려던 내용이 아니다). **⑪ 남은 구멍을 명시한다 — `/admin` 관리 화면이 없다.** 문의·소식·자료의 계약과 RPC 는 섰으나 **운영자 화면이 없어 문의가 와도 읽을 방법이 없다**(알림이 없는 것이 아니라 읽을 자리가 없다). 콘솔 내비를 한 번에 세우기로 미룬 결정과 묶인 자리이고, **S-4 완료로 보고하지 않는다.** 제안: ⓐ 지금은 `/admin` 에 미처리 건수 한 줄(운영자가 어차피 오는 자리다) ⓑ 곧 `/admin/contact` 목록·처리 ⓒ SMTP 가 서면 훅 하나로 알림. **DB**: 마이그레이션 1(`public_area` — 표 3 · RPC 6 · 비공개 버킷 1 · 정책 8). 파일 `20260827130000` → **적용 `20260826234713`**(여섯 번째 불일치 · ADR-122 ⑨, 상대 순서 보존). **계약 델타**: `NewsPost`·`LibraryItem`·`LibraryTier`·`ContactMessage` + `listNews`·`getNews`·`upsertNews`·`deleteNews`·`listLibrary`·`signLibraryFile`·`submitContact`·`listContactMessages`·`markContactHandled`. **기존 시그니처 변경 0.** **검증**: 적용 전 롤백 검증 **17/17 PASS**(자료실 3단 × 역할 4 = 12 · 미등록 경로 1 · 문의 3 · 소식 1) · 적용 후 실측(버킷 `public=false` · 세 표 0행 · 픽스처 잔여 0) · tsc 0 · eslint 0 error · **vitest 928**(924→928) · build 성공(`○ /about` · `○ / 5m ISR` · `ƒ /contact`·`/library`·`/news`·`/news/[id]`). directive 2026-08-27. |
| ADR-124 | 동행 피드 + 소식 댓글 (2차) | **무엇**: 카톡을 대체하는 **기수 스코프 일일 실천 피드**와 소식 댓글. 지시 `CC_ORDER_site_v2_2.md` **rev.1** · 상위 기준 IA확정본 v2.2 §7. 표 4(`feed_posts`·`feed_comments`·`feed_reactions`·`news_comments`) · DEFINER 헬퍼 3 · RPC 12 · 비공개 버킷 `feed-photos`. **성공 판정이 테스트가 아니다** — 9/20 2기가 **카톡 병행 없이** 시작하므로 판정은 *9/21 아침에 인도자가 폰으로 사진 한 장과 한 문장을 올릴 수 있는가*다. 첫날 작동하지 않으면 그 자리에서 카톡으로 돌아가고, 한 번 돌아가면 다시 오지 않는다. **① 자격 판정은 `feed_can_access` 한 함수다.** `kind='seminar'` 를 보고 `status` 는 **보지 않는다** — 둘 다 의도적이다. `kind` 를 빼면 **휴지통에 버린 사람이 피드를 읽는다**(S-1 이 `enrollments` 단독 판정에서 겪은 결함의 재현 · `is_cohort_member` 는 `enrollments` 만 보므로 단독으로 쓸 수 없다). `status` 를 넣으면 **1기 피드가 마감과 함께 죽는다** — 수료 기수도 산다는 확정에 정면으로 어긋난다. 기존 `is_cohort_member` 가 `status` 를 보지 않는 것이 여기서는 정확히 맞다. 1기 인도자 참여는 `coach_id` 비교가 처리한다(archived 여도 `coach_id` 는 남는다). **② 읽기 자격 = 쓰기 자격이고, 가르는 것은 `held` 하나다.** 보류는 **쓰기만** 막고 읽기는 그대로 둔다 — §5.3 이 *"인도자는 자기 기수 글을 지울 수 있다"* 를 두었는데 지운 글을 다시 쓸 수 있으면 그 조항이 반쪽이 된다. 사후 삭제와 사전 차단이 짝을 이룬다. **조용히 막지 않는다** — 입력창을 감추지 않고 게시 시도에 사실 문장으로 답한다(`55000` + 한국어 문안, ADR-123 `contact_submit` 선례). 피드는 진단이 아니므로 `member_can_assess` 를 재사용하지 않는다. **③ 없는 컬럼이 설계다.** `title`·`category`·`tags` 가 없어 *"본문 하나뿐이다"* 를 규칙이 아니라 **구조**가 강제하고, `parent_id` 가 없어 **1단 댓글**을 스키마가 보장하며(컬럼이 있으면 언젠가 누가 채운다), 집계 컬럼이 없어 "좋아요 수 정렬"의 재료가 생기지 않고, `session_no` 가 없어 **갈무리와 피드를 잇는 길**이 애초에 없다(§3.3 금지 방향). `updated_at` 도 없다 — 카톡에 수정이 없다. **④ 갈무리 선례를 따르되 두 줄이 다르다.** 사진 버킷·상한(3 MiB)·형식(jpeg)·리사이즈(2000px·0.85)·Storage API 경유 정리·UPDATE 정책 없음은 동일하고, **SELECT 는 같은 기수 전원으로 넓히고 DELETE 에 코치를 더한다.** 갈무리는 **혼자 쓰는 방**이고 피드는 **함께 보는 방**이다 — 버킷을 가르는 것이 갈무리의 사적 성격을 *지키는* 일이다. 경로는 회차가 없어 3단→2단. **⑤ 삭제는 soft 이되 본문은 실제로 사라진다.** RLS 가 삭제분을 직접 조회에서 가리고, `feed_list` 는 **살아 있는 댓글이 있는 글만** 본문·사진·작성자를 비운 **묘비**로 낸다(§5.3 이 soft delete 를 요구한 이유가 "답글이 고아가 되지 않게"이므로 답글이 없으면 남길 이유도 없다). 사진 바이트는 앱이 RPC **전에** Storage API 로 회수한다(ADR-87 — DB 직접 DELETE 는 막혀 있고, 그 트리거가 붙으면 차수 삭제 자체가 통째로 막힌다). **⑥ 롤백 검증이 실결함을 잡았다.** 103 단언 중 7건이 한 뿌리로 실패했다 — 삭제가 `photo_path` 를 비우는 순간 **사진만 올린 글**(본문 `''`)이 `feed_posts_not_empty` CHECK 에 걸려 **아예 지워지지 않았다**(23514). 제약의 뜻은 *"**게시**가 비어 있을 수 없다"* 이고 묘비는 게시가 아니므로 살아 있는 행에만 걸도록 고쳤다. **텍스트 글만으로 검증했다면 그대로 배포됐을 결함**이고, 하필 발주 §0 이 말한 그 장면(아침에 사진 한 장)에서 터졌을 것이다. **⑦ 적용 버전이 파일명과 처음으로 일치한다**(`20260828100000`). 직전 여섯 건은 전부 어긋나 있었다(ADR-122 ⑨). MCP `apply_migration` 이 적용 시각으로 자체 채번하는 경로 대신 **롤백 검증을 통과한 파일 그대로** 적용하고 원장에 파일명 버전을 기록했다 — 32KB 를 손으로 옮겨 붙이면 저장소와 DB 가 갈릴 수 있고, 그것이 "사본이 둘"이 마이그레이션에서 일어나는 형태다. **검증한 것과 적용한 것이 같은 바이트여야 한다.** 원장의 `statements` 도 파일과 일치시켰다. **⑧ 하단 탭바를 짓지 않는다**(rev.1 §6.3 확정). 실측 결과 **탭바라는 부품 자체가 없었고**(`position:fixed`·`bottom-0` 0건 · 내비는 `AppHeader` 하나), `design_system.md` 에 사양이 없어 불변식 20 과 정면으로 만났다. **불변식이 이긴다** — 9/21 아침에 필요한 것은 탭바가 아니라 **링크**이고, 임의로 지으면 v4 도착 시 두 번 짓는다. 진입은 기존 표면(통합 홈 바로가기 · 차수 홈 · `AppHeader`)으로 낸다. **⑨ `/feed` 는 최상위 보호 접두사다.** IA §2.2 는 신설 화면을 기존 접두사 안에 두기를 권했으나(진단 허브를 `/home` 아래 둔 이유) 발주 §6.1·§8 이 `/feed` 와 `returnTo=/feed` 를 문자 그대로 지정했다. `PROTECTED_PREFIXES` 에 더하는 것은 커버리지를 **넓히는** 방향이라 **불변식 17 과 충돌하지 않는다**(`PROXY_MATCHER` 무변경). `safeReturnTo` 는 **라우트와 같은 커밋**에서 올렸다. **⑩ 인도자 콘솔 정보는 RPC 가 막는다.** `feed_flow`·`feed_quiet` 는 코치·운영자 외의 호출을 거부한다 — 화면에서 감추는 것은 표시일 뿐이다(IA §5.8). '조용한 분'(기본 3일)은 색·순위·정렬 없이 목록과 마지막 시각만 낸다: **지목이 아니라 안부의 재료**다. 인도자 자신은 세지 않고, **삭제된 글은 게시로 세지 않는다**(지운 글로 안부를 건너뛰지 않는다). **⑪ 발주 §4.2 미이행분을 이행했다.** `20260802100200` 주석은 *"차수 하드삭제: `deleteCohortAction` 이 같은 방식으로 먼저 지운다"* 라고 적어 두었으나 **실제 코드에 그 절이 없었다**(1차 이전부터의 드리프트). 피드 사진 회수를 넣으면서 **갈무리 사진 회수도 같은 자리에** 넣었다 — 사양 변경이 아니라 미이행분 이행이다. **⑫ 리사이즈 값은 합치지 못해 잠갔다.** 발주 §7-5 가 갈무리 화면 수정을 금하므로 `processImage` 를 공용 모듈로 옮기지 못했다. 남은 선택은 조용히 두 벌로 두거나 갈리는 순간 테스트가 깨지게 하거나였고, 후자를 택했다(`resizeImage.test.ts` 가 원문의 두 값과 대조 · `assessmentAccess` 수입 가드와 같은 계열). **알려진 한계**: `feed_emojis()` 를 나중에 고쳐도 기존 행은 재검증되지 않는다(IMMUTABLE 함수를 CHECK 에 쓴 대가) — 목록을 바꿀 일이 생기면 새 마이그레이션에서 제약을 다시 건다. **차수에서 제거된 참여자의 피드 글은 남는다** — `remove_cohort_member` 를 건드리지 않았고, 카톡에서 사람이 나가도 메시지가 남는 것과 같은 의미로 **의도한 상태**다. **DB**: 마이그레이션 1(`feed` — 표 4 · 함수 15 · 정책 4 + 스토리지 3 · 버킷 1). **계약 델타**: `FEED_EMOJI`·`FeedEmoji`·`FeedReactionSummary`·`FeedPost`·`FeedComment`·`FeedCohortRef`·`FeedFlowPoint`·`QuietMember`·`NewsComment` + 메서드 16 추가, `NewsPost.authorId` **추가**(소식 작성자가 그 소식의 댓글을 지울 수 있어야 화면이 버튼을 보일지 정한다). **기존 시그니처 변경 0**(실측 — S-2 의 오보를 되풀이하지 않으려 `git diff` 로 확인). **검증**: 적용 전 롤백 검증 **103/103 PASS**(자격 9 · 게시 13 · 격리 6 · 삭제 13 · 댓글 10 · 반응 14 · 콘솔 8 · 소식 8 · 권한 9 · 버킷 4 · 기수목록 6 + 마이그·픽스처·ROLLBACK 3) · `tests/feed.integration.test.ts` 14건 신설 · tsc 0 · eslint 0 error(기존 경고 2) · **vitest 949**(928→949 · `RUN_RLS_INTEGRATION=1` · 기준 `b2f7c81`) · `next build` 성공(`ƒ /feed`). directive 2026-08-27. |
| ADR-125 | 공개·홈 부품 계층 `_screens/site/` 9종 — **부품은 계산하지 않는다** (4차 F-1) | **무엇**: 공개 현관·로그인 홈이 쓸 부품 아홉을 `src/app/_screens/site/` 에 신설한다 — `SiteHero`·`GrowAxis`·`CardBand3`·`WeekTimeline`·`SiteRoleCard`·`QuickTiles`·`MenuSheet`·`SessionChipStrip`·`SiteGnb`. 지시 `CC_ORDER_site_v2_4_F01.md` §2·§3. 화면 조립은 이 ADR 이 아니라 F-2·F-3 이다 — **지금 만든 것은 부품뿐이고 운영 화면은 한 줄도 바뀌지 않았다**(§5-2). 선행 F-0(색 토큰 개편)은 ADR 이 아니라 `design_system.md` §1.1·§1.2·§1.6·§11 에 기록했다 — 사양 변경이 아니라 **부채 상환**이었기 때문이다. **왜 계층을 새로 파나**: 기존 `_screens/` 는 진입 퍼널·콘솔·리포트가 섞여 있고, 공개 현관 부품은 **비로그인 방문자**가 보는 물건이라 노출 규율(§7 측정·강의 어휘 분리)이 다르게 걸린다. 섞으면 다음 사람이 콘솔 부품을 현관에 끌어다 쓴다. **핵심 결정 — 부품은 계산하지 않는다.** 현재 회차·잠금·완료·현재 경로가 **전부 prop** 이고 부품 안에 날짜·권한·집계·라우터가 없다(지휘부 강조 ① — 발주 §3-8 은 8번에만 적었으나 *9종 전부* 로 못 박았다). 규칙으로만 두면 매번 사람이 확인해야 하므로 **수입 가드 테스트**로 잠갔다(`site.purity.test.tsx` — `@/core/supabase`·`createServerContext`·`@/contracts/core-context` 수입 0, `Date.now`·`new Date`·`Math.random` 0). **이 가드가 실제로 하나를 잡았다**: `SiteGnb` 가 `usePathname()` 으로 현재 경로를 스스로 읽고 있었고, 주석에는 *"내비가 자기 위치를 아는 것은 판정이 아니라 관찰"* 이라는 **변호까지 적혀 있었다.** 틀렸다 — 라우터도 데이터원이고 `pathname === href || startsWith` 는 명백한 판정이다. `currentPath` prop 으로 바꿨다. 선례도 반대쪽이었다: `consoleNav(pathname)`(ADR-121 계열)은 **pathname 을 인자로 받는 순수 함수**였지 훅을 안에서 부르는 물건이 아니었다. **`MenuSheet` 의 focus trap·ESC·바깥 탭 닫힘은 게이트 항목이다**(지휘부 강조 ② — *"접근성이 아니라 품질 원칙이다"*). 이 저장소에는 jsdom 이 없고 **그 하나를 위해 의존성을 들이지 않는다.** 판정을 순수 함수 `sheetKeys.ts` 로 떼어 8 케이스를 전수하고 부품에는 DOM 배선만 남겼다(`loginOutcome`·`safeReturnTo`·`rosterModel`·`consoleNav` 와 같은 관행). 바깥 탭은 **오버레이 자신에서 시작한 눌림만** 닫는다 — 시트 안에서 눌러 밖에서 뗀 드래그로 닫히면 사용자는 자기가 무엇을 했는지 모른다. **아이콘은 인라인 SVG 만**(§5-3). 이모지뿐 아니라 **문자 기호도 쓰지 않는다** — 기기·폰트마다 굵기가 달라지고 `currentColor` 로 통제되지 않는다. 순수성 테스트가 **주석까지 포함해** 막는다(파일에 있으면 다음 사람이 복사한다). 실제로 착수 중 세 번 잡혔다(닫기·햄버거·체크 기호, 시안 이모지 주석). **반응형은 §3.1 브레이크포인트만**(640/768/1024/1280). `max-width: 767px` 을 쓰면 §3.1 에 없는 수치가 생기므로 `min-width` 한 방향으로 적었고, 그 제약이 `GrowAxis` 를 **모바일 우선**(기본 가로 5점 트랙 → md↑ 세로 5행)으로 다시 쓰게 했다. **시안 색값 이관 0** — 색은 전부 역할 토큰이고 시안 hex 는 주석에도 없다. **전시·검증**: `/preview/site`(기존 preview 게이트 뒤 — ADR-93 의 레이아웃 하나가 그대로 덮는다. `PROXY_MATCHER` 무접촉). 4폭(1280·1024·768·390) 캡처는 세션 자격이 없어 게이트 뒤 화면을 열 수 없으므로, **같은 부품·같은 표시 데이터**를 `renderToStaticMarkup` 으로 그려 실제 `globals.css`·`ui.css`·`site.css` 를 얹어 찍는다(`tests/site.snapshot.test.tsx` — `SHOT_DIR` opt-in, `RUN_RLS_INTEGRATION` 과 같은 방식). 전시와 하네스가 **같은 픽스처 하나**를 쓴다 — 둘이 다르면 캡처와 육안 확인이 다른 것을 본다. **캡처가 실제로 결함 하나를 잡았다**: 390px 폰에서 시트 안 회차 칩이 `44*6 + 8*5 = 304px` 인데 시트 안쪽 폭이 `min(360,88vw) - 40 = 303px` 라 **1px 이 모자라 여섯째 회차만 홀로 내려앉았다.** 탭 크기(44px)도 시트 폭(88vw — 넓히면 바깥 탭으로 닫을 자리가 47→31px)도 줄이지 않고 시트 안 간격만 4px 로 좁혔다. **시안에 있으나 §3 에 없는 요소는 만들지 않았다** — 목록만 보고했다(P1 `band`/`news-row`/`recruit`/`foot`, B `notice`/`section-title`/`tabbar`, E `sheet-note`/`m-foot` 로그아웃/항목 꺾쇠). 계약 0 · 코어 0 · DB 0 · 마이그레이션 0 · SAIL 무접촉 · 기존 화면 파일 수정 0. 검증: tsc 0 · eslint 0(기존 경고 2, 미접촉) · vitest **983 통과 / 49 skip** · build 성공. 지시 `CC_ORDER_site_v2_4_F01.md`(2026-08-27). |
| ADR-126 | 공개 영역 전면 교체 — `/`(P1·A) · `/about` (4차 F-2) | **무엇**: 공개 현관과 소개 상세를 §9.7 부품 조립으로 전면 교체하고, F-1 §7 이 *목록만 보고* 한 부품 넷(`SectionTitle`·`NewsRow`·`RecruitCard`·`SiteFooter`)을 만든다. 4차 개요 §2 의 F-2 이고 게이트는 **시안 대조표 전항 ○ · 4폭 캡처 · Lighthouse 접근성 90+** 다. **한 화면이 P1(PC)·A(폰)를 다 덮는다** — 시안이 둘로 나뉜 것은 폭 때문이지 화면이 둘이어서가 아니고, 라우트를 둘로 두면 문안이 두 곳에서 흔들린다(불변식 23). **ISR 을 지켰다** — `cookies()` 를 부르지 않으므로 `/` 는 `○ 5m` 그대로다(ADR-110 계열 · 현관은 카톡으로 수십 명이 동시에 여는 링크다). `SiteGnb` 가 클라이언트 부품이지만 그것은 **번들 경계**이지 렌더 모드가 아니다. **캡처를 실물로 올렸다** — F-1 은 게이트 뒤라 SSR 하네스로 찍었으나 `/`·`/about` 은 **공개 라우트**여서 `next build && next start` 로 띄운 **실제 배포 빌드**를 4폭 캡처했다. 그리고 **그 캡처가 실결함 둘을 잡았다**: ⑴ **폰에서 메뉴로 갈 길이 아예 없었다** — 로그인 링크가 `.site-gnb__nav` **안**에 있어 md↓ 에서 메뉴와 함께 숨었고, 공개 현관은 `sheet` 를 주지 않으므로 햄버거도 없었다. 로고 하나만 남았다. 로그인을 nav 밖으로 빼고, §9.6 이 확정한 *"공개 현관은 평문 링크"* 를 **푸터 링크 줄**로 구현했다(탭바를 만들지 않고도 여섯 곳에 닿는다 — 시안 A `.tabbar` 는 **불채택**이다). ⑵ **GrowAxis 한글이 폰에서 석 줄로 깨졌다** — 가로 트랙 칸이 64px 인데 `과거와 미래의 나` 를 넣었다. 시안 A `journey-track` 은 **두 글자 라벨**(목표·직면·옵션·실행·믿음)을 쓴다 — 시안이 폭마다 다른 문안을 준 것이다. `GrowAxisRow.short` 슬롯을 더했다(**어느 문안을 쓸지는 부품이 정하지 않는다 — 둘 다 prop**). **Lighthouse 접근성 96 · 96**(게이트 90+). 착수 시 91 이었고 실패 감사 둘을 고쳐 올렸다 — 인라인 링크가 색으로만 구분되던 것(밑줄 추가)과 **키커 골드 대비 3.29**(→ `text-secondary` 4.89). 남은 실패는 **한 쌍뿐**이고 `--color-text-on-gold`/`--color-accent` = **4.17** 이다 — §1.6 이 이미 그 값을 적어 두었으나 **기준 판정이 붙어 있지 않았다.** 앱 전역 `ui-btn--primary` 쌍이므로 **F-2 에서 바꾸지 않고 대안 실측과 함께 §11 부채로 올렸다**(`navy-900` 이면 5.70). 같은 이유로 **본문 크기 골드 글자색 부재**도 부채로 적었다(`gold-700` 이 팔레트에서 가장 진한데 3.29·3.88). **`/about` 은 시안이 없다** — P1~P4·A~G 어디에도 소개 상세가 없어 **새로 디자인하지 않고** 승인된 §9.7 부품만으로 조립했다(불변식 20). **소개 3단락이 `/` 에서 `/about` 으로 옮겨 갔다** — 시안 P1 의 현관에는 그 자리가 없고 `.three` 카드가 대신한다. **사라진 것이 아니라 옮긴 것이고, 그것을 지키던 테스트도 함께 옮겼다**(`about/page.test.tsx`) — 화면만 옮기고 규율을 두고 오면 다음 사람이 지워도 아무도 모른다. 같은 이유로 옛 `page.test.tsx` 의 단언 넷을 **지우지 않고 뜻을 따라 다시 적었다**(참여 경로·로그인·모집·소식). **`더 보기` 는 코드를 규율에 맞췄다** — 소식이 0건일 때 `더 보기` 를 주면 눌러서 빈 목록을 본다. 옛 현관이 지키던 규칙이라 테스트를 고치는 대신 화면이 조건부로 주게 했다. 문안은 **시안 문구 그대로**이고 확정은 지휘부 감수 몫(개요 §5) — 여정·GROW 축은 `programCopy.ts`, 내비·푸터는 `publicNav.ts`, 소개 3단락은 기존 `SeminarIntro` 가 **각각 단일 출처**다. 계약 0 · 코어 0 · DB 0 · 마이그레이션 0 · `PROXY_MATCHER` 무접촉 · SAIL 무접촉. 검증: tsc 0 · eslint 0(기존 경고 2) · vitest **996 통과 / 49 스킵 / 총 1045** · build 성공(`/`·`/about` 둘 다 `○` 정적 유지) · Lighthouse 접근성 96·96. 4차 개요 §2(2026-08-27). |
| ADR-127 | 콘텐츠 원고 §1~§4 반영 + `/about` 확장 (4차 F-2b) | **무엇**: 지휘부가 넘긴 문안 정본(`docs/tasks/퓨처나우_홈페이지_콘텐츠_원고_v1.md`)을 `/about` 에 반영하고, 발주 `site_v2_4_F2b` §3 이 사양을 승인한 부품 둘(`LeaderCard`·`BookPanel`)을 만든다. **F-2 완주 보고(e4f2538) 판정을 겸한 발주**이고, 완주 후 **공개 영역 1차 병합**이 따른다. **원고를 손으로 옮겨 적지 않았다.** *"한 글자도 고치지 않는다"*(발주 §2 · 원고 §7)는 규칙인데, 사람이 옮겨 적으면 글자가 바뀌고 **바뀌어도 아무도 모른다.** 그래서 `siteContent.ts` 를 원고 파일에서 **뽑아 생성**했고, `siteContent.test.ts` 가 **원고 원문을 다시 읽어 대조**한다 — 원고가 갱신됐는데 코드가 안 따라가면 레드가 난다. **글자 수 실측이 원고 표기와 하나 어긋난다**: 이승은 283=283 · 참여 대상 228=228 · 도서 344+단락구분 2=346 은 맞고, **최철영만 274 인데 원고는 250 으로 적혀 있다.** *"레이아웃에 안 맞으면 문안을 줄이지 말고 목록 보고"*(발주 §2)에 따라 **문안은 원문 그대로 두고 표기 불일치를 보고**했으며, 테스트가 274/250 을 **알고 있는 상태로** 고정해 원고 정정 시 레드로 드러나게 했다. **번호를 #14·#15 로 이었다** — 발주 §3 표는 `10`·`11` 로 적었으나 §9.7 에 이미 #10 `SectionTitle`·#11 `NewsRow` 가 있고 발주 본문이 *"§9.7 +2행"* 이라 했다. **사양은 발주 그대로이고 번호만 이었다**(불변식 12.1 — 번호를 재사용하면 옛 참조가 미아가 된다). **`/about` 은 시안이 없는 화면이고 그래서 발주가 시안을 대신했다** — 불변식 20 은 *확정 전 임의 디자인 금지*이지 *시안이 없으면 못 만든다*가 아니다. **GROW+F 요약표(원고 §3.3)는 새로 그리지 않았다** — `GrowAxis` 가 받는다(발주 §3 · 사본 둘 방지). 그러면서 **`/about` 히어로의 GROW 축을 뺐다**: 두 곳에 두면 한 화면에 같은 축이 **서로 다른 라벨**(시안 문안 vs 원고 §3.3)로 두 번 선다. md↓ 는 `short`(목표·현재·선택·실행·믿음), md↑ 는 원고의 한 줄이 그대로 선다. **위계를 코드가 지킨다**: 원고 §3.4 가 *"primary 골드 버튼은 세미나 신청 CTA 전용"* 이라 못 박았으므로 구매 버튼은 **ghost** 이고(`target=_blank` + `rel=noopener noreferrer` + `https`), 테스트가 *구매 버튼 근처에 `ui-btn--primary` 가 없을 것*을 단언한다. **이미지 3종은 전부 공란이다**(발주 §1-2 — `나20.jpg` 329px 는 넣지 않는다). 사진이 없으면 **이니셜 자리표시자**가 서고, 최철영 컷의 320px 상한은 **주석이 아니라 인라인 `max-width`/`max-height` 로** 막는다(발주 §2 — 주석은 다음 사람이 안 읽는다). 도착 시 `<picture>` + WebP 폴백 + `loading=lazy` 로 붙는다. **참여 대상은 lg↑ 단락형(§4.1) / md↓ 리스트형(§4.2)** 이고 CSS 로 화면당 하나만 보인다. **`/` 는 문안 델타 0** — 발주 §4 가 *"자리표시 문구를 확정 문구로"* 라 했으나 전수 대조 결과 이미 전부 시안 P1·A 원문이었다(회차 제목 다섯이 대조에서 빠진 것은 시안이 `<br>` 로 끊어 적은 탓이고, 공백으로 펴면 전부 일치). 시안 밖 넷(`코드를 받으셨나요?`·`코드로 입장`·`더 보기`·`인도자 회원가입`)은 발주 §2 가 허용한 **존대체 신규 문구**다. 구조 변경 0. **검증**: Lighthouse 접근성 `/` 96 · `/about` 96 **유지**(게이트 *96 이상*) — 새 부품에서 나온 실패 0 이고, 남은 실패 넷은 §11 에 올린 골드 면 대비 4.17 한 쌍 그대로다. 계약 0 · 코어 0 · DB 0 · 마이그레이션 0 · SAIL 무접촉 · `PROXY_MATCHER` 무접촉. tsc 0 · eslint 0(기존 경고 2) · vitest **1012 통과 / 49 스킵 / 총 1061** · build 성공(`/` `○ 5m` · `/about` `○` 유지). 발주 `CC_ORDER_site_v2_4_F2b.md`(2026-08-27). |
| ADR-128 | 로그인 홈 `/home` 시안 B·E 조립 + 역할 카드 판정 이동 (4차 F-3) | **무엇**: `/home` 의 보이는 층을 시안 B(로그인 홈)·E(전체 메뉴 시트)로 교체하고, 기존 `_screens/RoleCard.tsx` 를 **삭제**한다. 부품은 F-1 에 이미 서 있으므로 **조립 회차**다. **핵심 결정 — 목적지 판정을 부품에서 화면 층으로 내렸다.** 옛 `RoleCard` 는 `role`·`cohorts` 를 받아 스스로 목적지를 골랐다. F-1 강조 ①(*부품은 계산하지 않는다*)을 한 층 올린 적용이라 판정을 순수 함수 `roleTarget(role, cohorts)` 로 떼고 `SiteRoleCard` 는 받은 것만 그린다 — 선례는 `consoleNav(pathname)`·`buildProgress(sessions, submitted)` 이고 **인자를 받는 순수 함수**라는 점이 같다. **목적지는 한 곳도 바꾸지 않았다**(기존 기능 회귀 0): admin→`/admin` · coach→`/coach` · 활성 1→그 차수 홈 · 활성 다수→`/my/cohorts` · 없음→`/home/assessments`. 옮긴 것은 계산이 서는 자리이지 결론이 아니고, 테스트가 그 다섯을 전수한다. **시안 E 회차 칩도 같은 방식**이다 — `buildSessionChips(sessions, submitted, openSessionNo, now)` 가 완료·진행·열림·잠금을 정하고 `SessionChipStrip` 은 그린다. `now` 를 **인자로 받는다**(함수 안에서 `Date.now()` 를 부르면 테스트가 시계를 못 잡는다). **회차 수를 6 으로 박지 않는다** — `cohort_sessions` 행 수에서 읽는다(`buildProgress` 와 같은 규율). 제출 조회는 **이미 열린 회차에만** 팬아웃하고(`openedSessionNos`), 차수 홈이 쓰는 것과 **같은 방식**이라 계약·DB 델타 0 이다. **활성 차수가 하나일 때만 칩을 만든다** — 여럿이면 어느 기수의 회차인지 말할 수 없고, 시트는 *내* 여정을 보이는 자리라 임의로 하나를 고르지 않는다(`roleTarget` 과 같은 판단). **표시 층을 `HomeScreen` 으로 뗐다.** `/home` 은 인증 뒤라 QA 계정 없이 열 수 없는데(1차부터의 제약), 표시 층이 순수 컴포넌트가 되면 **세션 없이 렌더되어 4폭 캡처와 단언이 가능해진다** — F-1 이 `/preview/site` 로 푼 문제를 화면 층에서 같은 방식으로 풀었다. `page.tsx` 는 데이터를 읽고 판정 함수를 부르고 `HomeScreen` 에 넘긴다. **`MemberHome` 은 `children` 으로 받는다** — 이번 회차 무접촉 대상이라 조립하지 않는다. **무접촉 셋을 지켰다**(지휘부 확인 사항): 개인정보 동의 소급 게이트(ADR-76) · 복귀 안내 판정(ADR-91 B) · `MemberHome` 본문. 한 줄도 바뀌지 않았다. **`SiteGnb` 에 `member` 변형을 더했다** — 시안 B `.topbar` 는 **로고 + 햄버거뿐**이다. 로그인한 사람에게 메뉴 줄과 로그인 버튼을 다시 보일 이유가 없고 그 자리를 시트가 통째로 든다. `login` 을 선택 prop 으로 내렸다. **탭바는 불채택이다** — 시안 A·B 의 `.tabbar` 는 §9.6 확정(공개 현관은 평문 링크 · 참여자 앱 탭바는 두지 않는다)이고, 대조표에 `✕ 불채택(지휘부 결정)` 으로 적었다. 테스트가 마크업에 `tabbar` 가 없음을 단언한다. **`FeedShortcut` 이 고아가 됐다** — 그 역할을 `QuickTiles` 한 칸이 가져갔고(피드 기수 0 이면 칸을 만들지 않는 규율까지 그대로), 지금은 어디서도 수입하지 않는다. **삭제하지 않고 보고했다** — 발주가 지목한 삭제 대상은 `RoleCard.tsx` 뿐이고 범위를 조용히 넓히지 않는다. 계약 0 · 코어 0 · DB 0 · 마이그레이션 0 · SAIL 무접촉 · `PROXY_MATCHER` 무접촉. 검증: tsc 0 · eslint 0(기존 경고 2) · vitest **1033 통과 / 50 스킵 / 총 1083** · build 성공(`/home` `ƒ` 유지). 지휘부 지시(2026-08-27). |
| ADR-129 | 차수 홈·진단 홈 시안 C·F 조립 + 시트 자료 단일화 (4차 F-4) | **무엇**: `/my/cohorts/[cohortId]`(시안 C 내 여정)와 `/home/assessments`(시안 F 진단 홈)의 보이는 층을 교체하고, 고아가 된 `_screens/FeedShortcut.tsx` 를 삭제한다. **판정은 한 줄도 바뀌지 않았다** — 갈무리 문구·버튼·목적지(ADR-86 · ADR-91 B), 순서 규칙(사전 미완이면 진단 먼저 · ADR-80), 진행 누적(ADR-102 축3), 가치 카드 4상태(ADR-121), 진단 홈 자격 게이트(`assessmentAccess` — 게이트를 데이터보다 먼저)와 열람 고지 분기가 전부 그대로다. **시안 C 의 진행 막대를 불채택했다.** `.p-track`/`.p-fill` 은 **불변식 11**(*갈무리에 막대·게이지를 두지 않는다* · ADR-86)에 걸린다. 시안 note 스스로 *"진행 표시는 위치 안내일 뿐 성취 지표가 아니다"* 라 적었는데 **연속 채움 막대는 시각 어휘상 성취 게이지**다 — **의도와 형태가 따로 놀았고 현행 점 표시가 그 의도의 정확한 구현이다**(지휘부 판정 2026-08-27 · 3차 §9.6 탭바와 같은 선례). 캡션급 위계도 유지한다(ADR-81 3단에 넷째를 끼우지 않는다). **표시 층을 뗐다** — `CohortHomeScreen` · `AssessmentsScreen`. F-3 `HomeScreen` 과 같은 구조이고 이유도 같다: 둘 다 인증 뒤라 QA 계정 없이 열 수 없는데, 순수 컴포넌트가 되면 **세션 없이 렌더돼 4폭 캡처와 단언이 가능**하다. **시트 자료를 한 곳으로 묶었다**(`_lib/memberSheet.ts`). `/home`(F-3)이 조립하고 차수 홈·진단 홈이 또 조립하면 메뉴 하나가 늘 때 **세 곳이 어긋난다**(불변식 23). 회차 칩은 활성 차수가 하나일 때만 만들고, 제출 조회는 이미 열린 회차에만 팬아웃한다 — 차수 홈이 쓰던 방식 그대로라 **계약·DB 델타 0**. 차수 홈만은 자기가 이미 뽑아 둔 `submittedNos` 를 재사용해 같은 조회를 두 번 하지 않는다. **회차 → 파트 매핑을 순수 함수로 뒀다**(`sessionPart.ts`) — 시안 C 머리의 `PART 1 · GOAL`. **모르는 회차는 지어내지 않고 `null`** 이며 화면이 그 줄을 그리지 않는다. **회차 수를 6 으로 박지 않는다**(`buildProgress`·`buildSessionChips` 와 같은 규율). **상태를 색으로 말하지 않는다** — 시안 F 의 `.a-item.done`/`.wait` 를 색 분기로 옮기지 않았고 `완료`·`대기`·`시작` 은 **낱말**이며 테두리 한 겹만 붙는다(불변식 9). 아이콘은 시안의 문자 기호(`◎ ◈ ◆ ◑ ♡`) 대신 **인라인 SVG** 다(§5-3). **자기 회귀 둘을 캡처가 잡았다**: ⑴ 진행 표시의 `7주 기록` **라벨을 떨어뜨렸다** — 점만 남으면 무엇을 세는지 알 수 없다(복원). ⑵ 진단 항목의 제목·보조 문장이 **한 줄에 붙었다**(`사전 체크이미 마치셨어요`) — `<span>` 이 inline 이라 블록으로 세웠다. **회귀 테스트를 지우지 않고 옮겼다** — `copyRegression.test.ts` 의 *"AppHeader 바로 아래 고정"* 단언은 그 이름들이 사라졌으므로 **지키던 셋**(라벨 존재 · 회차 수 비하드코딩 · **상태 무관 앵커로 카드보다 먼저**)으로 다시 적었고, 막대 부재(`p-fill` 0)를 함께 잠갔다. **`.tabbar` 는 C·F 둘 다 불채택**(§9.6). **`/recruit` PC 2단은 보류했다가 지휘부 사양을 받아 같은 회차에 적용했다**(↓ 후속). 보류 사유는 개요 §3 의 한 줄이 배치 사양이 아니고 P1~P4 에 recruit 시안이 없다는 것이었다(불변식 20 · F-2b 가 §3 사양 승인으로 푼 것과 같은 자리). 계약 0 · 코어 0 · DB 0 · 마이그레이션 0 · SAIL 무접촉 · `PROXY_MATCHER` 무접촉 · **ISR 무손상**(`/recruit` `○ 5m` 유지). 검증: tsc 0 · eslint 0(기존 경고 2) · vitest **1042 통과 / 51 스킵 / 총 1093** · build 성공. 지휘부 지시(2026-08-27). |
| ADR-130 | `/recruit` lg 2단 + 카드뉴스 파생물 (4차 F-4 후속) | **무엇**: `/recruit` 을 lg 이상에서 2단으로 세우고 우측에 모집 카드뉴스 4장을 정적 그리드로 놓는다. **지휘부 사양**(2026-08-28): 좌는 기존 신청 흐름 그대로(폼·CTA·남은 자리 **로직 무접촉**), 우는 **4장 내외 2열 정적 그리드**, md 이하는 **현행 1단(520px) 유지**, **캐러셀·라이트박스 등 새 위젯 금지 — 정적 `img`**. **선정은 지휘부가 했다** — 클코1 이 13장 한 줄 요약을 올리고(원고에서 뽑았다 · 이미지 추측 아님) 지휘부가 **2·3·6·7** 을 골랐다. **순서도 고정이다**: *"문제에서 답, 증언으로 가는 흐름이 순서에 실려 있다"* — 문제(2) → 문제(3) → 답(6) → 증언(7). 화면은 배열을 **다시 정렬하지 않는다**(테스트가 `sort`·`reverse`·`filter` 부재를 잠근다). **부적합 둘을 공개 면에서 뺐다**: 카드 11(참가비)은 **입금 계좌가 이미지에 박혀 있어** 지울 수 없고 캐시·공유·검색에 남는다(금액은 공개 가능하나 그 장은 못 쓴다). 카드 13(신청)은 QR·코드가 박혀 **기수 교체 시 이미지가 낡고**(3기에 2기 코드가 남는다) 좌측 CTA 와 **신청 경로가 둘이 된다**. 중복 넷(1·5·9·10·12)은 `/`·`/about`·좌측이 이미 말한다. **원본은 무접촉이다.** `docs/tasks/cards/` 는 확정 원본이고 `scripts/recruit-cards.mjs` 가 **읽기만** 한다 — `public/recruit/*.webp` 는 **파생물**이며 사본이 둘이 아니라 **원본 하나와 파생 하나이고 그 관계를 스크립트가 잠근다**(지휘부 판정). 스크립트는 변환 전후 원본 바이트를 비교해 같지 않으면 멈춘다. 규격 WebP 장변 800px · **580KB → 116KB(20%)** · `fit: 'inside'`(카드뉴스는 글자가 가장자리까지 차 있어 크롭하면 문장이 잘린다). **선정·순서·`alt` 의 단일 출처는 앱이다**(`src/app/recruit/cards.ts`) — 스크립트가 그 파일을 읽어 파생물을 만들고, 파싱이 실패하면 조용히 넘어가지 않고 멈춘다. 목록이 둘이면 파일과 alt 가 어긋난다. **`alt` 는 각 장의 내용 한 줄**이다 — 이미지 속 글자는 스크린리더가 못 읽으므로 그 문장이 **유일한 접근 경로**이고, 장식어로 시작하지 않는다(테스트가 잠근다). **`recruit.css` 최초의 `@media` 다** — 그전까지 `/recruit` 은 폭에 반응하지 않았다(F-4 실측 `@media` 0). 기본값이 `display: none` 이라 **lg 미만에서는 그리지도 않는다**(768 캡처 실측 카드 0). 우측은 `position: sticky` 로 스크롤을 따라온다 — 좌측이 길어 카드가 위에서만 보이면 있으나 마나다. **ISR 무손상**(`/recruit` `○ 5m`) · `page.tsx` 의 좌측 로직 **0 줄 변경**. 계약 0 · 코어 0 · DB 0 · 마이그레이션 0. 검증: tsc 0 · eslint 0(기존 경고 2) · vitest **1048 통과 / 51 스킵 / 총 1099** · build 성공 · 카드 4장 `200 image/webp` 실측. 지휘부 판정(2026-08-28). |
| ADR-131 | F-5 총검증 하네스 — 화면 목록 단일 출처 + 상한 있는 대기 + 가로 넘침 검사 (4차 F-5) | **무엇**: F-1~F-4 가 회차마다 만든 캡처 경로를 하나로 묶고(`scripts/screens.mjs` 가 **화면 목록 단일 출처** · 공개 11 · 인증 11 · 폭 4), `scripts/shots.mjs` 가 모드 넷(`public`·`fixture`·`auth`·`all`)으로 그것을 찍는다. **목록이 다섯 곳에 흩어져 있으면 F-5 에서 한 화면이 조용히 빠진다.** 두 묶음의 경계는 추측이 아니라 **`PROTECTED_PREFIXES` 실측**이다. **`playwright` 를 devDependency 로 들였다**(지휘부 승인 · 버전 **1.62.1 고정**). 쿠키를 손으로 합성해 세션을 만드는 길은 **취하지 않았다** — *"Supabase 쿠키 형식에 결합된 자가제작 우회라 형식이 바뀌면 조용히 깨진다"*(지휘부 판정). 조건 셋(`devDependencies` 전용 · 버전 고정 · **런타임 import 0**)을 `tests/devDeps.guard.test.ts` 가 잠그고, 그 가드는 **하네스가 실제로 그것을 쓰는지도** 본다 — 안 쓰면 들일 이유가 없다. **핵심 규율 — 기다림에는 끝이 있어야 하고, 끝났는지 확인할 수 있어야 한다**(`CLAUDE.md` §11 로 승격). 고정 대기(`--wait-for-timeout=2500`)를 없앤 것은 옳았으나 그 자리에 **상한 없는 대기**를 놓은 것이 새 함정이었다(44장에 11분을 태우고 미완). **이 스택 고유의 함정 넷을 하네스 주석에 고정했다**(지휘부 지시 — 지우지 말 것): ⑴ **`networkidle` 이 아예 안 잡힌다**(Next 가 RSC 프리페치로 연결을 계속 여닫는다) → 주 신호는 `load`, idle 은 보조(`IDLE_MS` 1200) ⑵ **숨겨진 lazy 이미지는 영영 로드되지 않는다**(`display:none` + `loading="lazy"` 면 `img.complete` 가 끝내 `false` — `/recruit` 카드가 lg 미만에서 그렇다) → `IMG_MS` 1500 ⑶ **`position: fixed` + 긴 full-page 캡처가 안 끝난다**(`/recruit` 5,947px + 하단 고정 바) → `SHOT_MS` 30초 + `animations: 'disabled'` ⑷ **눌러야 보이는 화면은 URL 이 같아 조용히 다른 것을 찍는다**(시트가 `/home` 과 같은 주소라 홈을 두 번 찍고 있었다 — `cmp` 로 바이트 동일 확인) → `open` 슬롯 + 실패 기록. **넷 다 *"찍히기는 했는데 다른 것을 찍었다"* 는 한 모양이다.** **`깨짐 0` 을 눈이 아니라 수로 잰다** — 각 화면 각 폭에서 `scrollWidth - clientWidth` 를 재고 1px 를 넘으면 화면·폭과 함께 실패로 남긴다. **85장을 사람 눈으로만 보는 것은 신뢰할 수 없다(32번째쯤에서 안 본다).** 실측 결과 **22화면 × 4폭 · 캡처 실패 0 · 가로 넘침 0**. **집계 줄이 그동안 붙지 않았던 것도 이때 드러났다** — 개별 실패는 `console.error` 로 나왔으므로 앞선 *"실패 0"* 보고는 사실이었으나, **끝의 요약 블록이 치환 실패로 빠져 있었다**(치환 성공을 확인하지 않았다). **없는 것을 없다고 말하는 줄**을 붙였다. **인증 뒤 캡처는 화면으로 로그인한다** — `.env.local` 의 QA 자격으로 `/login` 을 실제로 채워 세션을 얻고 `user`·`coach` 두 컨텍스트로 찍는다. **자격 값은 출력·로그에 남기지 않는다**(없을 때도 키 이름만 말한다 · 발주 §0.3). 캡처는 **무손실 WebP** 로 저장한다(9.0MB → 5.3MB) — 매 회차 원본 PNG 를 쌓으면 저장소가 무거워지고, 전량은 명령 하나로 다시 만들 수 있다. 계약 0 · 코어 0 · DB 0 · 마이그레이션 0 · **런타임 코드 0줄**(`scripts/`·`tests/` 만). 검증: tsc 0 · eslint 0(기존 경고 2) · vitest **1052 통과 / 51 스킵 / 총 1103**. 지휘부 판정(2026-08-28). |
| ADR-132 | 문서 잠금 둘 — `design_system.md` §9.7 부품 목록 · ADR 채번 (4차, "적히는 모든 값은 둘 중 하나다" 승격 직후) | **무엇**: 문서에 맨숫자로 박혀 있던 ⑴ 따라가야 하는 값 둘에 게이트 테스트를 붙였다 — `tests/designParts.test.ts`(§9.7 머리의 `부품 15` · 표 행 · `현재` 칸 파일 · `_screens/site/*.tsx` 실물이 **넷 다 같아야 한다**) · `tests/adrNumbers.test.ts`(중복 0 · 이유 없는 결번 0 · 미아 참조 0 · `정의 수 + 결번 수 = 최대 번호`). **왜**: CLAUDE.md §11 이 규율을 올린 그 잣대로 문서를 훑어 나온 것들이다. 둘 다 **오늘은 맞았다** — 문제는 틀릴 때를 아무도 모른다는 것이었다. §11-3 의 `중복·결번 확인`을 매번 손으로 했고, 손 확인은 세는 단위가 흔들려 같은 표에서 `127`/`128`, 결번 `[12]`/`[12,115,116,117]` 이 나왔다 — 갈린 것은 값이 아니라 **`| ADR-115~117 |` 범위 행을 세느냐**였다. 테스트가 그 단위를 못 박는다. **부수 발견**: 손 확인이 여섯 달 동안 `결번 1건(ADR-12, 기존 상태)` 으로 넘겨 온 것이 실은 **미아 참조**였다 — `design_system.md` §2 가 *브랜드 토큰은 진단이 주입, 중립·의미 토큰은 코어 소유*의 근거로 **ADR-12 를 살아 있게 인용**하는데 이 표에 행이 없다. 3차 T-6 이 불변식에서 찾아낸 것(참조는 살아 있고 정의가 0)과 같은 모양이다. **행을 되살리는 것은 정본 변경이라 지휘부 판정 사안**이므로 여기서는 고치지 않고, `KNOWN_GAPS` 예외 한 줄로 사실을 기록했다. 정의가 생기면 그 예외가 **스스로 실패해** 지우게 만든다(예외가 눌러앉지 않는다). **검증**: 두 테스트 모두 드리프트 경로를 하나씩 실제로 만들어 **무는 것을 확인한 뒤** 되돌렸다 — 부품 파일 추가 · 머리 숫자만 변경 · `현재` 칸 오타 · 중복 채번 · 행 삭제 · 없는 번호 인용 · 결번 해소. 도구가 재는 것이 재려던 것인지 먼저 쟀다(§11 기다림 조항의 짝). |
| ADR-133 | 색 토큰 소유 경계 — **브랜드는 진단이 주입, 중립·의미는 코어 소유** (내용은 2026-06-27 확정 · 번호는 2026-08-28 채번) | **무엇**: 퓨처나우 = 네이비+골드를 **진단이 주입**하고, 중립(surface·text·border)과 의미(danger·warning·care) 토큰은 **코어가 소유**한다. **출처**: `design_system.md` §0-2 의 문장이 정본이고, `838e761`(2026-06-27 · 디자인 시스템 v1)이 그것을 세웠다. **왜 지금 번호를 받나**: 그 문장은 처음부터 `(ADR-12)` 를 인용했는데 **ADR-12 는 존재한 적이 없다.** 지휘부 판정 — *소급 부여 금지 · 원문이 없으면 결번 · 문장에 결정 가치가 있으면 새 번호로 채번하고 출처를 적는다.* 그래서 **내용은 옛것이고 번호만 새것**이며, 이 행이 그 사실을 함께 적는다. **왜 결정 가치가 있나**: 불변식 2(코어 ↛ 인스트루먼트)의 색 층 표현이다 — 코어가 브랜드 색을 알면 진단마다 코어를 고쳐야 하고, 진단이 의미색을 소유하면 경고의 뜻이 진단마다 갈린다. `designTokens.test.ts` 가 이미 이 경계를 잰다. **부수 발견(판정 대기)**: 같은 블록의 §0-1 *색은 3단으로만 흐른다* 는 `(ADR-11)` 을 인용하는데, ADR-11 은 그 시점에 이미 **폐기된 `core` 스키마 격리 건**이고 색과 무관하다. **결번이 아니라 오귀속**이라 성격이 달라 여기서 함께 고치지 않았다. |
| ADR-134 | 로그인 체감 수리 — `/login` 인증 리다이렉트 · `replace` · `name`/`id` · 쿠키 `Secure` · 로그인 유지 스위치 (5차 소건 1) | **무엇**: 다섯을 한 덩이로 고쳤다. ⑴ `/login` 서버 컴포넌트가 세션을 보고 인증자를 돌려보낸다(착지 판정은 `loginOutcome` 재사용 — 사본 둘 방지). ⑵ 로그인 성공을 `router.push` → **`replace`**(히스토리에 `/login` 을 남기지 않는다). ⑶ 로그인 입력에 `name`·`id`. ⑷ 인증 쿠키에 **`Secure`** — `@supabase/ssr` 0.12 기본값에 없다(`DEFAULT_COOKIE_OPTIONS`). ⑸ 계정 화면에 **이 기기에서 로그인 유지** 스위치(기본 켬 · 끄면 세션 쿠키). **왜 한 덩이인가**: 지휘부가 *구 세션 건*과 *뒤로가기 건*을 **한 뿌리 가설**로 묶었고 실측이 그것을 뒷받침했다 — 운영에서 뒤로가기는 `/feed → /home → /login → / → 이탈` 이었고 **그 시점에 세션이 살아 있었다**(`signOut()` 호출 지점은 저장소 전체에 `LogoutButton` 하나뿐). *로그아웃이 아니라 로그인 화면을 본 것*이 증상의 정체였다. ⑴⑵ 가 그 화면을 없앤다. **자격 저장은 기각**됐다(지휘부) — 세션은 이미 400일 영속이었으므로 *다시 칠 필요가 없다*는 결과에 자격 저장이 **불필요**하기도 하다. ⑶ 은 앱이 아무것도 들지 않으면서 브라우저 관리자만 돕는다. **⑸ 의 구현 제약**: 라이브러리가 `setCookieOptions` 에서 `maxAge` 를 **강제로 덮어써서** `cookieOptions.maxAge` 로는 세션 쿠키를 만들 수 없다. 그래서 `cookies` 어댑터를 넘겨 **수명 한 칸만** 지운다 — 이름·값·나머지 옵션은 라이브러리가 만들어 건네준 것 그대로이고 토큰 형식을 읽지도 만들지도 않는다(하네스 머리가 경고한 *자가제작 우회*와 구별된다). 끈 순간 **이미 깔린 쿠키까지** 다시 쓴다 — 다음 로그인부터 듣는다면 스위치가 거짓말을 한 것이 된다. **판정은 전부 순수 함수**(`src/core/supabase/cookiePolicy.ts`)이고 54 케이스로 잰다. `Secure` 는 `NODE_ENV` 가 아니라 **관측한 프로토콜**로 정한다 — http 에 붙이면 브라우저가 쿠키를 저장하지 않아 개발 로그인이 통째로 깨진다. **남은 것**: 최박사 실기기 재확인으로 1·3번 증상이 남는지 잰다. 남으면 그때 history sentinel 을 논의하고, **PWA 는 이 발주 밖**이다(다음 페이즈). |
| ADR-135 | 동행 피드 반응 **복수 선택** — 2차 확정의 개정 (5차 소건 2) | **무엇**: 한 사람이 한 글에 이모지를 **여럿** 켤 수 있다(박수와 기도를 함께). 네 층이 한 덩이로 움직였다 — 표 `PRIMARY KEY (post_id, user_id)` → **`(post_id, user_id, emoji)`**(새 마이그레이션 · 불변식 15) · `feed_react` 를 **교체에서 토글로** · 목록 RPC `my_reaction text` → **`my_reactions text[]`** · 계약 `myReaction: FeedEmoji|null` → **`myReactions: FeedEmoji[]`**(지휘부 **사전 승인** · 불변식 3). **빈 배열이 무반응**이고 `null` 을 쓰지 않는다 — 없음을 두 가지로 표현하면 화면마다 다르게 검사하고 언젠가 한쪽을 빠뜨린다. **왜 개정인가**: 2차(ADR-124)는 단일 선택을 **규칙이 아니라 구조**(PK)로 박았다. 그래서 화면만으로는 열리지 않는다. PK 를 **넓히는** 방향이라 기존 행이 전부 그대로 유효하고 데이터 소실이 없다. **순서를 고정했다** — 반환·목록 두 경로 모두 `feed_emojis()` 선언 순서로 정렬한다. 기준이 없으면 같은 상태가 호출마다 다른 배열로 와서 화면이 깜빡이고 테스트가 흔들린다. **낙관적 갱신을 순수 함수로 뺐다**(`reactionState.ts`) — 단일일 때는 두 줄이었지만 복수는 집합 연산이고, 인라인으로 두면 경계 조건이 테스트가 닿지 않는 곳에 숨는다. **서버가 준 배열을 진실로 삼는다**(경합 시 추측은 어긋나고 서버 값은 어긋나지 않는다). **대상 함수는 `feed_list` 다** — 초안이 다른 이름(`feed_post_list`)을 가리켰고 **적용 직전 실물 조회로 잡았다** (지휘부 지시 2026-08-29 · *다음 사람이 같은 자리에서 이름을 지어내지 않게*). 적용했다면 `DROP ... IF EXISTS` 가 조용히 지나가고 아무도 안 부르는 함수가 하나 생겼을 뿐, 진짜 `feed_list` 는 옛 모양으로 남아 피드가 닫혔을 것이다. 인자 순서도 정본과 같아야 한다 — `(cohort, before, before_id, limit, mine)`. 다르면 `DROP` 이 빗나가 같은 이름의 함수가 둘이 된다. `tests/feedReactionsMulti.migration.test.ts` 가 **실재 여부를 양방향으로** 잠근다. **불변식 11 재확인이 수용 기준이었다**(지휘부) — 복수 선택은 **합계를 최대 4배로 키우는 변경**이라 *많이 받은 글*이 눈에 띄고, 띄면 정렬하고 싶어지고, 정렬하면 순위가 생긴다. 주장하지 않고 **소스를 읽어 잠갔다**(`reactionRanking.test.ts`): 화면·순수함수에 `sort` 0 · 반응 줄에 막대/게이지/백분위 0 · 이모지 순서는 선언 순서 · DB `ORDER BY` 는 시간과 `e.ord` 뿐. |
| ADR-136 | `/login` 회원가입 진입 링크 — 한 줄 (5차 소건 3) | **무엇**: `/login` 하단 문구를 *인도자로 활동하실 분은 인도자 회원가입* → *아직 계정이 없으신가요? **회원가입*** 으로 고쳤다(인도자 신청도 그곳에서 함께 한다는 보조 문장 포함). **왜**: `/signup` 은 **일반 가입 경로**이고 인도자 신청은 그 안의 체크박스일 뿐인데(`AuthGate allowCoachApply`), 옛 문장은 **인도자만 쓰는 길로 읽혔다.** 로그인 화면에 도착한 미가입자에게 출구가 없는 것과 같았다. **퍼널 골격 재작업은 의제 2 몫**이므로(지휘부) 여기서는 문장 하나만 고쳤다. |
| ADR-137 | 가입 경위를 **대조 키로 승격** · 포럼 정보 선택화 (5차 소건 4) | **무엇**: `/signup` 의 강제 축을 뒤집었다 — `signupNote`(가입 경위)가 **필수**, `forumName`·`forumPhone` 이 **선택**. 승인 큐 표시도 경위 중심으로 바꾸고(열 순서·본문 굵기) 동의 문안의 수집 항목 문장을 함께 뒤집었다. **왜**: §4.3 의 근거(*이 필드가 없으면 승인 큐가 판단 불가로 쌓이고, 나중에 필드를 더해도 이미 들어온 신청 건은 영원히 대조할 수 없다*)는 그대로 유효하고 **그 자리를 무엇이 채우느냐만 바뀌었다.** 포럼 이름·연락처를 필수로 두면 **포럼을 거치지 않고 온 사람이 가입할 수 없거나 지어낸다.** 지어낸 값은 대조를 돕는 게 아니라 **운영자를 헷갈리게 한다** — 명단에 없는 이름이 오타인지 남의 것인지 판단할 수 없다. 반면 가입 경위는 **누구나 사실대로 쓸 수 있고** 포럼 경유자면 거기에 포럼이 적힌다. **문안과 강제를 함께 뒤집었다** — 어긋나면 *동의는 받았으나 동의한 내용이 아닌 것*이 된다. 사본 둘이 생기기 쉬운 자리라 테스트가 둘을 묶는다(불변식 23 계열). **하위호환을 확인했다**(지휘부 지시): 소건 4 이전 신청은 경위 칸이 비어 있다. 그 행을 *부실 신청*으로 보이게 하면 운영자가 잘못 판단하므로, 화면이 **양식 개편 전 신청임**을 말하고 포럼 정보가 있으면 그것으로 대조하라고 안내한다. 처리 버튼도 그대로 살아 있다 — 표시가 바뀌었다고 처리가 막히면 큐가 쌓인다. 새 신청에서 포럼이 빈 것은 **정상**이므로 옛 문구 `명단 대조 안 됨`(실패로 읽혀 승인을 주저하게 만든다)을 `미기재(선택 항목)` 로 바꿨다. |
| ADR-138 | 공개 헤더가 세션을 본다 — 버튼 문구·목적지 일치 (5차 소건 1-바 · 최박사 승인) | **무엇**: `/`·`/about` 의 오른쪽 골드 버튼이 **로그인 상태면 `내 홈`(`/home`)**, 아니면 `로그인`(`/login`) 이다. **왜**: 소건 1 이 *로그아웃된 것 같다* 의 절반(뒤로가기로 닿은 `/login` 화면)을 닫았지만 **나머지 절반이 이 버튼**이었다 — 리다이렉트를 고쳐도 **문구는 그대로 남는다.** **ADR-86 계열**(문구와 목적지를 일치시킨다)을 공개 현관에 적용한 것이다. **왜 `내 홈` 이고 `로그아웃` 이 아닌가**(화면을 보고 정한 근거 셋) — ⑴ **자리의 위계**: 이 슬롯은 골드 면 + 700 굵기로 헤더에서 가장 강하고 현관의 유일한 상시 CTA 다. 나가는 동작을 그 자리에 두면 돌아온 참여자에게 앱이 권하는 첫 행동이 *나가기* 가 된다(§9.7 #15 `BookPanel` 의 *골드는 전진 CTA 전용* 위계와 같은 결). ⑵ **증상의 정체**: 필요한 것은 로그인 상태임을 알리고 **돌아갈 길**을 주는 것이다. `로그아웃` 도 상태는 알리지만 제공하는 동작이 원하는 것의 정반대다. ⑶ **로그아웃은 이미 있다**: 어포던스는 멤버 셸 `LogoutButton` 이고 시트에 넣지 않은 것도 의도였다(폼 액션 · `memberSheet.ts`). `내 홈` 을 누르면 곧 그 셸에 닿는다. 현관에 두면 같은 동작이 두 곳이 된다(불변식 23). **ISR 을 깨지 않았다** — 서버에서 `cookies()` 를 부르면 `/`(5m)·`/about` 이 동적이 된다. 그래서 세션 읽기를 **브라우저로 내렸다**(`PublicGnb`, `useSyncExternalStore` · 서버 스냅샷 = 비로그인이라 하이드레이션 불일치 없음). **대가는 한 프레임 깜빡임**이고 감추지 않고 적었다. **쿠키 값을 해석하지 않는다** — 이름 접두사만 본다(`isAuthCookieName`). 값 안의 만료를 읽으려면 토큰 형식에 결합돼야 하고 그것이 하네스가 경고한 자가제작 우회다. 만료 쿠키가 남으면 눌렀을 때 `/login` 으로 가는데 **지금과 같은 결과이고 더 나빠지지 않으며**, `proxy` 가 매 요청 검증·갱신하고 공개 경로도 그 matcher 안이라(불변식 17) 어긋나는 창이 좁다. **판정은 순수 함수**(`publicHeaderAction`) — 부품은 여전히 계산하지 않는다. `PublicGnb` 는 부품이 아니라 화면 층이며 두 잠금(`designParts`·`site.purity`)에 **명시된 예외**로 등록했다. |
| ADR-139 | 색은 **3단으로만 흐른다** — 원천 hex → 역할 → 컴포넌트 (내용은 2026-06-27 확정 · 번호는 2026-08-29 채번) | **무엇**: 색값이 **한 방향으로만** 내려간다. ⑴ 원천 hex(팔레트) → ⑵ 역할 토큰(semantic) → ⑶ 컴포넌트. **컴포넌트는 hex·원천 토큰을 직접 참조하지 않는다.** 그래서 색을 갈 때 **1차 한 곳만** 고치면 된다. **출처와 경위**(지휘부 조건 ①): 정본 문장은 `design_system.md` §0-1 이고 `838e761`(2026-06-27 · 디자인 시스템 v1)이 세웠다. **그 문장은 처음부터 번호 없이 있었다** — 인용은 `(ADR-11)` 로 적혔는데 **ADR-11 은 이 결정이 아니다.** `git` 이력 실측: ADR-11 은 저장소 **첫 커밋 `b7ab2fa`(2026-06-27)부터 이미 폐기 표시된 `core` 스키마 격리 건**이고 색과 무관하며 **행이 바뀐 적이 없다.** 인용을 붙인 것은 같은 날 **나중** 커밋(`838e761`)이고, 그 커밋이 `(ADR-11)`·`(ADR-12)`·머리 참조 줄 `ADR-04·11·12·20` 을 **한 블록에서 함께** 적었다. **원인은 하나**(번호를 먼저 인용하고 행을 쓰지 않는 습관)이고 결과만 갈렸다 — 12 는 **없는 번호**(미아 참조 → 결번), 11 은 **있는 번호인데 다른 결정**(오귀속). 그러므로 이것은 *번호가 다른 결정에 붙은 것*이 아니라 **출처 표기가 틀린 것**이다: 이 결정이 **자기 번호 없이 남의 번호를 빌려** 인용돼 왔다. **왜 지금 번호를 받나**(지휘부 판정 2026-08-29): *이미 인용되며 유효하다면 결정으로 실재하는 것이고, 실재하는 것은 번호를 가져야 한다.* **소급 부여가 아니다** — ADR-11 에 뜻을 덧붙이지 않았고 그 행은 그대로 살아 있다(정정 주만 달렸다). 번호를 재사용하지도 지우지도 않았다. **왜 결정 가치가 있나**: 이것이 **불변식 23**(사본이 둘이면 잠금으로 묶는다)의 색 층 표현이다 — 컴포넌트가 hex 를 직접 들면 같은 색이 수십 곳에 사본으로 살고, 그 순간 *색 교체* 가 전수 수색이 된다. ADR-133(소유 경계)과 **짝이되 다른 축**이다: 133 은 *누가 소유하는가*, 139 는 *어떤 방향으로 흐르는가*. `src/app/_lib/designTokens.test.ts` 가 이미 이 흐름을 잰다. **ADR-11 과의 관계는 양방향으로 적었다**(지휘부 조건 ②) — 그 행의 정정 주가 이 번호를 가리킨다. |
| ADR-140 | 회원 표시 축 셋 — `tier` · `underReview` · `cohortRoles` (5차 T-3·T-4 · **최박사 승인** · 불변식 3) | **무엇**: 계약에 **표시 축** 셋을 더했다. `tier`(`visitor`·`forum`·`suspended`) = 자격이며 **늘 하나** · `underReview`(불리언) = `held` 의 **진행 표시**이며 tier 를 덮지 않는다 · `cohortRoles` = 기수와 역할의 짝 목록이며 **여럿**. **무변경 넷**: `member_state()` 산출 · `member_can_assess` · RLS · 진실표. **마이그레이션 0.** **왜 `participant` 를 tier 값으로 두지 않나**: 참여는 **기수마다 하나씩 여럿**이라 택일 필드에 들어갈 수 없다. 최박사 지시가 그것을 문장으로 못 박았다 — *포럼회원, 00기참여자, 00기인도자는 택일이 아니라 병행표현되어야 한다.* **기수와 역할을 한 항목으로 묶은 이유**: 화면 표기가 늘 붙어 다닌다(`2기 참여자`). 따로 두면 조립할 때 다시 짝지어야 한다. **tier 의 입력은 `memberships.status` 저장값이다**(행 없으면 `null`) — `member_state()` 산출값이 **아니다**. **〔정정 2026-08-29〕** 초판 구현이 산출값을 받아 `cohort` 가 `forum` 으로 앉았고, **승인받은 적 없는 18명이 화면에 `포럼회원` 으로 표시**됐다. 최박사가 기각하신 그것이다 — *포럼회원이라는 이름을 붙이는 순간 아닌 것을 그렇다고 말한 것이 된다.* `cohort` 는 **권한**이지 자격이 아니므로 tier 의 입력이 될 수 없다. 매핑: `individual`→`forum` · `expired`→`suspended` · `pending`·`held`·행 없음→`visitor`. **지금 세미나에 참여 중이라는 사실은 `cohortRoles` 가 말한다** — 축이 둘인 이유가 이것이고, 같은 사실을 두 번 말하지 않는다. **보고와 코드가 다른 그림을 그린 것이 발단이다** — 완주 보고 Q4 는 *18명이 visitor* 라 적었는데 코드는 `forum` 으로 돌았다. 릴레이가 그 어긋남을 감지해 정본을 열었고, 짐작해서 올리지 않은 판단이 이 건을 막았다. **테스트도 산출값을 넣고 있어 초록이었다**(초록은 입력이 옳다는 증거가 아니다). 지금은 저장값 넷 + 행 없음을 전수로 잰다. **권한 판정은 무변경**이다. **실측 근거**(2026-08-29 · 단위를 밝힌다): 기수를 이끌면서 참여도 하는 사람 **2명**(`cohorts.coach_id` 기준) · 포럼회원이면서 참여자 **3명**. `role='coach'` 로 세면 1명, `role in (coach,admin)` 으로 세면 3명이다 — **셋 다 맞고 단위가 다르다**(§11). |
| ADR-141 | 회원 표시 **어휘·조립 규칙 단일 출처** — `core/membershipVocab.ts` (5차 T-3) | **무엇**: tier 이름 셋 · tier 별 한 줄 설명 · 소속 역할 이름 둘 · 조립 규칙 · `underReview` 진행 문안 · 문의 안내 · **`held` 의 뜻 한 줄**을 한 파일에 둔다. 읽는 곳은 셋이다(내 정보 · 공개 현관 칩 · 운영자 화면) + 5-2 운영자 도구. 그래서 화면 옆이 아니라 **코어**에 둔다. **상태 하나에 이름 하나인 매핑 표로는 안 된다**(지휘부) — 축이 둘이 되면서 조립 규칙이 어휘만큼 중요해졌다. **서버는 문자열을 만들지 않는다** — 값만 내리고 조립은 화면이 한다. 문언이 서버에 박히면 단일 출처가 둘이 된다(테스트가 서버 값에 문언이 섞이지 않음을 잠근다). **문안은 최박사 원문이고 한 글자도 다듬지 않았다** — *진단* 이 아니라 **진단 등 모든 도구**다(원문이 초안보다 넓다). `expired` 의 이름은 **이용 보류**다(*이용 중지* 에서 되돌리셨다 — 더 완곡해서). **`held` 의 뜻이 정본 어디에도 한 줄로 없었다** — `member_state()` 주석·ADR-122·`/pending` 문안 **세 자리에 흩어져** 있었고 그것 자체가 결함이라 여기 모았다(지휘부 지시). **T-5 와의 관계**: `roleTargets` 와 소속 칩은 **사본이 아니다**(입력이 같고 출력이 다른 두 함수). 사본이 될 수 있는 자리는 **어휘뿐**이라 역할 이름을 여기 두고 `roleTarget.ts` 가 읽는다. **기수명은 그대로 쓴다** — 최박사 문안의 `○○기` 는 자리표시자이고 실제 이름은 `퓨처나우2026예봄2기` 처럼 길다. **축약 규칙은 확정에 없으므로 줄이지 않는다**(지어내면 계열 8번). 규칙이 정해지면 `cohortRoleLabel` 한 곳만 고친다. |
| ADR-142 | 회원 표시 확정 넷 — 버튼 이름 · 설명문 대체 · 기수명 축약 · 칩은 이름만 (5차 T-3·T-4 · **최박사 확정 2026-08-29**) | **① 승인 큐 `보류` → `확인 대기`.** 하는 일은 같다(`decide_membership('held')`) — **이름만 구분했다.** 회원 상태의 **이용 보류**(`expired`)와 한 화면에서 헷갈리기 때문이다. 둘은 뜻이 다르다: `held` 는 *자격 확인이 끝나지 않아 붙들어 둔 상태*, `expired` 는 *한때 있던 자격이 끝난 것*. **② 참여 칩이 있으면 자격 줄의 설명문을 소속 기준 문장으로 대체한다.** 최박사 확정 사실은 *18명 모두 참여자이고 그 안에 포럼회원은 없다* 이고, 그 사실을 그대로 읽으면 `승인을 기다리는 중입니다. 세미나 참여와…` 가 **이미 참여 중인 사람에게 어긋난다.** **⚠ 이 대체는 지휘부의 읽기이지 최박사 문장이 아니다**(2026-08-29 명시) — 최박사께 한 마디 더 여쭙는 중이고 `나` 로 정정되면 **바꿀 곳은 자격 줄 하나**다. **이름은 방문회원 그대로다** — 승인받은 적이 없기 때문이다. 이름까지 `참여자` 로 바꾸면 자격과 소속을 다시 한 줄로 합치는 것이고 그것이 처음부터 금지된 것이다. **바뀌는 것은 설명문뿐.** **③ 기수명 축약 — 이름 끝의 `n기` 를 뽑는다.** `퓨처나우2026예봄2기` → `2기 참여자`. **규칙이 이름 형식에 의존하므로 끝이 `n기` 가 아닌 기수는 전체 이름을 그대로 쓴다** — 실물로 `[QA] 검증 전용`·`체험 진단` 이 그것이다. 억지로 줄이면 **없는 회차 번호를 만들어 낸다.** **뽑히지 않는 경우를 테스트에 넣었다**(지휘부 지시) — 가운데 `2기` 가 있는 이름도 뽑지 않는다. **④ 소속 칩은 이름만 단다** — 설명은 위 자격 줄이 든다(②로 그 문장이 그리 옮겨 갔다). **축은 여전히 둘이다** — ②가 바꾸는 것은 설명문이고 이름·칩은 각자의 자리를 지킨다. |
| ADR-143 | 병행이 원칙 · 우선은 좁을 때의 잘라내기 · 운영자 넷째 축 (5차 T-3·T-4 · **최박사 확정 2026-08-29**) | **무엇**: 최박사 원문 — *쓸 수 있는 공간이 되는 곳에는 병행표현하고(내정보 등), 병행해서 쓸 공간이 안 되는 곳에서는 참여자·인도자·운영자 우선*. **두 지시는 층이 달랐다** — 병행이 원칙이고 우선은 **자리가 좁을 때의 잘라내기 규칙**이다. 앞선 *택일이 아니라 병행표현* 과 부딪히지 않는다. **넓은 자리(내 정보) — 층 나눔을 유지한다.** 390px 에서 **실제로 렌더해 쟀다**(짐작하지 않았다): 축약된 이름이면 한 층이 1줄(56px)·두 층이 2줄(95px)로 한 층이 낫지만, **축약 안 되는 이름 둘이면 한 층도 2줄(88px)** 이 된다. 한 층은 **내용에 따라 줄 수가 흔들리고**(1→2줄) 두 층은 언제나 2줄로 모양이 고정된다. 더 중요한 것은 한 층에서 줄이 접히면 `[QA] 검증 전용 참여자` 와 `포럼회원` 이 **같은 굵기·같은 층으로 줄만 바뀐 채** 서서 어디까지가 소속이고 어디부터가 자격인지 알 수 없다 — **축이 둘이라는 사실이 시각에서 사라진다.** 높이 차이 39px 로 *경계가 늘 보이는 것* 을 산다. **좁은 자리 — 전수로 셌다**(짐작 금지): **`MenuSheet` 머리의 `role` 한 칸뿐**이고 세 화면이 먹인다(`/home`·차수 홈·진단 홈). 확인해 배제한 것: `SiteRoleCard.who`(T-5 로 카드가 여럿이라 각자 칸을 갖는다 — 좁지 않다) · **공개 현관 칩(실재하지 않는다** — 발주 §3 이 노출 자리로 적었으나 화면에 없다) · `admin/MemberRow`(남의 역할이라 축이 다르다). **좁은 자리의 값은 비워 두었다** — 최박사가 적으신 `참여자·인도자·운영자` 가 **우선순위인지 열거인지** 갈리고 겸직자가 실재하므로(2명), 임시 값을 넣으면 그것이 확정으로 굳는다. `NARROW_PRIORITY = null` 이고 `narrowLabel()` 이 `null` 을 돌려주면 부르는 쪽은 **지금 쓰던 값을 그대로 쓴다**. 답이 오면 **한 줄**로 꽂는다. **운영자는 넷째 축**(`isAdmin`) — 기수에 매이지 않아 `cohortRoles` 에 못 들어간다(`cohortId` 가 없다). 값은 `users.role` 이고 `currentUser()` 가 이미 들고 있어 **새로 읽어 올 것이 없다**. 칩 줄 맨 앞에 선다(기수에 안 매인 것이 매인 것보다 먼저 읽힌다) 실측 `role='admin'` **2명**. **사본 후보 하나를 보고한다**: `admin/MemberRow.ROLE_LABEL` 이 `user` 를 **멤버**라 부른다(여기는 **참여자**). 축이 달라(시스템 권한 대 소속 역할) **합치지 않았다.** |
| ADR-144 | 회원 모델 확정 — 자동 승급 폐지 · 자동 만료 폐지 · `member_tool_access` · `expired` 차단 (**최박사 확정 2026-08-30** · 마이그레이션 **미적용**) | **최박사 모델**: 포럼회원은 **오직 운영자가 메뉴에서 승급을 지정해야** 된다 · 승급 안 된 사람은 `○○기 참여자` 이고 기수 종료 뒤 **본인 회기 콘텐츠는 영구 열람**(회원 보류가 아니면) · 바깥 도구는 방문회원과 같되 **본인 검사 데이터는 보고 신규는 안 된다** · 회기 진행 중에는 바깥 도구도 무제한. **① 자동 승급 폐지** — `membership_on_cohort_archived` 와 트리거를 지운다. 마감이 등록자 **전원**을 승급시켰는데 모델에서 포럼회원은 **결정**이지 사실이 아니다. **아직 한 번도 돌지 않았다**(`archived` seminar 0개 · 자동 전이 행 0) — 1기 마감이 첫 실행이 될 뻔했고 그러면 11명이 지정 없이 승급됐다. **되돌릴 데이터가 없다.** **② 자동 만료 폐지** — `member_state` 가 `valid_until` 을 보지 않는다. 지정한 승급이 **시간으로 풀리면** 지정의 뜻이 사라진다(확정 6 무기한). **컬럼도 이미 붙은 값(3행)도 건드리지 않는다** — 데이터 변경은 별도 승인 사안이고, 판정이 보지 않으면 값이 남아도 효력이 없다. 운영자가 손으로 `expired` 를 정하는 길은 그대로 산다. **③ `expired` 를 `cohort` 보다 앞에 둔다**(신설) — `expired` 는 최박사 정의로 **탈퇴에 준하는 처리**다. `cohort` 에 지면 기수 등록만으로 탈퇴가 무력화된다(`held` 를 앞에 둔 것과 같은 근거). **④ `member_tool_access`** — 반환 `full`·`read_only`·`none`. 최박사 지시 *진입하는 사람이 어떤 자격자인지만 패러미터가 넘어가면 된다* 가 곧 이 반환이다. `member_can_assess` 는 **지우지 않고 얇은 래퍼**로 남겨 기존 호출부가 한 줄도 안 바뀐다(T-5 `roleTarget` 선례). **⑤ `expired` 차단 네 자리** — `feed_can_access`·`feed_assert_writable`·`cohorts_select`·`my_cohorts`. **`is_admin` 을 첫 OR 로 유지**(운영자 감독이 끊기지 않게)하고, **목록을 감추는 것은 방어가 아니라** 정책(`cohorts_select`)이 실제 문이다. `my_cohorts` 본문은 **라이브 정의를 그대로 가져와 `WHERE` 한 줄만** 더했다 — 초판 기준으로 재작성하면 뒤에 얹힌 변경이 조용히 사라진다(ADR-122 ⑰ 선례). **지우려는 대상이 그 이름으로 실재하는지 먼저 조회했다**(§11 *초록은 대상이 실재한다는 증거가 아니다*): 함수·트리거 다섯 ✅ · `member_tool_access` ❌(새로 만듦). **⚠ 이 마이그레이션은 적용하지 않았다** — 파일만 쓰고 멈췄고 적용은 파일 이름을 올려 **따로 승인**을 받는다(지난번 `feed_reactions` 때 그 멈춤이 가짜 함수 이름을 잡았다). |
| ADR-145 | 도구 게이트 잠금 + 화면 게이트 걷기 — 한 쌍 (5차 · 최박사 확정 2026-08-30) | **최박사 지시**: *메인 서비스 입장에서는 각 도구 진입을 막지 말고, 진입하는 사람이 어떤 자격자인지만 파라미터로 넘긴다.* **걷는 일과 잠금은 한 쌍이고 잠금을 먼저 세웠다** — 화면 게이트를 걷으면 **서버 게이트가 유일한 방어선**이 되는데, 그 방어선에 **강제가 없다는 것**이 조사로 드러났기 때문이다(게이트가 함수마다 손으로 적혀 있고 빠뜨려도 아무것도 알려주지 않는다). **잠금**(`tests/toolGate.test.ts`): 도구 RPC 목록을 한 곳에 선언 · 각 이름이 저장소에 실재 · 각 정의에 창구 호출 · **역방향으로 목록에 없는 도구 쓰기 RPC 를 이름 규약으로 훑어 등록 누락**을 잡는다. 규약은 새로 만들지 않았다 — 실물이 이미 `<도구>_<동작>` 꼴이다. 변이 셋으로 무는 것을 확인했다(미등록 새 RPC · 게이트 제거 · 반환값 누락). **못 막는 것을 파일이 스스로 적는다**(테스트가 그 문장의 존재를 잰다): **호출은 하는데 결과를 무시하는 경우** · 잘못된 인자 · 일부러 안 거는 것과 실수의 구별(`NO_GATE` 예외 목록이 낡으면 헐거워진다) · RPC 밖 경로. **그러므로 이 잠금은 「게이트가 있는가」를 재지 「게이트가 작동하는가」를 재지 않는다** — 후자는 실DB 통합 테스트의 몫이다. **걷은 자리 둘**: 진단 홈(`/home/assessments`)과 개인 가치 카드(`/my/values`)의 `redirect`. **걷기 전에 걷어도 안전한지 먼저 쟀다** — `pending` 인 사람에게 전 항목이 그려지고 링크만 `undefined` 가 되며, 신규 응시의 진짜 강제는 RPC 안에 있다. **왜 걷나**: 종료된 회기 참여자는 `pending` 이 되어 두 문에서 모두 튕겼고 **자기 검사 데이터를 보러 갈 문이 그 문뿐**이었다. 상시분(`cohort_id = NULL`)은 회기 경로로 갈 수도 없다. |
| ADR-146 | 표시 확정 반영 — 명칭·최근 기수·승급 안내 (5차 T-3·T-4 · **최박사 확정 2026-08-30**) | **① 명칭은 `○○기 참여자` 이고 방문회원이 아니다.** 최박사가 방문회원을 정의하셨다 — *회원가입을 했는데 세미나 참여도 하지 않고 포럼회원 승격도 안 된 사람*. 기수에 참여한 사람은 종료 뒤에도 그 이름을 잃지 않는다. **권한이 같다는 것이지 이름이 같다는 것이 아니다.** 자격 줄의 이름(`방문회원`)과 소속 칩(`1기 참여자`)이 **함께** 선다. **② 최근 기수는 첫 회차일로 잰다**(`CohortRole.firstSessionAt`). *1기참여자 5기운영자 이면 5기운영자* — **역할 사이 우선순위가 아니라 최근 기수의 포지션**이다. 최박사가 적으신 `참여자·인도자·운영자` 는 그 셋이 **자격 이름보다 앞선다**는 뜻이었다. **이름 끝의 숫자로 재지 않는다** — 끝이 `n기` 가 아닌 기수가 섞이면 못 가린다(실물 넷). 첫 회차일은 이름 형식에 의존하지 않는다. 실측: 1기 2026-07-26 · QA 2026-08-14 · 2기 2026-09-20. **회차가 없는 기수는 가장 오래된 것으로 친다**(시작한 적이 없다). **③ 승급 방법 병기** — *촉진자포럼에 가입하고 정회원자격을 취득하시면 된다.* 최박사 원문이고 **한 글자도 고치지 않았다.** 방문회원에게만 붙인다(포럼회원에게는 뜻이 없고 이용 보류에게는 문의 안내가 따로 있다). **좁은 자리는 `MenuSheet` 머리 한 칸뿐**이고(전수 확인 · 공개 현관 칩은 실재하지 않는다) `narrowLabel` 이 `null` 을 주면 부르는 쪽이 쓰던 값을 그대로 쓴다 — 임시 값을 화면에 내보내지 않는다. |
| ADR-147 | 회원 모델 적용 + `cohorts_select` InitPlan 정정 (5차 · 적용 2026-08-30) | **무엇**: `20260830090000_membership_model.sql` 적용. 롤백 문을 먼저 확인하고(적용 전 == 롤백 뒤 · 실데이터 무변경) 파일 그대로 적용, 원장에 파일명 버전 기록. 실측 — memberships 7행·`valid_until` 3행 **불변** · `member_tool_access` 오버로드 1 · `membership_on_cohort_archived` 0 · 트리거 0 · 나머지 다섯 함수 각 1. **⚠ 내가 적은 근거가 틀렸다.** `20260830090000` 주석에 *`member_state` 가 행에 의존하지 않으므로 InitPlan 으로 승격해 쿼리당 한 번만 평가한다* 고 적었는데 **적용 뒤 실측에서 승격되지 않았다** — `Filter` 안에 남아 **행마다 돈다.** PostgreSQL 은 `STABLE` 함수 호출을 그 이유만으로 승격하지 않는다(InitPlan 은 **서브쿼리**에 적용된다). **원리로 추정하고 계획을 보지 않은 것이 원인**이고, 지휘부의 *적용 후 실측하라* 가 정확히 이것을 잡았다. 고침은 후속 `20260830100000_cohorts_select_initplan.sql` — 호출을 **스칼라 서브쿼리로 감싼다**(Supabase 의 `(select auth.uid())` 관용구와 같은 꼴). 트랜잭션 안에서 실증했다: `InitPlan 1 … loops=1`. **판정 결과는 한 글자도 바뀌지 않는다.** 아직 **적용 전**이다. **낡은 테스트 넷을 새 기준으로 고쳤다** — 지휘부가 짚은 넷(`member_can_assess`·`feed_assert_writable`·진실표·표시 층)보다 **파장이 넓었다**: ⑴ `ACCESS_TABLE` 의 `expired` 를 *지난 날짜*로 만들던 방법이 통하지 않는다(저장값으로 만든다) ⑵ 승인 큐의 *이미 만료 → 임박 아님* 이 뒤집혔다(자격은 시간으로 풀리지 않으므로 **임박 갈래에 남는 것이 맞다**) ⑶⑷ 자동 전이 테스트 둘을 **폐지 확인으로 뒤집었다** — 지우면 *마감이 무엇을 하지 않는가* 를 아무도 재지 않아 트리거를 되살려도 조용하다. **실동작 실측**(BEGIN…ROLLBACK · 실계정 무변경): `cohort` → tool=full·피드 읽기/쓰기 ○ · **`expired` → tool=none·피드 읽기 ✕·쓰기 55000·내 기수 0·기수 단건 0** · `held` → tool=read_only·피드 읽기 ○·쓰기 55000. 최박사 모델 그대로다. **측정을 두 번 다시 했다** — 처음엔 운영자를 골라(첫 `OR` 로 통과) 아무것도 재지 않았고, 두 번째는 사람과 기수를 짝짓지 않아 남의 기수를 쟀다. 세 번째에야 맞았다. *측정 도구를 먼저 측정한다* 가 같은 회차에 두 번 걸렸다. **마감 시뮬레이션**(ROLLBACK): 1기 마감 시 등록 11명이 `pending` 9 · `cohort` 2 로 내려앉고 **본인 진단 응답·피드는 그대로 열린다** — *권한은 방문회원과 같되 회기 기록물은 영구 열람* 이 실측으로 성립한다. |
| ADR-148 | **만료 임박 갈래 폐지** — 승인 큐를 대기 하나로 (5차 · **최박사 승인 2026-08-30** · 적용 전) | **무엇**: `list_membership_queue` 에서 `expiring` 갈래를 걷어내고 계약 `MembershipQueueRow.bucket` 을 `'pending'` 하나로 좁혔다. 승인 화면의 임박 카드·구역이 사라진다. 마이그레이션 `20260831090000_membership_queue_pending_only.sql` — **작성했고 적용하지 않았다.** **왜**: ADR-147 이 자동 만료를 폐지하자(`member_state` 가 `valid_until` 을 보지 않는다) **임박이 뜻을 잃었다.** 실측 — `valid_until` 3행이 전부 `2027-08-28` 이라 **2027-07-29 부터 목록에 뜨기 시작해 사라지지 않는다**: 옛 출구(지난 날짜 → `expired` 산출)가 없어졌기 때문이다. 운영자는 보고 할 일도 없고(자격이 꺾이지 않으니 연장할 이유가 없다) 치울 수단도 없다 — **거짓이자 영구히 쌓이는 신호**다. **ADR-122 ⑧ 의 안전망 논거도 함께 소멸했다** — 그 절은 *복수 차수 수료자 기간 미갱신(대상 3명)* 을 임박 목록이 받쳐 준다며 남겼는데, 그 한계를 만들던 **마감 트리거 자체가 ADR-147 에서 폐지됐다.** 지키던 위험이 없으므로 안전망만 남을 이유가 없다. **남기는 것 둘**(최박사 못 박음): ⑴ `memberships.valid_until` **값은 지우지 않는다** — 없애는 것은 **갈래이지 값이 아니고**, 3행은 *운영자가 그날 12개월 기본값으로 승인했다* 는 기록이며 **값이 살아 있어야 만료 정책을 되돌릴 수 있다.** ⑵ `decide_membership` 의 유효기간 인자도 건드리지 않는다 — 이 변경은 **화면 갈래이지 판정이 아니다.** **배포 순서를 못 박았다** — 인자(`p_expiring_days`)가 사라지므로 조합 넷 중 **「적용 먼저」 하나만 깨진다**(옛 코드가 없는 인자를 보낸다). 그래서 **① 코드 배포 → ② 적용**이고, 그 사이 창은 매퍼의 `filter(bucket === 'pending')` 한 줄이 닫는다 — 옛 함수는 `DEFAULT 30` 이라 인자 없는 호출에도 응답하며 임박 행을 딸려 보내기 때문이다. **그 한 줄은 방어적 잡음이 아니라 창을 닫는 장치**이고 주석이 그렇게 말한다. `bucket` 열도 같은 이유로 남겼다(되돌릴 자리 + 그 필터가 설 자리). **함께 걷은 문장 하나**: 화면 하단의 *만료된 계정은 새 체크 응시가 닫히되 열람은 열려 있습니다* 는 **지금 참이 아니다** — `expired` 는 이제 운영자가 손으로 두는 `이용 보류` 뿐이고 ADR-147 이 그것으로 피드·기수 조회까지 막는다. **틀린 문장을 남기지도, 묻지 않은 새 문장을 지어내지도 않았다**(문안은 최박사 소관 · 자리는 주석이 지킨다). **테스트**: 앞 판이 *지난 날짜도 임박에 남는다* 를 단언하고 있었는데 **그것은 물었어야 할 자리에서 테스트를 고쳐 초록을 만든 것이었다.** 이제 **갈래의 부재를 재는 쪽으로 뒤집었다**(트리거 폐지를 다룬 두 테스트와 같은 형태 — *큐가 무엇을 담지 않는가* 를 아무도 재지 않으면 되살려도 조용하다). 대조군을 함께 둔다(대기인 사람은 뜬다 ↔ 옛 임박 조건은 안 뜬다). 적용 전에는 **레드로 두지 않고 시끄럽게 건너뛴다**(ADR-147 의 진실표 방식과 같은 도구). **변이로 검증했다** — 게이트를 강제로 `true` 로 두자 라이브에서 `expected 'expiring' to be '-'` 로 빨개졌다. **재는 것이 실재한다.** **계약 델타**: `MembershipQueueRow.bucket` 축소 · `listMembershipQueue()` 인자 제거(둘 다 **축소**이고 지휘부 승인분). **검증**: tsc 0 · eslint 0 error · vitest **1302 통과 / 7 스킵 / 총 1309**(실DB) · **1252 / 57 / 1309**(실DB 꺼짐). |
| ADR-149 | `cohorts_select` 미인증 가드 + InitPlan 정정 **적용** · `이용 보류` 문안 확정 · `is_admin` 절반 확정 (5차 · **적용 2026-08-30**) | **무엇**: `20260830100000_cohorts_select_initplan.sql` 적용. 정책을 `TO authenticated` 로 한정하고 `member_state` 호출을 스칼라 서브쿼리로 감쌌다. **왜 `CASE` 가 아니라 `TO` 인가**: 지휘부 원안은 단락 평가가 보장되지 않으므로 `CASE` 로 감싸라는 것이었고 그 지적 자체는 옳으나 **이 문제는 그것이 아니었다** — **함수 EXECUTE 권한은 실행 시점이 아니라 계획 시점에 검사된다.** `CASE` 가 실행을 건너뛰어도 **함수 이름이 표현식에 있는 한** `anon` 은 계획조차 못 한다. 실측 — `CASE` 안: anon ❌ 42501 · `TO authenticated`: anon ✅ 0행. **지휘부가 원안을 철회했다**(*"실측이 이겼다 … 원리로 추정하고 계획을 보지 않은 것이 내 쪽에서 두 번째다"*). **이중 가드를 두지 않았다** — `TO` 가 이미 미인증을 거르므로 안에 `auth.uid() IS NOT NULL` 을 또 두면 **사본 둘의 위험을 새로 만든다**(불변식 23). **방어는 `TO`, 문서는 주석**으로 나눴고 지휘부도 원안을 철회하며 이 갈래를 확인했다. **적용 절차**: 롤백 예행(트랜잭션 안에서 적용 → 되돌리는 문 → **표현식·역할이 원문과 일치**하는지 확인 → 전부 ROLLBACK · 라이브 무변경 확인) → 적용 → 검증 셋. 되돌리는 문은 **라이브 `pg_policy` 에서 그대로 옮겼다**(추측 아님). **A 와 B 는 따로 되돌릴 수 있고 한 덩이로 묶지 않았다** — A 는 옛 정책 복원(코드 무관), B 는 옛 함수 복원(새 코드가 `DEFAULT` 로 계속 동작). **검증 셋 전항 통과**: ⑴ `anon` — **JWT 를 한 번도 설정하지 않은 깨끗한 연결**에서 `auth.uid()=NULL` 확인 후 0행·오류 없음(같은 트랜잭션에 `set_config` 가 남으면 미인증을 재는 것이 아니게 된다 — 실제로 한 번 그렇게 잘못 쟀다) · ⑵ `EXPLAIN` 에 `InitPlan 1` + `loops=1` — 운영자가 아니면서 보이는 행이 있는 신원으로 쟀다(운영자는 첫 `OR` 로 빠져나가 뒤가 평가되지 않는다) · ⑶ **25 신원 전후 동일**, 합계 37 → 37, 불일치 0. **⑴ 을 상주 테스트로 올렸다**(지휘부 지시) — 일회성이면 나중에 누가 `anon` 정책을 추가할 때 아무것도 울지 않는다. **변이로 검증했다**: 트랜잭션 안에서 `TO` 를 뺀 옛 정책으로 되돌리자 `roles={-}` 로 변이가 성립했고 `anon` 이 **42501 permission denied for function member_state** 로 터졌다 — 상주 테스트가 재는 조건이 실재한다(라이브 원상 확인). **최박사 결정 둘**: ⑴ *이용 보류는 누구에게도 걸 수 있다* → `is_admin` 첫 `OR` 자리의 **대상은 확정**됐으나 **걸린 운영자가 콘솔에서 무엇을 잃는지는 미정**이라 **코드는 한 줄도 고치지 않고 주석만 갱신**했다(자리 넷 그대로 · 지휘부 지시). ⑵ `expired` 안내 문안 → `이용이 보류되었습니다. 운영자에게 문의해 주십시오.` **사본이 둘**이라 함께 맞췄다 — DB(`feed_assert_writable`)는 **새 마이그레이션 `20260831100000`**(적용된 파일을 고치지 않는다 · 불변식 15) · 화면(`membershipVocab.TIER_LEAD.suspended`)은 직접. 두 사본이 한 글자도 다르지 않음을 `toolGate` 가 잠근다. `held` 문장은 건드리지 않았다(*확인 중* 과 *보류됨* 이 뭉개지면 안 된다). **규율 승격**: *넓히는 변경은 스키마가 먼저, 좁히는 변경은 코드가 먼저다* → `CLAUDE.md` §5. **검증**: tsc 0 · eslint 0 error · vitest **1253 통과 / 59 스킵 / 총 1312**(실DB 꺼짐) · **1305 / 7 / 1312**(실DB · 레드 0). |
| ADR-150 | 5차 첫 덩이 **병합·배포** + 남은 마이그레이션 둘 적용 (**배포 `f437fbf` 2026-08-30**) | **무엇**: `feat/site-v2-5-1` 15 커밋을 `main` 에 병합(`f437fbf`)해 배포하고, 그 **뒤에** `20260831090000`(승인 큐 갈래 축소)과 `20260831100000`(`이용 보류` 문안)을 적용했다. **순서가 요점이다** — `CLAUDE.md` §5 의 *좁히는 변경은 코드가 먼저* 를 처음으로 실제 적용에 썼다. 인자를 없애는 변경이라 **「적용 먼저」였다면 옛 코드가 없는 인자를 불러 승인 화면이 깨졌다.** 코드를 먼저 내보내고 실배포를 확인한 뒤 적용했다. **배포 확인**: `postdeploy` 를 **인수 없이** 돌렸다 — 기대 커밋을 `git ls-remote origin main` 에서 뽑는다(ADR-148 에서 고친 것이 여기서 값을 냈다). 신원 `f437fbf`·`ref=main`·`env=production` 일치 후 실브라우저 5건 전항 통과(현관 부품 4/4 · 소개 원고 4/4 · `/recruit` `x-vercel-cache=PRERENDER` · 딥링크 · `/feed` 307→`/login` 누출 0). **적용 절차**: 둘 다 **롤백 예행 먼저** — 트랜잭션 안에서 적용 → 되돌리기 → `pg_get_functiondef` **정의와 권한까지** 원문 일치 확인 → 전부 ROLLBACK → 라이브 무변경 확인. 되돌리는 문은 라이브 정의에서 받아 만들었고 장부에 `statements`·`rollback` 을 함께 실었다. **B·C 를 한 덩이로 묶지 않았다.** **B 검증**: ⑴ 오버로드가 `list_membership_queue()` 하나(`(integer)` 잔여 0) · ⑵ `authenticated` EXECUTE 있음 · `anon` 없음(**`DROP`+`CREATE` 는 권한을 승계하지 않는 자리**) · ⑶ 대기 1 → 1 이고 돌아온 갈래가 `pending` 뿐 · ⑷ `valid_until` 3 → 3(**값은 지우지 않는다**) · ⑸ 비운영자 42501. **갈래 폐지 상주 테스트가 이제 실제로 돈다** — 적용 전에는 시끄럽게 건너뛰던 것이 장부를 보고 살아났고, 대조군 포함 전항 통과했다. **C 검증**: 정의 문자열을 읽지 않고 **띄워서 받았다** — `expired` 가 `55000` 과 함께 최박사 문안을 던지고, `held` 문장은 그대로이며, **두 문장이 서로 다름까지** 단언한다(상주). *문자열이 있는가* 와 *실제로 던지는가* 는 다른 사실이라 후자를 잰다(게이트 잠금의 *있는가/작동하는가* 와 같은 형식). **발견 — 잠금이 거짓을 지키고 있었다**: `20260830090000` 은 2026-08-30 에 적용됐는데 머리에 *아직 적용되지 않았다* 가 남아 있었고, `toolGate` 가 *적용 금지가 파일 머리에 적혀 있다* 며 **그 거짓을 요구하고 있었다.** 잠금이 있었는데도 틀렸다 — **잠근 대상이 「그 문장이 있는가」였고 「사실인가」가 아니었다.** 적용 상태는 **따라가야 하는 값**인데 **얼어야 하는 값처럼 박혀** 있었다(§11). 세 파일 머리를 *적용됨 <날짜> + 검증 결과* 로 맞추고 잠금도 그쪽으로 옮겼다. **적용된 마이그레이션 셋의 편집은 주석뿐이고 DDL 델타 0 이다**(불변식 15). **하네스도 한 번 틀렸다** — 검증 스크립트의 `oid` 가 모호해(`pg_proc`·`pg_namespace` 둘 다 가진다) 42702 로 멈췄다. **마이그레이션이 아니라 자가 오류였고, 도구를 먼저 재는 습관이 그것을 첫 실행에서 드러냈다.** **검증**: tsc 0 · eslint 0 error · vitest **1253 통과 / 60 스킵 / 총 1313**(실DB 꺼짐) · **1306 / 7 / 1313**(실DB · 레드 0) · `next build` 성공 · 커밋 검산 `333 − 325 = 8`. |
| ADR-151 | 이용 보류는 **운영자에게도 전부** · 문안 확정 셋 (최박사 확정 2026-08-30 · **적용 전**) | **무엇**: ⑴ `is_admin` 이 이용 보류를 보게 한다(`20260831110000_admin_hold.sql` · **작성만, 적용 0**). ⑵ 방문회원 줄 대체 ⑶ 참여자 줄 대체 ⑷ `주십시오` 를 정본으로 확정. **왜 `is_admin` 인가 — 자리 넷을 고치면 안 된다**: 라이브 실측으로 `is_admin` 을 쓰는 **정책이 48개**(전체 88개 중)이고 부르는 **함수가 27개**다. 넷만 고치면 나머지가 다음 사고의 자리가 된다 — 한 곳을 고치면 75곳이 따르는 자리가 있는데 넷을 골라 고치는 것은 규칙을 네 곳에 두는 일이다. **지휘부가 42개라 했으나 실측은 48개다**(세어 보고 적는다). **`member_state` 를 부르지 않는다 — 상호 재귀다**: `member_state` 가 열람 권한 검사에서 **이미 `is_admin` 을 부른다**(ADR-24 패턴). 거기서 `is_admin` 이 다시 `member_state` 를 부르면 서로가 서로를 부른다. **정본을 읽지 않았으면 그대로 넣었을 자리**다(계열 ⑦). 대신 **저장값(`memberships.status`)을 직접 읽는다** — ADR-147 이 만료 산출을 폐지해 `member_state = 'expired'` ⟺ `status = 'expired'` 이기 때문이다. **이 등가는 「만료를 산출하지 않는다」에 기대므로** 통합테스트가 두 값이 같은지를 단언한다(사본 둘을 잠금으로 묶는다 · 불변식 23). **근거**(최박사 확정 · 답은 한 단어 *"전부."*): `이용 보류` 는 탈퇴에 준하는 처리인데 보류된 사람이 콘솔에서 남의 갈무리·리포트를 계속 보면 **무엇을 보류한 것인지 말할 수 없다.** **복구 경로**: 운영자 2명이고 `decide_membership` 이 **자기 자신에게는 걸 수 없게** 막으므로(실측) 한 사람이 걸리면 남은 운영자가 푼다. 둘 다 걸리면 앱으로는 못 풀고 되돌리는 문이나 DB 소유자 권한이 필요하다 — **알고 두는 위험**으로 파일에 적었다. **적용 없이 예행으로 검증했다**(트랜잭션 안 · 전부 ROLLBACK · 라이브 원상 확인): 대조 쌍 성립(A `true`→`false`, **B 는 끝까지 `true`** — 운영자 전체가 꺼진 것이 아니다) · 재귀 없음(`member_state` 가 값을 돌려준다) · 등가 3/3(`expired`→`false` · `held`·`individual`→`true`). **남은 구멍을 보고한다**: 앱 계층 **21곳이 `users.role` 을 직접 읽고** 보류를 보지 않는다(`/admin` 진입 게이트 · `MembershipView.isAdmin` 포함). 적용하면 **DB 는 닫히는데 화면은 열려** 보류된 운영자가 콘솔에 들어가 빈 화면을 본다. **결함이 아니라 미완**이고, 무엇을 보여 줄지는 문안 결정이라 이번에 만들지 않았다. **문안 셋**: 방문회원 — *승인을 기다리는 중입니다…* 를 걷었다(**방문회원은 기다리는 사람이 아니라 아직 아무것도 하지 않은 사람**). 참여자 — **두 문장**으로 바꿨다(뒷 문장이 없으면 *기간이 끝나면 기록도 사라진다* 로 읽힌다). 넷 모두에서 **`만료` 라는 낱말을 금지**하는 잠금을 뒀다 — 폐지된 개념을 가리키게 되기 때문이다. **사본 셋을 둘로 줄였다**: `AccountForm.test.tsx` 가 문안을 하드코딩하고 있어 확정 때 함께 빨개졌다 → 상수를 참조하게 바꿔 **화면 테스트는 「화면이 상수를 쓰는가」만** 재고 문장 자체는 `membershipVocab.test.ts` 가 잠근다. **검증**: tsc 0 · eslint 0 error · `next build` 성공 · vitest **1253 통과 / 64 스킵 / 총 1317**(실DB 꺼짐) · **1306 / 11 / 1317**(실DB · 레드 0). **부수 발견 — 「시끄럽게 건너뛴다」가 시끄럽지 않았다**: 조기 반환 + `console.log` 는 **기본 리포터에서 출력이 삼켜지고**(실측: `--reporter=verbose` 에서만 4줄이 보인다) 건너뛴 것이 **통과로 세어져** 초록의 뜻이 달라진 것을 아무도 모른다 — 이 회차에 잠금이 거짓을 지킨 것과 **같은 모양**이다. → `it.skipIf` 로 바꾸니 **스킵 수가 7 → 11 로 움직여 기본 실행에서도 보인다.** 그것이 진짜 소리다. |
| ADR-152 | **회원자격 보류 = 강퇴** — 로그인 차단 · 슈퍼어드민 · 운영자 상시 자격 (최박사 확정 2026-08-30 · **모델 개정 · 적용 전**) | **무엇**: 보류의 뜻이 *화면 막힘* 에서 **강퇴**로 바뀌었다. `20260831110000_admin_hold.sql` 을 **새 모델로 다시 썼다**(앞 판은 적용된 적이 없다). 바뀐 것 넷 — 로그인이 막힌다 · 빈 화면이 아니라 문구가 뜬다 · 뜻이 강퇴다 · 거는 사람이 **슈퍼어드민만**으로 좁아진다. **최박사 원문**: *"보류라 함은 사실은 회원으로서의 모든 지위가 박탈되는 것이다. 강퇴를 완곡하게 말한 것이다… 회원기능이 마비되어 로긴도 안되고 '회원자격이 보류되었습니다. 운영자에게 문의하십시오.' 라고 뜨는 것이다. 그것은 슈퍼어드민 loginheaven@gmail.com 만 갖는 권한이어야 한다."* **★ 조건은 「보류만 막는다」이지 「승인된 회원만 통과」가 아니다** — `member_state(uid) <> 'expired'` 형태여야 한다. **실측**: 슈퍼어드민의 저장 상태가 **`pending`** 이고 활성 세미나에도 없어 판정이 `pending` 이다. 승인 기준으로 쓰면 **슈퍼어드민이 첫 희생자가 되고 그를 풀어 줄 사람이 없다.** 이 문장을 마이그레이션과 테스트 양쪽에 남겼다. **파일 순서가 규칙이다** — ① 운영자 둘 승급 → ② `is_super_admin` → ③ `is_admin` → ④ `decide_membership`. 조건을 먼저 걸고 승급을 나중에 두면 **그 사이에 잠기는 창**이 생긴다. **승급은 이메일이 아니라 id 로 심고 잡힌 수가 2가 아니면 멈춘다**(`RAISE`). `decided_by` 는 NULL 이다 — 사람이 누른 결정이 아니라 마이그레이션이고, 아무 운영자 id 를 적으면 **기록이 거짓말을 한다.** 화면으로는 할 수 없는 일이다(둘째 계정은 행이 없어 큐에 안 뜨고 자기 자신은 처리 불가). **`is_admin` 한 곳을 고친다** — 실측 **정책 48개**(전체 88) · **함수 27개**가 따른다(지휘부는 42개라 했으나 세어 보니 48개다). **`member_state` 를 부르지 않는다 — 상호 재귀**(그쪽이 이미 `is_admin` 을 부른다). 저장값을 직접 읽고 그 등가를 통합테스트가 잠근다. **계정 잠금은 `auth.users.banned_until`** — 컬럼 실재 · `auth.users` 는 `supabase_auth_admin` 소유에 RLS 켜짐/정책 0개 · 함수 소유자 `postgres` 가 **`rolbypassrls`** 임을 **셋 다 조회로 확인**했다. 보류가 아닌 결정으로 바꾸면 **함께 푼다** — 풀지 않으면 *되돌렸는데 로그인이 안 된다* 가 남는다. **로그인 화면 번역**: `user_banned`(auth-js `ErrorCode` 에 실재 · 정본 확인)를 최박사 문구로 옮긴다. **코드가 이기고 메시지는 보조** — 문자열로만 재면 서버 판이 바뀔 때 조용히 깨진다. 뭉개면 **보류된 사람이 자기 비밀번호를 의심하며 헤맨다.** **적용 없이 예행으로 검증했다**(트랜잭션 · 전부 ROLLBACK · 라이브 원상 확인): 승급 2/2(`valid_until` NULL · `decided_by` NULL) · **`pending` 운영자 통과** · 대조 쌍(A `false` / B `true`) · 슈퍼어드민 대상 42501 · 일반 운영자가 운영자 보류 42501 · 슈퍼어드민은 통과 · **일반 회원 보류는 운영자 누구나 통과**(대조군) · `banned_until` `infinity` → 되돌리면 NULL · 재귀 없음. **보고한 어긋남 셋**: ⑴ 지휘부 검증 지시가 *운영자 둘의 `member_state` 가 `individual` 인지* 였으나 **한 명은 활성 세미나에 있어 `cohort` 가 이긴다**(우선순위상 정상) — 단언을 *저장이 `individual` 이고 판정이 `expired`·`pending` 이 아니다* 로 바꿨다. ⑵ **보류를 거는 화면이 없다** — 오늘 UI 는 `individual`·`held` 뿐이라 확인 화면을 붙일 자리가 없다. 그래서 내용을 **순수 모듈**(`holdConfirm.ts`)로만 내고 버튼·배선은 만들지 않았다. ⑶ **로그인 문구와 `내 정보` 문구가 다르다**(`회원자격이…문의하십시오` / `이용이…문의해 주십시오`). 둘 다 최박사 원문이고 자리가 다르다 — **임의로 합치지 않았다.** **확인 화면**(지휘부 초안 · 최박사 확정 전, 그 사실을 파일과 테스트가 표시): 대상 · 잃는 것(운영자면 문장이 다르다) · **근거 메모 필수**(공백만은 막는다 — 통과시키면 규칙이 형식만 남는다) · 되돌리는 법. **일반 회원 보류에도 같은 확인**을 건다(갈리면 한쪽만 고치는 날이 온다). 서버 가드를 대신하지 않는다. **적용 상태 규칙을 한 걸음 밀었다**(지휘부 판정): 마이그레이션 파일이 **적용 여부를 아예 말하지 않는다.** 정본은 원장이고 파일이 복사하면 반드시 낡는다 — 앞 회차에 두 번 고친 잠금이 그 증거다. **검증 결과는 남긴다**(한 번 일어난 사실이라 얼어야 하는 값). 새 잠금이 다섯 파일에서 상태 주장 0 과 **되돌리는 법 존재**를 잰다 — 그 잠금이 곧바로 `20260831100000` 에 **되돌리는 문이 없다**는 것을 잡았고 더해 넣었다. **채번 잠금도 두 번 잡았다** — 표에 없는 ADR-152 인용과, 주석이 **결번 ADR-12 를 살아 있는 근거로 인용**한 것. 후자는 소급 부여 금지의 실질 형태다. **검증**: tsc 0 · eslint 0 error · `next build` 성공 · vitest **1266 통과 / 67 스킵 / 총 1333**(실DB 꺼짐) · **1319 / 14 / 1333**(실DB · 레드 0). 보류 테스트 일곱은 `it.skipIf` 라 **스킵 수로 보인다**(적용 전이므로). |

---

## 12. 용어집

- **불변식(invariant)** — 번호로 인용되는 절대 규칙(예: *"불변식 17 — matcher 를 좁히지 말 것"*).
  **정본은 `CLAUDE.md` §12 「불변식 24 — 어기지 않는 것」이다**(2026-08-27 지휘부 정리).
  번호는 발주서·메모·코드 주석이 인용하는 **영구 식별자**이므로 바꾸거나 재사용하지 않는다.
  규율이 폐기되면 항목을 지우지 말고 `폐기` 표시와 사유를 남긴다 — 번호를 지우면 옛 주석이 미아가 된다.
  인용은 **번호와 요지를 함께** 적는다(`// 불변식 17 — matcher 를 좁히지 말 것`).
  > **기록으로 남긴다.** 3차 T-6 이 이 번호들의 **목록 부재**를 발견했다 — 1차 발주서 §11에서
  > 붙인 번호가 이후 계속 인용됐으나 저장소에 원본이 없었고, **참조만 살아 있었다.**
  > 추정으로 목록을 만들지 않고 지휘부에 올려 정본을 받았다(ADR-121 정신 — 추정치를 문서에 박지 않는다).
- **프로파일 특징(플래그)**: 인도자 박스에 뜨는 **축의 사실**(예 `원씽 낮음`). 원형 가설의 재료이고 원형 판정이 아니다 — 판정은 인도자가 가이드 식별표로 한다(ADR-114).
- **코어 / 런타임**: 채점하지 않는 1층 공유 인프라(인증·차수·코치·응답봉투·알림·UI).
- **인스트루먼트(진단)**: 코어에 꽂히는 전용 모듈(퓨처나우·SAIL).
- **계약(contract)**: 진단이 코어에 제공하는 4종 구현 규격(응답흐름·채점·리포트·알림).
- **봉투(envelope)**: `responses`의 코어 소유 메타. 속(answers·subjectProfile)은 진단 소유.
- **wave**: 사전(pre)·사후(post). 단발 진단은 null.
- **돌봄/Red Flag**: 위기 신호 → 인도자 즉시 알림.
- **인도자 / 코치**: 화면·안내·문서·AI 프롬프트에 노출되는 역할명은 **'인도자'**다. 코드 식별자·DB 컬럼·RPC·라우트는 `coach`를 유지한다(마이그레이션 위험 대비 편익 없음). '코치'라는 낱말은 워크북 STEP 1의 은유 — 참여자가 자기 인생을 대하는 태도 — 를 가리킬 때만 쓴다. AI 프롬프트(`interpretation.ts`)는 화면 어휘를 따른다(출력 오염 방지 — `INTERPRETATION_SYSTEM_PROMPT`·`buildInterpretationInput` 회귀 단언으로 고정). `CoreForbiddenError` 메시지의 '코치'도 '인도자'로 교체(refineActionError는 '가능합니다' 접미사 매칭이라 정제 유지).
