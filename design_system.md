# 퓨처나우 플랫폼 — design_system.md

> **v7 (2026-08-27 · 4차 F-2b).** §9.7 에 #14 `LeaderCard` · #15 `BookPanel`. 번호는 그대로다.
>
> **v6 (2026-08-27 · 4차 F-2).** §9.7 에 #11~#13 · §11 에 골드 대비 부채 둘. 번호는 그대로다.
>
> **v5 (2026-08-27 · 4차 F-1).** §9.7(공개·홈 부품 9) 신설 · §9.6 사실 정정. 번호는 그대로다.
>
> **v4 (2026-08-27 · 3차 T-1).** 번호를 한 자리도 바꾸지 않고 하위 절로만 증축했다 —
> `architecture.md`·ADR·보고서가 `design_system §1.3` 형태로 가리키고 있어 옮기면 참조가 끊긴다.
> 신설: **§1.6**(상태 대비 실측) · **§3.1**(브레이크포인트) · **§9.3~9.6**(부품 12 + 두지 않는 부품).
> 증축: §10 접근성 한 줄. 해소: §11 의 브레이크포인트 항목 → §3.1.

> 지휘부(설계자+AI)가 시안까지 확정한 디자인 시스템이다. 클로드코드는 이 문서를 따라
> 공용 UI·응답 위젯(B①)·리포트 시각화(B③)·참여 진입 흐름·코치/본부 콘솔을 구현한다.
>
> 범위: **색 토큰 · 타이포 · 응답 위젯 5종 · 리포트 시각화 5종 · 종합 배치 · 참여 진입 흐름 · 코치 콘솔 · 본부 콘솔.**
> 참조: architecture.md §10·§8·§5.2·§1. ADR-04·11·20·133.
> 버전: **v5** (v1.0 구현 정합 — X1 색 확정·라이트 고정·AppHeader variant·토큰 보충·격차 정정. 2026-07-01)
>
> **갱신 규약**: 이 파일을 받으면 기존 design_system.md를 **덮어쓴다**(새 파일 생성 금지 — 동명 충돌 방지).

---

## 0. 핵심 원칙

1. **색은 3단으로만 흐른다**(ADR-11). 원천 hex → 역할 → 컴포넌트. 컴포넌트는 hex·원천 토큰 직접 참조 금지.
2. **브랜드 토큰은 진단이 주입, 중립·의미 토큰은 코어 소유**(ADR-133). 퓨처나우 = 네이비+골드.
   <!-- 문장은 2026-06-27 확정 그대로이고 **번호만 2026-08-28 에 새로 받았다**(내용 변경 없음).
        원래 인용하던 번호는 원문이 존재한 적이 없어 결번으로 잠겼다 — 경위는 ADR-133 행에 있다.
        **여기에 그 결번을 다시 적지 않는다**: 살아 있는 사양이 결번을 인용하면
        tests/adrNumbers.test.ts 가 소급 부여로 보고 막는다. -->
3. **색값은 X1 확정(2026-06-30)**이다(§1.1). 화면은 **라이트 고정** — 다크 모드 미지원(§1.3). 색이 구인·척도·활력 구간을 전달하므로 PC/모바일 색 일관성이 정보 정확성보다 앞선다.
4. **참여자 화면엔 경고색을 쓰지 않는다.** danger/warning/care 의미색은 **인도자 리포트·콘솔에서만**.
5. **명명은 리포트·콘솔에서만.** 측정값('활력 8')이 강의 어휘('시들음')로 명명되는 것은 인도자 화면뿐.
6. **돌봄 우선.** 인도자 화면(리포트·코치 콘솔·본부 콘솔)은 "먼저 챙길 분"·"승인 대기"처럼 할 일·사람을 늘 위에 둔다. 관리 도구가 아니라 돌봄 도우미.

---

## 1. 색 토큰 — 3단 구조

### 1.1 원천 팔레트 (1차 — 색 교체 시 여기만)

```css
:root {
  /* 브랜드 — 네이비 (X1 확정 2026-06-30: 따뜻한 톤, 구조 전용) */
  --navy-900:#102338; --navy-700:#1A3A5C; --navy-500:#3A5475; --navy-300:#7F94AE;
  /* 브랜드 — 골드 (X1 확정 2026-06-30: 점 강조 전용) */
  --gold-700:#A8791A; --gold-500:#C8911F; --gold-300:#DBB05A; --gold-100:#F2E6CC;
  /* 중립 (warm gray) */
  --gray-0:#FFFFFF; --gray-50:#F7F6F4; --gray-100:#EEECE8;
  --gray-150:#E4E1DA;   /* 4차 F-0 신설 — 카드 위에 놓이는 면(입력창·트랙) */
  --gray-200:#DEDAD3;
  --gray-400:#A8A29A; --gray-600:#6B655C; --gray-800:#3A352F; --gray-900:#211E1A;
  /* 의미색 — 인도자 화면 전용 (참여자 응답 화면 사용 금지) */
  --green-600:#2E7D6B; --amber-600:#C0852E; --red-600:#B23B3B;
  /* 의미색 — 리포트·콘솔용 저채도 톤 (절제 표시) */
  --care-fill:#FAECE7;  --care-line:#D85A30;  --care-text:#993C1D;
  --thrive-soft:#9FE1CB; --mid-soft:#E9D8A6;  --languish-soft:#F0997B;
}
```
> **PDF 팔레트 미러**: `report/pdf.tsx`(react-pdf)는 CSS var 미지원이라 위 원천 hex를 **하드코딩 미러**로 둔다. **원천값(특히 navy-700·gold-500)을 X1과 반드시 일치**시킨다 — 현재 드리프트(navy #1B2A41·gold #C9A24B)는 PDF 라이브(3.6) 착수 시 동기화한다. 화면↔PDF 색이 달라지지 않게.

### 1.2 역할 토큰 (2차 — 라이트). 컴포넌트는 이것만 참조

```css
:root {
  color-scheme: only light;             /* 라이트 고정(§1.3) — 브라우저 강제 다크 반전 차단 */
  --color-primary:var(--navy-700);      --color-primary-strong:var(--navy-900);
  --color-accent:var(--gold-500);       --color-accent-soft:var(--gold-100);
  --color-bg:var(--gray-0);             --color-surface-1:var(--gray-50);
  --color-surface-2:var(--gray-100);    --color-surface-sunken:var(--gray-150);
  --color-surface-input:var(--color-surface-sunken);   /* 4차 F-0 신설 — 입력창 전용(별칭 §1.6) */
  --color-surface-inverse:linear-gradient(150deg,var(--navy-900),var(--navy-700)); /* 4차 F-0 신설 — 네이비 면 */
  --color-border:var(--gray-200);       --color-border-strong:var(--gray-400);
  --color-text:var(--gray-900);         --color-text-secondary:var(--gray-600);
  --color-text-muted:var(--gray-600);   --color-text-on-accent:var(--gray-0);
  --color-accent-strong:var(--gold-300);/* 4차 F-0 신설 — 네이비 면 위 강조어 전용 */
  --color-text-on-gold:var(--navy-700); /* 골드 면 위 글자 — 대비 확보(흰 글자는 골드 위 2.79:1 미달) */
  --color-success:var(--green-600); --color-care:var(--amber-600); --color-danger:var(--red-600);
}
```
> **4차 F-0 개편(2026-08-27).** 3차 §11 부채 셋을 닫았다 — `surface-2` 가 배경과 같은 흰색이던 것,
> `border-strong`=`text-muted`, `text-muted` 가 §10 대비 기준 미달이던 것. **면이 네 단으로 섰다**:
> `bg`(1.00) → `surface-1`(1.08) → `surface-2`(1.18) → `sunken`(1.31). 실측은 §1.6.
> **발주 원안은 `sunken` 을 그대로 두어 `surface-2` 와 같은 값이 됐다**(입력창이 카드와 동색).
> `gray-150` 을 넣어 갈랐다 — F-0 중단 보고 §3.1·§4 대안 ②(지휘부 채택).

> **`--color-text-on-gold`(navy-700)**: 골드 배경 위 글자 전용 — 랜딩 CTA·척도 선택 셀·완료 배지 등. 네이비 바 위 글자는 `--color-text-on-accent`(흰색) 유지. 흰 글자를 골드 위에 쓰면 2.79:1로 §10 대비 미달이라 분리(X2a 보완).

### 1.3 라이트 고정 (2026-07-01 확정) — 다크 모드 미지원

```css
:root { color-scheme: only light; }   /* §1.2 에 포함 */
```
```tsx
// app/layout.tsx — <meta name="color-scheme" content="only light"> 방출
export const viewport: Viewport = { colorScheme: 'only light', themeColor: '#1A3A5C' };
```
- **결정(지휘부 2026-07-01)**: 진단·리포트는 색으로 구인·척도·활력 구간을 전달 → **PC·모바일 색 일관성이 정보 정확성**이다. 자동 다크 반전에 맡기면 브랜드가 통째로 뒤집혀(네이비↔골드) 같은 앱이 기기마다 달라 보인다.
- `color-scheme: only light` + viewport meta로 **OS 다크·브라우저 강제 다크(Chrome Auto Dark Theme·삼성 인터넷)의 반전을 모두 차단**한다. `@media (prefers-color-scheme: dark)` 블록은 **폐기**.
- 다크 팔레트가 필요해지면 자동 반전이 아니라 **X1 기준으로 직접 큐레이션**한다(향후·별도, 수동 토글 포함 검토).

### 1.4 규약
- 컴포넌트 CSS·className은 **2차 역할 토큰만**. `#`hex·`--navy-*`·`--gold-*` 직접 참조 금지.
- 선택(selected) = `--color-accent`(골드). 색 교체는 §1.1 원천만 수정.
- **색 렌더링**: 색은 전부 sRGB hex. 이미지·로고는 sRGB 프로파일 임베드(untagged 금지 — P3 화면 과채도 방지). True Tone·Night Shift·OLED 채도 등 기기 요인은 코드로 제어 불가 → §10 색-비의존 단서(라벨 병기)·명암비로 견고화(픽셀 일치를 목표로 삼지 않음).

### 1.5 색 역할 모델
> **네이비 = 앱의 틀 / 골드 = 참여자의 흔적.**
- **네이비**(`--color-primary`): 헤더·화면 제목·블록 헤딩·주요/보조 버튼.
- **골드**(`--color-accent`): 선택(도트·세그먼트·체크)·진행바·슬라이더 값·OTP 입력 중 칸.
- **헤더 부제**(차수명 등)는 **옅은 네이비**(`--navy-300`) — 골드 아님. 진입 흐름 일관화(진입-1b).
- **본부·코치·참여자 홈 헤더는 동일 셸**(AppHeader `variant='root'`, 배경 `--color-primary`=navy-700). 본부 전용 색 깊이 구분(옛 `--hq-*` 토큰)은 **미채택·폐기** — 계층은 색이 아니라 진입 게이트(운영자 전용 라우트)로 구분.

### 1.6 상태 대비 실측 (v4 신설 · 3차 T-1 → **4차 F-0 개편**)

**이 절은 2차 §11 결함에서 나왔다.** 기수 선택 칩의 "선택됨"을 `--color-surface-2` 면으로
표시했는데 그 토큰이 `--color-bg` 와 **같은 색**이라 아무것도 보이지 않았다.
**이름이 다르면 색도 다를 것이라는 가정이 틀렸다.**

수치는 손으로 적었으므로 갈릴 수 있다. `src/app/_lib/designTokens.test.ts` 가 `globals.css` 에서
다시 계산해 이 표와 대조하고, **선언되지 않은 같은 축 충돌이 생기면 레드가 난다.**

#### 대비비 실측 (WCAG · 4차 F-0 이후)

| 구분 | 앞 | 뒤 | 대비 | 비고 |
|---|---|---|---:|---|
| 글자 | `--color-text` | `--color-bg` | **16.60** | 본문 |
| 글자 | `--color-text` | `--color-surface-2` | **14.07** | 카드 위 본문 |
| 글자 | `--color-text-secondary` | `--color-bg` | **5.77** | |
| 글자 | `--color-text-secondary` | `--color-surface-2` | **4.89** | 카드 위 |
| 글자 | **`--color-text-muted`** | `--color-bg` | **5.77** | **F-0: 2.53 → 5.77** |
| 글자 | `--color-text-muted` | `--color-surface-2` | **4.89** | 카드 위 |
| 글자 | `--color-text-on-accent` | `--navy-700`(면 최악단) | **11.64** | 네이비 면 위 흰 글자 |
| 글자 | `--color-text-on-accent` | `--navy-900` | **15.91** | |
| 글자 | **`--color-accent-strong`** | `--navy-700`(면 최악단) | **5.75** | 강조어 |
| 글자 | `--color-accent-strong` | `--navy-900` | **7.86** | |
| 글자 | **`--color-text-on-gold`** | `--color-accent` | **5.70** | 골드 면 위 · **F-5 전: navy-700(4.17 · AA 미달) → navy-900** |
| 면 | `--color-surface-1` | `--color-bg` | **1.08** | 패널 |
| 면 | **`--color-surface-2`** | `--color-bg` | **1.18** | **F-0: 1.00 → 1.18. 카드가 면으로 선다** |
| 면 | `--color-surface-sunken` | `--color-surface-2` | **1.11** | 카드 위에 놓이는 것 |
| 면 | `--color-surface-input` | `--color-surface-2` | **1.11** | 〃(별칭) |
| 면 | `--color-accent-soft` | `--color-bg` | **1.24** | |
| 테두리 | `--color-border` | `--color-bg` | **1.39** | |
| 테두리 | `--color-border` | `--color-surface-2` | **1.18** | 카드 위 |
| 테두리 | `--color-border-strong` | `--color-bg` | **2.53** | |

**`--color-surface-inverse` 는 그라디언트다**(navy-900 → navy-700). **대비는 최악단(navy-700)으로
판정한다** — 글자가 가장 안 보이는 곳이 판정 기준이다.

#### 같은 축 충돌 — **선언된 별칭 외에 0**

값이 같아도 **축이 다르면 충돌이 아니다.** 흰 배경(`bg`)과 "네이비 면 위 흰 글자"(`text-on-accent`)가
같은 값인 것은 당연하다. **면끼리·글자끼리 같은 값일 때만** 구분이 불가능해진다.

| 값 | 토큰 | 축 | 판정 |
|---|---|---|---|
| `#FFFFFF` | `--color-bg` · `--color-text-on-accent` | 면 / 글자 | 충돌 아님 |
| `#1A3A5C` | `--color-primary` · `--color-text-on-gold` | 브랜드 / 글자 | 충돌 아님 |
| `#E4E1DA` | `--color-surface-sunken` · `--color-surface-input` | **면 / 면** | **선언된 별칭 ①** |
| `#6B655C` | `--color-text-secondary` · `--color-text-muted` | **글자 / 글자** | **선언된 별칭 ②** |

#### 선언된 별칭 둘 — **구분에 쓰지 않는다**

> **별칭 ① `--color-surface-input` = `--color-surface-sunken`**
> 입력창은 지금 "카드 위에 놓이는 것"과 같은 면이다. **이름을 따로 둔 이유는 값이 달라서가 아니라
> 나중에 갈릴 자리이기 때문이다** — 입력창 면을 조정할 때 트랙·접힌 블록까지 함께 움직이지 않도록
> 미리 갈라 두었다. F-1 부품은 입력 슬롯에 **반드시 `--color-surface-input`** 을 쓴다.

> **별칭 ② `--color-text-muted` = `--color-text-secondary`**
> **의도된 별칭이다.** 흰 바탕에서 §10 기준(4.5)을 넘으면서 `gray-600` 보다 눈에 띄게 옅은 회색이
> **없다**(`#7D7770` 이 4.43 으로 0.07 차이 미달). WCAG 가 두 단계를 하나로 밀어붙인다.
> **위계 구분은 색이 아니라 크기·굵기로 한다** — `t-caption`/`t-micro` 의 크기 차이와 `font-weight`
> 가 그 일을 맡는다. 두 단계가 있는 척하지 않는다.

#### 금지 — 이 쌍으로 상태를 가르지 말 것

> **① 선언된 별칭 둘로 구분하지 않는다**(위). 같은 색이다.
> **② 면색만으로 상태를 가르려면 `--color-primary`(11.64) 다.** 면 사다리
> (`bg` 1.00 → `surface-1` 1.08 → `surface-2` 1.18 → `sunken` 1.31)는 **깊이**를 말하지
> **선택 상태**를 말하지 못한다 — 단 차이가 0.1 안팎이라 "선택됨"으로 읽히지 않는다.
> 상태는 **면과 테두리의 차이 + `aria-current`/`aria-pressed`** 로 가른다(§10).
> **③ `--color-accent-strong` 은 네이비 면 위 강조어 전용이다.** **흰 바탕 금지 · 장문 금지.**
> 최악단 5.75 라 기술적으로는 본문도 되지만 **용도 제한은 유지한다** — 골드 장문은 읽기 피로가 크다.

**면 위의 면이 생겼다.** 3차 §1.6이 *"이 시스템에 없다"* 고 적었던 것을 F-0이 만들었다 —
`ui-card` 가 이제 테두리가 아니라 **면으로 선다**(1.18). 3차 §11 부채 ①·②·③이 함께 닫혔다.

**어떻게 갈랐는가 — 2차 §11 수정의 실물.** 기수 전환 칩을 `ui-btn--primary`(네이비 면·11.64) /
`ui-btn--ghost`(투명 + 네이비 테두리)로 되돌렸다. 색이 아니라 **면과 테두리의 차이**다.

---

## 2. 타이포

- **본문 폰트**: Pretendard (CDN/npm). fallback: system sans. 숫자 `tabular-nums`.

| 토큰 | size/weight/lh | 용도 |
|---|---|---|
| display | 32/600/1.3 | 표지·결과 헤드라인 |
| h1 | 24/600/1.35 | 화면 제목 |
| h2 | 20/600/1.4 | 블록 제목 |
| body-lg | 17/400/1.7 | **참여자 응답 문항·본문** |
| body | 15/400/1.6 | 리포트·콘솔 본문 |
| caption | 13/400/1.5 | 보조·안내 |
| micro | 12/400/1.4 | 글자수·메타·축 레이블 |

- 참여자 body-lg(17)·존대체. 정보 화면 body(15)·평어체.

---

## 3. 간격·형태 토큰

```css
:root{ --radius:10px; --radius-lg:12px; --radius-pill:999px; --tap-min:44px;
  --space-1:4px; --space-2:8px; --space-3:12px; --space-4:16px;
  --space-5:20px; --space-6:24px; --space-7:28px; --space-8:32px; --border-hair:0.5px; }
```
- 사다리 +4 일관: 4·8·12·16·20·24·28·32. (5/7/8은 핫픽스로 보충 — 랜딩·리포트·폼 여백.)
- **모든 탭 가능 요소 ≥44px**(`--tap-min`).

### 3.1 브레이크포인트·영역별 최대 폭 (v4 확정 · §11에서 승격)

**§11의 "반응형 브레이크포인트 수치 — 미규정"을 여기로 옮겨 해소한다.**
값은 지휘부가 이미 정했고 2차부터 코드가 그 값으로 돌고 있었다 — **문서만 비어 있었다.**

```css
--bp-sm: 640px;   --bp-md: 768px;   --bp-lg: 1024px;   --bp-xl: 1280px;
--w-public: 1200px;   --w-participant: 720px;   --w-sidebar: 250px;
```

| 영역 | 최대 폭 | 거동 |
|---|---|---|
| 공개 영역 | **1200px** 중앙 정렬 | `lg` 미만 다단 → 1단 |
| 인도자·운영자 콘솔 | 제한 없음 | `lg` 이상 좌측 고정 사이드바 **250px** · 미만은 상단 탭 |
| **참여자 화면** | **720px** 중앙 정렬 | **전 구간 1단. 브레이크포인트를 쓰지 않는다** |

**세 번째가 요점이다.** 참여자 화면은 `max-width` 하나로 끝난다 — 모바일 우선(§10)을 유지하고
재설계하지 않는다는 결정이며, 일정의 안전판이기도 하다.

> **CSS 변수는 `@media` 조건에 쓸 수 없다.** `@media (min-width: 1024px)` 의 숫자는 리터럴이어야 하고,
> 위 토큰은 **사람이 대조할 한 곳**으로 둔다. 값이 갈리면 토큰 옆에서 먼저 보인다(ADR-123 ⑧).

---

## 4. 응답 위젯 5종 (B① 렌더 사양) · **참여자 화면**

공통: 노출 문자열은 prompt·title·intro·label·양극 레이블뿐. 선택색 골드. 위젯 모양=문항 성격(세그먼트=방향/도트=강도/슬라이더=거리). **경고색 배제.**

### 4.1 나침반 — 양극 세그먼트 바 (bipolar 5점)
- 5칸 연결 세그먼트, 중앙 유지. 선택 칸 골드. 칸 높이 ≥46px.
- 양극 레이블: 컨트롤 위, 좌/우 각 ~44% 폭 블록 분리(좌·우 정렬, 2줄 허용, 중앙 여백). 한 줄 배치 금지.

### 4.2 지금의 나 — 리커트 (likert 5점) · 2단 구조
- 행 = 질문 한 줄(위) → 5원 전체 폭(아래). 좌우 분할 금지.
- 척도 헤더(min·center·max) 블록 상단 sticky 1회. 헤더·도트 동일 전체 폭 5등분 → min↔1·center('보통')↔3·max↔5 정렬.
- 도트 22px·탭타깃 44px·선택 골드. centerLabel 없으면 중앙 생략(ADR-20).

### 4.3 간격 — 슬라이더 + 숫자 (numeric 0~10)
- 0~10 슬라이더 + 옆 큰 숫자(accent, tabular-nums). 5영역 한 화면 세로 스택.

### 4.4 주관식 — 텍스트 영역 (text)
- 한 문장 2줄·~200자 / 여러 줄 4줄·~500자. 입력 배경 surface-sunken. placeholder 존대체. 글자수 우하단 micro.

### 4.5 체크 — 행 토글 (check)
- 텍스트(좌) + 체크박스(우측, 세로 중앙). 카드 전체 탭 타깃. 경고색·느낌표 금지. 선택 시 행 골드 소프트·박스 골드. 돌봄 체크도 동일한 담담한 외양.

---

## 5. 리포트 시각화 5종 (B③ 렌더 사양) · **인도자 화면**

여기서 의미색이 켜지고 측정값이 강의 어휘로 명명된다. 단 절제 — 낮은 값은 낙인이 아니라 돌봄 신호. 본문은 네이비·회색, 의미색은 돌봄 신호 한 곳에만.

### 5.1 나침반 이동 — 덤벨 (4축)
- NAV1~4 가로 트랙(좌극↔우극). 사전=빈 점(회색), 사후=찬 점(네이비), 연결선. 오른쪽 이동=강화. 다이얼은 장식용만.

### 5.2 간격 — 레이더 (5영역)
- 오각. 사후=면 채움(네이비 13%+실선), 사전=점선 윤곽(회색). 정밀 수치는 영역별 덤벨/표 보조.

### 5.3 GROW+F — 충전 막대 (5축)
- G·R·O·W·F 가로 막대, 사전(회색)·사후(네이비) 나란히. 가장 짧은 사후=보강 포인트. 그룹 한 시점은 단계 사슬(원 채도=충전도).

### 5.4 활력 지수 — 띠 이동 (5~25) · **구간 확정**
- 구간: **시들음 ≤10**(`--languish-soft`) / **중간 11~17**(`--mid-soft`) / **번성 18~25**(`--thrive-soft`).
- 사전→사후 화살표 이동. 옆/아래 상태 배지 + 돌봄 안내. 명명은 여기서. 사후 없으면 단일 구간 게이지 폴백.

### 5.5 돌봄 신호 — 조건부 배너
- 활력 시들음 OR Red Flag OR 돌봄 체크 시에만 최상단 저채도 띠(`--care-*`). 우선순위 안내. 경보·낙인 아님. 신호 없으면 배너 미렌더.

---

## 6. 종합 리포트 배치 (B③ 개인 리포트)

```
[네이비 헤더] 이름 · 사전·사후 비교 · 인도자 전용
[돌봄 신호]   조건부. 시들음·Red Flag·돌봄 체크 시에만
[헤드라인]    활력 띠 이동  |  나침반 덤벨        ← "달라졌는가"
[깊이]        간격 레이더   |  GROW+F 충전막대    ← "어디가 자랐고 약한가"
[그 사람의 말] E1~E3 주관식 · 돌봄 메모
```
- 데스크톱 2×2, 모바일 1열. 위계 동일. 의미색은 돌봄 배너에만. react-pdf도 동일 위계.

---

## 7. 참여 진입 흐름 (참여자 화면)

```
① 코드 입력 → ② 차수 미리보기 → [로그인/가입] → ③ 시작 안내 → 응답
```

### 7.1 코드 입력
- **5칸 분할(OTP 스타일)**, 한 칸당 1글자. 입력 중 칸 골드 테두리, 빈 칸 회색. 5칸 미완성 시 확인 버튼 비활성. 헤더 네이비.

### 7.2 차수 미리보기 (CohortPreview)
- `resolve_cohort_by_code` 메타 표시: 차수명(네이비 크게)·인도자명·현재 인원·진단 종류·예상 시간. 민감정보(응답·명단·전화) 미노출.
- "이 모임에 들어갑니다" + [들어가기](네이비)·[아니에요](테두리). 비로그인 표시 가능(SECURITY DEFINER 메타).
- 메타용 별도 타입 **CohortPreview**(coach_name·member_count, Cohort 도메인 밖) — 계약 추가 시 협의.

### 7.3 로그인/가입 (② 다음)
- '들어가기' 시점 인증 필요(enrollByCode가 auth.uid() 요구). 신규=가입·기존=로그인 분기.
- 가입 최소화: 이메일+비밀번호 또는 소셜. **이름·전화 강요 금지**(ADR-03). 이후 enrollByCode.

### 7.4 시작 안내
- 호흡 고르기("정답 없음·떠오르는 대로·중간에 멈춰도 됨", 존대체) + **보안 고지**("여기 적는 모든 것은 인도자 한 사람만 봅니다") 회색 박스. 헤더 부제(차수명)는 옅은 네이비(`--navy-300`, §1.5).
- **동의는 버튼=동의** + 보안 고지로 갈음. 명시 체크박스 없음. 개인정보 수집 확대 시 명시 동의 추가(plan.md).

### 7.5 참여자 완료 — 부드러운 자기 반영

러너 종료 후 참여자가 보는 마지막 화면. 성찰에 대한 격려를 돌려주되, 모든 신호를 결핍이 아닌 **갈망/지향의 언어**로 번역한다. severity·돌봄 신호는 코치 전용 — 이 화면엔 0건.

골격(세로·모바일·참여자 팔레트 = 네이비 틀 + 골드 흔적 + 중립. 의미색 0):

- **① 마무리 헤더** — 감사·존중. 예: "수고하셨어요. 스스로를 가만히 들여다보는 건 쉽지 않은 일이에요. 끝까지 함께해 주셔서 고마워요."
- **② 나침반 거울(방향, 측정 아님)** — 골드 톤 단순 방향 표시. NAV 응답에서 도출한 *지향*을 한 마디. 점수·눈금 없음. 지향 어휘 = 연결·성장·안정·자유 등(우열 없음). 예: "지금 당신의 마음은 **연결** 쪽으로 향하고 있어요."
- **③ 갈망의 한 문장(핵심 재해석)** — 가장 두드러진 신호를 갈망으로 번역한 한 문장. 인스트루먼트가 고르되 표현은 언제나 갈망. 예(활력 낮음): "지금 당신은 더 깊이 살아있기를 갈망하고 있어요." / 예(활력 높음): "당신 안에 살아있음이 차오르고 있어요. 그 감각을 기억해 두세요."
- **④ 인도자 연결(부드러운 핸드오프)** — 예: "당신이 적어 준 마음을, 당신의 인도자가 함께 살펴볼 거예요. 곧 따뜻한 이야기로 만나요." 돌봄(care/red_flag) 참여자도 여기선 아무 신호도 보지 않는다.
- **⑤ (선택) 믿음 한 줄** — F1·F2 응답 시에만 부드러운 마무리 한 줄. 미답이면 생략.
- **⑥ 닫기** — 단일 동작 [마치기], 골드.

**하드 룰**:
- 참여자 팔레트만. danger/warning/care 의미색·severity·red flag·돌봄 배너 **절대 금지**.
- 활력 버킷 라벨(시들음/중간/번성)·점수·숫자·등급 노출 금지.
- 거울은 방향·언어이지 측정이 아니다.
- 모든 신호 = 결핍 아닌 **갈망/지향**으로 번역.
- 돌봄 신호는 화면에 0건 — 코치 알림 경로로만.

**채점 두 갈래**: 같은 B② 산출에서 참여자엔 *갈망 거울*(나침반 방향 + 갈망 문장), 코치엔 *전체 리포트 + 알림*. 갈망 번역 언어(지향 어휘·갈망 문장)는 퓨처나우 고유 지식 → 인스트루먼트 소유. **CoreContext 계약 변경 없이** 인스트루먼트 내부 컴포넌트로 렌더(G1 보호).

---

## 8. 코치 콘솔 (인도자 화면)

원칙: 코치는 코칭 전문가이나 시스템 초보자(architecture §1). **헤매지 않게 — "지금 할 일"이 먼저.** 돌봄 우선(§0.6).

### 8.1 콘솔 홈 — 돌봄·할 일 중심
- 진입 기본. 위계: [네이비 헤더] → **[먼저 챙길 분]**(저채도 코랄, 이름+사유, 탭→개인 리포트) → [진행 중 차수 요약] → [모든 차수]·[+ 새 차수].

### 8.2 차수 개설 — 3스텝 + 완료
- ① 차수 정보 → ② 진단 종류(카드 선택)·정원(−/+) → ③ 완료·코드 공유.
- 진행바·"n/3". **③ 코드 공유 핵심**: 코드 네이비 박스 크게 + [복사]·[공유] + **안내 문구 예시**(회색 박스). 코드 자동 생성(5자리, 혼동글자 제외).

### 8.3 차수 상세 — 돌봄 우선 명단
- [헤더(←·차수명·상태)] → [3숫자: 응답 완료(네이비)·대기(회색)·돌봄(코랄)] → 명단 3묶음:
  먼저 챙길 분(코랄, 맨 위) / 응답 완료(회색, 각자 `›`→개인 리포트) / 아직 안 함(흐림·화살표 없음).
- 하단 [참여 코드 + 다시 공유]. 덜 쓰는 관리(마감·정원)는 헤더 메뉴.

### 8.4 모든 차수
- 헤더 ←(홈). 차수 카드 = §8.1 요약 카드 동일 양식. 카드 탭 → 차수 상세.

### 8.5 그룹 리포트 (선택)
- 차수 상세에서 진입. GROW+F 단계 사슬(§5.3) + 간격 그룹 평균 레이더. 1주차 오프닝용.

### 8.6 본부 콘솔 (운영자 전용) — 코치 콘솔 확장형
운영자는 3계층 꼭대기(architecture §1). 코치 콘솔과 **같은 레이아웃·부품**에 본부 전용 섹션만 더한다(운영자=코치 겸임 잦음). 돌봄 우선을 본부에도 일관.

- **헤더**: 코치와 **동일 셸**(AppHeader `variant='root'`, navy-700) + "퓨처나우 · 본부" + 부제 "운영자". (본부 전용 색 깊이 `--hq-*`는 미채택 — §1.5. 계층은 색이 아니라 운영자 전용 라우트 게이트로 구분.)
- **위계** (홈):
  1. **[승인 대기]** — 코치 등록 신청자(이름·신청 시점), **행 안에 [승인]·[거절]** 버튼으로 즉시 처리. 본부의 가장 잦은 일. 표준 surface 카드(`--color-surface-1`/`--color-border`).
  2. **[전체 현황 3숫자]** — 활성 차수·참여자·**돌봄 신호**(코랄). 돌봄만 의미색.
  3. **[코치별 현황]** — 코치명·담당 차수·인원·돌봄 수. 돌봄 많은 코치 = 지원 우선순위.
  4. **[연락처 열람] (운영자 전용)** — 평소 **잠금 버튼**(자물쇠). 필요 시에만 연다. 전화는 운영자+본인만(ADR-04, user_contacts). 민감정보 상시 노출 금지.
- 본부 전용 권한: 코치 승인(coach_applications)·전 차수 집계·연락처 열람. 그 외 차수 보기·개인 리포트는 코치 콘솔 부품 재사용.
- 의미색은 돌봄(전체 현황·코치별 돌봄 수)에만 저채도.

---

## 9. 공용 컴포넌트 (코어 /src/core/ui) — **인스트루먼트 중립만**

| 컴포넌트 | 비고 |
|---|---|
| Button | primary(네이비)·ghost(네이비 테두리). ≥44px |
| Card | surface-2 + border-hair + radius-lg |
| ProgressBar | 트랙 surface-sunken, 채움 accent |
| SegmentBar · DotScale · NumberSlider · TextArea · CheckRow · StickyScaleHeader | 응답 위젯 §4 |
| OtpInput | §7.1 코드 5칸 |
| Stepper | §8.2 정원 −/+ |
| ListRow | §8 명단 행(이름+상태+화살표/액션). 코치·본부 공용 |
| StatTriple | §8.3·§8.6 3숫자 요약 |

- **코어는 인스트루먼트 중립 부품만.** 퓨처나우 전용 색·문구·구조 비포함.

### 9.1 리포트 차트군 — **인스트루먼트 소유** (정정)
- Dumbbell·Radar·ChargeBars·VitalityBand·CareBanner는 **퓨처나우 전용**이므로 `/src/instruments/futurenow/report/`에 둔다. **코어에 두지 않는다.**
- 근거: 퓨처나우 측정 구조(4축·5영역·GROW+F·활력)에 묶임. SAIL은 2축이라 무의미. ADR-01·코어 경계 준수.
- 단 저채도 의미색 토큰(`--care-*`·`--*-soft`)은 globals.css 공용(토큰은 코어, 차트는 인스트루먼트).
- (v2 §7의 "리포트 차트군 → 코어"는 오기였음. 본 절로 정정.)

### 9.2 앱 레이어 공용 컴포넌트 (/src/app/_screens) — 코어/인스트루먼트 합성

코어 UI가 아니라 **앱 합성 레이어**의 공용 화면·셸. 동선 규칙을 셸이 강제한다.

| 컴포넌트 | 비고 |
|---|---|
| **AppHeader** | 동선 셸. `variant` **필수** 3종: **root**(로고=홈 링크 + 우측 액션, 뒤로 없음) / **sub**(‹뒤로 backHref + 제목 + 홈 아이콘 + 액션) / **flow**(제목 + 부제만, 로고·뒤로·홈·액션 **없음** — 진단 선형성 강제). 부제는 옅은 네이비(`--navy-300`). |
| **HeaderActions · LogoutButton** | 셸 우측 액션(내 정보·로그아웃). 역할 기반 홈 링크. |
| **SeminarIntro** | 공통 소개(어떤 시간인가요·무엇이 달라지나요·어떻게 진행되나요) **단일 출처** — 랜딩 `/`·CohortPreview 공유. |
| **MirrorView** | 참여자 **갈망 거울**(§7.5 ②③④) — 완료 화면(Completion)·멤버 내 리포트(`/my/cohorts/[id]/report`) 공용. severity·점수·돌봄 0. |

> 진입 5화면(CodeInput·CohortPreview·AuthGate·StartGuide·ProfileForm)은 전부 `variant='flow'` — 출구 없는 선형 흐름. 코치/본부/홈은 `root`, 상세·리포트는 `sub`.

### 9.3 동행 피드 부품 6 (v4 신설 · 2차 ADR-124)

부품마다 같은 여섯 칸으로 적는다 — **쓰임 · 구성 · 토큰 · 상태 · 금지 · 현재.**
"현재" 칸이 이 절의 성격이다: **문서가 코드를 따라가는 것이 아니라, 코드가 이미 한 선택을 확정한다.**

| # | 부품 | 쓰임 · 구성 | 토큰·상태 | 금지 | 현재 |
|---|---|---|---|---|---|
| 1 | **피드 입력창** | `/feed` 최상단 **상시 노출**(접히지 않는다). 본문 + 사진 한 장 슬롯 + 게시 | `ui-textarea` · `Button` | 제목·분류·태그 슬롯을 두지 않는다(DB 에 컬럼도 없다) | `FeedClient` |
| 2 | **피드 아이템** | 작성자 · 시각(KST) · 본문 · 사진 · 반응 · 댓글수 | `ui-card`(면이 아니라 테두리로 선다 — §1.6) | 배지·순위·미게시 표시 | `FeedClient.FeedItem` |
| 3 | **이모지 반응 바** | 4종 토글(👏 🙏 💪 ❤️) · 한 사람 한 반응 | 선택은 **면과 테두리**로 가른다(§1.6 금지 ①) | **수로 정렬하지 않는다** | `FeedClient` |
| 4 | **댓글 1단** | 목록 + 한 줄 입력 | `ui-btn--ghost` | **대댓글 슬롯을 만들지 않는다** — DB 에 `parent_id` 가 없다 | `FeedClient.CommentToggle` |
| 5 | **날짜 구분선** | 하루 경계. 오늘이 위 | 상단 hairline + `--color-text-secondary` | 요일 강조·색 구분 | `FeedClient` |
| 6 | **인도자 피드 요약** | 최근 7일 흐름 · N일째 소식 없는 분 | `ui-card` + 목록 | **판정 없음 · 색 없음 · 정렬 없음.** 지목이 아니라 안부의 재료다 | `FeedSummary` |

### 9.4 내비·선택 부품 2 (v4 신설)

| # | 부품 | 쓰임 · 구성 | 토큰·상태 | 금지 | 현재 |
|---|---|---|---|---|---|
| 7 | **기수 전환 셀렉터** | 피드를 가진 기수가 **둘 이상일 때만** 노출. 기수당 칩 하나 | **선택 `ui-btn--primary`(11.64) / 미선택 `ui-btn--ghost`(8.36)** + `aria-current="page"` | **`--color-surface-2` 로 선택을 표시하지 않는다**(2차 §11 결함) | `FeedClient` |
| 8 | **콘솔 사이드바** | `/admin`·`/coach` 계열 내비. `lg` 이상 좌측 고정 **250px** · 미만 상단 탭(§3.1) | 현재 항목 `aria-current="page"` + `ui-btn--primary` 계열 | **참여자 화면에 넣지 않는다 — 콘솔 전용** | **미구현(T-4)** |

### 9.5 이월 부품 4 (v4 신설 · 1차부터 대기)

| # | 부품 | 쓰임 | 금지 |
|---|---|---|---|
| 9 | **날짜 입력** | 회차 일정·유효기간 | — |
| 10 | **단일행 입력** | 가치 카드 라벨(≤60자) 등 | — |
| 11 | **승인 큐 표** | 운영 콘솔 가입 승인. `ListRow` 확장으로 될지 별도 부품일지 판단 | 전화번호는 **마스킹 기본**(불변식 13) |
| 12 | **상태 배지** | 회원 상태 5종 | **참여자 화면 비노출** — 상태는 운영 어휘다 |

### 9.6 두지 않는 부품 — 참여자 앱 하단 탭바

**빈 칸으로 남겨 두면 다음 사람이 만든다.** 그래서 "없음"이 아니라 **"두지 않는다"** 로 적는다.

| 확인 | 사실 |
|---|---|
| 시안 v1.4 A(공개 홈)의 `.tabbar` | **있다.** **공개 현관용 5칸**(홈·소개·신청·자료실·로그인) |
| 시안 v1.4 **B·C·F 의 `.tabbar`** | **있다 — 참여자 앱 4칸**(홈·내 여정·피드·자료실). G 는 4칸(홈·소식·자료실·계정) |
| 현재 공개 현관 | **평문 링크 줄**(소개·소식·자료실·문의) + 로그인 버튼 — **역할은 같고 형태가 다르다** |
| 현재 참여자 앱 | 하단 탭바 **없다.** 내비는 `AppHeader` 하나 |

> **⚠ 사실 정정 (2026-08-27 · 4차 F-1).** 이 표의 원래 행은 *"참여자 앱 하단 탭바 — 시안에도
> 코드에도 없다"* 였다. **틀렸다.** 3차 T-1 에서 A 화면의 5칸 탭바만 보고 v1.4 전체를 단정했고,
> B·C·F 의 4칸 참여자 탭바를 세지 않았다. **코드에 없다**는 맞고 **시안에 없다**는 틀렸다.
> 사실 칸만 고쳤고 **아래 결정은 그대로 둔다** — 결정은 지휘부의 것이다.
> 다만 **근거 하나가 사라졌음을 명시한다**: "시안에도 없다"가 아니라 **"시안에는 있으나 두지 않는다"**
> 이므로, 아래 이유는 이제 §9.6 첫 문단의 *채택하지 않았다* 와 같은 종류의 판단이다. → **질의 항목**

> **공개 현관은 평문 링크로 확정한다**(3차 판정 A). 현관은 외부인이 처음 만나는 화면이고
> **아이콘은 뜻을 배워야 읽힌다.** 시안의 아이콘 탭바는 모바일 앱 관습을 웹 현관에 옮긴 것이었다.
> **시안이 확정과 어긋나면 확정이 이긴다** — 시안에는 있으나 **채택하지 않았다.**
>
> **참여자 앱 탭바를 두지 않는 이유**: 내비는 `AppHeader` 하나이고, 진입은 통합 홈 바로가기 ·
> 차수 홈 · 헤더로 낸다. 필요한 것은 탭바가 아니라 **링크**다.
> 임의로 지으면 부품 사양이 도착했을 때 **두 번 짓는다**(2차 rev.1 §6.3 확정).

> **확정(2026-08-27 · 지휘부 회신).** 사실 정정을 수용하고 **결정은 유지**한다.
> 대조표에서 탭바는 **`✕ 불채택(지휘부 결정)`** 으로 적는다 —
> **미구현과 불채택을 표가 갈라야 한다.** 미구현은 남은 일이고 불채택은 끝난 일인데,
> 같은 기호로 적으면 다음 사람이 남은 일로 읽고 만든다.

### 9.7 공개·홈 부품 15 (v5 신설 · F-1 아홉 + F-2 넷 + **F-2b 둘**)

`src/app/_screens/site/`. **공개 현관·로그인 홈 전용 계층**이고 콘솔·측정 부품과 섞지 않는다.

**열다섯 모두에 공통으로 걸리는 한 줄** — **부품은 계산하지 않는다.** 현재 회차·잠금·완료·현재 경로가
전부 prop 이고, 부품 안에 날짜·권한·집계가 없다. 판정은 화면(page) 층이 하고 부품은 받아 그린다.
(불변식 10 계열. §9.3 의 "현재" 칸과 같은 성격으로 **코드가 이미 한 선택을 확정한다.**)

| # | 부품 | 쓰임 · 구성 | 토큰·반응형 | 금지 | 현재 |
|---|---|---|---|---|---|
| 1 | **`SiteHero`** | 공개 현관 머리. eyebrow · 헤드라인(`<b>` 강조 슬롯) · lead · CTA 0~2 | `surface-inverse` 150deg · 헤드라인 **52px(lg↑)/26px** · 강조·eyebrow `accent-strong` · lg↑ 2단 1.05:0.95 | 빈 슬롯을 자리로 남기지 않는다 — 없으면 **그리지 않는다** | `SiteHero.tsx` |
| 2 | **`GrowAxis`** | GROW+F 5행. 글자·영문·한글·회차 4슬롯 | 라벨 `--color-accent` 36px(md↑) · **md↓ 가로 5점 트랙**(모바일 우선) | 부품에 축 이름을 박지 않는다 — **G·R·O·W·F 도 prop** | `GrowAxis.tsx` |
| 3 | **`CardBand3`** | 3장 카드 띠. 키커·제목·본문 | lg↑ 3열/1열 · 키커 `gold-700` · 면은 `surface-2` | 키커 없으면 그리지 않는다 | `CardBand3.tsx` |
| 4 | **`WeekTimeline`** | 6주 여정. 회차·제목·산출물 3슬롯 | lg↑ 6열/2열 · 칸 상단 2px 규칙선, 현재만 `--color-accent` | **현재 회차는 `currentIndex` prop.** 없으면 아무 칸도 강조하지 않는다 | `WeekTimeline.tsx` |
| 5 | **`SiteRoleCard`** | 로그인 홈 역할 카드. 기수 배지 · 소속 · 제목 · 부제 · CTA 1 | `surface-inverse` + 우상단 골드 방사 원 · 배지 `accent-strong` 테두리 | CTA 없으면 **링크가 아니다** — 갈 곳 없는 링크를 만들지 않는다 | `RoleCard.tsx` |
| 6 | **`QuickTiles`** | 바로가기 2×2. 아이콘·제목·힌트 | `surface-1` + hairline · `min-height: var(--tap-min)` | **이모지 금지** — 인라인 SVG 스트로크(`currentColor`) | `QuickTiles.tsx` |
| 7 | **`MenuSheet`** | 우측 전체 메뉴 시트. 네이비 헤더(이름·역할·기수) · 회차 칩 · 그룹 목록 | `role="dialog"` · `aria-modal` · 폭 `min(360px, 88vw)` | **열림은 prop.** 시트가 스스로 열리지 않는다 | `MenuSheet.tsx` |
| 8 | **`SessionChipStrip`** | 회차 칩. 완료·진행·열림·잠금 4상태 | 완료 `--color-primary` · 진행 `--color-accent`+`text-on-gold` · 잠금 점선+`surface-sunken` | **잠긴 회차를 감추지 않는다.** 상태를 **색만으로** 말하지 않는다(`aria-label`) | `SessionChipStrip.tsx` |
| 9 | **`SiteGnb`** | 네이비 상단 바. **두 모습** — `public`(시안 P1 `.gnb`: 로고 · 메뉴 6 · 골드 로그인) / `member`(시안 B `.topbar`: **로고 + 햄버거뿐**) | `public` md↑ 메뉴 · md↓ 햄버거 · `member` 는 폭과 무관하게 햄버거(시트가 내비다) · `aria-current="page"` | 현재 경로 판정을 부품이 하지 않는다 — **`currentPath` prop**(라우터를 읽지 않는다). **로그인한 사람에게 로그인 버튼을 다시 보이지 않는다** | `SiteGnb.tsx` |
| 10 | **`SectionTitle`** | 소구획 머리. 제목 · 부제 · 우측 액션 3슬롯 | 하단 hairline · lg↑ 부제가 제목 옆으로 붙는다 | 부제·액션 없으면 그리지 않는다 | `SectionTitle.tsx` |
| 11 | **`NewsRow`** | 소식 줄. 배지 · 제목 · 날짜 | 배지 골드 면 · 날짜 `text-muted` · 줄 사이 hairline | **날짜를 만들지 않는다** — 이미 만들어진 문자열을 받는다. `href` 없으면 링크가 아니다 | `NewsRow.tsx` |
| 12 | **`RecruitCard`** | 모집 안내 띠. 키커 · 제목 · 본문 · CTA 1 | `surface-2` + hairline · 골드는 **CTA 에만** | **모집 개폐를 판정하지 않는다** — CTA 를 줄지는 화면이 정한다 | `RecruitCard.tsx` |
| 13 | **`SiteFooter`** | 푸터. 소속 · 보조 문장 · 링크 줄 | `navy-900` 면 · md↑ 좌우 배치 | **링크를 박아 두지 않는다** — 없는 페이지로 보내지 않으려면 화면이 줘야 한다 | `SiteFooter.tsx` |
| 14 | **`LeaderCard`** | 인도자 소개. 사진 · 이름 · 직함 · 태그라인 · 약력 5행 · 소개문 | `surface-2` + hairline · 사진 정사각 `--radius-lg` · lg↑ 2열 | **사진이 없으면 이니셜 자리표시자.** `maxSize` 상한을 **코드로** 막는다(주석 아님) | `LeaderCard.tsx` |
| 15 | **`BookPanel`** | 도서 소개. 표지 · 서지 표 · 소개 단락 · 구매(ghost) · 고지 · 단체 구매 | lg↑ 표지 좌 300px / 본문 우 · 표지 정면 평면 + 얇은 그림자 | **3D 목업 금지** · **골드 primary 금지**(세미나 신청 CTA 전용 위계) · 단락을 합치지 않는다 | `BookPanel.tsx` |

**#10 은 P1 `.band-h` 와 B `.section-title` 을 하나로 합친 것이다**(2026-08-27 지휘부 확정).
F-1 §7 목록에서 *"역할이 겹친다 — 하나로 볼지 판단 필요"* 로 올렸고 **같은 것**으로 판정됐다.

**#14~#15 는 시안이 아니라 발주서가 사양을 준 부품이다**(`site_v2_4_F2b` §3).
`/about` 은 P1~P4·A~G 어디에도 없어 대조할 시안이 없다 — **그래서 발주가 시안을 대신했다.**
불변식 20 은 *"확정 전 UI 를 임의로 디자인하지 않는다"* 이지 *"시안이 없으면 못 만든다"* 가 아니다.
**발주서의 사양 승인이 곧 확정이다.**

**번호는 #14·#15 다.** 발주 §3 표는 `10`·`11` 로 적었으나 §9.7 에는 이미 #10 `SectionTitle` ·
#11 `NewsRow` 가 있다. 발주 본문이 *"§9.7 +2행"* 이라 했으므로 **이어 붙이는 것**이 뜻이고,
번호를 재사용하면 옛 참조가 미아가 된다(불변식 12.1). **사양은 발주 그대로이고 번호만 이었다.**

**#11~#13 은 F-1 §7 이 *만들지 않고 목록만 보고* 한 것들이다** — `.news-row`·`.recruit`·`.foot`.
F-2 게이트가 *"시안 대조표 전항 ○"* 라 그때 비로소 만들었다.
**목록으로 넘긴 것이 다음 단계의 입력이 된다** — 그래서 만들지 않은 것도 적어 둔다.

**섹션 밴드(시안 `.band`)는 부품이 아니라 클래스 하나다**(`.site-section`).
여백과 최대 폭 한 벌뿐이라 부품으로 만들면 `children` 만 넘기는 껍데기가 늘고, 그 껍데기를 또 테스트해야 한다.

**#7 의 focus trap 은 게이트 항목이다**(4차 강조 ②). 판정은 순수 함수 `sheetKeys.ts` 로 떼어
`sheetKeys.test.ts` 가 전수하고, 부품에는 DOM 배선만 남는다 —
이 저장소는 jsdom 이 없고, **그 하나를 위해 의존성을 들이지 않는다**(`loginOutcome`·`consoleNav` 관행).

**#7 안의 칩 간격만 4px 다.** 390px 폰에서 시트 안쪽 폭은 `min(360, 88vw) - 40 = 303px` 인데
기본 8px 간격이면 `44*6 + 8*5 = 304px` — **1px 이 모자라 여섯째 회차만 홀로 다음 줄로 내려앉는다.**
44px 탭 크기도 88vw 시트 폭도 줄이지 않고 **간격만** 좁혀 한 줄에 세웠다
(시트를 넓히면 바깥 탭으로 닫을 자리가 47px→31px 로 좁아진다). ~365px 미만에서는 감기지만,
감기더라도 **감추지는 않는다.**

---

## 10. 접근성

- 탭 타깃 ≥44px. 라디오/체크 `role`·`aria-checked`·키보드(Space/Enter).
- 명도 대비 본문 ≥4.5:1, 큰 텍스트·그래픽 ≥3:1(라이트·다크). 의미색 띠는 텍스트 라벨 병기(색 비의존).
- 모바일 우선. 슬라이더·세그먼트 터치·탭 양쪽.
- **상태를 색만으로 말하지 않는다** (v4 신설 · 2차 §11). 선택·활성·눌림은 **면·테두리·굵기 중 하나 이상**과
  `aria-current`·`aria-pressed` 를 함께 쓴다. 색 하나에 기대면 그 색이 배경과 같아지는 날 조용히 사라진다 —
  실제로 그렇게 났다. 쓸 수 있는 면 대비는 `--color-primary` 뿐이다(§1.6).

---

## 11. 미확정 (다음 세션)

- **코치 등록 신청(신청자 측)** 화면·승인 후 알림 — 미정(운영자 측 승인/거절 `decide_coach_application`·AdminMembers는 구현).
- ~~**면 위의 면이 없다**~~ · ~~**`--color-text-muted` 가 §10 대비 기준을 어긴다**~~ ·
  ~~**`--color-border-strong` = `--color-text-muted`**~~ — **셋 다 4차 F-0 에서 닫혔다**(2026-08-27).
  `surface-2` 가 `gray-100`(1.18)이 되어 카드가 면으로 서고, `text-muted` 가 `gray-600`(5.77)으로
  올라가 §10 기준을 넘으며, 그 이동으로 `border-strong` 충돌이 함께 풀렸다.
  **남은 것은 `text-muted`=`text-secondary` 별칭 하나이고 그것은 회피 불가라 §1.6에 선언했다**
  (4.5 를 넘으면서 더 옅은 회색이 없다). 항목을 지우지 않고 취소선으로 남긴다 —
  **왜 부채였는지가 사라지면 다음에 같은 자리에서 또 넘어진다.**
- ~~**참여자 앱 하단 탭바 — 근거 하나가 사라졌다**~~ — **해소(2026-08-27 · 지휘부 회신)**.
  §9.6 의 사실 오류는 고쳤고 **결정("두지 않는다")은 유지**한다. 근거가 *"시안에도 없다"* 에서
  *"시안에는 있으나 채택하지 않았다"* 로 바뀌었을 뿐 결론은 같다.
  **표기 규율이 함께 확정됐다** — 대조표에서 **✕(미구현)과 ✕(불채택)을 가른다.**
  탭바는 **불채택(지휘부 결정)** 으로 적는다. → §9.6

- ~~**골드 면 위 글자가 4.17 이다**~~ — **해소(2026-08-28 · A안 채택)**.
  `--color-text-on-gold` 를 `navy-700` → **`navy-900`** 으로 내렸다: **4.17 → 5.70.**
  **함께 움직인 셋이 한 커밋이다** — 토큰(`globals.css`) · 게이트 테스트(`4.17 → 5.70`) ·
  **PDF 하드코딩**(`report/labels.ts` `ink: '#1A3A5C' → '#102338'`).
  **CSS 만 고치면 화면과 PDF 가 갈라진다**(§1.1 PDF 팔레트 동기화 의무).
  회귀 실측: Lighthouse 접근성 `/`·`/about`·`/recruit` **96 → 100** · `color-contrast` **전부 통과** ·
  vitest 1048 통과 · 육안 회귀 0. 항목을 지우지 않고 취소선으로 남긴다 —
  **왜 부채였는지가 사라지면 다음에 같은 자리에서 또 넘어진다.**
  *(대안 실측 기록: 면을 `gold-700` 으로 내리는 안은 오히려 **3.00** 으로 나빠졌다.
  골드를 어둡게 하면 네이비 글자와 가까워지기 때문이다. 크기 기준 명문화도 불가 —
  골드 면 글자 실측 최대가 17px 이라 대형 텍스트 기준 18.66px 굵게에 닿지 않는다.)*
- **본문 크기로 쓸 수 있는 골드 글자색이 없다**(4차 F-2 실측). 팔레트에서 가장 진한 골드가
  `--gold-700`(#A8791A)인데 `surface-2` 위 **3.29** · 흰 바탕 **3.88** 로 둘 다 4.5 미달이다.
  시안의 **골드 키커**(`.tcard .k`·`.recruit .k`)를 그대로 쓸 수 없어 F-2 는
  `--color-text-secondary`(4.89)로 내렸다 — **골드 정체성이 그 자리에서 사라졌다.**
  해소하려면 `gold-800` 급(예 #8A5F10 → surface-2 위 4.78 · 흰 바탕 5.64)을 신설해야 하고
  그것은 팔레트 확장이라 **지휘부 확정 사항**이다. 임의로 만들지 않았다.
- **다크 팔레트** — 라이트 고정(§1.3)이 현 정책. 필요해지면 X1 기준 직접 큐레이션 + 수동 토글 검토.
- **B③ 코치 리포트 해석 문구** — AI 초안 생성·저장(구현) + 코치 수정 UI(B③-3) + 첫 열람 비차단 로딩 + PDF 반영(B③-4).
- **PDF 라이브**(renderToBuffer 라우트) + 팔레트 X1 동기화(§1.1 노트) — 3.6 단위.

> 해소됨(기록): 색 1차 팔레트(X1 확정 §1.1)·CohortPreview 계약(ADR-22 `CohortPreviewMeta` 추가)·다크 방향(라이트 고정 §1.3).

---

> **정합 갱신(2026-07-01)**: 본 문서를 v1.0 구현 현실에 맞췄다 — X1 색 확정·라이트 고정(다크 폐기)·`--color-text-on-gold`·`--space-5/7/8`·AppHeader `variant`·부제 navy-300·본부=root 셸(`--hq-*` 폐기)·PDF 팔레트 동기화 의무. 감사 근거는 지휘부 요청 감사(design-system-audit).
