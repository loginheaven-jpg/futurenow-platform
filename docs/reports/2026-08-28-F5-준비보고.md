# F-5 총검증 준비 보고

- **지시**: 지휘부(2026-08-28) — *"대기하지 않는다. 계정 도착 즉시 실행만 남게."*
- **브랜치**: `feat/site-v2-4`
- 둘을 낸다: **⑴ 전 화면 4폭 하네스 통합 · ⑵ F-5 시나리오 표 초안**
- **발견 셋**을 §4 에 따로 모았다 — 그중 둘은 **지휘부 판정이 필요**하다

---

## 1. 하네스 통합 — 명령 하나로 **62장**

F-1~F-4 는 회차마다 캡처 경로를 따로 만들었다(`captures/f1`·`f2`·`f2b`·`f3`·`f4`).
**목록이 다섯 곳에 흩어져 있으면 F-5 에서 한 화면이 조용히 빠진다.** 하나로 묶었다.

| 파일 | 무엇 |
|---|---|
| **`scripts/screens.mjs`** | **화면 목록 단일 출처** — 공개 11 · 인증 11 · 폭 4 |
| **`scripts/shots.mjs`** | 모드 셋(`public` · `fixture` · `all`)으로 그 목록을 찍는다 |

### 두 묶음 — 경계는 **`PROTECTED_PREFIXES` 실측**이다

추측이 아니라 `src/proxy.guard.ts` 의 `['/home','/my','/coach','/admin','/account','/preview','/feed']` 로 갈랐다.

| 묶음 | 화면 | 지금 |
|---|---|---|
| **`public/`** | `/` · `/about` · `/recruit` · `/news` · `/library` · `/contact` · `/login` · `/signup` · `/join` · `/pending` · `/reset` — **11** | **오늘 바로 실라우트 44장** |
| **`auth-fixture/`** | 표시 층 SSR 6종 — `gallery` · `gallery-sheet` · `home` · `home-sheet` · `cohort` · `assess` — **18장** | **QA 계정 대역** |
| `auth/`(예정) | 위 + `my-cohorts` · `journey` · `feed` · `account` · `coach-console` · `coach-cohort` — **11** | 계정 대기 · ↓ §4-③ |

```
node scripts/shots.mjs public  <출력>     # 서버가 떠 있어야 한다(SHOT_BASE 기본 :3100)
node scripts/shots.mjs fixture <출력>
node scripts/shots.mjs all     <출력>
```

**실행 확인** — `public` 44장 · `fixture` 18장, **총 62장**이 명령 하나로 나온다.

`screens.mjs` 는 **계정 없이 못 보는 화면 목록**(`AUTH_ONLY_NO_FIXTURE`)도 내보낸다 —
보고서에 그대로 적기 위해서다. **빠뜨린 것과 못 본 것은 다르다.**

---

## 2. F-5 시나리오 표 초안 (지휘부 확정용)

개요 §2 의 F-5 게이트 셋(**전 화면 4폭 · 실기기 시나리오 · 회귀 0**)을 행으로 폈다.
각 행에 **판정 기준**과 **담당**을 적었다.

### A · 전 화면 4폭 (1280 · 1024 · 768 · 390)

| # | 화면 | 담당 | 판정 기준 |
|---|---|---|---|
| A1 | `/` 공개 현관 | 클코1 브라우저 | 시안 P1·A 대조 전항 ○(F-2 표 재사용) · 4폭 깨짐 0 |
| A2 | `/about` 소개 | 클코1 | 원고 §1~§4 반영 ○ · lg↑ 단락형 / md↓ 리스트형 전환 |
| A3 | `/recruit` 모집 | 클코1 | **lg 2단 · md↓ 카드 0** · **ISR `○ 5m` 유지** |
| A4 | `/news` · `/library` · `/contact` | 클코1 | **↓ §4-② 판정 뒤 기준 확정** |
| A5 | `/login` · `/signup` · `/join` · `/pending` · `/reset` | 클코1 | 같음 |
| A6 | `/home` 로그인 홈(시안 B) | **최박사 실기기** + 클코1 | 로고+햄버거만 · 역할 카드 · 2×2 · 소식 · `MemberHome` |
| A7 | **전체 메뉴 시트(시안 E)** | **최박사 실기기** | 기수 배지 · **회차 칩 3상태** · 그룹 4 |
| A8 | `/my/cohorts/{id}` 차수 홈(시안 C) | 클코1 | 오늘 카드 · **진행 점 표시(막대 0)** · 나의 기록 4행 |
| A9 | `/home/assessments` 진단 홈(시안 F) | 클코1 | 두 계열 · 상태 **낱말** · 열람 고지 분기 |
| A10 | `/feed` 동행 | 클코1 | 기수 칩 · 입력창 상시 · 댓글 1단 |
| A11 | `/coach` 콘솔 | 클코1 | **사이드바 lg↑ 고정 250px / lg 미만 상단 탭**(3차 T-4) |
| A12 | `/coach/cohort/{id}` 차수 상세 | 클코1 | 명단 3묶음 · 회차 일정 |
| A13 | `/my/cohorts` · `/journey` · `/account` | 클코1 | 깨짐 0 |
| A14 | `/preview/site` 부품 전시 15 | 클코1 | 부품 15종 4폭 |

### B · 실기기 시나리오 (최박사 폰)

| # | 시나리오 | 판정 기준 |
|---|---|---|
| B1 | 로그인 → `/home` 착지 | 역할 카드가 **한 번의 탭**으로 거점을 가리킨다 |
| B2 | 햄버거 → 시트 열기 | 열린다 · **회차 칩이 세 상태로 갈린다** · 바깥을 탭하면 닫힌다 |
| B3 | 시트 → `내 기수` | 차수 홈으로 간다 |
| B4 | 차수 홈 → `쓰러 가기` | 갈무리 카드가 열린다(**QA 차수만**) |
| B5 | 갈무리 작성 → 저장 → 홈 복귀 | 진행 점이 하나 찬다 |
| B6 | `/feed` 사진 한 장 + 한 문장 | **2차 실기기에서 유일하게 못 닫은 칸**이 여기다 |
| B7 | 가로/세로 전환 | 레이아웃 깨짐 0 |
| B8 | 뒤로 가기 연타 | 상태가 어긋나지 않는다 |

### C · 회귀 0

| # | 항목 | 담당 | 판정 기준 |
|---|---|---|---|
| C1 | `tsc` · `eslint` | 클코1 | 0 오류 · 경고 **2**(기존) 초과 0 |
| C2 | `vitest` | 클코1 | **통과/스킵/총** 한 줄 · 실패 0 |
| C3 | `next build` | 클코1 | 성공 · **`/` `○ 5m` · `/about` `○` · `/recruit` `○ 5m`** |
| C4 | **Lighthouse 접근성** | 클코1 | `/` · `/about` · `/recruit` **100 유지**(골드 A안 이후) |
| C5 | **오염 0** | 클코1 | `cohort_seats_taken('ZR4KB')` 전후 동일 · 실기수 3표 행 수 동일 |
| C6 | 배포 신원 | 클코1 | `/api/version` 이 병합 커밋을 가리킨 **뒤에** 실브라우저 판정 |
| C7 | 게이트 화면 | 클코1 | `/feed` 미인증 **307 → /login** · 본문 콘텐츠 0(불변식 19) |

---

## 3. 하네스 `auth` 모드 — **구현 전. 두 길 중 하나를 골라 주십시오**

`npx playwright screenshot` CLI 는 **로그인을 못 한다.** 인증 뒤 실라우트를 찍으려면 세션이 필요하다.

| 길 | 방법 | 비용 | 위험 |
|---|---|---|---|
| **①** | `playwright` 를 **devDependency** 로 들여 프로그램으로 로그인 후 캡처 | **의존성 +1**(dev 전용) | 없음 — 이미 `npx` 로 같은 엔진을 쓰고 있다 |
| **②** | 이미 있는 `@supabase/supabase-js` 로 로그인해 **세션 쿠키를 만들고** `--load-storage` 로 넘긴다 | 의존성 0 | `@supabase/ssr` **쿠키 형식을 실측**해야 하고, 형식이 바뀌면 조용히 깨진다 |

**의존성 추가는 지휘부 판단 사항이라 임의로 고르지 않았다.**
①이면 반나절, ②면 쿠키 실측이 붙어 하루쯤이다. **무응답 시 `fixture` 대역을 유지**하고,
그 경우 보고서에 *"인증 뒤 화면은 실라우트가 아니라 표시 층"* 을 그대로 적는다.

---

## 4. 발견 셋

### ① 하네스가 **로딩 셸을 찍고 있었다** — 고쳤다

`/library` 를 찍었더니 **`불러오는 중…` 한 줄**만 나왔다. 화면이 빈 것이 아니라
**캡처가 이른 것**이었다 — 서버가 셸을 먼저 내려보내고 하이드레이션 뒤에 본문을 그린다.

`--wait-for-timeout`(기본 2500ms · `SHOT_WAIT` 로 조절)을 붙여 고쳤고,
다시 찍으니 `자료실 · 아직 올라온 자료가 없습니다` 가 제대로 나온다.

> **F-5 에서 이대로 갔으면 클라이언트 화면이 전부 빈 껍데기로 남고,
> 사람이 볼 때는 "화면이 비었다"로 읽혔을 것이다.**
> `/join?code=` 위양성(1차 병합)과 같은 계열이다 — **내가 본 시점**의 문제다.

### ② 공개 화면 **여덟이 아직 옛 골격**이다 — **판정 요청**

`site-gnb`·`site-foot` 존재로 실측했다.

| 화면 | 새 골격 | 비고 |
|---|---|---|
| `/` · `/about` | **○** | F-2·F-2b 에서 교체 |
| `/recruit` | — | **별도 디자인**(카드뉴스 웹판). 옛 골격이 아니라 자기 디자인이고 F-4 에서 2단을 붙였다 |
| `/news` · `/library` · `/contact` · `/login` · `/signup` · `/join` · `/pending` · `/reset` | **✕ 8** | **4차 범위 밖이었다** — 개요 §3 산출물 지도에 없다 |

**개요 §2 의 F-5 게이트는 "전 화면 4폭 통과"** 인데, 이 여덟의 **"통과"가 무엇인지 정해지지 않았다.**

| 안 | 뜻 | 비용 |
|---|---|---|
| **A** | **깨짐 0 만 본다** — 시안이 없으므로 대조하지 않는다 | 0. F-5 그대로 |
| B | GNB·푸터만 씌운다(본문 무접촉) | 화면당 소, 8화면 · 회귀 검증 필요 |
| C | 전면 교체 | 별도 발주 |

> **권장은 A다.** 이 여덟은 4차 발주 범위에 한 번도 들어온 적이 없고,
> **9/20 가동에 필요한 것은 통과 판정이지 재작업이 아니다.**
> B 를 고르시면 **F-5 전이 아니라 F-5 후 별건**으로 두시길 권한다 —
> 총검증 직전에 8화면을 건드리면 회귀 면적이 그만큼 는다.
> **무응답 시 A 로 판정하고 그 사실을 보고서에 적는다.**

### ③ `auth` 모드 미구현 — §3

---

## 5. 다음 순서 (지휘부 확정 그대로)

```
최박사 수행 완료 통보(차수 코드 · env)
  → §4 확인 목록(로그인 · 저장값 individual · 산출 cohort · 실기수 소속 0 · 오염 0 전후)
  → F-3 잔여 실기기(홈 B · 시트 E · 콘솔 사이드바)
  → F-5 본검증(위 A·B·C 표)
```

**대기 중 판정 요청 둘** — §3 `auth` 모드 길 · §4-② 공개 8화면 통과 기준.

---

## 6. 원격 확인

*(회신에 `git ls-remote origin feat/site-v2-4` 실행 출력을 붙인다.)*
