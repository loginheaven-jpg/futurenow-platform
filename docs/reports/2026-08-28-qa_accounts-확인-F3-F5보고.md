# QA 확인 목록 · F-3 잔여 · F-5 본검증 (1차)

- **지시**: 지휘부(2026-08-28) — 최박사 수행 완료 통보 후
- **QA 차수**: `YYGYP` · `[QA] 검증 전용` · **`kind=seminar`**(A안대로) · `status=active` · 담당 코치 `qa-coach` ✓
- **자격 값은 이 보고서 어디에도 없다** — 판정만 적었다(발주 §0.3)

---

## 1. §4 확인 목록 — **전항 통과**

| # | 확인 | qa-user | qa-coach | 판정 |
|---|---|---|---|---|
| 1 | 로그인 | **○** | **○** | ○ |
| 2 | `users.role` | `user` | **`coach`** | ○ |
| 3 | **저장값** `memberships.status` | **`individual`** | **`individual`** | ○ |
| 4 | **산출** `member_state()` | **`cohort`** | `individual` | ↓ 주 |
| 5 | QA 차수 소속 | **`YYGYP`(seminar)** | 없음(**담당 코치**) | ○ |
| 6 | **실기수 소속** | **0건** | **0건** | **○** |

> **`qa-coach` 의 산출이 `individual` 인 것은 정상이다.** 코치는 `enrollments` 에 등록되지 않고
> `cohorts.coach_id` 로 붙는다 — 설계 보고 §1.3 의 `cohort` 산출은 **등록 멤버(qa-user)** 기준이었다.
> 저장값은 둘 다 `individual` 이고, 발주 §1 의 `membership individual` 이 그대로 지켜졌다.

### 오염 0 — **전후 완전 동일**

검증 **시작 전**과 **끝난 뒤**를 같은 쿼리로 쟀다.

```
[전] {"seats_ZR4KB":7,"checkins":45,"feed_posts":3,"value_assessments":0,"real_enrollments":20}
[후] {"seats_ZR4KB":7,"checkins":45,"feed_posts":3,"value_assessments":0,"real_enrollments":20}
```

**차이 0.** 덧붙여 `cohort_seats_taken('ZR4KB')` 는 **최박사 수행 전에 내가 잰 값과도 같다(7)** —
계정 생성·승인·차수 신설 전 과정이 공개 집계를 건드리지 않았다는 뜻이다.

DB 직접 쓰기 **0**(읽기만). 검증용 임시 스크립트는 **저장소에 남기지 않았다**.

---

## 2. F-3 게이트 잔여 — **둘 닫고 하나 남았다**

| 항목 | 판정 | 근거 |
|---|---|---|
| **콘솔 사이드바 lg↑** | **○** | 1280 — 좌측 고정 250px · `콘솔 홈` 골드 현재 표시 |
| **콘솔 사이드바 lg 미만** | **○** | 768 — **상단 탭으로 전환**(3차 T-4 사양대로) |
| **홈 B (브라우저)** | **○** | 로고+햄버거만 · `[QA] 검증 전용` 배지 · 역할 카드 · 바로가기 · `MemberHome` |
| **시트 E 골격** | **○** | 기수 배지 · 이름+역할 · 닫기 · 그룹 4 · 회차 칩 |
| **시트 E 회차 칩 3상태** | **✕ 미충족** | ↓ §3-① — **열림·잠금 둘만 나온다** |
| **실기기(최박사 폰)** | **대기** | B 표 8행 · B6 피드 사진 포함 |

---

## 3. 발견 넷

### ① 열린 회차가 **0** 이다 — 시간대 경계 (**최박사 손 한 번 더**)

QA 차수 회차 일정 실측(지금 `2026-08-27T17:39Z` = KST 8/28 02:39):

| 회차 | opens | closes | 상태 |
|---|---|---|---|
| 1 | 08-13 15:00Z | 08-20 14:59Z | 지남 |
| **2** | 08-26 15:00Z | **08-27 14:59Z** | **지남 — 2시간 40분 전에 닫혔다** |
| 3~7 | 08-31 이후 | | 잠금 |

**최박사 잘못이 아니다.** 절차서대로 *"2회차는 오늘이 포함되게"* 넣으셨는데,
`closes_at` 이 **KST 자정**(UTC 14:59)이라 자정을 넘긴 지금은 이미 지났다.

그 결과 **`current`(진행·골드) 칩이 없고**, 홈의 **`오늘의 갈무리` 타일도 안 뜬다**(둘 다 정상 동작이다 —
열린 회차가 없으니 그린 것이 없다). **`done`(완료·네이비+체크)** 도 없다 — 제출이 0이라서다.

> **닫으려면**: `qa-coach` 로 `/coach/cohort/{QA}` 회차 일정에서 **3회차 `opens_at` 을 오늘로 당기면** 된다.
> 그러면 `진행` 칩이 서고, `qa-user` 가 그 회차 갈무리를 **QA 차수 안에서** 한 번 제출하면 `완료` 칩까지 선다.
> **1분이면 되고, 그 뒤 `auth` 캡처를 다시 돌리면 F-3 마지막 칸이 닫힌다.**

### ② 회차가 **7개**다 — 절차서를 고쳐야 한다

절차서 6단계는 *"6회차를 만듭니다"* 라고 적었는데 화면 기본 편성이 **7회차**다(`seed_cohort_sessions`).
코드는 **회차 수를 박지 않으므로**(`buildSessionChips`·`buildProgress`) 동작은 정상이다 —
**틀린 것은 절차서 문구**다. §5 에서 고쳤다.

### ③ 칩 **7개가 한 줄에 안 들어간다**

F-1 에서 시트 칩 간격을 **6개 기준**으로 좁혔다(390px 안쪽 폭 303px · `44×6 + 4×5 = 284px`).
7개면 `44×7 + 4×6 = 332px` 라 **일곱째가 다음 줄로 내려간다**(실측 그대로).

**결함으로 적지 않는다.** 시안 E 가 6회차이고 §9.7 사양도 6 기준이며,
**잠긴 회차를 감추지 않는다는 약속은 지켜진다**(줄이 바뀔 뿐 일곱째가 보인다).
다만 **7주 편성이 실제로 쓰이면** 시트에서 칩이 두 줄이 된다는 사실을 적어 둔다 → **판단 요청**.

### ④ 하네스가 **시트를 못 찍고 있었다** — 고쳤다

`member-sheet` 의 URL 이 `/home` 과 같은데 **햄버거를 누르지 않아** 홈을 두 번 찍고 있었다
(`cmp` 로 바이트 동일 확인). `screens.mjs` 에 **`open`(누를 버튼 이름)** 과 `full` 슬롯을 더했고,
누르지 못하면 **조용히 넘어가지 않고** 실패로 기록한다.

> F-5 준비의 ①(로딩 셸)·①-b(상한 없는 대기)에 이은 **셋째 하네스 결함**이다.
> 셋 다 *"찍히기는 했는데 다른 것을 찍었다"* 는 같은 모양이다.

---

## 4. F-5 본검증 (1차) — 브라우저 몫

### A · 전 화면 4폭 — **88장 · 실패 0**

`docs/reports/captures/f5/` — `public/` 44 · `auth/` 44.
**무손실 WebP** 로 넣었다(9.1MB → 5.4MB · 59%) — 매 회차 원본 PNG 를 쌓으면 저장소가 무거워진다.
전량은 명령 하나로 다시 만들 수 있다.

| # | 화면 | 판정 |
|---|---|---|
| A1~A3 | `/` · `/about` · `/recruit` | **○** — 시안 대조는 F-2·F-2b·F-4 표 그대로 |
| A4·A5 | `/news` `/library` `/contact` `/login` `/signup` `/join` `/pending` `/reset` | **○ 깨짐 0**(A안 · 시안 대조 안 함) |
| A6 | `/home` 시안 B | **○** |
| A7 | 시트 시안 E | **골격 ○ · 칩 3상태 ✕**(§3-①) |
| A8 | 차수 홈 시안 C | **○** — 진행 **점 표시**(막대 0) |
| A9 | 진단 홈 시안 F | **○** — 상태 낱말 · 고지 분기 |
| A10 | `/feed` | **○** — QA 차수가 `seminar` 라 열린다(A안이 실제로 작동했다) |
| A11 | `/coach` 콘솔 | **○** — lg↑ 사이드바 / lg 미만 상단 탭 |
| A12~A14 | `/coach/cohort/{id}` · `/my/cohorts` · `/journey` · `/account` · `/preview/site` | **○** |

### C · 회귀 0 — **전항 통과**

| # | 항목 | 결과 |
|---|---|---|
| C1 | `tsc` · `eslint` | **0 오류 · 경고 2**(전부 기존) |
| C2 | `vitest` | **1052 통과 / 51 스킵 / 총 1103** |
| C3 | `next build` | 성공 · **`/` `○ 5m` · `/about` `○` · `/recruit` `○ 5m`** |
| C4 | **Lighthouse 접근성** | `/` **100** · `/about` **100** · `/recruit` **100** · `color-contrast` 전부 통과 |
| C5 | 오염 0 | **전후 완전 동일**(§1) |
| C7 | 게이트 화면 | `/feed` 미인증 **307 → `/login?returnTo=%2Ffeed`** · 본문 **23B** · 콘텐츠 **0**(불변식 19) |

> **C6(배포 신원)은 아직이다** — 이번 검증은 **로컬 프로덕션 빌드** 대상이고,
> 브랜치가 `main` 에 병합되지 않아 운영 배포에는 F-3·F-4·골드 A안이 아직 없다.
> **병합 후 `/api/version` 확인 뒤 실브라우저로 다시 판정한다.**

### B · 실기기 — **최박사 대기**

B1~B8 여덟 행. **B6(피드 사진 한 장)을 이 회차에 묶었다**(지휘부 지시) —
2차 실기기에서 유일하게 못 닫은 칸이다.

---

## 5. 절차서 정정 (§3-②)

`docs/reports/2026-08-28-qa_accounts-절차서-최박사.md` 6단계를 고쳤다 —
*"6회차를 만듭니다"* → **화면 기본이 7회차**임을 적고, 날짜 3분할을 7 기준으로 다시 썼다.
**그리고 KST 자정 경계**를 경고로 넣었다(§3-① 이 실제로 걸린 자리다).

---

## 6. 남은 것

| # | 항목 | 누구 |
|---|---|---|
| 1 | **3회차 `opens_at` 을 오늘로** → 진행 칩 · 갈무리 제출 → 완료 칩 | **최박사**(1분) · 그 뒤 클코1 재캡처 |
| 2 | **실기기 B1~B8**(B6 피드 사진 포함) | **최박사 폰** |
| 3 | 7주 편성 시 시트 칩 두 줄 | **판단 요청**(§3-③) |
| 4 | `main` 병합 후 C6 배포 신원 + 실브라우저 | 클코1 |

---

## 7. 원격 확인

*(회신에 `git ls-remote origin feat/site-v2-4` 실행 출력을 붙인다.)*
