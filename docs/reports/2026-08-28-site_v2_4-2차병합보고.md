# 4차 2차 병합 보고

> **골격을 채운 정본이다.** 승인 전 준비해 둔 틀(`…-골격.md`)을 그대로 실행하고 값을 채워
> 이름을 바꿨다 — 골격을 남겨 두면 다음 사람이 보고서로 읽는다.
> **실행 순서 7단계를 한 칸도 건너뛰지 않았다.**

- **대상**: `feat/site-v2-4` — **F-3 · F-4 · `/recruit` 2단 · 골드 A안 · F-5 검증 자산**
- **1차 병합 이후분**(공개 영역은 `1bb05f3` 로 이미 나갔다)
- **승인**: 지휘부 2차 병합 승인(2026-08-28)
- **B행(실기기)은 병합 후에 한다** — 최박사가 **병합 먼저** 길을 골랐다.
  **운영 판 절차서**가 이제 유효하다(↓ §9)

---

## 0. 실행 순서 (이대로 한다)

```
1) git branch --show-current          # main 직접 커밋 금지 — 확인이 막는다(CLAUDE §11)
2) git checkout main && git merge --ff-only origin/main
3) git merge --no-ff feat/site-v2-4 -F <메시지 파일>
4) npx tsc --noEmit && npx vitest run && npx next build     # push 전에 병합본을 다시 잰다
5) git push origin main
6) node scripts/postdeploy.mjs <병합 해시 짧은형>            # 배포 대기 + 실브라우저 5건
7) git ls-remote origin main                                # 원격 조회가 증거다
```

> **4번을 건너뛰지 않는다.** 병합 자체가 무언가를 깨뜨릴 수 있고, 그것을 push 뒤에 알면 늦다.
> **6번은 손으로 `curl` 을 치지 않는다** — 1~3차에서 별칭 응답으로 **위양성을 두 번** 냈다.
> 스크립트가 **배포 신원이 기대 커밋과 같아진 뒤에야** 5건을 잰다.

---

## 1. 병합 결과

| 항목 | 값 |
|---|---|
| **병합 해시** | **`774b634`** (`774b63409d6fccb2fa693f7783409aee3897f8a7`) |
| 병합 전 `main` | `6a59cb0` |
| 방식 | `--ff-only origin/main` 선행 → `--no-ff` |
| 변경 규모 | **155 파일 · +4,318 / −391** |
| **배포 id** | **`dpl_Er9j99EepG2MZC1tYor72u6j8BrW`** |
| 배포 커밋 · env | `774b634` · `ref=main` · **`env=production`** |

### 커밋 수 — **명령과 기준선을 미리 박는다**

1~3차에서 커밋 수가 **두 번 갈렸고(24/26 · 9/8) 둘 다 범위 차이**였다.
그래서 값을 재기 전에 **기준선과 명령을 여기 고정**한다 — 아침의 값이 논쟁 없이 서게.

**기준선은 `6a59cb0` 이다** — 1차 병합 보고 커밋이자 **지금의 `origin/main`** 이다.

```
$ git rev-parse --short origin/main                        → 6a59cb0   (기준선 확인. 다르면 멈춘다)

$ git rev-list --count 6a59cb0..774b634                     → 23   (병합 커밋 포함)
$ git rev-list --count --no-merges 6a59cb0..774b634         → 22   (실작업 커밋)
```

**둘의 차는 정확히 1**이어야 한다(`--no-ff` 병합 커밋 하나). **실측 23 − 22 = 1 — 통과.**

> 밤중 준비 때 잰 값은 21이었다. 그 뒤 준비 커밋 하나(`5c5427f`)가 더해져 **22**가 됐다 —
> **범위가 아니라 시점 차이**이고, 기준선을 적어 둔 덕에 그 사실이 한눈에 선다.

> **⚠️ `1bb05f3..` 로 재지 않는다.** `1bb05f3` 은 1차 **병합 커밋**이고 그 뒤에
> **`6a59cb0`(1차 병합 보고)이 `main` 에 하나 더 올라가 있다.** 그래서 기준선을 그리로 잡으면
> 그 한 개가 더 세어진다. **밤중 실측으로 재현했다**:
>
> ```
> $ git rev-list --count 1bb05f3..origin/feat/site-v2-4   → 22
> $ git rev-list --count 6a59cb0..origin/feat/site-v2-4   → 21   ← 이쪽이 맞다
> ```
>
> **1 차이가 곧 지난 두 번의 논쟁이었다.** 기준선을 적어 두는 것이 그것을 끝낸다.

| 회차 | 내용 | ADR |
|---|---|---|
| F-3 | `/home`(시안 B) · 시트(E) · 역할 카드 판정 이동 · `RoleCard` 삭제 | **128** |
| F-4 | 차수 홈(C) · 진단 홈(F) · 시트 자료 단일화 · `FeedShortcut` 삭제 | **129** |
| F-4 후속 | `/recruit` lg 2단 + 카드뉴스 4장 파생물 | **130** |
| 골드 | `text-on-gold` navy-700 → navy-900 (4.17 → 5.70) + PDF 동반 | — (`design_system` §1.6·§11) |
| F-5 | 검증 하네스(단일 출처 · 상한 · 가로 넘침) · QA 자산 · 캡처 85 | **131** |

### 병합 전 재검증 (push 전에 잰다)

| | 기대 | 실측 |
|---|---|---|
| tsc | 0 오류 | **0** |
| eslint | 0 오류 · 경고 **2**(기존) | **0 오류 · 경고 2** |
| vitest | **통과/스킵/총** 한 줄 | **1052 통과 / 51 스킵 / 총 1103** |
| build | `/` `○ 5m` · `/about` `○` · `/recruit` `○ 5m` | **그대로 — ISR 무손상** |

---

## 2. 배포 확인 — **위양성을 세 번째로 내지 않기 위해**

```
$ node scripts/postdeploy.mjs 774b634

[배포 확인] https://future.yebom.org 가 774b634 를 서빙할 때까지 (상한 10분)
  O 배포 신원                        774b634 · ref=main · env=production · dpl_Er9j99EepG2MZC1tYor72u6j8BrW

[실브라우저 5건]
  O / 공개 현관                      200 · 24KB · 부품 4/4
  O /about 소개                    200 · 원고 부품 4/4 · 구매 ghost
  O /recruit ISR                 200 · x-vercel-cache=PRERENDER · 34KB
  O /join?code= 딥링크              200 · 8KB · 로딩 셸(차수 미리보기는 하이드레이션 후 — 실브라우저로 눈 확인 필요)
  O /feed 미인증 게이트                307 → /login?returnTo=%2Ffeed · 본문 15B · 누출 0

O 배포 확인 + 실브라우저 5건 전항 통과
```

**배포 신원이 병합 커밋과 같아진 뒤에** 5건을 쟀다. 폴링에는 **상한**이 있고 넘기면 실패한다
(`CLAUDE.md` §11 — 기다림에는 끝이 있어야 한다).

---

## 3. 실브라우저 5건

| # | 대상 | 판정 기준 | 결과 |
|---|---|---|---|
| 1 | `/` 공개 현관 | 200 · 부품 4/4 | **○** 24KB |
| 2 | `/about` 소개 | 200 · 원고 부품 4/4 · 구매 ghost | **○** |
| 3 | `/recruit` | `x-vercel-cache=PRERENDER` | **○ ISR 무손상** |
| 4 | `/join?code=ZR4KB` | 200 + **눈 확인** | **○** ↓ |
| 5 | `/feed` 미인증 | 307 · 누출 0 | **○** 본문 15B |

> **4번을 200 으로 통과시키지 않는다.** `/join` 은 셸을 먼저 내려보내고 `?code=` 자동 진입은
> 하이드레이션 후다 — 1차 병합에서 그 사실을 규명했다. 스크립트가 그것을 **문구로 남기고**,
> 차수 미리보기는 **눈으로** 확인한다.

### 4번 눈 확인

운영에서 실브라우저(JS 실행)로 렌더해 **눈으로 확인**했다 — `captures/merge2/join.webp`.

**차수 미리보기 자동 진입** — `퓨처나우2026예봄2기` · 인도자 `운영자` · **현재 인원 9명** ·
체크 `퓨처나우 체크` · 예상 `약 5분` · `SeminarIntro` 3단락 · CTA 둘(`아니에요`·`들어가기`).

**200 만으로 판정하지 않는다** — 초기 HTML 은 로딩 셸이고 `?code=` 자동 진입은 하이드레이션 후다.

---

## 4. 인증 뒤 화면 — 병합 후 재확인

병합 전 검증은 **로컬 프로덕션 빌드**와 **프리뷰** 대상이었다. 운영에서 한 번 더 본다.

```
SHOT_BASE=https://future.yebom.org SHOT_COHORT=<QA 차수 id> \
  node scripts/shots.mjs auth <출력>
```

| | 기대 | 실측 |
|---|---|---|
| 캡처 실패 | 0 | **0** |
| 가로 넘침 | 0 | **0** |
| 장수 | — | **41장** (`captures/merge2/`) |

`preflight` 도 운영에서 통과했다 — 배포 `774b634` · QA 로그인 ○ · QA 차수 보임 ○.

---

## 5. 오염 0 — 병합 전후

| | 전 | 후 |
|---|---|---|
| `cohort_seats_taken('ZR4KB')` | **7** | **7** |
| 실기수 `checkins` | **45** | **45** |
| 실기수 `feed_posts` | **3** | **3** |
| 실기수 `value_assessments` | **0** | **0** |
| 실기수 `enrollments` | **20** | **20** |

**차이 0.** 이 값들은 QA 계정 생성 **전**에 잰 것과도 같다 — 4차 전 과정이 실기수를 건드리지 않았다.

---

## 6. 라이브에 열린 채로 나가는 것

**병합했다고 닫힌 것이 아니다.** 1차 병합 보고와 같은 자리를 둔다.

| # | 항목 | 상태 |
|---|---|---|
| 1 | **공개 8화면이 옛 골격** | **A안 확정** — 깨짐 0 만 본다. **후속 발주 후보 표**는 F-5 준비 보고 §4-② |
| 2 | 본문 크기 골드 글자색 부재 | `gold-700` 이 팔레트 최대인데 3.29·3.88 — `design_system` §11 |
| 3 | 최철영 소개문 표기 250 vs 실측 274 | 문안은 원문 유지 · 원고 v1.1 대기 |
| 4 | 7주 편성 시 시트 칩 두 줄 | **현행 유지 확정** — 부품 주석에 남겼다 |
| 5 | 차수명 출처 이원(DB vs `intake.ts`) | 통일하지 않음 — 판정 대기 |

**부재**: 인도자 사진 2 · 도서 표지 · `이용약관`·`개인정보처리방침` 라우트 · favicon 자산 · SAIL 주소.

---

## 7. 브랜치

`feat/site-v2-4` 는 **남겨 둔다**(기존 관행).

---

## 8. 커밋과 원격 확인

```
$ git ls-remote origin main
774b63409d6fccb2fa693f7783409aee3897f8a7	refs/heads/main
```

원격 트리에도 실물을 확인했다 — **해시만으로는 내용을 말할 수 없다**:

```
$ git ls-tree -r origin/main --name-only | grep -c 'captures/'
124

$ git ls-tree -r origin/main --name-only | grep -c '_screens/site/'
27

$ git ls-tree -r origin/main --name-only | grep -cE '_screens/(RoleCard|FeedShortcut).tsx'   # 삭제 확인
0

$ git show origin/main:src/app/globals.css | grep -c 'color-text-on-gold: var(--navy-900)'
1
```

---

## 9. 다음 — **운영 판 B행 절차서가 이제 유효하다**

병합이 끝났고 `postdeploy` 가 통과했으므로, **운영 판 절차서의 두 조건이 모두 충족**됐다.

**`docs/reports/2026-08-28-F5-실기기-절차서-최박사.md`** — `future.yebom.org` 기준 8단계.
프리뷰 판은 더 쓰지 않는다(같은 코드지만 주소가 둘이면 헷갈린다).

| # | 남은 것 | 누구 |
|---|---|---|
| 1 | **B행 실기기 8단계** — 단계별 `○/✕` 한 줄 · B6(피드 사진) 포함 | **최박사 폰** |
| 2 | 열린 항목 5(§6) | 지휘부 판정 |
