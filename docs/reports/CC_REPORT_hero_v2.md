# 완주 보고 — 홈 히어로 v2 (ADR-171)

- 브랜치 : `feat/hero-v2` → `main` 병합 · 후속 수정 둘 포함
- 근거 : 지휘부 지시서 `히어로 개선 v2` + 토론 2회차 판정(㉮㉯㉰ 권고안 승인) 2026-09-02
- 잰 날 : 2026-09-02

---

## 0. 확정대로 지었습니다

| # | 판정 | 한 일 |
|---|---|---|
| ㉮ | 헤드벨트 — 홈에서만 | `PublicGnb` 가 `currentPath === '/'` 로 판정, 부품은 `transparent` prop 을 받아 그린다 |
| ㉯ | 버튼 — 형태 전역 · 색 배경별 | 코어에 호버·화살표·전이 + **`--on-dark` 갈래 신설**. **밝은 면 무변경** |
| ㉰ | 회차 이름 — 공개를 갈무리에 맞춤 | `WEEK_CELLS` **한 글자도 안 바꿈** |
| 2 | `design_system` §9.7 #1·#2 개정 | 표 두 줄 개정 |
| 4 | 배경 WebP ≤200KB · 폭 2벌 | **1600w 45KB · 2560w 101KB**(원본 5.7MB) |
| — | 모바일 이미지 제외 | **md↓ 는 바이트 0** — 실행으로 확인 |
| — | 한 번에 간다 | 한 회차로 마쳤다 |

---

## 1. 검증 — `node scripts/verify.mjs` 출력 전문 (2026-09-02)

```
── tsc ──
오류 0

── eslint ──
23:8  warning  Unused eslint-disable directive (no problems were reported from '@next/next/no-html-link-for-pages')
✖ 3 problems (0 errors, 3 warnings)
  0 errors and 1 warning potentially fixable with the `--fix` option.

── vitest ──
Test Files  132 passed | 6 skipped (138)
      Tests  1526 passed | 78 skipped (1604)
   Duration  6.60s (transform 13.27s, setup 0ms, import 35.56s, tests 6.98s, environment 25ms)

── next build ──
성공

O 네 지표 전항 통과
```

**총수 대조** : 직전 `1517 / 78 / 1595` → 이번 `1526 / 78 / 1604`.
늘어난 9 = 히어로 v2 잠금 6 + 헤드벨트 범위 잠금 2 + 토큰 잠금 1. 줄어든 것 없음.
**스킵은 78 그대로다** — §3 의 사고를 고친 뒤 원복됐다.

---

## 2. 지시서를 그대로 따르지 않은 자리 셋 (전부 판정을 받았다)

### 2.1 공용 버튼을 안 건드렸다

`.ui-btn`·`.ui-card` 는 **코어 소유**이고 `site.css` 는 참여자 화면도 import 한다 —
실측 **28 파일**이 쓴다. 시안 ghost 가 `color: white` 인데 참여자 바탕이
`--color-bg: var(--gray-0)` 로 밝아 **흰 바탕에 흰 글자**가 된다.

그래서 **일관성을 색이 아니라 형태가 주게** 했다 :

| 층 | 어디에 | 내용 |
|---|---|---|
| 형태 | `core/ui/ui.css` | 호버 들림 · 화살표 슬롯 · 전이 · `prefers-reduced-motion` 존중 |
| 색 | 같은 파일 | **`--on-dark` · `--on-dark-ghost` 신설**. 밝은 면 갈래는 **한 글자도 안 바뀜** |

**모서리·높이는 전역으로 옮기지 않았다** — 2회차 권고에는 넣었으나 실측해 보니
`--radius-lg`(12px)·`--tap-min`(44px) 토큰이 **이미 갈래를 주고 있어** 옮길 필요가 없었다.
전역으로 키우면 28곳 버튼이 함께 커진다. **권고를 실물이 줄여 준 자리다.**

### 2.2 회차 여섯 이름을 안 바꿨다

1·2회차 제목이 갈무리 카드와 **완전히 같은 문자열**이고, `원씽`·`존재가치 선언문`·`인생 조감도` 는
모집 결재 문안에도 있다(각 38·16·12 파일). `WEEK_CELLS` 는 `/about` 도 쓴다.
실기수 갈무리 **46건·22명**이라 공개만 바꾸면 **신청할 때 본 이름과 카드 이름이 달라진다.**

잠금이 양방향으로 잰다 — 갈무리와 같은 이름이 **있는지**, 지시서의 새 이름이 **안 들어왔는지**.

### 2.3 배경을 원본대로 안 썼다

원본 **2560×1440 · 5.7MB PNG**. WebP 두 벌로 냈고 **목표를 못 맞추면 시끄럽게 실패**하도록
품질을 낮춰 가며 만든다(고정값을 박지 않았다).
**md↓ 는 이미지를 아예 안 받는다** — `background-image` 는 미디어 쿼리가 안 맞으면 내려받지도 않는다.

---

## 3. ★ 이 회차가 잡은 것 — 잠금이 초록인데 화면이 틀렸다

**세 가지가 「검증 전항 통과」 상태에서 살아 있었고, 셋 다 눈이나 실측이 잡았다.**

### 3.1 여섯 잠금이 **조용히 스킵**됐다 (붉지 않고 사라졌다)

새 describe 에서 `renderToStaticMarkup(<Home />)` 를 썼는데 `Home` 은 **서버 비동기 컴포넌트**라
suspend 로 터지고 그 블록이 통째로 스킵됐다. `Tests 10 passed | 6 skipped` 였다 —
**붉지 않으므로 보고만 보면 통과다.**

**같은 파일의 두 블록이 `await Home()` 을 쓰며 그 이유를 주석으로 이미 적어 두었다.**
읽지 않고 관용구를 지어낸 것이 원인이다(계열 ⑦). 스킵 수(78→84)를 대조해 잡았다.

### 3.2 헤드벨트를 투명으로 뒀더니 **흰 바탕에 흰 글자**가 됐다

이 띠는 히어로 **위에 겹치지 않고 흐름상 위**에 있어서 투명하면 body 가 비친다.
**「투명하게 한다」가 뜻하는 것을 화면이 정정했다** — 필요한 것은 투명이 아니라 **이어 붙이기**였다.
배경 이미지 맨 위 띠 색을 **실측**해(원본 `rgb(5,10,19)` · 오버레이 합성 후 `rgb(6,12,23)`) 그 색을 뒀다.

### 3.3 ★★ **없는 토큰**을 써서 글이 검정이 됐다

GROW 설명이 배포된 화면에서 안 보였다. 색을 실측하니 **`rgb(33,30,26)` 거의 검정**.
원인은 `var(--navy-200)` 인데 **그 토큰이 존재하지 않는다** — navy 는 900·700·500·300 넷뿐이다.
**CSS 는 없는 변수에 오류를 내지 않고 상속색으로 떨어진다.**

지시서가 `--navy-200` 을 여러 번 쓰는데 저장소에 없는 토큰이고 내가 옮겨 적었다.
「이름이 있으면 토큰도 있을 것」이라는 가정이 틀렸다 — `design_system` §1.6 이
*「이름이 다르면 색도 다를 것이라는 가정이 틀렸다」* 로 **같은 뿌리의 사고**를 적어 두었다.

→ **`site.css` 가 부르는 `var()` 가 전부 어딘가에 선언돼 있는지 재는 잠금**을 지었다.
이 부류는 조용히 검정이 되므로 잠금이 없으면 눈에 걸릴 때까지 산다.

---

## 4. 잠금을 물려 봤다 (§11 ⑪) — 열이 붉어졌다

| 변이 | 결과 |
|---|---|
| 회차 이름을 지시서대로 바꾼다(갈무리와 갈린다) | **붉어짐** |
| 히어로 CTA 를 밝은 면 갈래로 되돌린다 | **붉어짐** (3건) |
| 히어로 아래 사실 셋을 뗀다 | **붉어짐** |
| CTA 화살표를 뗀다 | **붉어짐** |
| **헤드벨트를 전 공개 화면에서 투명하게** | **처음엔 초록 ★** → 잠금을 짓고 다시 물려 **붉어짐** |
| 부품이 prop 을 무시하고 늘 투명하게 | **붉어짐** |
| 부품이 경로를 스스로 판정한다 | **붉어짐** |
| 없는 토큰 `--navy-200` 을 되살린다 | **붉어짐** |
| 아예 지어낸 토큰을 쓴다 | **붉어짐** |

**헤드벨트 범위에 잠금이 없었다** — 물려 보지 않았으면 몰랐다.
그 자리에 둘을 새로 짓고 다시 물려 셋을 붉혔다.

**저장소의 기존 잠금이 나를 두 번 물었다** — `site.purity` 가 `1440px` 과 `max-width: 767px` 을
「§3.1 브레이크포인트가 아니다」로 잡았다. 폭 목록을 늘리지 않고
**1280 으로 옮기고 방향을 뒤집어**(폰이 기본, md↑ 에서 바꾼다) 풀었다.

---

## 5. 실행으로 잰 검증 (배포 후)

`main` `2da5090` · `/api/version` → `2da5090` · `postdeploy` 전항 통과.

「시안과 비슷한가」는 사람이 눈으로 재는 값이라 **기계가 재는 항목으로 번역**했다(2회차 §2.4 약속).

```
[PC 1440]
  O 배경 이미지  적용 · 내려받은 파일 hero-dawn-2560.webp
     헤드벨트  rgb(6, 12, 23)      ← 이미지 상단색과 같다
     히어로 높이 738px · 원형 노드 78x78 · 사실 3 · 설명(보임) 5 · 화살표 2
     CTA  ui-btn ui-btn--on-dark  |  ui-btn ui-btn--on-dark-ghost
     GROW 설명 색 rgb(127, 148, 174)   ← 고친 뒤

[모바일 390]
  O 배경 이미지  없음 · 내려받은 파일 0개      ← 「이미지 빼고 심플하게」
     헤드벨트  rgb(16, 35, 56)
     원형 노드 없음(가로 5점 트랙) · 설명 숨김 · 사실 3 · 화살표 2

[/about]  O 헤드벨트 class="site-gnb" (투명 아님)
```

화면은 `docs/shots/home-v2-1440.png` · `home-v2-390.png` 다.

---

## 6. 판단을 청합니다 — 둘

### 6.1 ★ 문안 여덟은 제가 지었거나 시안에서 뽑았습니다

`programCopy.ts` 에 **지었다고 적어 두었습니다**(`_vocab/library.ts` 의 `TIER_NOTE` 선례).

| 열쇠 | 문장 | 출처 |
|---|---|---|
| `HERO_FEATURES` 여섯 | `소수 정예 그룹 / 함께 성장하는 동료` 외 둘 | **시안 HTML 에서 뽑음**(손으로 옮기지 않았다) |
| `GROW_ROWS[].desc` 다섯 | `묻어 둔 갈망을 꺼내 이름을 붙인다.` 외 넷 | **제가 지었습니다** |

히어로 4 · 카드 9 는 **지시서 문안 그대로**입니다.

### 6.2 카드 밴드에서 **사실 둘이 사라졌습니다**

지시서 문안을 그대로 적용했더니 옛 문안에 있던 두 가지가 없어졌습니다 :

| 사라진 것 | 옛 문안 | 왜 짚는가 |
|---|---|---|
| `7~12명이 한 기수를 이룬다` | 카드 2 | **구체적 수**였고 다른 문서가 그 수를 근거로 씁니다(익명성 판단 등) |
| `등수를 매기지 않는다` | 카드 3 | **불변식 11 의 공개 약속**이었습니다. 코드는 여전히 순위를 안 만들지만 **말로 하지 않게 됐습니다** |

**지시서 문안이라 제가 고치지 않았습니다.** 되살릴지 판단을 청합니다.

---

## 7. `architecture.md`·`design_system.md` 정합

- **ADR-171** 신설(채번은 실행 시점에 재서 이었습니다).
- **`design_system.md` §9.7 #1·#2 개정** — 지휘부 승인분. `SiteHero` 에 `features` 슬롯·배경 장면·`--on-dark` CTA,
  `GrowAxis` 에 `desc`·78px 원형 노드. **금지 칸에 「md↓ 로 이미지를 내리지 않는다」를 더했습니다.**
- **집계 수치를 ADR 본문에 적지 않았습니다** — 산출 명령만 적었습니다.

## 8. 계약(`/contracts`) 대비 이탈

**이탈 0.** `/contracts` 무접촉 · 마이그레이션 0 · `PublicShell` 무접촉(트리 규약).
