# CC ORDER - 3회차 갈무리 (블록 타입 일반화 + 이중 STEP 문안)

> 발신: 지휘부(최박사 + Claude) · 수신: 클로드코드(클코1)
> 작성: 2026-08-03
> 기준 커밋: `78b47b0` (main, 2026-08-03 10:25) — **발행 직전 fresh clone 으로 대조함**
> 선행: ADR-85·88 완료 확인함(레지스트리·`Disclosure`·요약 줄·목적 세 질문 모두 서 있음)
> 범위: `CheckinSession` 타입 일반화 + `session3.ts` 신설 + 되비추기 일반화 + `readModel` 7키. **DB 마이그레이션 없음.**

---

## 1. 배경

3회차는 **STEP 3(현재 직면) + STEP 4(습관 재구성)** 을 한 카드에 담는다. 1·2회차가 단일 STEP 이었으므로 이중 STEP 은 이번이 처음이고, 4·5회차가 같은 형태로 이어진다.

문안만 추가하면 되는 작업이 아니다. 현재 `CheckinSession` 타입이 **회차 이름으로 슬롯을 나눠 두어** 3회차가 세 군데서 걸린다.

| 걸리는 곳 | 현재 | 3회차 |
|---|---|---|
| `today.identity` | 필수 | **없다.** 3회차에 정체성 문장이 없다 |
| `today.desire` / `today.futureArea` | 회차 이름 | ERRC 짝은 `desire` 와 **같은 모양**, 간절한 영역은 `futureArea` 와 **같은 모양** |
| 되비추기 | `identity.mirror` · `lastStep.mirrorEmpty` 로 고정 | ① · 심화① · ⑤ **세 곳**에 되비춘다 |
| 블록 순서 | 슬롯 선언 순서로 고정 | 3회차는 영역 → 서술 → 짝 순 |

슬롯을 회차마다 늘리면 7회차에 여덟 개가 된다. `index.ts` 주석이 "회차 추가 = 파일 하나 + 한 줄"이라 적어 둔 목표와 어긋난다.

**슬롯 이름을 회차가 아니라 '모양'으로 바꾸면 3회차는 새 블록 타입 없이 조립된다.** 이번에 한 번 일반화하면 4~7회차는 문안 파일만 추가하면 된다.

---

## 2. 절대 건드리지 말 것

1. **1·2회차 문안 문자열을 바꾸지 않는다.** 타입 이름과 배치만 바뀐다. `session1.ts`·`session2.ts` 의 문자열 diff 는 0이어야 한다.
2. **DB·RLS·RPC·`CoreContext` 를 건드리지 않는다.** 3회차 신규 키는 전부 `answers` JSONB 에 들어간다.
3. **`Disclosure` 부품의 규격을 바꾸지 않는다**(ADR-88). 3회차는 그대로 쓴다.
4. **3면 구조를 4면으로 늘리지 않는다.** 6·7회차는 다시 단일 STEP 이라 면 수가 회차마다 달라진다.
5. **금지어** — 참여자 문안에 `설문 · 진단 · 지각 · 미제출 · 워크북` 을 쓰지 않는다.
6. **`함정` 을 카드 문안에 쓰지 않는다.** 대상을 가리키는 강의 어휘(원씽·첫 도미노)는 쓰되, 사람을 판정하는 낱말은 쓰지 않는다. 카드는 이틀 뒤 혼자 열리고 그때 판정어는 자기 낙인이 된다.
7. **필수는 6칸이다.** 늘리지 않는다.

---

## 3. Phase 1 — 블록 타입 일반화

### 3-1. 슬롯 이름을 모양으로 바꾼다

| 현재 | 변경 후 | 모양 | 쓰는 회차 |
|---|---|---|---|
| `desire` | **`pairText`** | 라벨 + 두 칸(from → to) | 1(갈망 한 쌍) · 3(ERRC 짝) |
| `futureArea` | **`areaPick`** | 칩 단일선택 + 한 줄 | 2(가슴 뛴 영역) · 3(간절한 영역) |
| `identity` | `identity` **(선택으로)** | 한 칸 | 1 · 2 |
| — | **`question`** (신규) | 한 칸 + `badge` | 3(오늘의 질문) |

`pairText` 에 `connector?: string` 을 더한다. 3회차 ERRC 는 두 칸 사이에 `↓ 그 자리에` 가 들어간다. 1회차는 없으므로 선택 속성이다.

### 3-2. 렌더 순서를 데이터로 뺀다

지금은 클라이언트의 선언 순서가 곧 화면 순서다. 3회차는 `areaPick → question → pairText → mood` 인데 2회차는 `areaPick → purpose → identity → mood` 다. 순서가 회차마다 다르므로 데이터가 소유해야 한다.

```ts
today: {
  order: readonly ('areaPick' | 'pairText' | 'purpose' | 'question' | 'identity')[];
  // mood 는 언제나 마지막이므로 order 에 넣지 않는다
  ...
}
```

클라이언트는 `order` 를 훑어 존재하는 슬롯만 그린다. **회차 번호로 분기하지 않는다.**

### 3-3. 되비추기를 블록 속성으로 일반화

현재는 `identity.mirror?: boolean` 과 `lastStep.mirrorEmpty` 로 자리가 박혀 있다. 3회차는 세 곳에 되비춘다.

```ts
type Mirror = {
  label: string;      // 회색 캡션
  keys: string[];     // 지난 회차 answers 에서 읽을 키. 복수면 ' · ' 로 잇는다
  empty?: string;     // 지난 회차 값이 없을 때 문구. 없으면 블록 자체를 그리지 않는다
};
```

`areaPick`·`identity`·`question`·`pairText`·`deepen.fields[]`·`step.lastStep` 에 `mirror?: Mirror` 를 붙일 수 있게 한다.

1·2회차는 동작이 같아야 한다. `session2.ts` 의 `identity.mirror: true` 를 `identity.mirror: { label: '지난 시간에 쓰신 문장', keys: ['identity_sentence'] }` 로 바꾸고, `lastStep` 도 같은 형태로 옮긴다. **화면 출력은 한 글자도 달라지지 않아야 한다.**

`page.tsx` 는 이미 `getMyCheckin(sessionNo - 1)` 을 넘기고 있다. 추가 조회는 필요 없다.

### 3-4. 그룹 이름 — 이중 STEP 회차의 UI

이중 STEP 회차는 1면 안에서 화제가 한 번 바뀐다. 표시가 없으면 참여자가 세 번째 문항에서 "갑자기 다른 이야기?" 하고 걸린다.

**부제를 쪼개 그룹 이름으로 쓴다.** 표지에서 본 두 마디가 스크롤하며 다시 나타나므로, 참여자는 카드의 모양을 이미 알고 있다. 새 어휘도, STEP 용어 노출도 없다.

```
표지 부제   현재를 직면하고, 습관을 다시 짜다
   ↓
① 영역        ← groupLabel: 현재를 직면하고
② 오늘의 질문
③ ERRC 짝     ← groupLabel: 습관을 다시 짜다
④ 마음
```

블록에 `groupLabel?: string` 을 더한다. 값이 있으면 그 블록 **위**에 hairline + muted 캡션을 그린다. 단일 STEP 회차(1·2·6·7)는 값을 두지 않으므로 화면이 바뀌지 않는다.

캡션은 제목이 아니라 이정표다. 크게 만들지 않는다 — 12px · `--color-text-muted` · 위아래 `--space-6`.

심화 블록에는 그룹 이름을 두지 않는다. 요약 줄(`정체성과 어긋난 하루 · 내 입에 붙은 말`)이 이미 두 갈래임을 말한다.

### 3-5. Phase 1 게이트

여기까지 마치면 **1·2회차 스크린샷을 먼저 보고**하고 승인을 받은 뒤 Phase 2 로 간다. 타입 일반화가 살아 있는 두 회차를 함께 건드리므로, 3회차를 얹기 전에 회귀를 잡는 편이 싸다.

보고할 것 - 1회차 1·3면, 2회차 1·2·3면 스크린샷 / `session1.ts`·`session2.ts` 문자열 diff 0 / 필수 5칸·6칸 유지 / 되비추기 세 곳 동작.

---

## 4. Phase 2 — 3회차 문안

`src/instruments/futurenow/checkin/session3.ts` 를 신설한다. **아래 문자열을 그대로 옮긴다. 조사 하나 바꾸지 않는다.**

### 4-1. 표지

```
brand     FUTURE NOW · 3회차
title     오늘의 갈무리
subtitle  현재를 직면하고, 습관을 다시 짜다
counter   필수 6칸 중 {n}칸 채움 · 약 5분
band      오늘 확인한 거리와, 오늘 바꾸기로 한 하나를 적어 둡니다.
```

`firstVisitOnce` 없음.

### 4-2. ① 간절한 영역 — `areaPick` · 필수

```
groupLabel  현재를 직면하고
label       오늘 다섯 영역에 점수를 매기셨지요. 그중 지금 가장 간절한 영역 하나를 골라 주세요. (책 94~95쪽)
help        간격이 가장 크게 벌어진 자리입니다. 크다는 것은 그만큼 바라고 있다는 뜻이에요.
key         gap_area          options  일 · 재정 · 관계 · 건강 · 기여
line.key    gap_want          line.label  그 영역에서 지금 가장 바라는 것 한 가지는
mirror      label: 지난 시간에 가장 가슴이 뛴다고 고르신 영역
            keys:  future_area, future_line
```

**`future_area` 를 재사용하지 않는다.** 2회차는 가장 가슴 뛴 영역, 3회차는 가장 간절한 영역이다. 둘이 다를 때가 인도자에게 가장 값진 정보인데 같은 키를 쓰면 그 차이가 덮인다.

칩과 문장이 **둘 다** 있어야 필수 1칸으로 센다.

### 4-3. ② 오늘의 질문 — `question` · 보이는 선택

```
key    stuck_named        badge  선택
label  오늘 나를 제자리에 붙잡아 두고 있던 것은 무엇이었나요? (책 96~103쪽)
help   이름을 붙여 보셨다면 그 이름을 그대로 적으셔도 좋습니다. 알아본 순간의 느낌을 함께 적으셔도 좋고요. 정확한 분석이 아니어도 됩니다.
```

접힘으로 감싸지 않는다. 그 회차의 핵심 경험을 받는 문항이라 눈에 보여야 한다.

**왜 필수가 아닌가.** 이중 STEP 회차에서 옮겨 적기 2 + 서술 1 을 전부 필수로 두면 필수가 7이 되어 상한을 넘는다. 4·5회차도 같은 규칙을 따른다.

### 4-4. ③ 습관 짝 — `pairText` · 필수

```
groupLabel  습관을 다시 짜다
label       오늘 다시 짠 습관 중에서, 짝 하나만 옮겨 주세요. (책 117~118쪽)
help        여러 개를 다 적지 않으셔도 됩니다. 짝이 맞는 한 쌍이면 충분합니다.
from.key    habit_stop     from.label  줄이거나 없애기로 한 것
connector   ↓ 그 자리에
to.key      habit_start    to.label    그 자리에 들이기로 한 것
to.help     비운 자리를 그냥 두면 며칠 안에 원래대로 돌아갑니다. 실패가 아니라 원래 그렇게 됩니다.
```

두 칸이 **둘 다** 채워져야 한 칸으로 센다. 이 회차의 핵심 원칙(제거는 창조와 짝을 이룬다)을 구조로 강제하는 자리다. 오른쪽 칸만 비워 저장되는 일이 없게 한다.

사분면 네 칸을 다 옮겨 적게 하지 않는다. 다 옮기라 하면 성실 노동이 되고, 대개 왼쪽만 길어진다.

### 4-5. ④ 오늘의 마음 — 필수

```
label      이 시간을 마치고 나온 지금, 마음은 어떤가요?
help       두 개까지 고르실 수 있습니다.
options    뜨끔함 · 홀가분함 · 조급함 · 담담함 · 해볼 만함 · 아직 모르겠음
exclusive  아직 모르겠음        max  2
moodCustom placeholder  직접 쓰기 (선택)
```

초안의 `오기` 를 `해볼 만함` 으로 교체했다. 이 회차는 자기를 낮게 보는 작업이 세 번 연속으로 걸리는 주간인데, 낱말 여섯 중 셋이 부정 쪽이면 골라 놓고 더 가라앉는다. 오기는 수치를 연료로 삼는 결기라 인도자도 접근 동기인지 수치 반응인지 구별할 수 없다.

### 4-6. 심화 — `Disclosure` · 기본 접힘

```
title    깊은 생각 갈무리하기
summary  정체성과 어긋난 하루 · 내 입에 붙은 말
```

| 키 | 라벨 | 보조 |
|---|---|---|
| `identity_gap` | 지금 내 하루 중에서, 지난 시간에 쓴 문장과 어긋난 채 반복되고 있는 것 하나는 무엇인가요? (책 108~111쪽) | 고쳐야 할 것을 찾는 칸이 아닙니다. 어긋난 곳이 보이면 그것만으로 충분합니다. |
| `speech_habit` | 요즘 내 입에서 가장 자주 나오는 말 한마디는 무엇인가요? (책 126~133쪽) | 좋은 말이든 아니든 상관없습니다. 그냥 자주 나오는 말이면 됩니다. |

`identity_gap` 에 되비추기를 붙인다 - `label: 지난 시간에 쓰신 문장` / `keys: ['identity_statement']`.

리셋 인사 발령은 넣지 않는다. ERRC 짝과 형태가 같아 참여자가 같은 것을 두 번 쓰게 된다.

### 4-7. 2면 한 걸음

**`lastStep` 은 2회차와 동일**하다(문안·선택지·되비추기 전부). 복사한다.

다음 한 걸음은 제목·칸 구성이 공통이고 보조 문구만 이 회차 것을 쓴다.

```
title       다음 시간까지 할 작은 실천 하나를 정해 봅시다.
help        오늘 마지막에 정하신 그 하나를 그대로 옮기시면 됩니다.
what.label  무엇을 하시겠어요?
when.label  언제, 어디서 하시겠어요?
when.help   이미 하고 있는 행동 뒤에 붙이면 훨씬 잘 켜집니다. '양치한 다음'처럼요.
blocker.label  못 하게 될 것 같은 때가 있다면 언제일까요?
blocker.help   사흘쯤 뒤에 한 번 무너지는 것이 보통입니다. 그때 다시 시작하시면 됩니다.
share       2회차와 동일
```

초안에 있던 `없애려 애쓰는 것보다, 그 자리에 무엇을 할지 정하는 편이 훨씬 잘 됩니다` 와 `(책 139~141쪽)` 을 **뺐다.** 앞엣것은 §4-4 의 `to.help` 와 같은 명제라 두 번 말하면 둘 다 약해진다. 뒤엣것은 한 걸음이 전 회차 공통 구조이지 그 회차의 책 활동이 아니기 때문이다.

이 회차의 `오늘 켤 것 하나` 가 곧 이번 주 한 걸음이다. 별도 문항으로 또 받지 않는다.

### 4-8. 3면 마무리

`confidence` · `facilitatorBox` 는 2회차와 **완전히 동일**하다. `defaultOpen` 을 두지 않는다(기본 접힘).

```
selfNote.label  오늘 자기를 정직하게 들여다본 나에게, 한마디만 건네주세요.
selfNote.help   꼭 칭찬이 아니어도 됩니다. 지금 나에게 필요한 말이면 됩니다.
```

성과가 아니라 태도를 세우는 자리다. 이 회차는 자기를 낮게 보는 작업이 세 번 걸렸으므로 마지막 문장이 특히 중요하다.

### 4-9. 필수 판정 (6칸)

```
1. gap_area 그리고 gap_want
2. habit_stop 그리고 habit_start
3. mood (1개 이상)
4. last_step_result
5. step_what 그리고 step_when
6. self_note
```

`stuck_named`·`confidence`·`last_step_note`·심화 두 칸은 세지 않는다.

### 4-10. 인도자 요약 열

| label | 키 |
|---|---|
| 간절한 영역 | `gap_area` |
| 습관 바꾸기 | `habit_stop` → `habit_start` |
| 붙잡아 두던 것 | `stuck_named` |

### 4-11. `readModel` 확장

신규 7키를 노출 정책표에 더한다. **23키 → 30키.**

`gap_area` · `gap_want` · `stuck_named` · `habit_stop` · `habit_start` · `identity_gap` · `speech_habit`

전부 본인·인도자 모두 표시한다(`save.notice2` 근거). `habit_stop`·`habit_start` 는 한 쌍으로 묶어 보여 준다.

### 4-12. 레지스트리 등록

`getCheckinSession` 에 `if (n === 3) return CHECKIN_SESSION_3;` 한 줄.

---

## 5. 수용 기준

**Phase 1 (게이트)**

1. `session1.ts`·`session2.ts` 문자열 diff **0건**.
2. 1회차 필수 5칸, 2회차 필수 6칸 유지.
3. 2회차 되비추기 두 곳(② 위·⑤ 위)이 이전과 **똑같이** 보인다. 스크린샷으로 대조.
4. `today.order` 가 데이터에 있고, 클라이언트에 회차 번호 분기(`sessionNo === n`)가 없다.
5. `groupLabel` 이 없는 회차는 화면이 변하지 않는다.

**Phase 2**

6. 3회차 문안 대조 - §4 의 모든 문자열이 화면과 한 글자도 다르지 않다. 1·2·3면 스크린샷을 §4 와 나란히 놓고 보고한다.
7. 금지어 0건. `함정` 도 0건.
8. 그룹 캡션 두 개가 ①·③ 위에 보이고, 크기가 문항 라벨보다 작다.
9. `gap_area` 칩만 고르고 `gap_want` 를 비우면 필수 미충족이다. `habit_stop` 만 채워도 미충족이다.
10. `stuck_named` 는 비워도 저장·제출된다. `선택` 표기가 보인다.
11. 되비추기 세 곳이 뜬다 - ① 위(2회차 영역+문장) · 심화① 위(2회차 정체성 문장) · ⑤ 위(2회차 한 걸음). 2회차가 빈 계정에서는 해당 블록이 뜨지 않거나 `mirrorEmpty` 문구가 뜨고, 카드는 정상 동작한다.
12. `pairText` 의 `connector`(`↓ 그 자리에`)가 두 칸 사이에 보인다. 1회차에는 보이지 않는다.
13. `readModel` 이 30키를 다룬다. 3회차 read 모드에서 신규 7키가 보인다.
14. `getCheckinSession(4)` 가 `null` 이고 `/checkin/4` 는 `준비 중` 안내가 뜬다.
15. `tsc` 0 · eslint clean · vitest 전량 통과 · build 성공.
16. DB 마이그레이션 파일이 생기지 않았다.

## 6. 보고 형식

- Phase 1 게이트 보고(§5-1~5) 후 승인 대기
- Phase 2 커밋 해시·변경 파일
- §5-6 문안 대조표(스크린샷 3장)
- §5-11 되비추기 두 경우(2회차 있음/없음) 스크린샷
- ADR 한 건 - 블록 타입 일반화(모양 기반 슬롯·`order`·`Mirror`·`groupLabel`)의 근거

---

## 7. 미결

**책 페이지 다섯 곳**(94~95 / 96~103 / 108~111 / 117~118 / 126~133)은 최종 조판 기준으로 확인 중이다. 확정 전에는 위 문안대로 넣고, 값이 오면 `session3.ts` 문자열만 고친다.

---

## 8. 이 지시서가 4~7회차에 남기는 것

Phase 1 을 마치면 남은 회차는 **문안 파일 하나 + 레지스트리 한 줄**로 끝나야 한다. 4회차(STEP 5+6)를 작업할 때 또 타입을 건드려야 한다면, 이번 일반화가 부족했다는 뜻이므로 그때 보고할 것.
