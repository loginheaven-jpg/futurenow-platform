# 퓨처나우 문안 원칙

> 적용 범위: 갈무리 카드 · 워크북 · 진행자 스크립트북 · 진단 안내문 · 인도자 가이드
> 지위: 문안에 관한 최상위 규범. 회차별 지시서·발주서가 이 문서와 충돌하면 이 문서가 이긴다.
> 제정: 2026-08-08 · 1기 1~3회차 실측 후

---

## 0. 우리가 말을 거는 사람

퓨처나우에 오는 사람은 **시간과 돈을 내고 온 사람**이다. 비자발적 참여자가 아니고, 설득해서 앉힌 사람도 아니다. 자기 5년을 바꾸겠다고 결정하고 온 사람이다.

그런데 1기 1~3회차 문안은 그 사실과 어긋나 있었다. 스물다섯 곳에서 "안 해도 된다"고 말하고 있었다.

**실측이 그 어긋남을 증명했다.**

| 항목 | 실측 |
|---|---|
| 심화 블록(선택) 펼침 | 8 / 11 |
| 못 하게 될 때(선택) | 7 / 11 |
| 목적 세 질문(선택) | 2 / 2 |
| **나에게 주는 한마디(필수)** | **4 / 8 비었다** |

**선택은 6~7할이 차고 필수는 절반이 빈다.** 이 사람들은 부담을 피하지 않는다. 자기가 값있다고 판단한 곳에 쓴다. 그리고 완충이 막으려던 문제는 하나도 관측되지 않았다 — 실행 자신감 값은 `2·5·7·8`로 부풀림이 없고, 예시 문구를 베낀 사람은 0명이었다.

없는 위험에 대비하느라 있는 기회를 잃고 있었다.

---

## 1. 네 축

### 축 1 — 허락을 걷어낸다

`~하셔도 됩니다` · `괜찮습니다` · `아니어도 됩니다` · `충분합니다` 계열을 전량 검토한다. **다만 세 갈래로 갈라 처리한다. 일괄 삭제는 다른 손실을 만든다.**

| 갈래 | 예 | 처분 |
|---|---|---|
| **정서 완충** | 지키지 못했어도 괜찮습니다 | **삭제** |
| **분량 지시** | 다섯 개를 다 적지 않으셔도 됩니다 | **명령형으로** — `가장 간절한 하나만 고르십시오` |
| **오해 방지** | 이건 진단이 아닙니다. 점수도, 정답도 없습니다 | **유지.** 오답을 막는 지시다 |

> **예시 교체(2026-08-14)**: 초판이 든 `고쳐야 할 것을 찾는 칸이 아닙니다` 는 ADR-100 이 그 문항을 통째로
> 삭제하며 코드에서 사라졌다(이 문서는 08-08 제정이라 08-10 개정을 몰랐다). 살아 있는 예시로 바꾼다.

분량 지시를 명령형으로 바꾸는 것이 값이 크다. 지금은 "안 해도 된다"로 읽히지만 실제 뜻은 **"하나만 골라라"는 더 어려운 요구**다. 다섯 개를 나열하는 것보다 하나를 고르는 것이 어렵다.

### 축 2 — 값을 말한다 (핵심)

허락을 지운 자리에 **왜 이걸 쓰는지**를 넣는다. 이것이 이 개편의 중심이고, 나머지 셋은 이것을 받친다.

| 지금 | 바꿀 방향 |
|---|---|
| 다듬지 않으셔도 됩니다 | 7주 뒤, 이 문장이 어떻게 바뀌었는지 나란히 보게 됩니다 |
| 한 줄이면 충분합니다 | 이 한 줄이 4회차에서 당신의 원씽이 됩니다 |
| 지키지 못했어도 괜찮습니다 | 정직한 기록만이 다음 한 주를 바꿉니다 |

**진취는 형용사가 아니라 구조다.** 어조만 딱딱하게 바꾸면 "요구는 높은데 왜 해야 하는지는 모르겠는" 물건이 된다. 이 칸이 7주 안에서 어디로 이어지는지 보이면 사람은 알아서 진지해진다.

> **제약(2026-08-14 · ADR-102)** — 값을 말할 때는 **제품이 지금 이행할 수 있는 것만** 말한다.
> 미래 기능을 근거로 든 문안은 약속이 아니라 **부채**다.
>
> 초판이 예시로 든 `7주 뒤, 일곱 마디가 나란히 남습니다` 가 그 사고였다. 여러 회차의 같은 항목을
> 한 화면에 나란히 보는 기능이 제품에 **없다**(`getMyCheckin` 은 단일 회차 전용이고, ADR-86 이 만든 것은
> 회차 링크 목록이다). ADR-94 B 가 세운 기준 — "문구가 약속을 만들면 도구가 있는지 먼저 묻는다" — 에
> 그대로 걸린다. **7주 뒤에 확인되는 거짓말이라 신뢰 손상이 크다.**
>
> 같은 이유로 **참·거짓이 회차마다 갈리는 문장을 공통 문구로 넣지 않는다.** 되비추기 사슬이 어디서
> 끊겼는지 확인한 뒤에 값을 말한다.

우리는 이미 종단 축을 갖고 있다.

```
갈망(1회차) → 미래의 한 문장(2회차) → 간절한 영역(3회차)
   → 원씽(4회차) → 환경(5회차) → 남은 시간(6회차) → 정체성 선언(7회차)
```

**지금까지 이것을 참여자에게 한 번도 말하지 않았다.** 값을 말한다는 것은 이 축을 문안에 드러내는 일이다.

### 축 3 — 완성을 기본값으로

`선택` · `건너뛰기` · `나중에` 를 초기 상태로 두지 않는다. 완성이 정상이고 미완성이 예외다.

- 진행 표시를 **잔여**가 아니라 **누적**으로 — `필수 2칸 남음` → `7주 기록 중 3회차 · 완성까지 2칸`
- 차수 홈에 **일곱 칸 진행 표시.** 채운 회차가 눈에 보이게
- 미완성 상태를 `작성 중` 이 아니라 **`아직 완성되지 않음`** 으로

투자한 사람은 자기 기록이 쌓이는 것을 본다. 그것이 다음 회차를 열게 만든다.

### 축 4 — 심화를 격상한다

심화 블록을 부록으로 두지 않는다. **8/11 이 펼쳤다.** 이것은 부가가 아니라 이 세미나에 값을 치른 이유에 가깝다.

- 기본 **펼침**
- 이름을 `깊은 생각 갈무리하기` 에서 **`여기서부터가 진짜입니다`** 로
- `선택` 표기는 유지한다 — 필수가 아닌 것은 사실이다. 다만 접어 두지 않는다

---

## 2. 문장을 쓰는 여섯 가지

1. **은유를 걷어낸다.** 현장에는 진행자 목소리가 있지만 워크북과 카드는 혼자 읽는다. `집에 오는 길까지 따라온 것` → `수업이 끝난 뒤에도 계속 생각난 것`.
2. **지시어를 쓰지 않는다.** `그 페이지` · `그 문장` 은 혼자 읽을 때 무엇인지 모른다. 이름을 부르거나 위치를 말한다.
3. **한 문항은 한 가지만 묻는다.** `무엇이었는지, 그리고 그때 어떠셨는지` 처럼 잇지 않는다. 둘 다 받고 싶으면 두 번째를 보조로 내린다.
4. **방향을 위로 둔다.** 세션이 풀어 놓은 것을 다시 묶지 않는다. `가장 마음에 걸리는` 대신 `가장 시원했던`.
5. **하나만 고르게 한다.** 세 줄 쓴 것을 다 옮기라 하면 성실 노동이 된다. 항상 `하나만` 이다.
6. **같은 명제를 두 곳에서 말하지 않는다.** 두 번 말하면 둘 다 약해진다.

---

## 3. 그래도 남기는 것 — 이 원칙의 한계

진취가 냉담이 되면 다른 것을 잃는다. **아래는 축 1을 적용하지 않는다.**

| 자리 | 이유 |
|---|---|
| 인도자 연락 요청 · 익명 안내 | 5주차에 실제로 무너진 사람이 여는 문이다. 그 문까지 딱딱해지면 소수를 잃는다 |
| ~~`selfNote` 의 `꼭 칭찬이 아니어도 됩니다`~~ | **제외한다(2026-08-14 · ADR-102 — ADR-91 D 판례 부분 파기).** 셋을 §6-2 로 묶어 남겼으나 이 하나는 성격이 다르다 — `self_note` 는 **자기에게 쓰는 말**이지 도움을 청하는 통로가 아니라 '무너진 사람이 여는 문'이 아니고, 남겨 둔 완충 넷 중 **유일하게 결측이 일어난 자리**다(나머지는 선택이라 비어도 정상인데 이 칸만 필수인데 4/8 이 비었다). 새 문안도 칭찬을 요구하지 않는다 — 허락이 값으로 바뀔 뿐이다 |
| 오해 방지 문구 | 오답을 막는 지시이지 위로가 아니다 |
| 사진 첨부 열람·보관 고지 | 프라이버시 고지는 부드러움이 아니라 정확함의 문제다 |
| 결측 안내 패널 | 채우게 만드는 장치다. 여기서 몰아세우면 이탈한다 |
| 침묵할 권리 | 정서 부하가 큰 회차(6회차 메멘토모리)의 심화는 권유 문구를 붙이지 않는다 |

**요구는 높게, 문은 열어 둔다.** 둘은 모순이 아니다.

---

## 4. 어조 — 구체 지침

**존대체를 유지한다.** 명령형으로 바꾼다는 것은 `~하십시오` 를 쓴다는 뜻이지 반말이나 군더더기 없는 명령을 뜻하지 않는다.

| 쓰지 않는다 | 쓴다 |
|---|---|
| ~하셔도 됩니다 | ~하십시오 · ~해 주세요 |
| ~하지 않으셔도 됩니다 | (분량이면) ~ 하나만 고르십시오 |
| 괜찮습니다 | (삭제하거나) 값을 말하는 문장으로 |
| 충분합니다 | ~ 하나면 됩니다 (사실 진술) |
| 잘 쓰려 하지 않으셔도 | (삭제) |

**과장하지 않는다.** `반드시` · `절대` · `놓치지 마십시오` 같은 압박 어휘를 쓰지 않는다. 값을 말하면 되지 다그칠 이유가 없다.

**참여자를 판정하지 않는다.** `지각` · `미제출` · `함정` 처럼 사람을 규정하는 낱말은 여전히 금지다. 진취적 어조와 판정어는 다른 것이다.

---

## 5. 데이터를 손상시키지 않는 세 규칙

이미 응답한 참여자가 있다. 문안을 바꾸되 그들의 기록을 깨뜨리지 않는다.

**규칙 1 — 라벨과 보조 문구만 바꾼다. 키는 손대지 않는다.**
저장은 키-값이므로 문안 교체는 저장값에 영향이 0이다.

**규칙 2 — 선택지 목록을 바꾸지 않는다.**
칩 컴포넌트는 목록에 있는 것만 그린다. 목록에서 뺀 값은 화면에 나타나지 않고, 배타 필터도 통과해 유령 값이 된다. **마음 낱말 · 지난 걸음 4지선다 · 영역 칩은 이번 개편에서 제외한다.** 2기 시작 전, 저장값이 없는 상태에서 한다.

**규칙 3 — 필수 구성을 바꾸지 않는다.**
필수 판정이 바뀌면 이미 제출된 기록이 소급으로 미완성이 된다.

즉 **바꿀 수 있는 것은 문안뿐이고, 그것으로 충분하다.**

---

## 6. 산출물별 적용

### 갈무리 카드
네 축 전부. 세부는 회차별 지시서가 정한다.

### 워크북
- 활동 안내문에 축 1·2 적용. `~해 보셔도 좋습니다` → `~하십시오` + 그것이 무엇으로 이어지는지
- **각 STEP 첫 장에 그 회차가 7주 안에서 맡는 자리를 한 줄로 적는다.** 종단 축을 참여자가 보게 하는 가장 값싼 방법이다
- 빈칸 옆 `(선택)` 표기를 줄인다. 선택이면 그렇게 두되 사과하지 않는다

### 진행자 스크립트북
- 진행자 대사에 축 1·2 적용
- **"안 하셔도 됩니다" 계열 대사를 삭제한다.** 현장에서 진행자가 이 말을 하면 문안을 아무리 고쳐도 톤이 되돌아간다
- 대신 그 활동이 무엇으로 이어지는지 말하게 한다
- 침묵할 권리 보장은 유지한다(§3)

### 진단 안내문
사전·사후 진단은 **측정 도구**라 어조 규칙이 다르다. 측정 어휘와 강의 어휘의 분리가 우선이며, 이 문서의 축 1·2를 기계적으로 적용하지 않는다. 진단 문안은 별도 규범을 따른다.

---

## 7. 2기부터 달라지는 전제

**갈무리는 선택이 아니라 과정의 일부로 처음부터 안내한다.** 모집 안내문·오리엔테이션·1회차 도입에서 명시한다.

1기는 갈무리가 나중에 붙었으므로 부가로 인식됐다. 2기는 처음부터 세미나의 일부로 선다. 그러면 문안이 안 해도 된다고 말할 이유가 더더욱 없다.

---

## 8. 자가 점검

문안을 발주하거나 확정하기 전에 훑는다.

```
□ '~하셔도 됩니다' 계열이 남아 있다면, 셋 중 어느 갈래인지 판정했다
□ 허락을 지운 자리에 값을 말하는 문장을 넣었다
□ 이 칸이 7주 안에서 어디로 이어지는지 참여자가 알 수 있다
□ 은유·지시어를 걷어냈다
□ 한 문항이 한 가지만 묻는다
□ 보조 문구끼리 같은 말을 두 번 하지 않는다
□ §3의 다섯 자리를 건드리지 않았다
□ 압박 어휘(반드시·절대·놓치지)를 쓰지 않았다
□ 사람을 판정하는 낱말이 없다
□ 선택지 목록·필수 구성을 바꾸지 않았다(데이터 보호)
```
