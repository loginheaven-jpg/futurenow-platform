// 서가 문안 — **최박사 확정분이다. 한 글자도 고치지 않는다**(발주 §1).
//
// **손으로 옮겨 적지 않았다** — 발주서 §1 의 블록을 그대로 옮겨 상수로 두고,
//   `library.copy.test.ts` 가 **글자 단위**로 잠근다(원고 §1~§4 를 다룬 방식과 같다 · 값의 두 분류 ⑵).
//   새 문안이 필요해지면 **멈추고 보고한다** — 여기에 지어 넣지 않는다.

/** 자료를 올릴 때 먼저 읽는 것. 다섯 줄이고 줄바꿈까지 확정분이다. */
export const UPLOAD_CONSENT = `자료를 올리기 전에 확인해 주세요.

올리신 자료는 열람 권한을 가진 분들께 공개됩니다.
다른 분의 저작물이라면 공유해도 되는 것인지 먼저 확인해 주세요.
개인정보나 참여자의 사적인 내용이 담기지 않았는지 살펴봐 주세요.
올리신 자료는 본인이 가릴 수 있고, 삭제는 운영자에게 문의해 주세요.`;

/** 올릴 자격이 없는 사람에게 구획이 꺼진 채로 하는 말. */
export const UPLOAD_CLOSED = '자료 올리기는 세미나 참여자와 포럼회원께 열려 있습니다.';

/** 주소로 거는 자료를 고를 때. */
export const LINK_NOTE = '주소로 거는 자료는 그쪽 공유 설정도 함께 확인해 주세요.';

/**
 * 고른 자료가 상한을 넘을 때. **고르는 즉시, 올리기 전에** 말한다 —
 *   5분 올리고 실패를 보는 것보다 낫고, 무엇보다 **기다려도 안 되는 것을 기다리라고 하지 않는다.**
 *
 * **막기만 하지 않고 갈 길을 준다**(최박사 결재 2026-08-29) — 서가에는 이미 «주소로 걸기» 가 있어
 *   이 문장이 가리키는 곳이 **실제로 있는 곳**이다.
 */
export const UPLOAD_TOO_LARGE = '자료는 50MB까지 올릴 수 있어요. 더 큰 자료는 주소로 걸어 주세요.';


/**
 * 등급을 고를 때 그 등급이 **무슨 뜻인지** 한 줄.
 *   **최박사 결재분이다**(2026-08-29). 위 넷과 **같은 급**이므로 한 글자도 고치지 않는다.
 *
 * **왜 필요한가**: 「전체 공개」가 **로그인 밖까지**라는 것이 실측으로 확인됐다 —
 *   `library_can_view` 에서 `public` 갈래가 로그인 검사보다 **위**에 있고,
 *   로그아웃 요청이 **200 + 실바이트**를 받았다(같은 자리에서 `forum` 은 404).
 *   목록에 제목만 설 때는 「공개 글 한 줄」이었지만 **사진이 바로 보이면 그 사진이 인터넷에 공개된다.**
 *   올리는 사람이 **그 뜻을 알고 골라야 한다.**
 *
 * 화면에서: 등급 고르는 칸 **바로 아래**, 고른 등급 **한 줄만** 작은 회색 글씨.
 *   등급을 바꾸면 문장도 바뀐다.
 *
 * 열쇠는 등급 값이다 — 이름표(`LIBRARY_TIER_LABEL`)와 짝이지만 **문장은 결재분이라 여기 산다.**
 */
export const TIER_NOTE = {
  public: '로그인하지 않은 사람도 봅니다.',
  forum: '로그인한 포럼회원이 봅니다.',
  coach: '인도자만 봅니다.',
} as const;

/**
 * 서가 B 문안 열 — **최박사 결재분**(2026-08-30 · 결재 ⑼). 위 넷·셋과 **같은 급**이다.
 *
 * **한 사람이 겪는 순서대로** 이어 읽고 썼다(발주 §2-4·§2-5):
 *   반응 → 댓글 → 이름 가림 → 신고. 「운영자」와 「한마디 남기다」가 각 갈래에서 **하나로 선다.**
 *
 * ★ **㉣ 「운영자에게 알리기」와 운영 화면의 「신고」는 일부러 다르다.**
 *   참여자에게는 **가르지 않는 말**이 필요하고 운영자에게는 **무엇이 쌓였는지 정확한 이름**이 필요하다.
 *   **하나로 통일하지 말 것** — 발주서 §2-4 에 사유가 있다.
 */
export const B_COPY = {
  /** ㉧ 로그아웃·권한 밖이라 이모지를 누를 수 없을 때. 「못 한다」가 아니라 **할 수 있는 길**을 말한다. */
  reactLocked: '로그인하시면 마음을 남길 수 있어요.',
  /** ㉠ 댓글 입력칸. 「한마디」는 **길게 쓰지 않아도 된다는 허락**이다. */
  commentPlaceholder: '이 자료에 한마디 남겨 주세요.',
  /** ㉡ 댓글이 없을 때. 비었다고만 하지 않고 **첫 사람이 되라고 권한다.** */
  commentEmpty: '아직 남긴 말이 없어요. 먼저 한마디 남겨 주세요.',
  /** ㉢ 지우기 확인. 되돌릴 수 없음을 **먼저** 말하되 겁주지 않는다. */
  commentDeleteConfirm: '남기신 말을 지웁니다. 되돌릴 수 없어요.',
  /** ㉨ 밖에서 댓글을 볼 때. **말하지 않으면 깨진 화면으로 읽힌다**(안 ㉮ — 짧은 쪽 채택). */
  maskedNote: '이름은 로그인하신 분께만 보입니다.',
  /** ㉣ 신고 단추. **누구를 고발하는 것이 아니라 운영자에게 알리는 것**으로 적는다. */
  reportButton: '운영자에게 알리기',
  /** ㉤ 사유. **무엇을 적는지**와 **누가 보는지**를 함께 말한다(결재 ⑺ 의 약속). */
  reportPrompt: '무엇이 마음에 걸리셨는지 알려 주세요. 이 글은 운영자만 봅니다.',
  /** ㉥ 보낸 뒤. **먼저 고마움을 말한다** — 알리는 일에는 용기가 든다. */
  reportDone: '알려 주셔서 고맙습니다. 운영자가 확인하겠습니다.',
  /** ㉦ 이미 알린 자료. 「중복입니다」는 거절의 말이라 쓰지 않는다. */
  reportAlready: '이미 알려 주셨어요. 운영자가 확인하고 있습니다.',
} as const;

/**
 * ㉩ 운영 첫 화면 — **문의 줄과 짝이다**(`admin/page.tsx` 실측).
 *   윗줄은 문의와 **글자 구조가 같고**(「확인하지 않은 ○○ N건」),
 *   **부제만 다르다** — 문의는 목록 화면이 «준비 중» 이지만 신고는 만들지 않기로 했으므로
 *   「준비 중」이라 쓰면 **없는 약속**이 된다. 대신 **실제로 하는 일**을 적는다.
 */
export const REPORT_NOTICE = {
  line: (n: number) => `확인하지 않은 신고 ${n}건`,
  sub: '자료를 열어 확인합니다',
} as const;
