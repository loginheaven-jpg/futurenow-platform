// 공개 영역 프로그램 문안 정본 (4차 F-2).
//
// **`/` 와 `/about` 이 같은 것을 말한다.** 여정 6회차·GROW+F 축은 두 화면에 다 나오는데
//   각 페이지가 배열을 따로 들면 회차 이름이 바뀔 때 한쪽만 고쳐진다
//   (불변식 23 — 사본이 둘이면 잠금으로 묶는다).
//
// **소개 3단락은 여기 없다** — 그것은 이미 `SeminarIntro` 가 단일 출처이고
//   `/`·`/about`·`CohortPreview` 셋이 공유한다. 여기에 옮겨 적으면 사본이 넷이 된다.
//
// 문안은 **시안 문구 그대로**다. 히어로 카피 확정은 지휘부 감수 몫(4차 개요 §5).
//
// ★ **v2 회차(ADR-171) — `WEEK_CELLS` 는 한 글자도 안 바꿨다.** 지시서가 여섯을 전부
//   새 이름으로 바꾸라 했으나 **그 이름은 여기만 사는 것이 아니다** — 1·2회차 제목이
//   갈무리 카드(`checkin/session1·2.ts`)와 **완전히 같은 문자열**이고 나머지 넷도 같은 뜻이며,
//   `존재가치 선언문`·`인생 조감도`·`원씽` 은 모집 랜딩 결재 문안에도 박혀 있다(실측: 각 16·12·38 파일).
//   공개만 바꾸면 **신청할 때 본 이름과 참여한 뒤 카드에 뜨는 이름이 달라진다.**
//   지휘부가 「메시지는 일관되어야 한다」고 했고, 그 일관성을 **공개를 갈무리에 맞추는 쪽**으로
//   지켰다(판정 2026-09-02). 갈무리 쪽을 바꾸는 것은 원고 개정이 선행하는 별건이다.

/**
 * 시안 P1 `.grid-f` — GROW + F 다섯 축.
 *
 * `short` 는 **시안 A `journey-track` 의 두 글자 라벨**이다(목표·직면·옵션·실행·믿음).
 * 폰 가로 트랙은 칸이 64px 뿐이라 긴 한글이 석 줄로 깨진다 — 시안이 폭마다 다른 문안을
 * 준 것이고, 그 둘을 **부품이 아니라 여기가** 들고 있다.
 */
export const GROW_ROWS = [
  { letter: 'G', short: '목표', en: 'GOAL', ko: '과거와 미래의 나', desc: '묻어 둔 갈망을 꺼내 이름을 붙인다.', note: '1-2회차' },
  { letter: 'R', short: '직면', en: 'REALITY', ko: '직면과 재구성', desc: '지금 서 있는 자리를 있는 그대로 본다.', note: '3회차' },
  { letter: 'O', short: '옵션', en: 'OPTIONS', ko: '펼치고 하나를 고른다', desc: '여러 길을 펼쳐 놓고 단 하나를 정한다.', note: '4회차' },
  { letter: 'W', short: '실행', en: 'WILL', ko: '의지가 아니라 환경', desc: '버티는 대신 버틸 필요가 없게 설계한다.', note: '5회차' },
  { letter: 'F', short: '믿음', en: 'FAITH', ko: '끝에서 오늘을 본다', desc: '마지막 날에서 되돌아보며 오늘을 정한다.', note: '6회차' },
];

/** 시안 P1 `.three` — 세 장 카드. */
export const BAND_CARDS = [
  {
    kicker: '꿈이 목표가 되는 순간',
    title: '여섯 주의 기록',
    body: '매주 쓰는 갈무리 카드가 쌓이면, 막연했던 꿈이 구체적인 목표로 바뀝니다. 6주 뒤, 당신은 다른 사람이 되어 있을 것입니다.',
  },
  {
    kicker: '혼자가 아닌 함께',
    title: '같이 걷는 사람',
    body: '같은 꿈을 꾸는 사람들과 함께 걷습니다. 서로의 목표를 응원하고, 실행을 점검하며, 변화를 나눕니다.',
  },
  {
    kicker: '변화를 확인하는 시간',
    title: '사전·사후 진단',
    body: '6주 전과 후, 같은 질문에 답해 봅니다. 달라진 답변 속에서, 당신의 변화를 확인합니다.',
  },
];

/**
 * 히어로 아래 **짧은 사실 셋**(v2 · ADR-171). 문안은 **시안 HTML 에서 뽑았다** —
 *   손으로 옮겨 적지 않았다(`hero-feature__title`·`__sub` 짝을 그대로).
 *
 * ※ **이 여섯 줄은 시안이 지은 것이고 결재분이 아니다 — 결재를 청한다.**
 *   `_vocab/library.ts` 의 `TIER_NOTE` 가 세운 선례 그대로 지었다고 적어 둔다.
 */
export const HERO_FEATURES = [
  { title: '소수 정예 그룹', sub: '함께 성장하는 동료' },
  { title: '실전 중심 커리큘럼', sub: '실천 가능한 전략과 도구' },
  { title: '지속 가능한 성장', sub: '6주 후, 새로운 시작의 나' },
];

/**
 * 시안 P1 `.weeks` — 6회차.
 *
 * 산출물 이름은 **손에 남는 물건**이다. §7 이 참여자 렌더 경로에서 막는 것은
 * 구인명·STEP·측정 어휘이고, 회차 결과물의 이름은 그것이 아니다
 * (`원씽` 은 강의 어휘이나 **여기는 공개 소개 문맥**이고 리포트 명명 경로가 아니다 — §9.4 계열).
 */
export const WEEK_CELLS = [
  { n: '1회차', title: '과거의 나를 만나다', output: '존재가치 선언문' },
  { n: '2회차', title: '미래의 나를 만나다', output: '시각화 보드' },
  { n: '3회차', title: '직면하고 다시 짜다', output: '빛 스위치 하나' },
  { n: '4회차', title: '펼치고 하나를 고르다', output: '원씽 · 첫 도미노' },
  { n: '5회차', title: '의지가 아닌 환경', output: '골든타임 설계' },
  { n: '6회차', title: '끝에서 오늘을 보다', output: '여정의 갈무리' },
];
