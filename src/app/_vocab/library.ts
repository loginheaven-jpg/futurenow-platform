// **서가 이름 — 한 곳**(서가 A §12 · 최박사 확정 ① 「자료실」 → 「서가」).
//
// 개명 대상이 **네 곳이 아니라 운영 코드 16 파일**이었다(실측 2026-08-29 · 지휘부가 실측을 참값으로 채택).
//   GNB · 푸터/공개 시트 · 회원 시트 · 콘솔 시트 · 콘솔 껍데기 · 화면 · 홈 · 피드 · 차수 홈 ·
//   pending · 픽스처 둘 · 계약 둘 · 코어 하나.
//   **하나라도 빠지면 두 이름이 공존한다.** 그래서 낱말을 여기 하나 두고 전부 읽게 한다(불변식 23).
//
// **URL 은 바꾸지 않는다**(`/library` 유지 · 지휘부 판정). 주소는 **얼어야 하는 값**이고,
//   바꾸면 이미 나간 링크가 죽는다 — ADR-155 의 OG 주소 교훈이 그대로 걸린다.
//   **보이는 이름과 주소는 다른 것이다.**

/** 화면·내비에 뜨는 이름. */
export const LIBRARY_NAME = '서가';

/** 주소. 이름이 바뀌어도 이것은 바뀌지 않는다. */
export const LIBRARY_HREF = '/library';

/** 등급을 사람 말로. **등급 이름을 화면이 직접 비교하지 않게** 표를 여기 둔다(§3 하지 말 것 3). */
export const LIBRARY_TIER_LABEL = {
  public: '전체 공개',
  forum: '포럼회원',
  coach: '인도자',
} as const;

/**
 * **올릴 수 있는 최대 크기(MB) — 재서 정한 값이다**(ADR-164).
 *   실측: **50MB 통과(올리기 5.1초 · 프록시 되받기 온전) · 51MB 실패** · 55·60MB 실패.
 *   저장소가 정한 벽이고 우리가 지어낸 값이 아니다.
 *
 * **문안과 검사가 이 하나를 읽는다** — 둘이 어긋날 수 없게 `library.copy.test.ts` 가 묶는다.
 *   숫자만 바꾸면 문안과 갈려 레드, 문안만 바꾸면 확정 문안 잠금이 레드다.
 */
export const LIBRARY_MAX_MB = 50;

