// 회원 표시 **어휘와 조립 규칙 — 단일 출처** (5차 T-3 · 최박사 확정 2026-08-29).
//
// **여기 있는 것과 없는 것.**
//   있다: tier 이름 셋 · tier 별 한 줄 설명 · 소속 역할 이름 둘 · 조립 규칙 ·
//         `underReview` 진행 문안 · 문의 안내 · `held` 의 뜻 한 줄.
//   없다: 완성된 문자열. **서버가 문자열을 만들지 않는다** — 값만 내리고 **조립은 화면이 한다**
//         (최박사 지시). 문언이 서버에 박히면 단일 출처가 둘이 된다.
//
// **읽는 곳이 셋이다**(발주 §3): 내 정보(`/account`) · 공개 현관 칩 · 운영자 화면.
//   5-2 운영자 도구가 나중에 이것을 읽는다. 그래서 화면 옆이 아니라 코어에 둔다.
//
// **T-5(`roleTargets`)와의 관계** — 사본이 아니다. 입력이 같고 **출력이 다른 두 함수**다
//   (하나는 갈 곳을, 하나는 표시를 낸다). 사본이 될 수 있는 자리는 **어휘뿐**이라
//   역할 이름을 여기 두고 양쪽이 읽는다.
//
// ─────────────────────────────────────────────────────────────────────────────
// **문안은 최박사 원문이다. 한 글자도 다듬지 않았다.**
//   좁히지 않는다 — *진단* 이 아니라 **진단 등 모든 도구**다. 원문이 초안보다 넓다.
// ─────────────────────────────────────────────────────────────────────────────
import type { CohortRole, CohortRoleKind, MemberTier, MembershipStatus, MembershipView } from '@/contracts/domain';

/**
 * **`held` 의 뜻 — 한 줄.**
 *
 * 지금까지 이 뜻은 정본 어디에도 한 줄로 적혀 있지 않았다. 세 자리에 흩어져 있었다 —
 * `member_state()` 주석(*운영자가 막은 사람을 코드 한 줄로 뚫는 길이 생긴다*) ·
 * ADR-122(같은 취지) · `/pending` 화면 문안(*확인이 필요한 신청입니다*).
 * **흩어져 있다는 것 자체가 결함이라** 여기에 모은다(지휘부 지시).
 *
 * > **`held` 는 자격 확인이 끝나지 않아 운영자가 붙들어 둔 상태다.**
 * > 한때 있던 자격이 끝난 것(`expired`)이 아니라 **아직 열어 준 적이 없는 쪽**이다.
 * > 운영자만 손으로 건다(`decide_membership`) — 자동으로 붙는 경로가 없고,
 * > 기수 마감 트리거도 `held` 를 건드리지 않는다(*트리거가 운영자 판단을 뒤집지 않는다*).
 * > 판정에서 **무엇보다 먼저**다(`held > cohort > 저장 > pending`) — 그러지 않으면
 * > 기수에 등록되는 것만으로 보류가 무력화된다.
 * > 유효기간을 달 수 없다(*보류·만료에 기간을 다는 것은 뜻이 없다*).
 *
 * **그래서 `held` 는 tier 가 아니라 진행 표시다** — 자격은 여전히 `visitor`(방문회원)이고,
 * 그 위에 *확인 중* 이라는 표시가 얹힌다. tier 로 두면 *보류* 가 자격 이름 자리에 앉아
 * `suspended`(이용 보류)와 한 화면에서 겹친다.
 */
export const HELD_MEANING =
  'held 는 자격 확인이 끝나지 않아 운영자가 붙들어 둔 상태다. 한때 있던 자격이 끝난 것(expired)이 아니라 아직 열어 준 적이 없는 쪽이고, 운영자만 손으로 건다.';

/** tier 이름 셋 — 참여자에게 보이는 자격 이름. */
export const TIER_LABEL: Record<MemberTier, string> = {
  visitor: '방문회원',
  forum: '포럼회원',
  // `expired` 의 이름. 한때 *이용 중지* 로 정해졌다가 최박사가 **이용 보류**로 되돌리셨다 —
  //   더 완곡해서 좋다는 것이다.
  suspended: '이용 보류',
};

/**
 * 승급 방법 — **최박사 원문**(2026-08-30). 방문회원 설명에 **병기**한다.
 *
 * *"정회원 승급 신청을 하지 않은 방문회원도 있다 그냥 승급방법 안내면 병기하면 된다."*
 * 그 안내가 이 한 문장이 전부다. 늘리지 않는다.
 */
export const UPGRADE_HOWTO = '촉진자포럼에 가입하고 정회원자격을 취득하시면 된다.';

/**
 * tier 별 한 줄 설명.
 *
 * **`visitor` 는 대체됐다**(최박사 위임 · 지휘부 작성 2026-08-30).
 *   옛 문장은 *승인을 기다리는 중입니다. 세미나 참여와 포럼회원 신청을 하실 수 있습니다.* 였다.
 *   **방문회원은 기다리는 사람이 아니라 아직 아무것도 하지 않은 사람**이므로 `기다리는 중` 이
 *   사실과 어긋났다. 뒷 문장은 새 한 줄에 흡수된다.
 *   최박사 원문 근거: *"세미나 참여와 촉진자포럼 가입을 유도하면 된다."*
 */
export const TIER_LEAD: Record<MemberTier, string> = {
  visitor: '세미나에 참여하시거나 촉진자포럼에 가입하시면 진단 등 모든 도구를 이용하실 수 있습니다.',
  forum: '포럼회원자격 유지기간 동안 진단 등 모든 도구를 이용하실 수 있습니다.',
  suspended: '이용이 보류되었습니다. 운영자에게 문의해 주십시오.', // 최박사 확정 2026-08-30(`주십시오` 가 정본) · SQL 사본은 `20260831100000`
};

/**
 * 로그인 차단 안내 — **최박사 원문 그대로**(2026-08-30).
 *
 * *"회원기능이 마비되어 로긴도 안되고 '회원자격이 보류되었습니다. 운영자에게 문의하십시오.'
 *   라고 뜨는 것이다."*
 *
 * ⚠ **`TIER_LEAD.suspended` 와 문장이 다르다** — 이쪽은 `회원자격이 … 문의하십시오`,
 *   저쪽은 `이용이 … 문의해 주십시오` 다. **둘 다 최박사 원문**이고 자리가 다르다
 *   (여기는 로그인 화면, 저쪽은 `내 정보`·글쓰기 차단). **임의로 하나로 합치지 않았다** —
 *   합치는 것은 문안 결정이고 그것은 최박사 소관이다. 어긋남 자체는 보고에 적었다.
 */
export const HOLD_LOGIN_NOTICE = '회원자격이 보류되었습니다. 운영자에게 문의하십시오.';

/** 소속 역할 이름 둘. T-5 `roleTargets` 도 이 어휘를 읽는다(사본 방지). */
export const COHORT_ROLE_LABEL: Record<CohortRoleKind, string> = {
  participant: '참여자',
  coach: '인도자',
};

/**
 * 참여자의 한 줄 설명 — **두 문장이다**(최박사 위임 · 지휘부 작성 2026-08-30).
 *
 * **뒷 문장을 빼면 안 된다.** 앞 문장만 두면 *기간이 끝나면 기록도 사라진다* 로 읽힌다 —
 *   실제로는 자기 회기 콘텐츠를 **영구히** 본다(회원 보류가 아닌 한). 문장이 실제보다
 *   좁으면 참여자가 없는 마감일을 상상한다.
 *
 * **`만료` 라는 말을 쓰지 않는다.** 자동 만료가 폐지됐으므로(ADR-147·148) 그 단어가 남으면
 *   **없는 개념을 계속 가리킨다.** 최박사 원문: *"사실 만료는 없다. 참여자는 회기 기간동안
 *   도구를 사용할 수 있다는 것만 인지되면 된다."*
 *
 * 인도자 칩에는 붙이지 않는다 — 원문이 참여자에 대해 한 말이고 인도자용 문장은 확정에 없다.
 * **없는 문장을 지어내지 않는다.**
 */
export const PARTICIPANT_LEAD =
  '세미나 기간 동안 모든 도구를 이용하실 수 있습니다. 기간이 끝나도 그동안의 기록은 계속 보실 수 있습니다.';

/** `underReview` 진행 문안 — 참여자 문안은 **현행 유지**다(`/pending` 과 같은 문장). */
export const UNDER_REVIEW_NOTE = '확인이 필요한 신청입니다.';

/** 등급 조정 문의 안내 — 최박사 지시로 노출한다. */
export const TIER_INQUIRY_NOTE = '회원등급 조정에 대해 궁금하시면 운영자에게 문의해 주세요.';

/**
 * 기수명 축약 — **이름 끝의 `n기` 를 뽑는다**(최박사 확정 2026-08-29).
 *
 * `퓨처나우2026예봄2기` → `2기`. 화면에는 `2기 참여자` 로 뜬다.
 *
 * **규칙이 이름 형식에 의존하므로, 끝이 `n기` 가 아닌 기수는 전체 이름을 그대로 쓴다**
 * (최박사 지시). 실물로 `[QA] 검증 전용` · `체험 진단` 이 그것이다 — 억지로 줄이면
 * 없는 회차 번호를 만들어 내게 된다. **뽑히지 않는 것이 정상 경로다.**
 */
export function shortCohortName(name: string): string {
  const m = /(\d+기)$/.exec(name.trim());
  return m ? m[1] : name;
}

/** 소속 칩 한 칸의 글자 — 축약된 기수명 + 역할 이름. */
export function cohortRoleLabel(r: CohortRole): string {
  return `${shortCohortName(r.cohortName)} ${COHORT_ROLE_LABEL[r.kind]}`;
}

/**
 * **자격 저장값을 표시 축으로 편다.**
 *
 * ── 입력이 무엇인지가 이 함수의 전부다 ──────────────────────────────────────
 *
 * 입력은 **`memberships.status` 저장값**이고, 행이 없으면 `null` 이다.
 * **`member_state()` 산출값이 아니다.**
 *
 * 처음 판은 산출값을 받았고 그래서 `cohort` 가 `forum` 으로 앉았다 —
 * **승인받은 적 없는 사람이 화면에 `포럼회원` 으로 표시됐다.** 최박사가 기각하신 바로 그것이다:
 * *"포럼회원이라는 이름을 붙이는 순간 아닌 것을 그렇다고 말한 것이 된다."*
 * 실측으로 18명이 그 경우였다(`member_state`=`cohort` 인데 `memberships` 행이 없다).
 *
 * **`cohort` 는 권한이지 자격이 아니므로 tier 의 입력이 될 수 없다.**
 * 지금 세미나에 참여 중이라는 사실은 `cohortRoles` 가 말한다 — 축이 둘인 이유가 이것이다.
 *
 * | 저장값 | tier | underReview |
 * |---|---|---|
 * | `individual` | `forum` | false |
 * | `expired` | `suspended` | false |
 * | `pending` | `visitor` | false |
 * | `held` | `visitor` | **true** — tier 를 덮지 않는다 |
 * | (행 없음 · `null`) | `visitor` | false |
 *
 * **권한 판정은 무변경이다** — `member_state()` 산출 · `member_can_assess` · RLS · 진실표를
 * 이 함수는 읽지도 바꾸지도 않는다.
 *
 * **이 매핑은 저장소에 여기 한 곳뿐이다**(2026-08-29 전수 확인). 사본이 생기면 그때부터
 * 두 화면이 같은 사람을 다르게 부른다.
 */
export function toMembershipView(
  stored: MembershipStatus | null,
  cohortRoles: CohortRole[],
  isAdmin = false,
): MembershipView {
  const tier: MemberTier =
    stored === 'individual' ? 'forum'
      : stored === 'expired' ? 'suspended'
        : 'visitor'; // pending · held · 행 없음
  return { tier, underReview: stored === 'held', cohortRoles, isAdmin };
}

// ── 좁은 자리 — **우선 규칙** (최박사 확정 2026-08-29) ────────────────────────
//
// 최박사 원문: *"00기참여자. 포럼회원. 이렇게 쓸수 있는 공간이 되는 곳에는 병행표현하고
// (내정보 등). 병행해서 쓸 공간이 안되는 곳에서는 참여자, 인도자, 운영자 우선"*
//
// **병행이 원칙이고 우선은 자리가 좁을 때의 잘라내기 규칙이다** — 층이 다르다.
// 앞선 0번(*택일이 아니라 병행표현*)과 부딪히지 않는다.
//
// **좁은 자리 전수**(2026-08-29 정본 조회 · 짐작하지 않고 셌다):
//   ⑴ `MenuSheet` 머리의 `role` — `<small>` 한 칸. 세 화면이 먹인다:
//        `/home`(`targets[0].who`) · 차수 홈(`'참여자'` 하드코딩) · 진단 홈(`'참여자'` 하드코딩)
//   그 밖에는 없다. 확인한 것들:
//     · `SiteRoleCard.who` — **좁지 않다.** T-5 로 카드가 여럿이 되어 각 카드가 자기 칸을 갖는다
//     · 공개 현관 칩 — **실재하지 않는다**(발주 §3 이 노출 자리로 적었으나 화면에 없다)
//     · `admin/MemberRow` 의 `ROLE_LABEL` — **남의 역할**이라 이 축이 아니다(아래 주 참조)
//
// **`MemberRow.ROLE_LABEL` 주의**: `{ admin:'운영자', coach:'인도자', user:'멤버' }` 로
//   `user` 를 **멤버**라 부른다. 여기 `COHORT_ROLE_LABEL.participant`(**참여자**)와 다르다.
//   같은 사람을 두 화면이 다르게 부르는 것이라 **사본 후보**이지만, 그쪽은 *시스템 권한*을
//   보이는 자리이고 이쪽은 *소속에서의 역할*이라 축이 다르다. **합치지 않았고 보고에 올린다.**

/**
 * **최근 기수는 첫 회차일로 잰다**(최박사 확정 2026-08-30).
 *
 * 실측(2026-08-30 · `min(cohort_sessions.held_at)`):
 *   `퓨처나우2026예봄1기` 2026-07-26 · `[QA] 검증 전용` 2026-08-14 · `퓨처나우2026예봄2기` 2026-09-20
 *
 * **이름 끝의 숫자로 재지 않는다** — 끝이 `n기` 가 아닌 기수가 섞이면 최신을 못 가린다
 * (실물 넷이 그렇다). 첫 회차일은 **이름 형식에 의존하지 않는다.**
 *
 * 첫 회차일이 없는 기수(회차 0)는 **가장 오래된 것으로 친다** — 시작한 적이 없으므로
 * *최근* 을 주장할 근거가 없다. 없는 값을 지어내지 않는다.
 */
export function isMoreRecent(a: CohortRole, b: CohortRole): boolean {
  const at = a.firstSessionAt ?? '';
  const bt = b.firstSessionAt ?? '';
  return at > bt;
}

/**
 * 좁은 자리에 하나만 넣을 때의 후보 — **순서를 여기서 정하지 않는다.**
 *
 * 최박사가 적으신 `참여자, 인도자, 운영자` 가 **우선순위인지 그냥 열거인지 갈린다.**
 * 실제로 겸직자가 있으므로(운영 실측: 기수를 이끌면서 참여도 하는 사람 2명) 답이 필요하다.
 * **지어내면 계열 ⑦(도구가 대상을 지어내고 그것을 쟀다)이므로 값을 비워 둔다.**
 * (앞서 `계열 8번` 이라 적었으나 그것은 **아직 없던 번호를 미리 가리킨 것**이었고,
 *  8번이 실재하게 된 지금은 다른 함정(변이 미성립)을 가리킨다. 목록 번호도
 *  **따라가야 하는 값**이라 손으로 박으면 낡는다 — 뜻을 함께 적어 둔다.)
 *
 * 답이 오면 `NARROW_PRIORITY` 한 줄에 순서를 꽂으면 되고, 그때 `narrowLabel` 이 바로 산다.
 */
/**
 * 좁은 자리 동점 규칙 — **최근 기수 포지션**(최박사 확정 2026-08-30).
 *
 * *"1기참여자 5기운영자 이면 5기운영자인 것이다. 최신의 정보가 중요하니까."*
 *
 * 그래서 **역할 사이에 우선순위를 두지 않는다** — 참여자·인도자를 줄 세우는 것이 아니라
 * **가장 최근 기수의 포지션**을 고른다. 최박사가 적으신 `참여자, 인도자, 운영자` 는
 * 그 셋이 자격 이름보다 앞선다는 뜻이지 셋 사이의 순서가 아니었다.
 */
export function narrowLabel(roles: readonly CohortRole[], isAdmin: boolean): string | null {
  // 소속이 있으면 **가장 최근 기수의 포지션**이다.
  let best: CohortRole | null = null;
  for (const r of roles) if (!best || isMoreRecent(r, best)) best = r;
  if (best) return COHORT_ROLE_LABEL[best.kind];
  // 소속이 없으면 운영자. 그것도 아니면 부르는 쪽이 쓰던 값을 그대로 쓴다.
  return isAdmin ? ADMIN_LABEL : null;
}

/**
 * 운영자 — **넷째 축**이다(최박사가 표시 대상에 넣으셨다).
 * 기수에 매이지 않아 `cohortRoles` 에 들어갈 수 없다(`cohortId` 가 없다).
 * 값은 `users.role` 에서 오고 서버가 이미 손에 들고 있다.
 */
export const ADMIN_LABEL = '운영자';
